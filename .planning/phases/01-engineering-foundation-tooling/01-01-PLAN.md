---
phase: 01-engineering-foundation-tooling
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .npmrc
  - .gitignore
autonomous: true
requirements: [TOOL-01, TOOL-03, TOOL-04]
threat_refs: [T-1-01]
user_setup: []

must_haves:
  truths:
    - "package.json exists at repo root with all production + dev dependencies declared at exact versions from STACK.md"
    - "npm ci completes against package-lock.json with no errors on a clean clone"
    - ".npmrc enforces engine-strict so Node < 22 fails install hard"
    - ".gitignore excludes Vite/Vitest/Firebase build artefacts and env files"
    - "Phase-level success criteria #6 substrate is in place: hashed-filename build infrastructure can run (D-13/D-14 — dist/ artefact only, no production deploy in Phase 1)"
  artifacts:
    - path: "package.json"
      provides: "Dependency manifest, npm scripts, lint-staged config, engines constraint"
      contains: '"firebase": "12.12.1"'
    - path: "package-lock.json"
      provides: "Reproducible install lockfile (committed)"
      min_lines: 100
    - path: ".npmrc"
      provides: "engine-strict enforcement"
      contains: "engine-strict=true"
    - path: ".gitignore"
      provides: "Augmented ignore list"
      contains: "dist/"
  key_links:
    - from: "package.json"
      to: "node_modules/"
      via: "npm ci"
      pattern: "firebase@12.12.1"
    - from: ".npmrc"
      to: "package.json#engines.node"
      via: "engine-strict"
      pattern: "engine-strict=true"
---

<objective>
Establish the npm package foundation. Without `package.json` and a populated
`node_modules/`, every subsequent wave (Vite, ESLint, Vitest, Husky, CI)
cannot run. This plan creates the dependency manifest at the exact STACK.md
versions (D-01), enforces Node 22 (D-02 + Pitfall D), augments `.gitignore`
to keep build artefacts out of git, and produces a committed
`package-lock.json` that locks the supply-chain state.

Purpose: Wave 0 is the dependency-graph root for all of Phase 1. Every
config file in Wave 1 imports from `node_modules/`; Husky 9 in Wave 2 needs
the `prepare` script to fire on `npm install`; CI in Wave 3 needs `npm ci`
to reproduce the same tree. Cutting corners here breaks every downstream
wave.

Output: `package.json`, `package-lock.json`, `.npmrc`, augmented `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-engineering-foundation-tooling/01-CONTEXT.md
@.planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md
@.planning/phases/01-engineering-foundation-tooling/01-VALIDATION.md
@.planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md
@.planning/research/STACK.md
@CLAUDE.md
@.gitignore

<interfaces>
<!-- Versions are LOCKED by D-01. Use these EXACT strings. Do not bump, do not use `^` ranges. -->

Production deps (from RESEARCH.md Standard Stack ~lines 118-126):

- firebase: 12.12.1
- chart.js: 4.5.1
- dompurify: 3.4.2
- @sentry/browser: 10.51.0

Dev deps (from RESEARCH.md Standard Stack ~lines 130-145):

- vite: 8.0.10
- vitest: 4.1.5
- @vitest/coverage-v8: 4.1.5
- happy-dom: 20.9.0
- typescript: 6.0.3
- eslint: 10.3.0
- @eslint/js: 10.0.1
- eslint-plugin-no-unsanitized: 4.1.5
- eslint-plugin-security: 4.0.0
- prettier: 3.8.3
- @firebase/rules-unit-testing: 5.0.0
- firebase-tools: 15.16.0
- husky: 9.1.7
- lint-staged: 16.4.0

Existing .gitignore (5 lines — DO NOT REORDER OR REWRITE):
.DS_Store
.vscode/
.idea/
\*.log
node_modules/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create package.json with pinned deps + scripts + lint-staged config</name>
  <read_first>
    - .planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md (Standard Stack section ~lines 118-173 + Wave 0 section ~lines 247-285 + Pitfall H ~lines 1126-1141)
    - .planning/phases/01-engineering-foundation-tooling/01-CONTEXT.md (D-01, D-02, D-03, D-04, D-16, D-27)
    - .planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md (`package.json` section)
    - .planning/research/STACK.md (cross-reference all version pins)
  </read_first>
  <files>package.json</files>
  <action>
Create `package.json` at the repo root with exactly the content below. Note three deviations from the RESEARCH.md skeleton — these are correct and load-bearing:

1. `"prepare": "husky || true"` (NOT `"husky"` alone) — required so `npm ci` in CI does not fail when `.git` is unavailable per Pitfall H. This is the officially recommended husky 9 pattern.
2. lint-staged config uses `eslint --fix` WITHOUT `--max-warnings=0` — required so the soft `no-restricted-imports` warn does not block local commits per Pitfall E. The CI `lint` job in Wave 3 uses `--max-warnings=0` as the authoritative gate.
3. Versions are pinned EXACTLY (no `^` or `~` prefixes) per D-01.

```json
{
  "name": "base-layers-diagnostic",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky || true"
  },
  "lint-staged": {
    "*.js": ["eslint --fix", "prettier --write"]
  },
  "dependencies": {
    "@sentry/browser": "10.51.0",
    "chart.js": "4.5.1",
    "dompurify": "3.4.2",
    "firebase": "12.12.1"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@firebase/rules-unit-testing": "5.0.0",
    "@vitest/coverage-v8": "4.1.5",
    "eslint": "10.3.0",
    "eslint-plugin-no-unsanitized": "4.1.5",
    "eslint-plugin-security": "4.0.0",
    "firebase-tools": "15.16.0",
    "happy-dom": "20.9.0",
    "husky": "9.1.7",
    "lint-staged": "16.4.0",
    "prettier": "3.8.3",
    "typescript": "6.0.3",
    "vite": "8.0.10",
    "vitest": "4.1.5"
  }
}
```

Use Conventional Commits for the commit message: `feat(01): add package.json with pinned 2026-05-03 stack`. Do NOT run `npm install` in this task — Task 3 handles install + lockfile generation.
</action>
<verify>
<automated>node -e "const p=require('./package.json'); if(p.dependencies.firebase!=='12.12.1') {console.error('firebase version mismatch'); process.exit(1)}; if(p.engines.node!=='>=22.0.0') {console.error('engines mismatch'); process.exit(1)}; if(p.scripts.prepare!=='husky || true') {console.error('prepare script must be husky || true per Pitfall H'); process.exit(1)}; console.log('OK')"</automated>
</verify>
<acceptance_criteria> - File `package.json` exists at repo root - `package.json` contains literal string `"firebase": "12.12.1"` (no `^`, no `~`) - `package.json` contains literal string `"vite": "8.0.10"` - `package.json` contains literal string `"eslint": "10.3.0"` - `package.json` contains literal string `"husky": "9.1.7"` - `package.json` contains literal string `"engines": { "node": ">=22.0.0" }` - `package.json` contains literal string `"type": "module"` - `package.json` contains literal string `"prepare": "husky || true"` (Pitfall H — `|| true` REQUIRED) - `package.json` lint-staged block contains `"eslint --fix"` (without `--max-warnings`) per Pitfall E - `package.json` `scripts.lint` contains `--max-warnings=0` (authoritative CI gate) - All 4 dependencies and all 14 devDependencies exactly match versions in RESEARCH.md Standard Stack - `node -e "require('./package.json')"` exits 0 (valid JSON) - Commit message follows Conventional Commits: `feat(01): ...` - `git rev-list --count HEAD` increased by 1 since plan start
</acceptance_criteria>
<done>
`package.json` exists, parses as valid JSON, declares all 18 deps at exact pinned versions, and the load-bearing deviations from the skeleton (`husky || true`, lint-staged without `--max-warnings`) are in place. Committed atomically with a Conventional Commits message.
</done>
</task>

<task type="auto">
  <name>Task 2: Create .npmrc and augment .gitignore</name>
  <read_first>
    - .planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md (Wave 0 .gitignore additions ~lines 287-299 + Pitfall D ~lines 1075-1082)
    - .planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md (`.npmrc` and `.gitignore` sections)
    - .gitignore (existing 5 lines — must preserve)
  </read_first>
  <files>.npmrc, .gitignore</files>
  <action>
**Step 1 — Create `.npmrc`:**

Create `.npmrc` at the repo root with EXACTLY this single line:

```ini
engine-strict=true
```

Rationale (D-02 + Pitfall D): Vite 8 requires Node ≥ 20.19 / 22.12. The `"engines"` field in `package.json` only warns by default; `engine-strict=true` in `.npmrc` upgrades that to a hard install error so `npm ci` on Node 18 fails fast rather than silently producing a broken environment.

**Step 2 — Augment `.gitignore`:**

Read the existing `.gitignore` (5 lines). DO NOT reorder or rewrite the existing 5 lines. APPEND the following block exactly after the existing line 5 (`node_modules/`):

```
dist/
coverage/
.env
.env.*
!.env.example
.firebase/
*.tsbuildinfo
firestore-debug.log
firebase-debug.log
.firebaserc
```

The `.firebase/`, `firebase-debug.log`, `firestore-debug.log`, and `.firebaserc` entries are forward-declared for Phase 3 (hosting) and Phase 5 (rules) — listing them now means later phases inherit them without amending `.gitignore`.

Commit message: `chore(01): add .npmrc engine-strict + augment .gitignore for build artefacts`.
</action>
<verify>
<automated>test -f .npmrc && grep -qx "engine-strict=true" .npmrc && grep -qx "dist/" .gitignore && grep -qx "coverage/" .gitignore && grep -qx ".env" .gitignore && grep -qx "!.env.example" .gitignore && grep -qx "node_modules/" .gitignore && echo "OK"</automated>
</verify>
<acceptance_criteria> - File `.npmrc` exists at repo root and contains exactly `engine-strict=true` - `.gitignore` first 5 lines unchanged from prior state (preserves `.DS_Store`, `.vscode/`, `.idea/`, `*.log`, `node_modules/`) - `.gitignore` contains literal lines `dist/`, `coverage/`, `.env`, `.env.*`, `!.env.example`, `.firebase/`, `*.tsbuildinfo`, `firestore-debug.log`, `firebase-debug.log`, `.firebaserc` - `git diff --stat HEAD~1 -- .gitignore` shows additions only (zero deletions) - Commit message follows `chore(01): ...` Conventional Commits - `git rev-list --count HEAD` increased by 1 from prior task
</acceptance_criteria>
<done>
`.npmrc` enforces engine-strict; `.gitignore` augmented with all 10 entries; existing 5 entries untouched. Atomic commit landed.
</done>
</task>

<task type="auto">
  <name>Task 3: npm install + commit package-lock.json</name>
  <read_first>
    - .planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md (Installation sequence ~lines 148-173 + Pitfall H ~lines 1126-1141)
    - package.json (just created)
  </read_first>
  <files>package-lock.json, node_modules/</files>
  <action>
**Step 1 — Verify Node version:**

Run `node --version`. If output is NOT `v22.x.x` or higher, STOP and surface the error — D-02 locks Node 22 and `engine-strict=true` will block install on lower versions. The user must `nvm use 22` or install Node 22 LTS before continuing. Do NOT proceed with `npm install` on Node < 22.

**Step 2 — Install dependencies:**

Run `npm install` (NOT `npm ci` — there is no lockfile yet) from the repo root. This will:

1. Resolve all deps and devDeps to exact versions per `package.json` pinning.
2. Generate `package-lock.json` (committed in step 4).
3. Trigger the `prepare` script which runs `husky || true` — the `|| true` will swallow the husky error if husky's binary is unavailable on the first install, which is the intended Pitfall H behaviour.
4. Husky 9 will create `.husky/_/.gitignore` and `.husky/_/h` if it runs successfully. If it does, that's fine — Wave 2 will then add `.husky/pre-commit`.

Expected duration: 30-90 seconds depending on network. Expected output: `added N packages, and audited N+1 packages in Xs`.

**Step 3 — Sanity-check the install:**

Run these checks in sequence; if any fails, STOP and surface the error:

- `npm list firebase` should output `firebase@12.12.1`
- `npm list vite` should output `vite@8.0.10`
- `npm list eslint` should output `eslint@10.3.0`
- `npm list husky` should output `husky@9.1.7`
- `npm ci --dry-run` should exit 0 (proves lockfile is valid + reproducible)

**Step 4 — Commit `package-lock.json`:**

Stage ONLY `package-lock.json` (NOT `node_modules/` — `.gitignore` handles that). Use:

```
git add package-lock.json
git commit -m "chore(01): commit package-lock.json after first npm install"
```

Do NOT add `.husky/` to git in this task — Wave 2 owns the pre-commit hook contents. If `npm install` created a `.husky/_/` directory, that's transient husky internal state; Wave 2 will write `.husky/pre-commit` and the `.husky/_/.gitignore` that husky 9 generates handles the internal state.
</action>
<verify>
<automated>npm list firebase | grep -F "firebase@12.12.1" && npm list vite | grep -F "vite@8.0.10" && npm list eslint | grep -F "eslint@10.3.0" && npm list husky | grep -F "husky@9.1.7" && npm ci --dry-run 2>&1 | grep -E "(added|up to date)" && test -f package-lock.json && echo "OK"</automated>
</verify>
<acceptance_criteria> - `node --version` output starts with `v22.` (Node 22 LTS) - `npm install` exits 0 - `node_modules/firebase/package.json` field `version` equals `"12.12.1"` (exact pinned version resolved) - `node_modules/vite/package.json` field `version` equals `"8.0.10"` - `node_modules/eslint/package.json` field `version` equals `"10.3.0"` - `node_modules/husky/package.json` field `version` equals `"9.1.7"` - `package-lock.json` exists at repo root - `npm ci --dry-run` exits 0 (proves lockfile reproducibility) - `git ls-files | grep -c node_modules` returns 0 (node_modules NOT committed — `.gitignore` working) - `package-lock.json` is committed (visible in `git log --name-only HEAD`) - Commit message follows `chore(01): ...` Conventional Commits
</acceptance_criteria>
<done>
All 18 packages resolved at exact pinned versions in `node_modules/`. `package-lock.json` exists, is committed, and `npm ci --dry-run` proves it is reproducible. `node_modules/` is correctly excluded from git via the augmented `.gitignore`.
</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries (Wave 0 scope)

| Boundary                             | Description                                        |
| ------------------------------------ | -------------------------------------------------- |
| npm registry → developer workstation | Untrusted package contents enter via `npm install` |
| developer workstation → repo         | Lockfile must capture exact resolved versions      |
| repo → CI                            | `npm ci` reproduces the same tree from lockfile    |

## STRIDE Threat Register

| Threat ID          | Category                 | Component                  | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                       |
| ------------------ | ------------------------ | -------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-1-01             | Tampering / Supply Chain | `package.json` deps        | mitigate    | Exact-version pinning (no `^` / `~`) means a malicious republish under the same version cannot drift in. Lockfile (`package-lock.json`) captures the integrity hash for every transitive dep. Subsequent `npm ci` verifies hashes. ASVS V14.2.1, V14.2.4. ISO 27001 A.8.30, A.12.6.1. SOC2 CC8.1. — Wave 4 layers Dependabot ongoing; Wave 5 layers Socket.dev install-time scanning. |
| T-1-04 (substrate) | Tampering                | dependency manifest format | mitigate    | `engine-strict=true` in `.npmrc` blocks installs on unsupported Node versions, preventing silent runtime version drift. ASVS V14.2.5. ISO 27001 A.8.31.                                                                                                                                                                                                                               |

</threat_model>

<verification>
After Task 3 completes, run the full Wave 0 acceptance check:

```bash
# All Wave 0 deliverables present and valid
test -f package.json && test -f package-lock.json && test -f .npmrc && test -f .gitignore
# Versions pinned exactly
grep -F '"firebase": "12.12.1"' package.json
grep -F '"vite": "8.0.10"' package.json
grep -F '"eslint": "10.3.0"' package.json
# Pitfall H mitigation
grep -F '"prepare": "husky || true"' package.json
# Engine-strict active
grep -qx "engine-strict=true" .npmrc
# Lockfile reproduces
npm ci --dry-run
# .gitignore augmented
grep -qx "dist/" .gitignore && grep -qx "coverage/" .gitignore
# Three atomic commits landed
[ "$(git rev-list --count HEAD ^HEAD~3)" = "3" ]
```

</verification>

<success_criteria>

1. `package.json` exists with all 18 deps pinned to STACK.md verified-2026-05-03 versions
2. `package-lock.json` is committed and `npm ci --dry-run` exits 0
3. `.npmrc` contains `engine-strict=true` (Pitfall D mitigation)
4. `.gitignore` augmented with 10 build-artefact entries; existing 5 lines unchanged
5. Three atomic commits with Conventional Commits messages (`feat(01)`, `chore(01)`, `chore(01)`)
6. Pitfall H mitigation in place: `prepare` script is `husky || true`
7. Pitfall E mitigation in place: lint-staged config has `eslint --fix` without `--max-warnings=0`
8. T-1-01 mitigated: every dep version is an exact match (no `^` / `~`)
   </success_criteria>

<output>
After completion, create `.planning/phases/01-engineering-foundation-tooling/01-01-SUMMARY.md` capturing:
- Three commit SHAs landed
- Versions resolved (output of `npm list --depth=0`)
- Any deviation from RESEARCH.md skeleton (the two Pitfall fixes are expected)
- Note: SECURITY.md "Build & Supply Chain" section will be appended in Wave 1 by the `vite.config.js` plan task — D-25 atomic-commit pattern. Wave 0 does NOT touch SECURITY.md (it does not exist yet — Wave 5 creates it).
</output>
