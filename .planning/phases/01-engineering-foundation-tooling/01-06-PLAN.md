---
phase: 01-engineering-foundation-tooling
plan: 06
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05]
files_modified:
  - tests/smoke.test.js
  - runbooks/phase-4-cleanup-ledger.md
  - runbooks/branch-protection-bootstrap.md
  - runbooks/firebase-oidc-bootstrap.md
  - runbooks/socket-bootstrap.md
  - CONTRIBUTING.md
  - SECURITY.md
  - docs/evidence/socket-install.png
  - docs/evidence/branch-protection-screenshot.png
autonomous: false
requirements: [TOOL-08, TOOL-09, TOOL-11, DOC-10]
threat_refs: [T-1-01, T-1-02, T-1-03, T-1-04, T-1-05]
user_setup:
  - service: socket-dev
    why: "Install-time behavioural malicious-package detection (TOOL-11)"
    env_vars: []
    dashboard_config:
      - task: "Install Socket Security GitHub App at the repository level"
        location: "https://github.com/apps/socket-security → Configure → AssumeAIhugh/base-layers-diagnostic"
  - service: github-branch-protection
    why: "Enforce required status checks on main (TOOL-08, TOOL-09 — protect the CI evidence trail)"
    env_vars: []
    dashboard_config:
      - task: "Run runbooks/branch-protection-bootstrap.md AFTER first green CI run"
        location: "Local terminal with gh CLI authenticated as repo admin"

must_haves:
  truths:
    - "tests/smoke.test.js exists and `npm test` runs green (no longer warns 'no tests found')"
    - "All four runbooks exist with concrete, executable content (no placeholders)"
    - "phase-4-cleanup-ledger.md enumerates every eslint-disable-next-line and @ts-nocheck introduced in Phase 1, with file:line + rule + intended fix"
    - "CONTRIBUTING.md documents gitleaks local install (scoop/brew) so contributors can run the pre-commit hook"
    - "SECURITY.md exists with header, vulnerability disclosure paragraph, and three FULLY POPULATED sections (Build & Supply Chain, Dependency Monitoring, Secret Scanning) — each with framework citations (OWASP ASVS / ISO 27001 / SOC2 / GDPR where applicable)"
    - "SECURITY.md has stub sections (TOC entries) for every later phase: Authentication, Authorization, Audit Logging, Data Lifecycle, Backup & DR, Observability, CSP & Headers, Threat Model"
    - "Socket.dev GitHub App is installed at repo level; evidence screenshot captured for DOC-09 evidence pack"
    - "Branch protection on main is APPLIED via gh api runbook (executed only AFTER Wave 3's first green CI run — Pitfall A); evidence screenshot captured"
    - "Phase 1 success criterion #5 (Dependabot opens weekly PRs) is on track to fulfil after merge — Wave 4 file is on main"
  artifacts:
    - path: "tests/smoke.test.js"
      provides: "Single arithmetic test — suppresses Vitest 'no tests found' warning"
      contains: "expect(1 + 1).toBe(2)"
    - path: "runbooks/phase-4-cleanup-ledger.md"
      provides: "Markdown table of every disable comment introduced in Phase 1"
      contains: "phase-4-cleanup-ledger"
    - path: "runbooks/branch-protection-bootstrap.md"
      provides: "One-shot gh api command to apply branch protection on main"
      contains: "gh api"
    - path: "runbooks/firebase-oidc-bootstrap.md"
      provides: "GCP commands for OIDC workload-identity-pool config (Phase 3 executes)"
      contains: "workload-identity-pool"
    - path: "runbooks/socket-bootstrap.md"
      provides: "Socket.dev GitHub App install steps + evidence screenshot location"
      contains: "socket.dev"
    - path: "CONTRIBUTING.md"
      provides: "Dev setup including gitleaks local install"
      contains: "gitleaks"
    - path: "SECURITY.md"
      provides: "Evidence-trail skeleton with three populated sections + stubs for every later phase"
      contains: "OWASP ASVS"
    - path: "docs/evidence/socket-install.png"
      provides: "Screenshot of Socket.dev installed on the repo (DOC-09 evidence)"
    - path: "docs/evidence/branch-protection-screenshot.png"
      provides: "Screenshot of branch-protection settings showing required status checks (DOC-09 evidence)"
  key_links:
    - from: "SECURITY.md § Build & Supply Chain"
      to: "vite.config.js + package.json"
      via: "TOOL-02, TOOL-03, TOOL-04 closure"
      pattern: "OWASP ASVS V14.4.2"
    - from: "SECURITY.md § Dependency Monitoring"
      to: ".github/dependabot.yml + .github/workflows/ci.yml audit job"
      via: "TOOL-10, TOOL-11, OSV-Scanner, npm audit"
      pattern: "OWASP ASVS V14.2"
    - from: "SECURITY.md § Secret Scanning"
      to: ".gitleaks.toml + .husky/pre-commit + ci.yml gitleaks-action step"
      via: "TOOL-12 closure"
      pattern: "OWASP ASVS V14.3.2"
    - from: "runbooks/phase-4-cleanup-ledger.md"
      to: "app.js + firebase-init.js + data/pillars.js disables"
      via: "git grep eslint-disable-next-line + @ts-nocheck enumeration"
      pattern: "Phase 4: replace"
    - from: "runbooks/branch-protection-bootstrap.md execution"
      to: "github.com required-status-checks on main"
      via: "gh api PUT"
      pattern: "required_status_checks"
---

<objective>
Close Phase 1 by landing the documentation evidence trail (DOC-10), the
runbooks for one-shot operational steps, the smoke test, and the two
post-first-green-CI checkpoint executions:

1. **Documentation deliverables** — smoke test, four runbooks,
   CONTRIBUTING.md, and SECURITY.md (DOC-10) with three populated sections
   referencing every Phase 1 closure with framework citations.

2. **Socket.dev install** — UI-only checkpoint at github.com/apps/socket-security
   to install the App at the repo level. Screenshot saved for DOC-09.

3. **Branch protection bootstrap** — execute the `gh api` runbook AFTER
   Wave 3's first green CI run has registered the status-check names
   (Pitfall A — running this earlier would lock the repo).

4. **DOC-10 atomic-commit fulfilment** — every Phase 1 SECURITY.md claim
   is committed in this wave with framework citations from RESEARCH.md
   "Compliance Mapping". Per D-25, the SECURITY.md create commit IS the
   atomic close for Phase 1's three sections (Wave 0-4 commits accrued
   the evidence; Wave 5 ships the SECURITY.md narrative wrapping that
   evidence — this is acceptable under D-25 because SECURITY.md did not
   exist until this wave).

Purpose: Without Wave 5, Phase 1 has CI infrastructure but no audit
narrative, no operational runbooks for the one-shot ops (Firebase OIDC,
branch protection, Socket.dev), no Phase 4 cleanup tracker, and no
declared evidence linking the closures to compliance frameworks. Wave 5
makes Phase 1 audit-credible.

Output: 1 test file + 4 runbooks + CONTRIBUTING.md + SECURITY.md + 2
evidence screenshots + branch protection applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-engineering-foundation-tooling/01-CONTEXT.md
@.planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md
@.planning/phases/01-engineering-foundation-tooling/01-VALIDATION.md
@.planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md
@.planning/phases/01-engineering-foundation-tooling/01-01-SUMMARY.md
@.planning/phases/01-engineering-foundation-tooling/01-02-SUMMARY.md
@.planning/phases/01-engineering-foundation-tooling/01-03-SUMMARY.md
@.planning/phases/01-engineering-foundation-tooling/01-04-SUMMARY.md
@.planning/phases/01-engineering-foundation-tooling/01-05-SUMMARY.md
@CLAUDE.md
@SECURITY_AUDIT.md
@package.json

<interfaces>
<!-- Disables / suppressions to enumerate in the cleanup ledger (gathered from prior SUMMARYs): -->
<!-- - app.js line 1: // @ts-nocheck (Wave 1 Task 2) -->
<!-- - app.js: per-line eslint-disable-next-line on every Math.random / innerHTML / outerHTML / insertAdjacentHTML site (Wave 1 Task 3) — count from 01-02-SUMMARY.md -->
<!-- - firebase-init.js: possibly @ts-nocheck (only if Wave 1 Task 2 added it — check 01-02-SUMMARY.md) -->
<!-- - data/pillars.js: possibly @ts-nocheck (same condition) -->
<!-- - ci.yml OSV-Scanner step: continue-on-error: true (D-20 soft fail; 30-day re-evaluation) -->

<!-- Existing repo state at Wave 5 start: -->
<!-- - 5 Action SHAs pinned in .github/workflows/ci.yml -->
<!-- - 3 Dependabot ecosystems configured -->
<!-- - 4 production deps + 14 dev deps in package.json (TOOL-01) -->
<!-- - .gitleaks.toml + .husky/pre-commit (TOOL-12) -->
<!-- - eslint.config.js (TOOL-05) blocking Math.random + innerHTML -->
<!-- - vite.config.js (TOOL-02) producing hashed-filename bundles -->
<!-- - tsconfig.json (TOOL-07) -->
<!-- - .prettierrc.json (TOOL-06) -->

<!-- Compliance citation matrix (from RESEARCH.md Compliance Mapping ~lines 1271-1289): -->
<!-- TOOL-01 → ASVS V14.2.1, ISO 27001 A.8.25 / A.8.28, SOC2 CC8.1 -->
<!-- TOOL-02 → ASVS V14.4.2, ISO 27001 A.8.25 / A.8.28, SOC2 CC8.1 -->
<!-- TOOL-03/04 → ASVS V14.2.1, V14.4.2, ISO 27001 A.8.28, SOC2 CC8.1 -->
<!-- TOOL-05 → ASVS V5.3.3 / V6.3.1, ISO 27001 A.8.28 / A.14.2.5, SOC2 CC6.6, GDPR Art. 32(1)(b) -->
<!-- TOOL-08 → ASVS V14.2.2, ISO 27001 A.8.29 / A.8.31, SOC2 CC8.1, GDPR Art. 32(1)(d) -->
<!-- TOOL-09 → ASVS V14.2.2, ISO 27001 A.8.25 / A.14.2.2, SOC2 CC8.1 -->
<!-- TOOL-10/11 → ASVS V14.2.1, ISO 27001 A.8.8, SOC2 CC8.1 -->
<!-- TOOL-12 → ASVS V14.3.2, ISO 27001 A.10.1, SOC2 CC6.1, GDPR Art. 32(1)(a) -->
<!-- DOC-10 → ASVS V14.2 (evidence), ISO 27001 A.5.36 / A.5.37, SOC2 CC2.3 / CC8.1, GDPR Art. 32(1)(d) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create smoke test + 4 runbooks (incl. cleanup ledger populated from prior commits)</name>
  <read_first>
    - .planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md (Wave 5 ~lines 691-731 + OQ-4 firebase-oidc runbook ~lines 779-846 + OQ-5 branch-protection runbook ~lines 854-895 + Pitfall A ~lines 1022-1029)
    - .planning/phases/01-engineering-foundation-tooling/01-CONTEXT.md (D-08, D-12, D-22, D-23, D-30, D-31)
    - .planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md (tests/smoke.test.js + all runbook sections)
    - .planning/phases/01-engineering-foundation-tooling/01-02-SUMMARY.md (per-file violation counts for the cleanup ledger)
    - .planning/phases/01-engineering-foundation-tooling/01-04-SUMMARY.md (the five Action+SHA+tag triples for ledger context)
    - app.js + firebase-init.js + data/pillars.js (read top 5 lines each to confirm @ts-nocheck presence)
  </read_first>
  <files>tests/smoke.test.js, runbooks/phase-4-cleanup-ledger.md, runbooks/branch-protection-bootstrap.md, runbooks/firebase-oidc-bootstrap.md, runbooks/socket-bootstrap.md</files>
  <action>
**Step 1 — Create `tests/smoke.test.js` (D-30):**

Create the directory `tests/` and the file `tests/smoke.test.js`:

```javascript
// tests/smoke.test.js
// @ts-check

/**
 * Smoke test — exists only so `npm test` doesn't emit "no tests found" warning.
 * Real test coverage is Phase 2 (TEST-01..07 + TEST-10).
 */
import { describe, it, expect } from "vitest";

describe("Smoke", () => {
  it("arithmetic works", () => {
    expect(1 + 1).toBe(2);
  });
});
```

Run `npm test` — must exit 0 with one passing test.

**Step 2 — Create `runbooks/firebase-oidc-bootstrap.md` (D-23, OQ-4):**

Create the directory `runbooks/`, then write `runbooks/firebase-oidc-bootstrap.md` with the EXACT content from RESEARCH.md OQ-4 (~lines 779-846). Header line should read:

```markdown
# Firebase OIDC Bootstrap Runbook

> Phase 1: Document only. Do NOT provision until Phase 3.
> Phase 3: Run these commands before wiring the deploy job.
```

Then include all six steps (Workload Identity Pool, OIDC Provider, Service Account, IAM binding, Workload Identity binding, Workflow snippet) plus the Notes section. Use literal `bedeveloped-base-layers` for the project ID and `AssumeAIhugh/base-layers-diagnostic` for the repo. Per D-23, this is documentation only — no commands run in Phase 1.

**Step 3 — Create `runbooks/branch-protection-bootstrap.md` (D-12, OQ-5, Pitfall A):**

Write `runbooks/branch-protection-bootstrap.md` with the EXACT content from RESEARCH.md OQ-5 (~lines 854-895). The PRE-CONDITION section is mandatory and load-bearing:

```markdown
# Branch Protection Bootstrap Runbook

> Run AFTER the first successful CI run on the repo (status checks must exist before
> referencing them). This is a one-shot operation. Re-run to update settings.

## Prerequisites

- `gh` CLI authenticated as a repo admin
- First CI run must have completed successfully (creates the check names)
```

Include the `gh api repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection` PUT command with all `required_status_checks[contexts][]` entries (`lint`, `typecheck`, `test`, `audit`, `build`), the verification command, and the evidence-screenshot instruction.

**Step 4 — Create `runbooks/socket-bootstrap.md` (D-22):**

No skeleton in RESEARCH.md — write the runbook directly:

```markdown
# Socket.dev GitHub App Bootstrap Runbook

> One-shot UI step. No CLI/API path in Phase 1 scope. Closes TOOL-11.

## Prerequisites

- Repository admin access on github.com/AssumeAIhugh/base-layers-diagnostic

## Steps

1. Visit https://github.com/apps/socket-security
2. Click "Configure"
3. Choose "AssumeAIhugh" → select "Only select repositories" → choose
   `base-layers-diagnostic` → click "Install".
4. Wait for the "Installed" confirmation page.
5. Open any open or closed PR on the repo (e.g., the Phase 1 tooling PR).
   Confirm a "Socket Security" check appears in the checks list with
   one of: passing / pending / failing-with-findings.

## Evidence

Take a screenshot of the Socket Security check appearing on a PR + the
GitHub Apps "Installed" confirmation page. Save BOTH images to:

    docs/evidence/socket-install.png

(Use a single composite image if needed — the goal is one file in the
evidence pack proving the App is installed and active.)

## Re-evaluation

If Socket.dev's behavioural rules surface false positives that block PRs
without a clear "fix it" path, document them in
`runbooks/phase-4-cleanup-ledger.md` and revisit at milestone close. Until
then, treat Socket.dev signals as advisory — they do not block merges
unless branch protection is configured to require the Socket Security
check (it is NOT required by Wave 5's branch-protection runbook;
required checks are limited to: lint, typecheck, test, audit, build).
```

**Step 5 — Create `runbooks/phase-4-cleanup-ledger.md` (D-08):**

This is the load-bearing artefact for Phase 4. It must enumerate EVERY suppression introduced in Phase 1 with an exact removal target. Use this header + structure:

```markdown
# Phase 4 Cleanup Ledger

> Every `// eslint-disable-next-line` and `// @ts-nocheck` introduced in Phase 1
> is enumerated here. Phase 4 (Modular Split) closes each entry as the
> corresponding code path is rewritten. The ledger empties to zero rows when
> Phase 4 is complete.

## Suppressions

| File   | Line | Annotation       | Rule / Check       | Intended Fix                      | Closure Phase     |
| ------ | ---- | ---------------- | ------------------ | --------------------------------- | ----------------- |
| app.js | 1    | `// @ts-nocheck` | tsc strict checkJs | Remove after IIFE → modules split | Phase 4 (CODE-01) |
| ...    | ...  | ...              | ...                | ...                               | ...               |

## Procedure to populate the ESLint disable rows

After Wave 1 was committed, the per-line `eslint-disable-next-line` annotations
were added to `app.js` (and possibly `firebase-init.js` / `data/pillars.js`).
To regenerate this table after the fact:

    git log --all --diff-filter=A --name-only -- runbooks/phase-4-cleanup-ledger.md
    # Find the Wave 1 commit that added eslint disables (commit message
    # 'feat(01): add eslint flat config + prettier + sweep app.js with phase-4 disables').
    # That commit body lists per-file violation counts from the executor's
    # _tmp_app_violations.json analysis.

    # To enumerate live state at any point:
    git grep -nE "eslint-disable-next-line" -- app.js firebase-init.js data/pillars.js
    git grep -nE "@ts-(nocheck|ignore|expect-error)" -- app.js firebase-init.js data/pillars.js

The output of those `git grep` commands feeds the table above. The executor
of THIS plan must run those greps NOW and write the resulting rows directly
into the table. Do not leave the table empty.

## Out-of-band soft-fail entries

| Source                                                         | Why soft-fail                                                               | Re-evaluation date | Hardening target                                                               |
| -------------------------------------------------------------- | --------------------------------------------------------------------------- | ------------------ | ------------------------------------------------------------------------------ |
| `.github/workflows/ci.yml` OSV-Scanner step                    | `continue-on-error: true` (D-20) — first 30 days of false-positive baseline | 2026-06-03         | Remove `continue-on-error` line; OSV-Scanner becomes hard-fail                 |
| `.github/workflows/ci.yml` ESLint `no-restricted-imports` rule | `"warn"` not `"error"` (D-26) — soft so existing files don't block CI       | Phase 4            | Change to `"error"` after modular split lands; lint-enforced module boundaries |

## Closure

This ledger MUST be empty (zero suppression rows) before Phase 4 is signed off.
Any rows remaining at Phase 4 close indicate either:

- A Phase 4 task missed a callsite — fix and re-run.
- A suppression was actually still needed — document under "Persistent
  suppressions" with rationale, and reflect in `SECURITY.md` so the audit
  narrative is honest.
```

**Now actually populate the ESLint table:**

```bash
# Capture live disable count + per-file breakdown
echo "=== app.js ===" > /tmp/disables.txt
git grep -nE "eslint-disable-next-line" -- app.js >> /tmp/disables.txt
echo "=== firebase-init.js ===" >> /tmp/disables.txt
git grep -nE "eslint-disable-next-line" -- firebase-init.js 2>/dev/null >> /tmp/disables.txt
echo "=== data/pillars.js ===" >> /tmp/disables.txt
git grep -nE "eslint-disable-next-line" -- data/pillars.js 2>/dev/null >> /tmp/disables.txt
echo "=== @ts-nocheck ===" >> /tmp/disables.txt
git grep -nE "@ts-(nocheck|ignore|expect-error)" -- app.js firebase-init.js data/pillars.js 2>/dev/null >> /tmp/disables.txt
cat /tmp/disables.txt
```

Parse the output. For each line in `/tmp/disables.txt`, add a row to the ledger table with the exact `file:line`, the rule name extracted from the disable comment, and the "Phase 4: <action>" text. For the `// @ts-nocheck` rows, the Rule/Check column reads `tsc strict checkJs`.

Delete `/tmp/disables.txt` after the table is filled.

Append the OSV-Scanner soft-fail entry and the `no-restricted-imports` warn entry to the "Out-of-band soft-fail entries" table (these are not eslint-disable lines but they're still trackable Phase 1 → Phase 4 cleanup items).

**Step 6 — Verify all 5 files + run tests:**

```bash
test -f tests/smoke.test.js
test -f runbooks/phase-4-cleanup-ledger.md
test -f runbooks/branch-protection-bootstrap.md
test -f runbooks/firebase-oidc-bootstrap.md
test -f runbooks/socket-bootstrap.md
npm test  # must exit 0 (smoke test passes)
grep -q "tsc strict checkJs" runbooks/phase-4-cleanup-ledger.md  # @ts-nocheck row present
grep -qE "^\| app\.js \| 1 \|" runbooks/phase-4-cleanup-ledger.md  # app.js line 1 row present
```

**Step 7 — Commit:**

`feat(01): add smoke test + 4 runbooks (incl. populated phase-4 cleanup ledger)`

Body should include the disable-count breakdown for the ledger so reviewers (and Phase 4) can verify the table is exhaustive.
</action>
<verify>
<automated>npm test && test -f tests/smoke.test.js && test -f runbooks/phase-4-cleanup-ledger.md && test -f runbooks/branch-protection-bootstrap.md && test -f runbooks/firebase-oidc-bootstrap.md && test -f runbooks/socket-bootstrap.md && grep -q "expect(1 + 1).toBe(2)" tests/smoke.test.js && grep -q "workload-identity-pool" runbooks/firebase-oidc-bootstrap.md && grep -q "gh api repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection" runbooks/branch-protection-bootstrap.md && grep -q "First CI run must have completed successfully" runbooks/branch-protection-bootstrap.md && grep -q "github.com/apps/socket-security" runbooks/socket-bootstrap.md && grep -q "@ts-nocheck" runbooks/phase-4-cleanup-ledger.md && grep -q "OSV-Scanner" runbooks/phase-4-cleanup-ledger.md && echo "OK"</automated>
</verify>
<acceptance_criteria> - File `tests/smoke.test.js` exists; contains `expect(1 + 1).toBe(2)`; first non-blank line is `// @ts-check` - `npm test` exits 0 with at least 1 passing test (no "no tests found" warning) - File `runbooks/firebase-oidc-bootstrap.md` exists; contains `workload-identity-pool` and `bedeveloped-base-layers` and `Phase 3` markers - File `runbooks/branch-protection-bootstrap.md` exists; contains exact `gh api` PUT command for `repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection`; Pitfall A pre-condition ("First CI run must have completed successfully") is present - File `runbooks/socket-bootstrap.md` exists; contains `https://github.com/apps/socket-security`; instructs saving evidence to `docs/evidence/socket-install.png` - File `runbooks/phase-4-cleanup-ledger.md` exists; contains a markdown table with rows for EVERY `eslint-disable-next-line` in `app.js` (count must match the violation count reported in 01-02-SUMMARY.md); contains a row for `app.js | 1 | // @ts-nocheck`; contains the OSV-Scanner `continue-on-error` soft-fail entry under "Out-of-band soft-fail entries"; contains the `no-restricted-imports` warn entry - The number of rows in the cleanup ledger's "Suppressions" table equals the actual count of `eslint-disable-next-line` lines + count of `@ts-(nocheck|ignore|expect-error)` lines in `app.js`, `firebase-init.js`, `data/pillars.js` (verifiable by `git grep -cE` and counting markdown table rows) - One atomic commit; Conventional Commits message; commit body contains the disable-count breakdown
</acceptance_criteria>
<done>
Smoke test runs green. Four runbooks ready for downstream phases. Cleanup ledger populated row-by-row from live grep — Phase 4 has a closure target. The Pitfall A pre-condition for branch-protection is documented load-bearingly.
</done>
</task>

<task type="auto">
  <name>Task 2: Create CONTRIBUTING.md + SECURITY.md (DOC-10 — three populated sections + framework citations + later-phase stubs)</name>
  <read_first>
    - .planning/phases/01-engineering-foundation-tooling/01-RESEARCH.md (Compliance Mapping section ~lines 1196-1289 — verbatim citation strings) (Wave 2 CONTRIBUTING.md ~lines 513-516)
    - .planning/phases/01-engineering-foundation-tooling/01-CONTEXT.md (D-17, D-24, D-25)
    - .planning/phases/01-engineering-foundation-tooling/01-PATTERNS.md (CONTRIBUTING.md + SECURITY.md sections)
    - SECURITY_AUDIT.md (top of file — confirms ASVS 5.0 L2 is the stated compliance target)
    - .planning/phases/01-engineering-foundation-tooling/01-01-SUMMARY.md through 01-05-SUMMARY.md (cite the closures wave-by-wave)
  </read_first>
  <files>CONTRIBUTING.md, SECURITY.md</files>
  <action>
**Step 1 — Create `CONTRIBUTING.md`** at the repo root (D-17, RESEARCH.md ~lines 513-516):

````markdown
# Contributing — Base Layers Diagnostic

## Local development setup

### Required tools

- **Node 22 LTS** — verified via `.npmrc` `engine-strict=true`. Install:
  - macOS: `brew install node@22`
  - Windows: download Node 22 LTS installer from nodejs.org or `scoop install nodejs-lts`
  - Linux: use `nvm install 22 && nvm use 22`
- **gitleaks** — required by `.husky/pre-commit`. Install:
  - macOS: `brew install gitleaks`
  - Windows: `scoop install gitleaks`
  - Linux: download from https://github.com/gitleaks/gitleaks/releases
  - Verify: `gitleaks version` should output a version ≥ 8.x
- **gh CLI** — used by some runbooks (e.g., branch protection). Install:
  - https://cli.github.com (covers all platforms)
  - Authenticate: `gh auth login`

### One-time setup

```sh
git clone https://github.com/AssumeAIhugh/base-layers-diagnostic
cd base-layers-diagnostic
npm ci   # reproduces the exact lockfile state
```
````

The `npm ci` triggers `husky || true` via the `prepare` script, which sets up
`.husky/_/`. The pre-commit hook (`.husky/pre-commit`) is then active for
every `git commit` in this repo.

### Daily commands

```sh
npm test          # Vitest run (Phase 2 will populate this with real coverage)
npm run lint      # ESLint flat config — blocks new Math.random / innerHTML / outerHTML
npm run typecheck # tsc --noEmit (JSDoc-as-typecheck; vanilla-JS — no .ts files)
npm run build     # Vite build → dist/ with hashed-filename bundles
npm run format    # Prettier write
npm run dev       # Vite dev server on port 5178 (matches .claude/launch.json)
```

### Pre-commit hook

`.husky/pre-commit` runs on every `git commit`:

1. `lint-staged` runs `eslint --fix` + `prettier --write` on staged `.js` files
2. `gitleaks protect --staged --config .gitleaks.toml` scans the staged diff
   for secrets (custom rule blocks SHA-256 hex literals labelled
   password/hash/secret/key/token/credential — see `.gitleaks.toml`)

If gitleaks is not installed locally the hook fails. CI is the backstop —
`.github/workflows/ci.yml` audit job runs gitleaks-action on every push +
PR.

## Commits

We use [Conventional Commits](https://www.conventionalcommits.org). Prefixes
in active use: `feat`, `fix`, `chore`, `docs`, `refactor`, `test`. Scope is
the phase number where applicable (e.g., `feat(01): ...`).

Commit-message linting (commitlint) is NOT enforced — convention only. If
this becomes inconsistent we will add a hook.

## Pull requests

- Open against `main`. CI must be green (lint + typecheck + test + audit +
  build) before merge.
- Branch protection requires 1 approving review and a linear history.
- Direct pushes to `main` are blocked.

## Where things live

See `.planning/codebase/STRUCTURE.md` for the current source layout. The
target post-Phase-4 layout is in `.planning/research/ARCHITECTURE.md`.

Tests live under `tests/`. Runbooks for one-shot operations live under
`runbooks/`.

````

**Step 2 — Create `SECURITY.md`** at the repo root (D-24 — the load-bearing DOC-10 deliverable). The structure follows the RESEARCH.md Compliance Mapping section (~lines 1196-1289) for the populated sections and CONTEXT.md D-24 for the stubs.

```markdown
# Security — Base Layers Diagnostic

**Last updated:** 2026-05-03 (Phase 1 close)
**Disclosure contact:** security@bedeveloped.com
**Supported versions:** main branch only (no released versions yet).

## Vulnerability disclosure policy

> Phase 11 finalises the policy wording. Current placeholder:
>
> If you believe you have found a security vulnerability in this codebase
> or in the deployed application, please email `security@bedeveloped.com`.
> We will acknowledge your report within 5 business days. We do not take
> legal action against good-faith security researchers acting under the
> terms of this policy.

---

## § Build & Supply Chain

**Control:** All production dependencies (Firebase JS SDK, Chart.js,
DOMPurify, Sentry) are declared in `package.json` at exact pinned versions
(no `^` / `~` ranges) and resolved via `package-lock.json` for reproducible
installs. The Vite 8 build pipeline produces content-hashed filenames
(`*-<hash>.js`) replacing the prior hand-bumped `?v=46` cache-busting
pattern. Build output is verified in CI on every PR via the `build` job
which uploads `dist/` as a workflow artefact (no production deploy in
Phase 1 — the live site continues to ship from GitHub Pages until
Phase 3's hosting cutover).

**Evidence:**
- Pinned dep versions: `package.json` (Wave 0)
- Lockfile reproducibility: `npm ci --dry-run` exits 0 (verified Wave 0)
- Hashed-filename build: `vite.config.js` `rollupOptions` (Wave 1)
- CI build verification: `.github/workflows/ci.yml` `build` job (Wave 3)

**Framework citations:**
- OWASP ASVS L2 V14.4.2 — all client-side resources are served from a
  controlled origin (substrate; Phase 3 deploys `dist/` to Firebase Hosting)
- OWASP ASVS L2 V14.2.1 — all components kept up to date; Dependabot
  automates monitoring (see § Dependency Monitoring)
- ISO/IEC 27001:2022 A.8.25 — secure development life cycle
- ISO/IEC 27001:2022 A.8.28 — secure coding (Vite build enforces ESM
  tree-shaking; no dead code paths served)
- SOC 2 CC8.1 — change management; all dependency changes gated by CI +
  human review

---

## § Dependency Monitoring

**Controls:**

- **Pinned versions** — `package.json` declares every production and dev
  dep at exact version. `package-lock.json` is committed. `npm ci` in CI
  verifies integrity hashes on every reinstall (closes the substrate of
  T-1-01 supply-chain compromise).
- **Dependabot** monitors three ecosystems weekly (Mondays):
  - `npm` at `/` (root deps)
  - `npm` at `/functions` (forward-declared for Phase 7)
  - `github-actions` at `/` (keeps the SHA-pinned Actions in
    `.github/workflows/ci.yml` fresh)
  All Dependabot PRs require human review (auto-merge is OFF —
  compliance posture per D-19).
- **Socket.dev GitHub App** provides install-time behavioural
  malicious-package detection (post-Shai-Hulud-class supply chain attacks)
  on every dependency update PR.
- **OSV-Scanner** runs in CI on every PR via
  `google/osv-scanner-action`, checking against the OSV.dev advisory
  database (broader coverage than npm advisory DB alone). Currently
  configured as soft fail (`continue-on-error: true`) per D-20 — first
  30 days of false-positive baselining; hardens to fail-fast at 2026-06-03
  per the Phase 4 cleanup ledger entry.
- **`npm audit --audit-level=high --omit=dev`** runs as a HARD CI gate
  on every PR. Production-dep high-severity findings block merge.

**Evidence:**
- `.github/dependabot.yml` (Wave 4) — three ecosystems weekly cadence
- `runbooks/socket-bootstrap.md` + `docs/evidence/socket-install.png`
  (Wave 5) — Socket.dev install evidence
- `.github/workflows/ci.yml` `audit` job (Wave 3) — npm audit HARD,
  OSV-Scanner SOFT, gitleaks-action backstop
- `.github/workflows/ci.yml` `setup` + per-job `npm ci` (Wave 3) — lockfile
  integrity-hash verification on every CI run

**Framework citations:**
- OWASP ASVS L2 V14.2.1 — components up to date and free from known
  vulnerabilities
- OWASP ASVS L2 V14.2.4 — risk-based dependency review (Dependabot PRs
  require human review)
- ISO/IEC 27001:2022 A.8.8 — management of technical vulnerabilities
- ISO/IEC 27001:2022 A.8.30 — outsourced development (supply chain
  oversight)
- SOC 2 CC8.1 — change management evidence trail
- GDPR Art. 32(1)(d) — process for regularly testing and evaluating
  effectiveness of security measures

---

## § Secret Scanning

**Controls:**

- **gitleaks pre-commit hook** (`.husky/pre-commit`) scans every staged
  diff. Blocks commits containing secrets matching the gitleaks default
  ruleset PLUS a custom rule (see below). Local install via
  `scoop install gitleaks` (Windows) or `brew install gitleaks`
  (macOS) — documented in `CONTRIBUTING.md`.
- **gitleaks CI step** (`gitleaks/gitleaks-action`) runs in the `audit`
  job on every PR + push as the backstop for developers who use
  `--no-verify` to skip the local hook.
- **Custom rule `sha256-hex-literal-regression`** (`.gitleaks.toml`)
  matches a 64-char hex literal preceded (within 20 chars) by one of the
  context words `password|hash|secret|key|token|credential`. This rule
  exists specifically to catch a regression of finding C2 — the prior
  `INTERNAL_PASSWORD_HASH = "<64-hex>"` constant in `app.js`. Synthetic
  probe in Wave 2 verified the rule fires.
- **Allowlist** for `tests/` and `runbooks/` permits legitimate
  fixtures (e.g., regression-test snapshots).

**Known gap:** The pre-existing `INTERNAL_PASSWORD_HASH` constant in
`app.js` line 444 is itself a finding the custom rule would block, but
it is grandfathered in (it predates Phase 1). Closure: Phase 6 AUTH-14
deletes the constant when real Firebase Auth replaces the
shared-password substrate.

**Evidence:**
- `.husky/pre-commit` (Wave 2) — local gate
- `.gitleaks.toml` (Wave 2) — config + custom rule + allowlist
- `.github/workflows/ci.yml` `audit` job gitleaks-action step (Wave 3) —
  CI backstop
- Wave 2 commit body — synthetic probe evidence (committing the literal
  C2 hash shape was BLOCKED by the pre-commit hook)
- Phase 6 AUTH-14 — closure of the grandfathered finding

**Framework citations:**
- OWASP ASVS L2 V14.3.2 — application source code does not contain
  secrets
- ISO/IEC 27001:2022 A.10.1 — use of cryptographic controls (ensures
  secrets remain secret)
- ISO/IEC 27001:2022 A.10.1.1 — policy on the use of cryptographic
  controls
- SOC 2 CC6.1 — logical and physical access controls (credentials
  cannot be committed to repo)
- GDPR Art. 32(1)(a) — appropriate technical measures for protection of
  personal data

---

## Sections planned for later phases

The sections below are stubs. Each phase that closes a SECURITY.md claim
appends to the corresponding stub in the same atomic commit (D-25 —
Pitfall 19 prevention).

### § Authentication & MFA — *TODO Phase 6*

Will document: real Firebase Auth (Email/Password + claims), TOTP MFA
enforced for internal users, recovery codes, two-admin recovery
procedure, password policy (HIBP leaked check, ≥12 chars), email
verification, account-enumeration mitigation. Deletes the prior
`INTERNAL_PASSWORD_HASH` constant (closure of the grandfathered §
Secret Scanning gap above).

### § Authorization & Tenant Isolation — *TODO Phases 5 & 6*

Will document: claims-based Firestore Rules (`role`, `orgId`),
Storage Rules (size + MIME + path scope), tenant-jump test, deploy
rollback procedure.

### § Audit Logging — *TODO Phases 7 & 9*

Will document: `auditLog/{eventId}` server-only collection, audit-event
schema, mirror Firestore-trigger audit writers, BigQuery sink + 7-year
retention, audited-user-cannot-read-own predicate.

### § Data Lifecycle & GDPR — *TODO Phase 8*

Will document: soft-delete + 30-day restore, `gdprExportUser`,
`gdprEraseUser` with consistent-token tombstone, audit-vs-erasure
conflict resolution, backup-rotation propagation.

### § Backup & DR — *TODO Phase 8*

Will document: daily Firestore export, GCS lifecycle (30d Standard / 90d
Nearline / 365d Archive), Firestore PITR (7-day rolling), Storage Object
Versioning + 90-day soft-delete, signed-URL TTL ≤ 1h, restore-drill
runbook.

### § Observability — *TODO Phase 9*

Will document: Sentry browser + node (EU region), PII scrubber, Slack
webhook auth-anomaly alerts, uptime monitor, Firebase budget alerts,
Sentry quota alert.

### § CSP & Headers — *TODO Phases 3 & 10*

Will document: Phase 3 baseline (HSTS, X-CTO, Referrer-Policy,
Permissions-Policy, COOP, COEP, CSP Report-Only). Phase 10 enforced CSP
(no `unsafe-inline`), HSTS preload submission.

### § Threat Model — *TODO Phase 11*

Link target: `THREAT_MODEL.md` (created Phase 11 — STRIDE-style coverage
of auth bypass, tenant boundary, file upload abuse, denial-of-wallet,
supply-chain compromise, insider misuse).

---

## Compliance posture statement

This codebase aims for **credible, not certified** compliance with
SOC 2 Common Criteria 2017, ISO/IEC 27001:2022 Annex A, GDPR Article 32,
and OWASP ASVS 5.0 Level 2. Certification is a separate workstream
(see PROJECT.md "Out of Scope"). Each section above maps controls to
the specific framework citations they address; the canonical mapping
lives in `docs/CONTROL_MATRIX.md` (created Phase 11).
````

**Step 3 — Verify both files:**

Run these gates before commit:

```bash
test -f CONTRIBUTING.md
test -f SECURITY.md

# DOC-10 grep gate from VALIDATION.md TOOL-10 row Wave 5:
grep -q "## § Build & Supply Chain" SECURITY.md
grep -q "## § Dependency Monitoring" SECURITY.md
grep -q "## § Secret Scanning" SECURITY.md
grep -q "OWASP ASVS" SECURITY.md
grep -q "ISO/IEC 27001" SECURITY.md
grep -q "SOC 2" SECURITY.md
grep -q "GDPR" SECURITY.md
grep -q "security@bedeveloped.com" SECURITY.md

# Stub sections present (D-24 — every later phase has a TOC entry)
grep -q "Authentication & MFA" SECURITY.md
grep -q "Authorization & Tenant Isolation" SECURITY.md
grep -q "Audit Logging" SECURITY.md
grep -q "Data Lifecycle & GDPR" SECURITY.md
grep -q "Backup & DR" SECURITY.md
grep -q "Observability" SECURITY.md
grep -q "CSP & Headers" SECURITY.md
grep -q "Threat Model" SECURITY.md

# Compliance posture statement — Pitfall 19 prevention (no false certification claim)
grep -q "credible, not certified" SECURITY.md
! grep -q "certified" SECURITY.md || ! grep -E "(SOC 2 certified|ISO 27001 certified)" SECURITY.md
```

**Step 4 — Commit:**

`feat(01): add CONTRIBUTING.md + SECURITY.md (DOC-10 — three sections + later-phase stubs)`

This is the atomic commit closing DOC-10 for Phase 1 per D-25. The commit body should reference each Wave's contribution to the three populated sections.
</action>
<verify>
<automated>test -f CONTRIBUTING.md && test -f SECURITY.md && grep -q "## § Build & Supply Chain" SECURITY.md && grep -q "## § Dependency Monitoring" SECURITY.md && grep -q "## § Secret Scanning" SECURITY.md && grep -q "OWASP ASVS" SECURITY.md && grep -q "ISO/IEC 27001" SECURITY.md && grep -q "SOC 2 CC8.1" SECURITY.md && grep -q "GDPR Art" SECURITY.md && grep -q "security@bedeveloped.com" SECURITY.md && grep -q "Authentication & MFA" SECURITY.md && grep -q "Threat Model" SECURITY.md && grep -q "credible, not certified" SECURITY.md && grep -q "scoop install gitleaks" CONTRIBUTING.md && grep -q "brew install gitleaks" CONTRIBUTING.md && echo "OK"</automated>
</verify>
<acceptance_criteria> - File `CONTRIBUTING.md` exists at repo root - `CONTRIBUTING.md` contains literal `scoop install gitleaks` AND `brew install gitleaks` (D-17) - `CONTRIBUTING.md` contains literal `Conventional Commits` - `CONTRIBUTING.md` contains literal `port 5178` - File `SECURITY.md` exists at repo root - `SECURITY.md` contains the three section headers `## § Build & Supply Chain`, `## § Dependency Monitoring`, `## § Secret Scanning` - Each populated section contains at least 4 framework citation lines (OWASP ASVS / ISO 27001 / SOC 2 / GDPR — RESEARCH.md Compliance Mapping) - `SECURITY.md` contains all 8 stub sections (Authentication, Authorization, Audit Logging, Data Lifecycle, Backup & DR, Observability, CSP & Headers, Threat Model) - `SECURITY.md` contains literal `credible, not certified` (Pitfall 19 — compliance theatre prevention) - `SECURITY.md` contains literal `security@bedeveloped.com` - `SECURITY.md` § Secret Scanning section explicitly acknowledges the pre-existing `INTERNAL_PASSWORD_HASH` finding and points to Phase 6 AUTH-14 for closure - `SECURITY.md` § Dependency Monitoring section enumerates ALL FOUR Phase 1 layers (pinned versions + Dependabot + Socket.dev + OSV-Scanner + npm audit) - One atomic commit; Conventional Commits message; commit body cross-references the Wave 0-4 evidence
</acceptance_criteria>
<done>
DOC-10 for Phase 1 is fulfilled. SECURITY.md tells an honest, evidenced, framework-cited story for the three Phase 1 control areas. The grandfathered C2 finding is acknowledged with a closure target. Eight later-phase stubs prevent the Phase 11 cliff (Pitfall 19). CONTRIBUTING.md unblocks any new contributor for the local pre-commit hook.
</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Install Socket.dev GitHub App + capture evidence screenshot</name>
  <files>(checkpoint - no files modified directly; checkpoint references state produced by prior tasks)</files>
  <action>See &lt;how-to-verify&gt; below. This is a checkpoint:human-verify / human-action task; the user verifies state produced by prior tasks and signals completion via &lt;resume-signal&gt;.</action>
  <verify>(checkpoint - verification is the user response described in &lt;resume-signal&gt;)</verify>
  <done>User signals completion per &lt;resume-signal&gt;; or the executor surfaces the failure mode encountered.</done>
  <what-built>
    `runbooks/socket-bootstrap.md` (Task 1) documents the install steps. The
    runbook is committed; the actual install is a UI-only step at
    github.com/apps/socket-security that requires repo-admin click-through
    (no CLI/API path in Phase 1 scope per D-22).

    This is a `checkpoint:human-action` because there is no `gh api` /
    `gcloud` / curl path to install a GitHub App. UI click-through is the
    only mechanism.

  </what-built>
  <how-to-verify>
1. Open `runbooks/socket-bootstrap.md` and follow the 5 steps verbatim.

2.  Visit https://github.com/apps/socket-security → Configure → AssumeAIhugh
    → "Only select repositories" → select `base-layers-diagnostic` → Install.

3.  Confirm the install succeeded by visiting any open or closed PR on the
    repo (the Phase 1 tooling PR works) and looking for a "Socket Security"
    check in the PR's checks list.

4.  Capture a screenshot showing BOTH: - The "Socket Security" check on the PR (proof it's active) - The GitHub Apps installed list showing Socket Security

        Combine into a single image if needed. Save to:

            docs/evidence/socket-install.png

        (Create the `docs/evidence/` directory first via `mkdir -p

    docs/evidence`.)

5.  Commit the screenshot:

```bash
git add docs/evidence/socket-install.png
git commit -m "docs(01): add Socket.dev install evidence (TOOL-11)"
```

After both the install + screenshot commit are landed, type `installed`
(with the evidence file path) and the executor proceeds to the
branch-protection bootstrap (Task 4).

If Socket.dev refuses the install (e.g., billing tier conflict or
permission issue), surface the error and request guidance.
</how-to-verify>
<resume-signal>Type "installed &lt;path-to-evidence-file&gt;" to confirm Socket.dev is active and the screenshot is committed. Or describe the error encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Execute branch-protection runbook + capture evidence screenshot + verify final phase state</name>
  <files>(checkpoint - no files modified directly; checkpoint references state produced by prior tasks)</files>
  <action>See &lt;how-to-verify&gt; below. This is a checkpoint:human-verify / human-action task; the user verifies state produced by prior tasks and signals completion via &lt;resume-signal&gt;.</action>
  <verify>(checkpoint - verification is the user response described in &lt;resume-signal&gt;)</verify>
  <done>User signals completion per &lt;resume-signal&gt;; or the executor surfaces the failure mode encountered.</done>
  <what-built>
    `runbooks/branch-protection-bootstrap.md` (Task 1) contains the exact
    `gh api` PUT command. This task EXECUTES that runbook now that Wave 3
    has produced the first green CI run (the status-check names
    `lint`, `typecheck`, `test`, `audit`, `build` now exist in GitHub's
    check registry — Pitfall A pre-condition satisfied).

    The executor is expected to run the runbook command end-to-end. The
    user verifies the result in the GitHub Settings UI.

  </what-built>
  <how-to-verify>
1. **Pre-condition check** — confirm Wave 3's first green CI run actually
   ran. From a terminal:
   ```sh
   gh run list --workflow=CI --limit 1 --json conclusion,status --jq '.[0]'
   ```
   The output should show `{"conclusion":"success","status":"completed"}`.
   If not, STOP — branch protection cannot be applied yet (Pitfall A).
   Wait for CI to complete or investigate the failure.

2.  **Execute the runbook** — from `runbooks/branch-protection-bootstrap.md`:

    ```sh
    gh api repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection \
      --method PUT \
      --header "Accept: application/vnd.github+json" \
      --field required_status_checks[strict]=true \
      --field "required_status_checks[contexts][]=lint" \
      --field "required_status_checks[contexts][]=typecheck" \
      --field "required_status_checks[contexts][]=test" \
      --field "required_status_checks[contexts][]=audit" \
      --field "required_status_checks[contexts][]=build" \
      --field required_pull_request_reviews[required_approving_review_count]=1 \
      --field required_pull_request_reviews[dismiss_stale_reviews]=true \
      --field required_linear_history=true \
      --field allow_force_pushes=false \
      --field allow_deletions=false \
      --field enforce_admins=false
    ```

3.  **Verify via API:**

    ```sh
    gh api repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection | jq .
    ```

    Confirm `required_status_checks.contexts` lists all five jobs.

4.  **Verify via UI + capture evidence:**
    Visit
    https://github.com/AssumeAIhugh/base-layers-diagnostic/settings/branches.
    Confirm:
    - "Require a pull request before merging" — ON, with 1 required review
    - "Require status checks to pass before merging" — ON, listing
      `lint`, `typecheck`, `test`, `audit`, `build`
    - "Require linear history" — ON
    - "Allow force pushes" — OFF
    - "Allow deletions" — OFF

    Take a screenshot of the page. Save to:

        docs/evidence/branch-protection-screenshot.png

    Commit:

    ```sh
    git add docs/evidence/branch-protection-screenshot.png
    git commit -m "docs(01): add branch-protection evidence (TOOL-08, TOOL-09)"
    ```

5.  **Final Phase 1 verification gauntlet:** Run the full local suite from
    a clean clone (or simulate via `rm -rf node_modules && npm ci`):

    ```sh
    npm ci
    npm run lint        # Phase 1 success criterion #1
    npm run typecheck   # Phase 1 success criterion #1
    npm test            # Phase 1 success criterion #1
    npm run build       # Phase 1 success criterion #1 + #6
    ls dist/assets/*.js | grep -E '\.[a-f0-9]{8,}\.js$'   # success criterion #6
    ```

    All four commands MUST exit 0; the `ls | grep` MUST match at least
    one file.

6.  **Phase 1 close summary:** Confirm to the user that all six Phase 1
    success criteria are evidenced:
    1. ✓ `npm test/lint/typecheck/build` all run + pass on a clean clone
       (verified Step 5)
    2. ✓ Every PR runs lint+typecheck+test+audit+build with all
       third-party Actions SHA-pinned (Wave 3 PR run + grep gate)
    3. ✓ ESLint blocks `Math.random()` / `innerHTML =` regressions
       (Wave 1 synthetic probes)
    4. ✓ gitleaks blocks committing the C2 hash shape (Wave 2 synthetic
       probe)
    5. ⏳ Dependabot weekly PRs — the schedule will fire on the first
       Monday after the Wave 3 PR is merged to main; document expected
       first-fire window
    6. ✓ Vite produces hashed-filename bundles in `dist/` (Step 5 grep)

    Type `phase-1-closed` (with the branch-protection screenshot path) to
    signal completion.

If any verification step fails:

- For step 1 pre-condition fail: do NOT run the runbook; go fix CI first.
- For step 2 API error: surface the error verbatim — likely auth or
  permission issue with `gh` CLI.
- For step 5 fail: regression in a prior wave; investigate which wave
  introduced the issue and revert/fix.
  </how-to-verify>
  <resume-signal>Type "phase-1-closed &lt;path-to-bp-screenshot&gt;" to confirm branch protection is applied, evidence is captured, and the final clean-clone gauntlet passes. Or describe the failure mode for diagnosis.</resume-signal>
  </task>

</tasks>

<threat_model>

## Trust Boundaries (Wave 5 scope)

| Boundary                                  | Description                                                                |
| ----------------------------------------- | -------------------------------------------------------------------------- |
| repo `main` branch → contributor pushes   | Branch protection enforces lint+typecheck+test+audit+build pass + 1 review |
| GitHub Apps (Socket.dev) → repo CI signal | Install-time behavioural detection                                         |
| Documentation → audit reviewer            | SECURITY.md must be evidenced + framework-cited (Pitfall 19)               |

## STRIDE Threat Register

| Threat ID              | Category                           | Component                        | Disposition            | Mitigation Plan                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------------------- | ---------------------------------- | -------------------------------- | ---------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-1-01                 | Tampering / Supply Chain           | npm + GH Actions ecosystems      | mitigate (final layer) | Socket.dev GitHub App installed (Task 3) provides install-time behavioural detection on every dep-update PR. Combined with Wave 3 (npm audit + OSV-Scanner) and Wave 4 (Dependabot weekly), this is the complete monitoring stack documented in SECURITY.md § Dependency Monitoring (Task 2). Branch protection (Task 4) prevents merging without these checks. ASVS V14.2.1, V14.2.4. ISO 27001 A.8.8, A.8.30. SOC2 CC8.1. |
| T-1-04                 | Tampering / CI Action Substitution | `.github/workflows/ci.yml`       | mitigate (final layer) | Branch protection (Task 4) requires the `audit` job to pass before merge — meaning a malicious PR that re-points an Action SHA to a compromised version would either fail the SHA grep validation in CI or fail OSV-Scanner. Plus Dependabot github-actions ecosystem (Wave 4) keeps SHAs current. ASVS V14.2.5. ISO 27001 A.8.31.                                                                                          |
| T-1-02, T-1-03, T-1-05 | (mitigations carry from Waves 1-3) | (audit narrative in SECURITY.md) | document               | SECURITY.md (Task 2) catalogues each closure with framework citations so the milestone has an audit narrative (DOC-10 — Pitfall 19 prevention).                                                                                                                                                                                                                                                                             |
| T-1-Wave5-01           | Repudiation                        | `main` branch protection         | mitigate               | Branch protection prevents force-push (`allow_force_pushes=false`) and direct push (PRs only via `required_status_checks[strict]=true`), preserving the CI evidence trail. ASVS V8.3.1. ISO 27001 A.8.31. SOC2 CC8.1. Evidence: `docs/evidence/branch-protection-screenshot.png` (Task 4).                                                                                                                                  |

</threat_model>

<verification>
After all four Wave 5 tasks complete (including the two checkpoints):

```bash
# All deliverables present
test -f tests/smoke.test.js
test -f runbooks/phase-4-cleanup-ledger.md
test -f runbooks/branch-protection-bootstrap.md
test -f runbooks/firebase-oidc-bootstrap.md
test -f runbooks/socket-bootstrap.md
test -f CONTRIBUTING.md
test -f SECURITY.md
test -f docs/evidence/socket-install.png
test -f docs/evidence/branch-protection-screenshot.png

# Smoke test passes
npm test

# DOC-10 grep gate (VALIDATION.md TOOL-10 row Wave 5)
grep -q '## § Build & Supply Chain' SECURITY.md
grep -q '## § Dependency Monitoring' SECURITY.md
grep -q '## § Secret Scanning' SECURITY.md
grep -q 'OWASP ASVS' SECURITY.md

# Branch protection in place
gh api repos/AssumeAIhugh/base-layers-diagnostic/branches/main/protection \
  --jq '.required_status_checks.contexts | length' \
  | grep -q "^5$"

# Final phase gauntlet from a clean clone
rm -rf node_modules
npm ci
npm run lint && npm run typecheck && npm test && npm run build
ls dist/assets/*.js | grep -qE '\.[a-f0-9]{8,}\.js$'

# At least 4 atomic commits since plan start (Tasks 1, 2, 3 commit, 4 commits)
[ "$(git rev-list --count HEAD ^HEAD~4)" -ge 4 ]
```

</verification>

<success_criteria>

1. Smoke test (`tests/smoke.test.js`) exists and passes — Vitest no longer warns "no tests found"
2. Four runbooks (`phase-4-cleanup-ledger`, `branch-protection-bootstrap`, `firebase-oidc-bootstrap`, `socket-bootstrap`) exist with concrete executable content
3. `phase-4-cleanup-ledger.md` enumerates every Phase 1 suppression (eslint disables + @ts-nocheck) — Phase 4 has a closure target table
4. `CONTRIBUTING.md` documents gitleaks local install + dev workflow
5. `SECURITY.md` exists with three populated sections (Build & Supply Chain / Dependency Monitoring / Secret Scanning) — each section evidenced + framework-cited (OWASP ASVS / ISO 27001 / SOC 2 / GDPR per RESEARCH.md Compliance Mapping)
6. `SECURITY.md` has eight stub sections for later phases (Pitfall 19 prevention)
7. `SECURITY.md` includes the "credible, not certified" compliance posture statement
8. Socket.dev GitHub App installed at repo level — evidence at `docs/evidence/socket-install.png` (TOOL-11)
9. Branch protection on `main` is APPLIED via `gh api` PUT runbook AFTER first green CI (Pitfall A respected) — evidence at `docs/evidence/branch-protection-screenshot.png` (TOOL-08, TOOL-09)
10. Final clean-clone gauntlet passes: `npm ci && npm run lint && npm run typecheck && npm test && npm run build && ls dist/assets/*.js` produces hashed-filename bundles
11. Phase 1 success criteria 1, 2, 3, 4, 6 are evidenced; criterion 5 (Dependabot weekly) is on track to fire on the first Monday after merge
    </success_criteria>

<output>
After all four tasks complete, create
`.planning/phases/01-engineering-foundation-tooling/01-06-SUMMARY.md`
capturing:

- All commit SHAs (Task 1, Task 2, Task 3 evidence commit, Task 4 evidence commit)
- Per-suppression count in `phase-4-cleanup-ledger.md` (rows of the
  Suppressions table)
- Output of the final clean-clone gauntlet (each command's exit code +
  the hashed bundle filename)
- Output of `gh api repos/.../branches/main/protection | jq .` proving
  branch protection is in place
- Phase 1 close statement: which of the 6 ROADMAP success criteria are
  verifiable now, which are pending merge to main (Dependabot's first
  scan fires on the first Monday after merge — document the date)
- Note: This plan completes the milestone's first phase. Run
  `/gsd-verify-work 1` next to officially close Phase 1, then
  `/gsd-plan-phase 2` to begin the Test Suite Foundation phase.
  </output>
