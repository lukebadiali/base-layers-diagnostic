---
phase: 03-hosting-cutover-baseline-security-headers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md
  - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md
autonomous: false
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
  - HOST-04
  - HOST-05
  - HOST-08
  - FN-10
tags:
  - phase-3
  - preflight
  - firebase-hosting
  - csp
must_haves:
  truths:
    - "Production Firestore region is recorded in writing (resolves Open Question A3 / D-06)"
    - "Firebase project ID + default hosting URL are recorded (resolves Open Question A1)"
    - "Domain registrar for bedeveloped.com is identified and DNS-admin access is confirmed (resolves Open Question A2)"
    - "OIDC Workload Identity pool from runbooks/firebase-oidc-bootstrap.md exists in GCP (resolves Open Question A7)"
    - "OIDC service account roles are pinned to least-privilege baseline expected by D-14 (resolves T-3-5)"
    - "index.html has no <meta http-equiv=Content-Security-Policy> tag (resolves T-3-7 / pre-flight #4)"
    - "dist/index.html font-CDN status (Google Fonts present or absent) is recorded (resolves Assumption A5)"
    - "Firebase JS SDK 12.x connect-src origin list is verified against current SDK source/docs (resolves A8 / Open Question 8)"
    - "All findings land in 03-PREFLIGHT.md so downstream plans (02..05) reference one canonical file"
  artifacts:
    - path: ".planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md"
      provides: "Canonical record of Wave 1 verifications consumed by Plans 02-05"
      contains: "## Firestore Region, ## Firebase Project, ## Registrar, ## OIDC Pool, ## OIDC SA Roles, ## index.html meta-CSP scan, ## dist/index.html font-CDN scan, ## SDK 12.x connect-src verification"
    - path: ".planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md"
      provides: "Addendum recording any divergence from D-06 (Firestore region) or D-09 (auth popup origin)"
      contains: "## Pre-Flight Addendum (Wave 1)"
  key_links:
    - from: "03-PREFLIGHT.md"
      to: "03-02-PLAN.md (firebase.json)"
      via: "verified font-CDN status feeds CSP style-src/font-src; verified connect-src list pins D-07; verified project ID populates .firebaserc"
      pattern: "preflight-feeds-config"
    - from: "03-PREFLIGHT.md"
      to: "03-04-PLAN.md (CI deploy)"
      via: "verified OIDC pool + service account roles unblock D-14 deploy job"
      pattern: "preflight-feeds-ci"
    - from: "03-PREFLIGHT.md"
      to: "03-05-PLAN.md (cutover)"
      via: "registrar identity + DNS admin access blocks the same-session CNAME flip (D-02)"
      pattern: "preflight-feeds-cutover"
---

<objective>
Resolve every Wave-1 pre-flight verification enumerated in CONTEXT.md §"Pre-Flight Verifications" + RESEARCH.md §"Open Questions" + §"Assumptions Log" before any code or config lands.
This is the gate that blocks every downstream plan (02..06): without verified Firestore region, project ID, registrar, OIDC pool, font-CDN survey, and SDK 12.x connect-src list, the plans below would either guess (Pitfall 14/15/16) or silently produce a broken deploy (Pitfall 1 in RESEARCH.md — silent rewrite shadowing).

Purpose: Eliminate every "[ASSUMED]" / "[Unknown]" tag in RESEARCH.md and CONTEXT.md before they propagate into firebase.json, the CSP directive list, the CI deploy job, or the cutover runbook.
Output: A single canonical `03-PREFLIGHT.md` artefact that Plans 02-06 reference. CONTEXT.md gets an addendum recording any divergence (e.g., Firestore region not europe-west2) so downstream agents do not have to re-derive.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-VALIDATION.md
@runbooks/firebase-oidc-bootstrap.md
@.planning/codebase/INTEGRATIONS.md
@index.html

<interfaces>
<!-- The pre-flight verifications produce inputs consumed by every later plan in this phase. -->
<!-- Format of the canonical 03-PREFLIGHT.md sections (consumers parse by ## heading): -->

## Firestore Region
- value: <region string e.g. europe-west2 / nam5>
- source: gcloud firestore databases describe --database='(default)' --project=bedeveloped-base-layers
- divergence_from_D06: <yes|no> — if yes, see CONTEXT.md addendum

## Firebase Project
- project_id: bedeveloped-base-layers
- default_hosting_site: <e.g. bedeveloped-base-layers> (firebase hosting:sites:list)
- default_url: <e.g. https://bedeveloped-base-layers.web.app>
- auth_domain: <e.g. bedeveloped-base-layers.firebaseapp.com>

## Registrar
- registrar: <Cloudflare | GoDaddy | Namecheap | other>
- ns_records: <output of dig +short NS bedeveloped.com>
- dns_admin_confirmed: <yes|no>

## OIDC Pool
- pool_name: github-actions
- pool_state: <ACTIVE|MISSING>
- provider: github-actions-provider
- workload_identity_provider_secret: <full resource path>
- service_account: <e.g. github-deployer@bedeveloped-base-layers.iam.gserviceaccount.com>

## OIDC SA Roles
- granted_roles: [list as returned by gcloud projects get-iam-policy]
- expected_baseline: roles/firebasehosting.admin, roles/cloudfunctions.developer, roles/iam.serviceAccountUser
- excess_roles_present: <yes|no>
- mitigation_for_T-3-5: <plain prose stating that excess roles will be reviewed in Phase 7 if any>

## index.html meta-CSP scan
- meta_csp_present: <yes|no>
- if yes: line number + tag content recorded; removal lands in 03-02-PLAN.md commit alongside firebase.json hosting headers

## dist/index.html font-CDN scan
- google_fonts_googleapis_present: <yes|no>
- google_fonts_gstatic_present: <yes|no>
- chartjs_cdn_present: <should be no — already self-hosted via npm in Phase 1>
- if any present: 03-02-PLAN.md adds matching origin to style-src/font-src/connect-src

## SDK 12.x connect-src verification
- verified_origins: [list]
- D07_list: [from CONTEXT.md D-07]
- additions_required: [diff]
- removals_safe: [diff]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run command-line pre-flights and record output verbatim</name>
  <read_first>
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md (§"Pre-Flight Verifications" — the 8-item list and §"Code Insights → Pre-Flight Verifications")
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md (§"Open Questions" — A1..A8; §"Assumptions Log")
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md (no analog exists for this task — pure verification)
    - runbooks/firebase-oidc-bootstrap.md (full file — extract pool name, provider name, service account name, expected role grants on lines covering `gcloud iam workload-identity-pools create` and `gcloud projects add-iam-policy-binding`)
    - .planning/codebase/INTEGRATIONS.md (project ID `bedeveloped-base-layers` confirmation)
    - index.html (full file — must verify in-source state of <meta http-equiv> tag)
    - package.json (confirm firebase devDep version is 12.12.1 — feeds SDK origin verification)
  </read_first>
  <action>
Create `.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md` with the canonical sections shown in the `<interfaces>` block of this plan.

Run each command below from the repo root. Capture the full stdout/stderr verbatim into the corresponding section of 03-PREFLIGHT.md. Run them in this order — items 1-3 are independent gcloud/firebase calls and can be run as a batch in parallel; items 4-7 depend on builds and reads and follow.

1. **Firestore region** — run `gcloud firestore databases describe --database="(default)" --project=bedeveloped-base-layers --format=json`. Extract `locationId` from JSON. Record under ## Firestore Region. Compare to `europe-west2` (D-06):
   - If equal: write "divergence_from_D06: no".
   - If different (e.g. `nam5`, `eur3`, `us-central`): write "divergence_from_D06: yes" + a single-paragraph note explaining that Phase 3 still pins the Cloud Function to `europe-west2` per D-06 (cross-region is acceptable for this single low-volume function), AND append the divergence to `03-CONTEXT.md` under a new section `## Pre-Flight Addendum (Wave 1)` so Phase 6 / Phase 11 know to re-open the residency conversation. Do NOT change D-06 — that is a locked decision; only document the divergence.
   - If gcloud is not installed or unauthenticated: do NOT skip. Surface this as a checkpoint blocker in Task 2 (manual gate) and record `value: UNVERIFIED — gcloud unavailable` along with the exact error.

2. **Firebase project + default URL** — run `npx firebase-tools@15.16.0 projects:list --json` and `npx firebase-tools@15.16.0 hosting:sites:list --project bedeveloped-base-layers --json`. Extract `projectId`, default site name, default URL (typically `https://<site>.web.app`), and the auth domain (typically `<projectId>.firebaseapp.com`). Record under ## Firebase Project.
   - The auth domain value populates D-09's `frame-src` directive used in 03-02-PLAN.md. If the auth domain is NOT `bedeveloped-base-layers.firebaseapp.com`, flag a BLOCKER for 03-02-PLAN.md so the firebase.json CSP directive picks up the actual value.

3. **OIDC pool + provider + service account** — run `gcloud iam workload-identity-pools list --location=global --project=bedeveloped-base-layers --format=json` and `gcloud iam workload-identity-pools providers list --workload-identity-pool=github-actions --location=global --project=bedeveloped-base-layers --format=json`. Then list IAM policy bindings on the deployer service account: `gcloud projects get-iam-policy bedeveloped-base-layers --format=json` and grep the bindings table for the deployer SA email documented in `runbooks/firebase-oidc-bootstrap.md`.
   - Record pool_state ACTIVE or MISSING under ## OIDC Pool.
   - Under ## OIDC SA Roles: list every role currently granted to the deployer SA. Compare against the expected baseline {roles/firebasehosting.admin, roles/cloudfunctions.developer, roles/iam.serviceAccountUser}.
   - If the runbook granted `roles/firebase.admin` (RESEARCH.md §"Codebase Findings → runbooks/firebase-oidc-bootstrap.md"): record `excess_roles_present: yes` + write a one-sentence mitigation note stating the over-grant is accepted for Phase 3 and will be narrowed in Phase 7 per the audit. This is the documented disposition for T-3-5.
   - If pool_state is MISSING: STOP. Surface a BLOCKER for 03-04-PLAN.md (CI deploy) — operator must execute `runbooks/firebase-oidc-bootstrap.md` end-to-end before Wave 3 can run.

4. **Registrar identification** — run `dig +trace bedeveloped.com NS` and `dig +short NS bedeveloped.com`. Record the NS records under ## Registrar. Map the NS provider to a registrar guess (e.g. `*.cloudflare.com` → Cloudflare, `*.domaincontrol.com` → GoDaddy, `*.registrar-servers.com` → Namecheap). Set `dns_admin_confirmed: pending` (Task 2 confirms with the user).

5. **index.html meta-CSP scan** — run `grep -n 'http-equiv' index.html || echo NONE`. Record under ## index.html meta-CSP scan. RESEARCH.md §"Codebase Findings" already states the result is `no` — this task confirms it has not changed. If a meta CSP is now present, record line number + content verbatim, and add a BLOCKER for 03-02-PLAN.md to remove it in the same commit as firebase.json hosting headers (atomic-commit per D-25 / T-3-7 mitigation).

6. **dist/index.html font-CDN scan** — run `npm run build` then `grep -nE 'fonts\.googleapis\.com|fonts\.gstatic\.com|cdn\.jsdelivr\.net' dist/index.html || echo NONE`. Record results under ## dist/index.html font-CDN scan. If `fonts.googleapis.com` or `fonts.gstatic.com` survive the Vite build, write a one-line directive override that 03-02-PLAN.md MUST apply: append `https://fonts.googleapis.com` to `style-src`, append `https://fonts.gstatic.com` to `font-src`. Do NOT modify firebase.json yet — that is 03-02-PLAN.md's job. This task only records the requirement.

7. **Firebase JS SDK 12.x connect-src verification** — open `node_modules/firebase/package.json` and confirm version 12.12.1. Then read `node_modules/@firebase/firestore/dist/index.node.cjs.js` (or whichever entry the package's `main` field points to) and grep for hard-coded URLs: `grep -onE 'https://[a-z0-9.-]+\\.googleapis\\.com|wss://[a-z0-9.-]+\\.firebaseio\\.com|https://[a-z0-9.-]+\\.firebaseio\\.com' node_modules/@firebase/firestore/dist/*.cjs.js node_modules/@firebase/auth/dist/*.cjs.js node_modules/@firebase/storage/dist/*.cjs.js node_modules/@firebase/installations/dist/*.cjs.js | sort -u`. Record the verified origin set under ## SDK 12.x connect-src verification. Diff against D-07's connect-src list:
   - For each verified origin not in D-07: add to `additions_required`.
   - For each D-07 origin not observed in SDK source: add to `removals_safe` (but RESEARCH.md §Firebase JS SDK 12.x connect-src Origin List recommends keeping `*.firebaseio.com` even though Firestore doesn't use it — record this as `removals_safe (but D-07 keeps for safety)`).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md'; const s=fs.readFileSync(p,'utf8'); const required=['## Firestore Region','## Firebase Project','## Registrar','## OIDC Pool','## OIDC SA Roles','## index.html meta-CSP scan','## dist/index.html font-CDN scan','## SDK 12.x connect-src verification']; const missing=required.filter(h=>!s.includes(h)); if(missing.length){console.error('MISSING SECTIONS',missing);process.exit(1)} console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md` exists.
    - File contains EXACTLY these eight `## ` headings: `## Firestore Region`, `## Firebase Project`, `## Registrar`, `## OIDC Pool`, `## OIDC SA Roles`, `## index.html meta-CSP scan`, `## dist/index.html font-CDN scan`, `## SDK 12.x connect-src verification`.
    - `## Firestore Region` section contains a key `locationId:` (or `value:`) with a non-empty value or the literal string `UNVERIFIED — gcloud unavailable`.
    - `## Firebase Project` contains keys `project_id: bedeveloped-base-layers` AND `default_url: https://...` AND `auth_domain: ...firebaseapp.com`.
    - `## OIDC Pool` contains either `pool_state: ACTIVE` or `pool_state: MISSING` AND if MISSING, a "BLOCKER for 03-04-PLAN.md" line.
    - `## OIDC SA Roles` lists at least one entry under `granted_roles:` AND a `excess_roles_present: yes|no` line.
    - `## index.html meta-CSP scan` contains `meta_csp_present: yes` or `meta_csp_present: no`.
    - `## dist/index.html font-CDN scan` contains `google_fonts_googleapis_present: yes|no` AND `google_fonts_gstatic_present: yes|no`.
    - `## SDK 12.x connect-src verification` contains a `verified_origins:` list AND at least one of `additions_required:` or `removals_safe:` (even if empty arrays).
    - If divergence_from_D06 is `yes`, file `03-CONTEXT.md` contains a new section `## Pre-Flight Addendum (Wave 1)` with the recorded divergence; verify with `grep -c "## Pre-Flight Addendum (Wave 1)" .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md`.
  </acceptance_criteria>
  <done>03-PREFLIGHT.md exists with all eight sections populated; any divergence from D-06 is mirrored into 03-CONTEXT.md addendum; any MISSING blockers are clearly tagged for the affected downstream plan number.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm registrar + DNS admin access + Firebase Console state</name>
  <read_first>
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md (the artefact Task 1 just produced — specifically ## Registrar, ## OIDC Pool sections)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md (§"Pre-Flight Verifications" items #3, #5)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md (§"DNS Cutover Specifics" — registrar TXT/A/AAAA requirements; §"Environment Availability" — "Domain registrar admin access: must be confirmed before cutover day")
    - runbooks/firebase-oidc-bootstrap.md (the runbook the user must confirm has been executed once already)
  </read_first>
  <what-built>
03-PREFLIGHT.md was just populated with command-line outputs in Task 1.
Three items in that file CANNOT be confirmed by Claude alone:
1. **Registrar identity + DNS admin access.** Task 1 recorded the NS provider guess from `dig`. Only the user can confirm "yes I have DNS admin login at <registrar> and can change A/AAAA records on `bedeveloped.com`."
2. **Firebase Console state.** Task 1 confirmed via CLI that the project + hosting site exist. Only the user can confirm Hosting is enabled in the Console UI for `bedeveloped-base-layers`. Without this the `firebase deploy --only hosting` call in Wave 3 fails with "Hosting site not initialised."
3. **OIDC Workload Identity pool.** Task 1 recorded `pool_state: ACTIVE` or `MISSING`. If MISSING, the user must execute `runbooks/firebase-oidc-bootstrap.md` end-to-end before Wave 3 (03-04-PLAN.md) can run. If ACTIVE, user confirms the GitHub repository secret names `GCP_WORKLOAD_IDENTITY_PROVIDER` and `GCP_SERVICE_ACCOUNT` are populated in https://github.com/lukebadiali/base-layers-diagnostic/settings/secrets/actions.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md` and read sections ## Registrar and ## OIDC Pool.
2. Confirm registrar — go to your DNS registrar's web UI for `bedeveloped.com`. Confirm you can edit A/AAAA records and that the NS records match what `dig` reported in Task 1. Reply with the registrar name (Cloudflare / GoDaddy / Namecheap / etc.) so it can be locked into 03-PREFLIGHT.md.
3. Confirm Firebase Console state — open https://console.firebase.google.com/project/bedeveloped-base-layers/hosting/sites. Confirm that a Hosting site exists with the default `web.app` URL recorded in `## Firebase Project`. Take the screenshot for the Phase 11 evidence pack (DOC-09) — save under `docs/evidence/phase-3-firebase-hosting-enabled.png` (file path noted in 03-PREFLIGHT.md but screenshot creation is part of Phase 11 — only confirm the Console state today).
4. Confirm OIDC pool state:
   - If Task 1 recorded `pool_state: ACTIVE`: open https://github.com/lukebadiali/base-layers-diagnostic/settings/secrets/actions and verify both repository secrets `GCP_WORKLOAD_IDENTITY_PROVIDER` and `GCP_SERVICE_ACCOUNT` exist (do NOT print their values; just confirm they exist).
   - If Task 1 recorded `pool_state: MISSING`: STOP. Run `runbooks/firebase-oidc-bootstrap.md` end-to-end in another terminal. Re-run Task 1 step 3 after the runbook completes. Re-enter this checkpoint with the updated 03-PREFLIGHT.md.
5. (Optional) If Task 1 recorded `divergence_from_D06: yes` (Firestore region is NOT europe-west2), confirm that you accept the cross-region note in 03-CONTEXT.md `## Pre-Flight Addendum (Wave 1)` — Phase 3 still pins the CF to europe-west2; Phase 6/11 will revisit residency.
  </how-to-verify>
  <resume-signal>Reply with: (a) registrar name (e.g. "Cloudflare"), (b) "Firebase Hosting site enabled in Console: yes|no", (c) "OIDC secrets present: yes|no" and "OIDC pool: ACTIVE|just-bootstrapped|need-help". If any answer blocks Wave 3, type "BLOCK Wave 3 — reason: ...". Otherwise type "approved".</resume-signal>
  <acceptance_criteria>
    - User has typed "approved" OR explicit per-item answers.
    - 03-PREFLIGHT.md ## Registrar section has `registrar: <name>` and `dns_admin_confirmed: yes` (Claude updates the file with the user's answers post-checkpoint, before declaring this task done).
    - 03-PREFLIGHT.md ## OIDC Pool section has `pool_state: ACTIVE` AND `github_secrets_present: yes`.
    - 03-PREFLIGHT.md contains a `firebase_hosting_console_enabled: yes` line under ## Firebase Project.
    - If user said "BLOCK Wave 3", a `BLOCKER for Wave 3:` line is appended to 03-PREFLIGHT.md AND ROADMAP.md is annotated with a `Phase 3 blocker:` line — execute-phase will then halt before reaching Wave 3.
  </acceptance_criteria>
  <done>User input recorded; 03-PREFLIGHT.md updated to reflect Console + secrets + registrar status; any blockers tagged so the orchestrator halts at Wave 3 boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → GCP API | Pre-flight gcloud calls cross this boundary; gcloud uses ADC (Application Default Credentials) tied to the operator. No untrusted input. |
| developer machine → DNS public | `dig` queries cross this boundary; only public DNS metadata returns. No secrets. |
| developer machine → npm registry | `npm run build` pulls from the locked package-lock.json; supply-chain controls (T-1-01) already mitigate. |
| developer machine → GitHub repo settings | Manual checkpoint Task 2 step 4 inspects (does not modify) repo secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-5 | Elevation of Privilege | OIDC service account over-grant (`roles/firebase.admin` instead of three narrow roles) | accept | Documented in 03-PREFLIGHT.md `## OIDC SA Roles → excess_roles_present: yes` with rationale that broader grant is acceptable for Phase 3 (single deploy SA, no per-function isolation required until Phase 7). Phase 7 narrows per FN-04 (per-function minimal-IAM service accounts). |
| T-3-7 | Tampering | Conflicting CSP source (meta tag + header) | mitigate | Pre-flight 5 confirms no `<meta http-equiv="Content-Security-Policy">` exists in `index.html`; if found, removal is forced into the same commit as 03-02-PLAN.md `firebase.json` hosting headers (atomic-commit per D-25). |
| T-3-2 | Tampering | Unknown SDK origins missing from CSP `connect-src` would cause silent feature breakage during the soak | mitigate | Pre-flight 7 produces a verified origin set; 03-02-PLAN.md applies `additions_required` before the Report-Only header ships. |
| T-3-OS-1 (pre-flight specific) | Information Disclosure | gcloud / firebase CLI leaking project IDs into logs | accept | Project ID `bedeveloped-base-layers` is not a secret; `.gitleaks.toml` allows commits referencing it. SA emails are not secrets either; only the WIF provider and signed tokens are. |
</threat_model>

<verification>
- 03-PREFLIGHT.md exists with all eight sections.
- All `[ASSUMED]` and `[Unknown]` markers in RESEARCH.md / CONTEXT.md have a corresponding entry in 03-PREFLIGHT.md (not necessarily resolved — UNVERIFIED is an acceptable answer if blocker is logged).
- If divergence_from_D06: 03-CONTEXT.md gains a `## Pre-Flight Addendum (Wave 1)` section.
- If OIDC pool MISSING: 03-PREFLIGHT.md has `BLOCKER for Wave 3` line; orchestrator must respect it.
- Manual checkpoint resolved with explicit user answer.
</verification>

<success_criteria>
- Every Open Question A1..A8 in RESEARCH.md has a recorded resolution in 03-PREFLIGHT.md.
- Every CONTEXT.md §"Pre-Flight Verifications" item (#1..#7 from the numbered list — #6 is the Wave 4 synthetic smoke, owned by 03-05-PLAN.md) is closed.
- T-3-5 disposition is documented.
- T-3-7 absence is confirmed (or removal task is queued in 03-02-PLAN.md).
- Downstream plans 02-06 can read 03-PREFLIGHT.md as their single source of truth for verified pre-flight state.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-cutover-baseline-security-headers/03-01-SUMMARY.md` with:
- Each pre-flight check resolution (8 sections, one line each).
- Any divergence-from-CONTEXT.md notes that propagate to downstream plans.
- Any BLOCKER lines that affect orchestrator wave gating.
- Specific input handed to 03-02 (font-CDN status, connect-src diff, project ID, auth domain).
- Specific input handed to 03-04 (OIDC pool state, secret names, role-grant disposition).
- Specific input handed to 03-05 (registrar name, DNS admin status).
</output>
