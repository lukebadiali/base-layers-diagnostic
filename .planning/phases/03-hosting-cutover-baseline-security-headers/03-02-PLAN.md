---
phase: 03-hosting-cutover-baseline-security-headers
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - firebase.json
  - .firebaserc
  - tests/firebase-config.test.js
  - tsconfig.json
  - SECURITY.md
autonomous: true
requirements:
  - HOST-01
  - HOST-03
  - HOST-04
  - HOST-05
  - DOC-10
tags:
  - phase-3
  - firebase-hosting
  - csp
  - security-headers
must_haves:
  truths:
    - "firebase.json declares the full HTTP security header set per D-13 (HSTS, X-CTO, Referrer-Policy, Permissions-Policy expanded list, COOP same-origin, COEP credentialless, CORP same-origin)"
    - "firebase.json declares Content-Security-Policy-Report-Only with the verbatim two-tier directive list per D-07 + Reporting-Endpoints + dual report-uri/report-to per D-08"
    - "firebase.json declares /api/csp-violations rewrite to function cspReportSink in europe-west2 BEFORE the SPA fallback ** rewrite (Pitfall 1 in RESEARCH.md / T-3-2 mitigation)"
    - "firebase.json declares cache-control overrides — index.html: no-cache; **/*.@(js|css|png|svg|woff2|ico): public, max-age=31536000, immutable"
    - ".firebaserc declares default project bedeveloped-base-layers"
    - "tests/firebase-config.test.js validates rewrite ordering AND header presence — exits 0 from `npm test`"
    - "Root tsconfig.json exclude array contains `functions/**` (was `functions/lib`) so root typecheck does not pick up real-TS function files in Wave 3"
    - "SECURITY.md gains §HTTP Security Headers section with rationale + framework citations (OWASP ASVS V14.4 / ISO 27001 A.13.1.3 / SOC2 CC6.6) — atomic commit with firebase.json"
  artifacts:
    - path: "firebase.json"
      provides: "Hosting + headers + rewrites + functions + emulators config"
      contains: "Strict-Transport-Security AND Content-Security-Policy-Report-Only AND /api/csp-violations rewrite AND cspReportSink AND europe-west2"
    - path: ".firebaserc"
      provides: "Default project mapping for firebase-tools"
      contains: "bedeveloped-base-layers"
    - path: "tests/firebase-config.test.js"
      provides: "Schema-validation guard that fails CI on rewrite-ordering regression or missing security header"
      contains: "/api/csp-violations AND content-security-policy-report-only AND fnIdx AND spaIdx"
    - path: "tsconfig.json"
      provides: "Root typecheck excludes functions/** so real-TS function files do not collide with JSDoc rules"
      contains: "functions/**"
    - path: "SECURITY.md"
      provides: "DOC-10 incremental: §HTTP Security Headers section"
      contains: "## § HTTP Security Headers AND OWASP ASVS L2 v5.0 V14.4 AND credentialless"
  key_links:
    - from: "firebase.json"
      to: "functions/src/csp/cspReportSink.ts (created in 03-03-PLAN.md)"
      via: "rewrites[0].function.functionId === cspReportSink AND rewrites[0].function.region === europe-west2"
      pattern: "\"functionId\":\\s*\"cspReportSink\""
    - from: "firebase.json"
      to: "Browser Reporting API"
      via: "Reporting-Endpoints header value names csp-endpoint AND CSP value contains report-uri /api/csp-violations AND report-to csp-endpoint"
      pattern: "report-uri /api/csp-violations.*report-to csp-endpoint"
    - from: "tests/firebase-config.test.js"
      to: "firebase.json"
      via: "fs.readFileSync(resolve(process.cwd(),'firebase.json'))"
      pattern: "process.cwd"
---

<objective>
Land the static configuration that turns Firebase Hosting into a production-grade serving substrate: `firebase.json` with the complete D-13 header set + the D-07 two-tier CSP Report-Only policy + the D-08 dual-reporting wiring + the rewrite that sends `/api/csp-violations` to the Cloud Function (built in 03-03-PLAN.md), plus `.firebaserc`, plus the schema-validation test that fails CI on header drop / rewrite reordering, plus the root tsconfig exclude expansion, plus the §HTTP Security Headers section in SECURITY.md.

Purpose: This plan ships the infrastructure that satisfies HOST-03 + HOST-04 the moment 03-04-PLAN.md's CI deploy job lands, and pins the rewrite ordering that makes HOST-05 / FN-10 reachable. T-3-1 (silent header drop) and T-3-2 (rewrite shadowing) are both mitigated here by the schema test plus the post-deploy curl assertion that 03-04-PLAN.md adds.

Output: `firebase.json`, `.firebaserc`, `tests/firebase-config.test.js`, modified `tsconfig.json`, and the §HTTP Security Headers section appended to `SECURITY.md` — all committed in two atomic commits per D-25 (one for the hosting config + SECURITY.md headers section; one for the test scaffolding + tsconfig exclude expansion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-VALIDATION.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md
@SECURITY.md
@tsconfig.json
@tests/setup.test.js
@tests/util/ids.test.js
@index.html

<interfaces>
<!-- 03-01-PLAN.md produces 03-PREFLIGHT.md which feeds this plan. Specifically: -->
<!-- - ## Firebase Project → project_id, default_url, auth_domain → .firebaserc + frame-src directive -->
<!-- - ## dist/index.html font-CDN scan → if google_fonts present, append style-src/font-src/connect-src origins -->
<!-- - ## SDK 12.x connect-src verification → additions_required → append to connect-src directive -->
<!-- - ## index.html meta-CSP scan → if present, removal lands in same commit as firebase.json -->

<!-- Verbatim baseline values that the executor MUST use (do NOT improvise): -->

Permissions-Policy header value (RESEARCH.md §Permissions-Policy Directive List + PATTERNS.md):
camera=(), microphone=(), geolocation=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=(), bluetooth=(), hid=(), midi=(), serial=(), display-capture=(), idle-detection=(), browsing-topics=(), interest-cohort=(), autoplay=(), encrypted-media=(), picture-in-picture=(), fullscreen=(), screen-wake-lock=(), web-share=()

Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
X-Content-Type-Options: nosniff
Referrer-Policy: strict-origin-when-cross-origin
Cross-Origin-Opener-Policy: same-origin
Cross-Origin-Embedder-Policy: credentialless
Cross-Origin-Resource-Policy: same-origin
Reporting-Endpoints: csp-endpoint="/api/csp-violations"

CSP-Report-Only directive (D-07 — single-line value; quotation in value strings is escaped per JSON):
default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; connect-src 'self' https://*.googleapis.com https://*.firebaseio.com wss://*.firebaseio.com https://firebasestorage.googleapis.com https://firebaseinstallations.googleapis.com https://identitytoolkit.googleapis.com https://securetoken.googleapis.com; frame-src https://bedeveloped-base-layers.firebaseapp.com; img-src 'self' data: https:; font-src 'self' data:; object-src 'none'; base-uri 'self'; form-action 'self'; frame-ancestors 'none'; upgrade-insecure-requests; report-uri /api/csp-violations; report-to csp-endpoint
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create firebase.json + .firebaserc with verified rewrite ordering</name>
  <read_first>
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md (D-04, D-04a, D-05, D-06, D-07, D-08, D-09, D-10, D-13)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md (§firebase.json Skeleton verified ordering, §Permissions-Policy Directive List, §Pitfall 1 rewrite ordering)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md (§firebase.json — copy-verbatim skeleton)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md (## Firebase Project for project_id + auth_domain; ## dist/index.html font-CDN scan for any font-src additions; ## SDK 12.x connect-src verification for any connect-src additions; ## index.html meta-CSP scan for the removal flag)
    - index.html (full file — must remove `<meta http-equiv="Content-Security-Policy">` if 03-PREFLIGHT.md flagged its presence)
  </read_first>
  <behavior>
    - Test 1: `firebase.json` parses as JSON.
    - Test 2: `firebase.json.hosting.public === "dist"`.
    - Test 3: First entry of `firebase.json.hosting.rewrites` has `source: "/api/csp-violations"` AND `function.functionId: "cspReportSink"` AND `function.region: "europe-west2"`.
    - Test 4: Second entry of `firebase.json.hosting.rewrites` has `source: "**"` AND `destination: "/index.html"`.
    - Test 5: `firebase.json.hosting.headers` first entry (source `**`) contains all 9 header keys: Strict-Transport-Security, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Embedder-Policy, Cross-Origin-Resource-Policy, Reporting-Endpoints, Content-Security-Policy-Report-Only.
    - Test 6: HSTS value === `max-age=63072000; includeSubDomains; preload` (verbatim).
    - Test 7: COEP value === `credentialless`.
    - Test 8: CSP-Report-Only value contains `frame-ancestors 'none'` AND `report-uri /api/csp-violations` AND `report-to csp-endpoint` AND `frame-src https://bedeveloped-base-layers.firebaseapp.com`.
    - Test 9: index.html cache override exists with `Cache-Control: no-cache`; hashed-asset cache override exists with `public, max-age=31536000, immutable`.
    - Test 10: `.firebaserc` parses as JSON and contains `projects.default === "bedeveloped-base-layers"`.
    - Test 11: If 03-PREFLIGHT.md flagged Google Fonts present in `dist/index.html`, the CSP `style-src` directive contains `https://fonts.googleapis.com` AND `font-src` contains `https://fonts.gstatic.com`.
  </behavior>
  <action>
Step A — Read 03-PREFLIGHT.md and extract:
- `auth_domain` from ## Firebase Project (default expected: `bedeveloped-base-layers.firebaseapp.com`)
- `additions_required` from ## SDK 12.x connect-src verification (each origin appended to the connect-src directive verbatim)
- `google_fonts_googleapis_present` and `google_fonts_gstatic_present` from ## dist/index.html font-CDN scan
- `meta_csp_present` from ## index.html meta-CSP scan

Step B — If `meta_csp_present === yes`: open `index.html`, remove the `<meta http-equiv="Content-Security-Policy" content="...">` tag, and stage `index.html` for the SAME commit as `firebase.json` (atomic D-25 / T-3-7 mitigation). If `meta_csp_present === no`: skip; index.html is not modified.

Step C — Build the CSP-Report-Only directive value:
- Start with the verbatim D-07 single-line value from the `<interfaces>` block above.
- If `google_fonts_googleapis_present === yes`: change `style-src 'self' 'unsafe-inline'` → `style-src 'self' 'unsafe-inline' https://fonts.googleapis.com`.
- If `google_fonts_gstatic_present === yes`: change `font-src 'self' data:` → `font-src 'self' data: https://fonts.gstatic.com`.
- For each origin in `additions_required`: append to `connect-src` directive (after the existing tokens, before the `;` separator).
- If `auth_domain !== bedeveloped-base-layers.firebaseapp.com`: replace the host in the `frame-src https://bedeveloped-base-layers.firebaseapp.com` token with the actual auth_domain. Otherwise leave the verbatim D-07 value.
- Do NOT add nonces, hashes, or strict-dynamic — D-07 explicitly excludes these for Phase 3.

Step D — Create `firebase.json` at the repo root with this EXACT structure (top-level keys: hosting, functions, emulators). The hosting object MUST have keys in this order: public, ignore, headers, rewrites. Within `headers`, the first object MUST have `source: "**"` and the `headers` array MUST contain the 9 keys above in this declaration order: Strict-Transport-Security, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Embedder-Policy, Cross-Origin-Resource-Policy, Reporting-Endpoints, Content-Security-Policy-Report-Only. Then a second header block with `source: "index.html"` and value `Cache-Control: no-cache`. Then a third header block with `source: "**/*.@(js|css|png|svg|woff2|ico)"` and value `Cache-Control: public, max-age=31536000, immutable`. The `rewrites` array MUST have `/api/csp-violations` (functionId: cspReportSink, region: europe-west2) BEFORE `**` (destination: /index.html). The `functions` array has one entry with `source: "functions"`, `codebase: "default"`, and `ignore: ["node_modules", ".git", "firebase-debug.log", "firebase-debug.*.log", "*.local.json"]`. Emulators block: hosting port 5002, functions port 5001, ui enabled port 4000.

Use the verbatim Permissions-Policy value from the `<interfaces>` block above (22-directive list including the deprecated `interest-cohort` AND its replacement `browsing-topics`, plus the full hardware/media set). Do NOT use the smaller 9-directive list shown in CONTEXT.md D-13 — that is superseded by RESEARCH.md §Permissions-Policy Directive List per the planner's discretion call.

Step E — Create `.firebaserc` at the repo root with verbatim content:
```json
{
  "projects": {
    "default": "bedeveloped-base-layers"
  }
}
```

Step F — Validate JSON: `node -e "JSON.parse(require('fs').readFileSync('firebase.json','utf8'))"` AND `node -e "JSON.parse(require('fs').readFileSync('.firebaserc','utf8'))"` must both exit 0.

Step G — Stage and commit. Two atomic commits per D-25:
- Commit 1: `firebase.json` + `.firebaserc` + (if applicable) `index.html` meta-CSP removal + the §HTTP Security Headers section in SECURITY.md (added in Task 3 — but Task 3 runs in this plan; the executor reorders as needed so SECURITY.md edit lands in the SAME commit as firebase.json).
- Commit 2: `tests/firebase-config.test.js` + tsconfig.json exclude expansion (Task 2).

Use the commit message format: `feat(03-02): land firebase.json hosting headers + CSP report-only` followed by a body listing the directive groups + the SECURITY.md section reference.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cfg=JSON.parse(fs.readFileSync('firebase.json','utf8'));const rc=JSON.parse(fs.readFileSync('.firebaserc','utf8'));const rw=cfg.hosting.rewrites;const hh=cfg.hosting.headers[0].headers;const keys=hh.map(h=>h.key);const csp=hh.find(h=>h.key==='Content-Security-Policy-Report-Only').value;const checks=[['public',cfg.hosting.public==='dist'],['rewrite0',rw[0].source==='/api/csp-violations'&&rw[0].function.functionId==='cspReportSink'&&rw[0].function.region==='europe-west2'],['rewrite1',rw[1].source==='**'&&rw[1].destination==='/index.html'],['rewriteOrder',rw.findIndex(r=>r.source==='/api/csp-violations')<rw.findIndex(r=>r.source==='**')],['hsts',hh.find(h=>h.key==='Strict-Transport-Security').value==='max-age=63072000; includeSubDomains; preload'],['coep',hh.find(h=>h.key==='Cross-Origin-Embedder-Policy').value==='credentialless'],['xcto',hh.find(h=>h.key==='X-Content-Type-Options').value==='nosniff'],['cspFrameAnc',csp.includes(\"frame-ancestors 'none'\")],['cspReportUri',csp.includes('report-uri /api/csp-violations')],['cspReportTo',csp.includes('report-to csp-endpoint')],['cspFrameSrc',csp.includes('frame-src https://')],['allHeaders',['Strict-Transport-Security','X-Content-Type-Options','Referrer-Policy','Permissions-Policy','Cross-Origin-Opener-Policy','Cross-Origin-Embedder-Policy','Cross-Origin-Resource-Policy','Reporting-Endpoints','Content-Security-Policy-Report-Only'].every(k=>keys.includes(k))],['firebaserc',rc.projects.default==='bedeveloped-base-layers']];const failed=checks.filter(([k,v])=>!v).map(([k])=>k);if(failed.length){console.error('FAILED',failed);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `firebase.json` exists at repo root.
    - `firebase.json` parses as valid JSON.
    - `firebase.json.hosting.public === "dist"`.
    - `firebase.json.hosting.rewrites[0].source === "/api/csp-violations"` AND `rewrites[0].function.functionId === "cspReportSink"` AND `rewrites[0].function.region === "europe-west2"`.
    - `firebase.json.hosting.rewrites[1].source === "**"` AND `rewrites[1].destination === "/index.html"`.
    - `firebase.json.hosting.headers[0].source === "**"` AND its `headers` array contains exactly these 9 keys: Strict-Transport-Security, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Cross-Origin-Opener-Policy, Cross-Origin-Embedder-Policy, Cross-Origin-Resource-Policy, Reporting-Endpoints, Content-Security-Policy-Report-Only.
    - HSTS value verbatim equals `max-age=63072000; includeSubDomains; preload`.
    - COOP value verbatim equals `same-origin`. COEP value verbatim equals `credentialless`. CORP value verbatim equals `same-origin`.
    - Reporting-Endpoints value verbatim equals `csp-endpoint="/api/csp-violations"`.
    - CSP-Report-Only value contains substring `frame-ancestors 'none'`, substring `report-uri /api/csp-violations`, substring `report-to csp-endpoint`, substring `frame-src https://`, substring `default-src 'self'`, substring `object-src 'none'`, substring `base-uri 'self'`, substring `upgrade-insecure-requests`.
    - File `.firebaserc` exists at repo root and `JSON.parse(content).projects.default === "bedeveloped-base-layers"`.
    - If `03-PREFLIGHT.md ## index.html meta-CSP scan` flagged `meta_csp_present: yes`: `grep -c http-equiv index.html` returns 0.
    - Permissions-Policy value contains `browsing-topics=()` AND contains `interest-cohort=()` AND contains `bluetooth=()`.
  </acceptance_criteria>
  <done>firebase.json + .firebaserc committed (with index.html meta-CSP removal if applicable) in a single atomic commit; the verify command above exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema-validation test for firebase.json + tsconfig.json exclude expansion</name>
  <read_first>
    - tests/util/ids.test.js (lines 1-12 — JS+JSDoc style + `// @ts-check` header pattern)
    - tests/setup.test.js (lines 1-24 — vitest import shape under root config)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md (§tests/firebase-config.test.js — full pattern, copy verbatim adjusted)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md (§Functions Workspace Config Gotchas → "Root tsconfig.json exclusion" — `functions/lib` → `functions/**`)
    - tsconfig.json (current state — line 19 has `"functions/lib"`)
    - firebase.json (just created in Task 1 — the file under test)
  </read_first>
  <behavior>
    - Test 1: rewrite array contains `/api/csp-violations` source AND `**` source.
    - Test 2: index of `/api/csp-violations` rewrite < index of `**` rewrite (T-3-2 mitigation — order pinned).
    - Test 3: each of the 9 required header keys appears in the `source: "**"` headers block (T-3-1 mitigation — silent header drop guard).
    - Test 4: HSTS includeSubDomains AND preload tokens present.
    - Test 5: CSP-RO directive contains `frame-ancestors 'none'`, `report-uri /api/csp-violations`, `report-to csp-endpoint`.
    - Test 6: hosting.public === "dist".
    - Test 7: function rewrite functionId === "cspReportSink" AND region === "europe-west2".
    - Test 8: `tsc --noEmit` (root) passes — i.e. tsconfig exclude correctly skips `functions/**`.
  </behavior>
  <action>
Step A — Create `tests/firebase-config.test.js` with this exact structure:

```javascript
// @ts-check
// Phase 3 (HOST-01, HOST-03, HOST-04, HOST-05): schema-validate firebase.json
// Guards against (a) silent header drop (T-3-1) and (b) rewrite reordering that
// would shadow the function rewrite behind the SPA fallback (T-3-2).
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

const cfg = JSON.parse(
  readFileSync(resolve(process.cwd(), "firebase.json"), "utf-8"),
);
const rewrites = cfg.hosting.rewrites;
const globalHeaders = cfg.hosting.headers.find((h) => h.source === "**")?.headers ?? [];
const headerByKey = (key) =>
  globalHeaders.find((h) => h.key.toLowerCase() === key.toLowerCase());

describe("firebase.json — hosting basics", () => {
  it("public is set to dist", () => {
    expect(cfg.hosting.public).toBe("dist");
  });
});

describe("firebase.json — rewrite ordering (T-3-2 mitigation)", () => {
  it("function rewrite for /api/csp-violations appears BEFORE the SPA fallback **", () => {
    const fnIdx = rewrites.findIndex((r) => r.source === "/api/csp-violations");
    const spaIdx = rewrites.findIndex((r) => r.source === "**");
    expect(fnIdx).toBeGreaterThanOrEqual(0);
    expect(spaIdx).toBeGreaterThanOrEqual(0);
    expect(fnIdx).toBeLessThan(spaIdx);
  });

  it("function rewrite points at cspReportSink in europe-west2", () => {
    const fn = rewrites.find((r) => r.source === "/api/csp-violations");
    expect(fn?.function?.functionId).toBe("cspReportSink");
    expect(fn?.function?.region).toBe("europe-west2");
  });

  it("SPA fallback destination is /index.html", () => {
    const spa = rewrites.find((r) => r.source === "**");
    expect(spa?.destination).toBe("/index.html");
  });
});

describe("firebase.json — security headers present (T-3-1 mitigation)", () => {
  it.each([
    "Strict-Transport-Security",
    "X-Content-Type-Options",
    "Referrer-Policy",
    "Permissions-Policy",
    "Cross-Origin-Opener-Policy",
    "Cross-Origin-Embedder-Policy",
    "Cross-Origin-Resource-Policy",
    "Reporting-Endpoints",
    "Content-Security-Policy-Report-Only",
  ])("header '%s' is present in the source:** block", (key) => {
    expect(headerByKey(key)).toBeDefined();
  });
});

describe("firebase.json — header values (HSTS preload + COEP + CSP)", () => {
  it("HSTS includes max-age, includeSubDomains, and preload tokens", () => {
    const v = headerByKey("Strict-Transport-Security")?.value ?? "";
    expect(v).toMatch(/max-age=63072000/);
    expect(v).toContain("includeSubDomains");
    expect(v).toContain("preload");
  });

  it("COOP is same-origin and COEP is credentialless", () => {
    expect(headerByKey("Cross-Origin-Opener-Policy")?.value).toBe("same-origin");
    expect(headerByKey("Cross-Origin-Embedder-Policy")?.value).toBe("credentialless");
  });

  it("CSP Report-Only contains frame-ancestors 'none' and dual reporting", () => {
    const v = headerByKey("Content-Security-Policy-Report-Only")?.value ?? "";
    expect(v).toContain("frame-ancestors 'none'");
    expect(v).toContain("report-uri /api/csp-violations");
    expect(v).toContain("report-to csp-endpoint");
  });

  it("Reporting-Endpoints names csp-endpoint pointing at /api/csp-violations", () => {
    const v = headerByKey("Reporting-Endpoints")?.value ?? "";
    expect(v).toContain('csp-endpoint="/api/csp-violations"');
  });
});
```

Step B — Edit `tsconfig.json`. Replace the existing `"functions/lib"` entry in the `exclude` array with `"functions/**"`. Do not change any other key. The full new exclude array becomes:
```
"exclude": [
  "node_modules",
  "dist",
  "functions/**",
  "coverage",
  "vite.config.js",
  "eslint.config.js",
  "tests/fixtures/_generators",
  "tests/crypto-parity.test.js"
]
```

Step C — Run `npm test -- --run tests/firebase-config.test.js`. All assertions must pass. If they fail, the firebase.json from Task 1 is wrong; fix Task 1 (do NOT loosen the assertions to make them pass — that violates §0(4) of SECURITY_AUDIT.md "no weakening of controls to make tests pass").

Step D — Run `npm run typecheck`. Must exit 0. If `tsc --noEmit` complains about JSDoc errors in `tests/firebase-config.test.js`, the `// @ts-check` annotation requires `globalHeaders` and `headerByKey` to be typed — the version above is already JSDoc-clean.

Step E — Commit as a separate atomic commit from Task 1: `test(03-02): schema-validate firebase.json + expand tsconfig exclude` with body explaining T-3-1 + T-3-2 mitigation.
  </action>
  <verify>
    <automated>npm test -- --run tests/firebase-config.test.js && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/firebase-config.test.js` exists.
    - `tests/firebase-config.test.js` first line is `// @ts-check`.
    - `tests/firebase-config.test.js` second line begins with `// Phase 3 (`.
    - File contains `process.cwd()` (CI-safe path resolution).
    - File contains `it.each([` with all 9 header keys in the table.
    - File contains assertion `expect(fnIdx).toBeLessThan(spaIdx)`.
    - `tsconfig.json` exclude array contains `"functions/**"` AND does NOT contain `"functions/lib"` (replaced).
    - `npm test -- --run tests/firebase-config.test.js` exits 0.
    - `npm run typecheck` exits 0.
  </acceptance_criteria>
  <done>tests/firebase-config.test.js committed; tsconfig.json updated; both verify commands exit 0.</done>
</task>

<task type="auto">
  <name>Task 3: SECURITY.md §HTTP Security Headers section (DOC-10 atomic with Task 1 commit)</name>
  <read_first>
    - SECURITY.md (full file — locate the existing section structure starting at §Build & Supply Chain lines 19-75 and the stub `### § CSP & Headers — *TODO Phases 3 & 10*` near line 235)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md (D-15 — three sections, this task lands the first; Section §Content Security Policy (Report-Only) is staged here too because it lands atomically with firebase.json per D-25; Section §Hosting & Deployment is owned by 03-04-PLAN.md)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md (§SECURITY.md — three new sections — the section shape pattern from existing §Build & Supply Chain)
    - .planning/research/SUMMARY.md §"Compliance Mapping Cheat-Sheet" (citation values for OWASP ASVS V14.4, ISO 27001:2022 A.13.1.3, SOC2 CC6.6, GDPR Art. 32)
  </read_first>
  <action>
Open SECURITY.md. Find the `### § CSP & Headers — *TODO Phases 3 & 10*` stub line (RESEARCH.md says ~line 235; verify with grep before edit). Replace the stub with TWO new top-level sections:

**Section A — "## § HTTP Security Headers"** (D-15 part 1):

```markdown
## § HTTP Security Headers

**Control:** Production traffic to `https://baselayers.bedeveloped.com` is served by Firebase Hosting with a static set of HTTP security response headers configured via `firebase.json hosting.headers` on the `**` source. The header set hardens the boundary against header-stripping downgrade attacks (HSTS), MIME-sniffing tampering (X-Content-Type-Options), referrer leakage (Referrer-Policy), high-risk web platform APIs in unaudited contexts (Permissions-Policy), cross-origin window-handle abuse (COOP), cross-origin embedding without isolation (COEP), and cross-origin resource reads (CORP). Cross-Origin-Embedder-Policy is set to `credentialless` rather than `require-corp` because Firebase CDN-hosted resources do not emit `Cross-Origin-Resource-Policy: cross-origin` and the credentialless variant tolerates that without breaking Storage downloads or Auth popups (Phase 6 verifies this assumption against real Auth flows).

**Evidence:**

- Header configuration: `firebase.json` `hosting.headers[0]` (source: `**`) — Phase 3 Plan 02 commit ${commit-sha-of-Task-1}
- Schema validation: `tests/firebase-config.test.js` — guards against silent header drop (T-3-1) on every `npm test` run
- Post-deploy assertion: `.github/workflows/ci.yml` `deploy` job step "Assert security headers" (Phase 3 Plan 04) — fails the deploy if any of HSTS / X-CTO / Referrer-Policy / Permissions-Policy / COOP / COEP / CSP-RO is missing from the live response
- Header values:
  - `Strict-Transport-Security: max-age=63072000; includeSubDomains; preload` (HSTS preload submission deferred to Phase 10 / HOST-06 — gated by ≥7-day stable policy)
  - `X-Content-Type-Options: nosniff`
  - `Referrer-Policy: strict-origin-when-cross-origin`
  - `Permissions-Policy: camera=(), microphone=(), geolocation=(), payment=(), usb=(), magnetometer=(), gyroscope=(), accelerometer=(), bluetooth=(), hid=(), midi=(), serial=(), display-capture=(), idle-detection=(), browsing-topics=(), interest-cohort=(), autoplay=(), encrypted-media=(), picture-in-picture=(), fullscreen=(), screen-wake-lock=(), web-share=()` (FLoC's `interest-cohort` retained for backward compatibility alongside its replacement `browsing-topics`)
  - `Cross-Origin-Opener-Policy: same-origin`
  - `Cross-Origin-Embedder-Policy: credentialless`
  - `Cross-Origin-Resource-Policy: same-origin`

**Framework citations:**

- OWASP ASVS L2 v5.0 V14.4 — HTTP Security Headers
- ISO/IEC 27001:2022 A.13.1.3 — Segregation in networks (header-enforced same-origin boundary)
- SOC 2 CC6.6 — Logical access security boundaries
```

**Section B — "## § Content Security Policy (Report-Only)"** (D-15 part 2 — lands here because it is atomic with firebase.json per D-25, even though the report sink it points at is built in 03-03-PLAN.md):

```markdown
## § Content Security Policy (Report-Only)

**Control:** A two-tier CSP is shipped in `Content-Security-Policy-Report-Only` mode this phase. The tight tier covers `script-src`, `connect-src`, `frame-src`, `object-src`, `base-uri`, `form-action`, and `frame-ancestors 'none'`. The temporary permissive tier is `style-src 'self' 'unsafe-inline'` — Phase 4 sweeps inline `style="..."` strings across the views (CODE-06 / M5) and Phase 10 (HOST-07) drops `'unsafe-inline'` to enforce the strict policy. Reports are dual-channeled — legacy `report-uri /api/csp-violations` plus modern `Reporting-Endpoints` / `report-to csp-endpoint`, both pointing at the same same-origin path served by the `cspReportSink` Cloud Function (built in Phase 3 Plan 03; deployed by Phase 3 Plan 04 / 05). The function filters extension and synthetic origins and dedupes within a 5-minute in-memory window before emitting structured `severity=WARNING` entries to Cloud Logging.

**Evidence:**

- Policy configuration: `firebase.json` `hosting.headers[0]` (source: `**`) — header keys `Reporting-Endpoints` + `Content-Security-Policy-Report-Only` — Phase 3 Plan 02 commit ${commit-sha-of-Task-1}
- Report sink: `functions/src/csp/cspReportSink.ts` (Phase 3 Plan 03)
- Filter and dedup: `functions/src/csp/{filter,dedup,normalise}.ts` (Phase 3 Plan 03)
- Schema validation: `tests/firebase-config.test.js` asserts dual-reporting tokens are present (T-3-1 + T-3-2 mitigation)
- Soak window: starts on Phase 3 cutover; ends when Phase 10 enforces (CSP enforcement lives at HOST-07)
- Note: The `csp-violations` endpoint is the **only** unauthenticated public Cloud Function in the milestone; every other callable enforces App Check + claims-based auth from Phase 7 onward. Browsers POST CSP reports natively without App Check tokens, so D-12 limits abuse protection to content-type allowlist + 64 KB body cap.

**Framework citations:**

- OWASP ASVS L2 v5.0 V14.4 — HTTP Security Headers
- ISO/IEC 27001:2022 A.13.1 — Network security management
- SOC 2 CC6.6 — Logical access security boundaries
- GDPR Art. 32(1)(b) — Confidentiality of processing systems and services
```

Stage SECURITY.md and include it in the SAME commit as `firebase.json` (Task 1 Step G commit 1 — D-25 atomic-commit pattern). The §Hosting & Deployment section is added by 03-04-PLAN.md atomically with the CI deploy job change.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('SECURITY.md','utf8');const need=['## § HTTP Security Headers','## § Content Security Policy (Report-Only)','OWASP ASVS L2 v5.0 V14.4','ISO/IEC 27001:2022 A.13.1.3','SOC 2 CC6.6','GDPR Art. 32','credentialless','frame-ancestors','browsing-topics','report-to csp-endpoint','Phase 3 Plan 02','Phase 3 Plan 03'];const missing=need.filter(t=>!s.includes(t));if(s.includes('TODO Phases 3 & 10')){console.error('TODO stub still present');process.exit(1)}if(missing.length){console.error('MISSING',missing);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md contains the literal heading `## § HTTP Security Headers`.
    - SECURITY.md contains the literal heading `## § Content Security Policy (Report-Only)`.
    - SECURITY.md does NOT contain the stub line `### § CSP & Headers — *TODO Phases 3 & 10*`.
    - SECURITY.md contains all four citation tokens: `OWASP ASVS L2 v5.0 V14.4`, `ISO/IEC 27001:2022 A.13.1.3`, `SOC 2 CC6.6`, `GDPR Art. 32`.
    - SECURITY.md contains `credentialless` and `frame-ancestors` and `browsing-topics` and `report-to csp-endpoint` substrings.
    - SECURITY.md contains the cross-references `Phase 3 Plan 02` AND `Phase 3 Plan 03`.
    - SECURITY.md edits are in the same commit as `firebase.json` (verify via `git log -1 --name-only` showing both files).
  </acceptance_criteria>
  <done>Two SECURITY.md sections appended atomically with firebase.json; verify command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Firebase Hosting CDN | TLS-terminated; CDN injects headers from firebase.json; rewrites match before path strikes function/static. |
| Firebase Hosting CDN → cspReportSink Cloud Function | `/api/csp-violations` rewrite; same-origin from the browser's perspective; built in 03-03-PLAN.md. |
| Browser DOM → CSP enforcement layer | Report-Only mode means the CSP advises but does not block; enforcement is Phase 10. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-1 | Tampering | Header drop / silent weakening — typo in `firebase.json` headers causes Firebase to silently NOT serve a header | mitigate | `tests/firebase-config.test.js` `it.each` over all 9 header keys runs on every `npm test` (CI gate). 03-04-PLAN.md adds the post-deploy `curl -I` assertion that fails the deploy job if any expected header is missing from the live response. Manual smoke (HOST-08): securityheaders.com rates ≥A. |
| T-3-2 | Tampering | Misordered firebase.json rewrites — `/api/csp-violations` shadowed by `**` SPA fallback | mitigate | `tests/firebase-config.test.js` asserts `fnIdx < spaIdx`. 03-05-PLAN.md's pre-cutover synthetic smoke (POST a real CSP report and verify it arrives in Cloud Logging within 30s) catches the live-traffic version of this regression. |
| T-3-7 | Tampering | Conflicting `<meta http-equiv="Content-Security-Policy">` tag in `index.html` co-existing with header CSP yields undefined browser behaviour | mitigate | 03-01-PLAN.md Wave 1 pre-flight detects presence; if found, removal is forced into the same commit as `firebase.json` (Task 1 Step B above). 03-PREFLIGHT.md ## index.html meta-CSP scan records the disposition either way. |
| T-3-6 | Tampering | Deprecated/missing Permissions-Policy directive — `interest-cohort` deprecated; missing hardware/media directives leave default-on | mitigate | The Permissions-Policy value used in Task 1 (verbatim from RESEARCH.md §Permissions-Policy Directive List, NOT the smaller list in CONTEXT.md D-13) extends to 23 directives covering FLoC's `browsing-topics` replacement plus full hardware/media set (`bluetooth`, `hid`, `midi`, `serial`, `display-capture`, `idle-detection`, `autoplay`, `encrypted-media`, `picture-in-picture`, `screen-wake-lock`, `web-share`, `fullscreen`). |
| T-3-3 (partial) | Information Disclosure / DoS | Abuse of un-authed `/api/csp-violations` endpoint by malicious flooding | accept (Phase 3 baseline); mitigate (in 03-03-PLAN.md function code) | Phase 3 baseline is content-type + 64 KB body cap (D-12). Per-IP rate limit deferred to Phase 7. SECURITY.md §CSP section above explicitly documents the un-authed exemption + escalation gate. |
</threat_model>

<verification>
- `firebase.json` exists and `npm test -- --run tests/firebase-config.test.js` exits 0.
- `npm run typecheck` exits 0 (validates tsconfig exclude expansion).
- `grep -c "## § HTTP Security Headers" SECURITY.md` returns 1.
- `grep -c "## § Content Security Policy (Report-Only)" SECURITY.md` returns 1.
- `git log -1 --name-only` for the firebase.json commit includes SECURITY.md (D-25 atomic-commit verification).
- 03-PREFLIGHT.md font-CDN scan + connect-src additions correctly applied to firebase.json CSP value.
</verification>

<success_criteria>
- `firebase.json` declares the full D-13 header set with the expanded 22-directive Permissions-Policy.
- `firebase.json` declares the D-07 Report-Only CSP with dual-reporting (D-08) and the D-09 frame-src auth popup origin.
- Rewrite ordering is provably correct (test asserts; T-3-2 mitigated).
- `tsconfig.json` excludes `functions/**` so 03-03-PLAN.md's TS files do not collide with root JSDoc typecheck.
- SECURITY.md has §HTTP Security Headers + §CSP (Report-Only) sections atomic with the firebase.json commit (DOC-10 D-25 pattern).
- T-3-1, T-3-2, T-3-6, T-3-7 explicitly mitigated; T-3-3 documented and escalated.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-cutover-baseline-security-headers/03-02-SUMMARY.md` with:
- firebase.json shape (hosting.public, header keys, rewrite ordering verbatim)
- .firebaserc content
- tests/firebase-config.test.js coverage list (9 header keys + rewrite order + 4 value-format assertions)
- tsconfig.json change (line N: `"functions/lib"` → `"functions/**"`)
- SECURITY.md sections added (with line numbers)
- Atomic commit list: 2 commits (Task 1+3 atomic; Task 2 separate) — record SHAs
- Hand-off to 03-03-PLAN.md: `cspReportSink` is now contractually expected at `region: europe-west2`, `functionId: cspReportSink`
- Hand-off to 03-04-PLAN.md: post-deploy curl assertion must check exactly the 9 header keys this plan landed
</output>
