---
phase: 03-hosting-cutover-baseline-security-headers
plan: 06
type: execute
wave: 6  # sequential after Wave 5 cutover; depends_on 03-04 + 03-05 enforces ordering. Executor (gsd-sdk phase-plan-index) accepts arbitrary integer waves and sorts numerically — verified against ~/.claude/get-shit-done/workflows/execute-phase.md + bin/lib/phase.cjs (parseInt(fm.wave,10)).
depends_on:
  - 03-04
  - 03-05
files_modified:
  - runbooks/phase-4-cleanup-ledger.md
  - runbooks/branch-protection-bootstrap.md
  - SECURITY.md
autonomous: false
requirements:
  - HOST-01
  - HOST-08
  - DOC-10
tags:
  - phase-3
  - cleanup-ledger
  - branch-protection
  - phase-close-out
must_haves:
  truths:
    - "runbooks/phase-4-cleanup-ledger.md gains a row 'Phase 3 GH-Pages rollback substrate' under §Out-of-band soft-fail entries with the exact day-14 deletion date computed from cutover_date in 03-PREFLIGHT.md ## Cutover Log"
    - "runbooks/phase-4-cleanup-ledger.md gains a row 'Phase 3 — meta-CSP regression guard' tracking the forward-looking concern that Phase 4's modular split should add a CI check ensuring `<meta http-equiv=Content-Security-Policy>` is not re-introduced into index.html (T-3-meta-csp-conflict mitigation)"
    - "runbooks/branch-protection-bootstrap.md is updated with two new required-status-checks: `Deploy to Firebase Hosting` and `Deploy PR Preview Channel` — added AFTER the first green deploy run registered the names in GitHub's check registry (Pitfall A pattern)"
    - "SECURITY.md gains a final §Compliance Citations Polish pass — every Phase 3 framework citation (OWASP ASVS V14.4 / V14.7, ISO 27001 A.13.1.3 / A.13.1 / A.5.7, SOC2 CC6.6 / CC8.1, GDPR Art. 32) is cross-referenced from a single ## § Phase 3 Audit Index sub-block to make the §Hosting & Deployment + §HTTP Security Headers + §CSP citations easy to walk for an auditor"
    - "Phase 3 SECURITY.md sections (§HTTP Security Headers, §CSP Report-Only, §Hosting & Deployment) all link to commit SHAs from Plans 02 + 04 — fills in the ${commit-sha-of-Task-1} placeholders with real SHAs"
  artifacts:
    - path: "runbooks/phase-4-cleanup-ledger.md"
      provides: "Two new soft-fail rows; updated §Out-of-band soft-fail entries table"
      contains: "Phase 3 GH-Pages rollback substrate AND Phase 3 — meta-CSP regression guard"
    - path: "runbooks/branch-protection-bootstrap.md"
      provides: "Updated required-status-checks list with Deploy + Preview entries (post first-green-run pattern)"
      contains: "Deploy to Firebase Hosting AND Deploy PR Preview Channel"
    - path: "SECURITY.md"
      provides: "Phase 3 Audit Index sub-block + commit-SHA backfill in §HTTP Security Headers / §CSP / §Hosting & Deployment"
      contains: "## § Phase 3 Audit Index AND ${commit-sha pattern absent}"
  key_links:
    - from: "runbooks/phase-4-cleanup-ledger.md (Phase 3 row)"
      to: "runbooks/hosting-cutover.md §Day-14 Cleanup"
      via: "cleanup ledger row points at the runbook section that describes the actual deletion commands"
      pattern: "Day-14 Cleanup"
    - from: "runbooks/branch-protection-bootstrap.md"
      to: ".github/workflows/ci.yml deploy + preview jobs"
      via: "required-status-checks registers `Deploy to Firebase Hosting` and `Deploy PR Preview Channel` job names"
      pattern: "Deploy to Firebase Hosting"
    - from: "SECURITY.md ## § Phase 3 Audit Index"
      to: "Phase 11 evidence pack (DOC-09 owned)"
      via: "audit index lists every framework citation Phase 3 closed; Phase 11 walks the index to populate CONTROL_MATRIX.md"
      pattern: "OWASP ASVS L2 v5.0 V14.4"
---

<objective>
Close out Phase 3 by completing three deferred housekeeping items that could not land before the cutover succeeded:

1. **Cleanup ledger entries.** Add a row tracking the GH-Pages rollback substrate deletion (calendar-driven, cutover_date + 14d), and a row tracking the forward-looking T-3-meta-csp-conflict concern that Phase 4's modular split should guard against re-introducing a meta CSP tag.
2. **Branch protection bootstrap.** Per Pitfall A from Phase 1 D-12, the `deploy` and `preview` job check names cannot be added to GitHub's required-status-checks until they have run at least once. The first green deploy run from 03-04-PLAN.md (which preceded the cutover in 03-05-PLAN.md) has now registered the check names; this plan adds them to the runbook so they get applied to branch protection in the next routine sync.
3. **SECURITY.md polish pass.** The §HTTP Security Headers, §CSP Report-Only, and §Hosting & Deployment sections (landed in 03-02 and 03-04) reference `${commit-sha-of-Task-1}` placeholders. Backfill the actual commit SHAs and add a final §Phase 3 Audit Index sub-block that lists every framework citation Phase 3 closed in one place — so a Phase 11 / Phase 12 auditor walk can hit one ## heading and find every Phase 3 control.

Purpose: Phase 3 close-out hygiene. None of these items are blocking the milestone, but each one lands a load-bearing cross-reference (cleanup ledger → day-14 deletion; branch protection → CI required-status; SECURITY.md → audit index) that Phase 11 / Phase 12 / future-phase planners will look for. T-3-4 (stale GH-Pages substrate accidentally re-enabled) and T-3-meta-csp-conflict mitigations are formalised here.

Output: Updated `runbooks/phase-4-cleanup-ledger.md` (2 new rows), updated `runbooks/branch-protection-bootstrap.md` (2 new required-status-check entries), and SECURITY.md edits backfilling commit SHAs + adding the §Phase 3 Audit Index. Two atomic commits per D-25.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-RESEARCH.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-PATTERNS.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-VALIDATION.md
@.planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md
@runbooks/phase-4-cleanup-ledger.md
@runbooks/hosting-cutover.md
@SECURITY.md

<interfaces>
<!-- Existing cleanup ledger §Out-of-band soft-fail entries table columns (lines 53-58 of runbooks/phase-4-cleanup-ledger.md): -->
<!-- | Source | Why soft-fail | Re-evaluation date | Hardening target | -->

<!-- Two new rows to add: -->
<!-- Row 1: Phase 3 GH-Pages rollback substrate -->
<!-- Row 2: Phase 3 meta-CSP regression guard -->

<!-- Existing branch protection runbook (need to read it first before edit) -->
<!-- New required-status-checks: -->
<!-- - "Deploy to Firebase Hosting" (the `name:` field of the deploy job in ci.yml) -->
<!-- - "Deploy PR Preview Channel" (the `name:` field of the preview job in ci.yml) -->

<!-- SECURITY.md placeholders to backfill: -->
<!-- - In §HTTP Security Headers: `${commit-sha-of-Task-1}` → SHA of 03-02-PLAN.md Task 1 commit -->
<!-- - In §CSP (Report-Only): `${commit-sha-of-Task-1}` → SHA of 03-02-PLAN.md Task 1 commit (same — atomic) -->
<!-- - In §Hosting & Deployment: `${commit-sha-of-this-task}` → SHA of 03-04-PLAN.md Task 1 commit -->

<!-- Phase 3 Audit Index sub-block to add: a flat bullet list mapping each framework citation -->
<!-- to the SECURITY.md section + ci.yml-or-firebase.json line that satisfies it -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cleanup ledger rows + update branch-protection runbook</name>
  <read_first>
    - runbooks/phase-4-cleanup-ledger.md (full file — line 53-58 contains the existing §Out-of-band soft-fail entries table; new rows append there)
    - runbooks/branch-protection-bootstrap.md (full file — locate the required-status-checks list; this plan appends two entries)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-PREFLIGHT.md (## Cutover Log → cutover_date — used to compute day-14 deletion date)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-CONTEXT.md (D-03 — 14-day rollback window; T-3-4 — stale GH-Pages substrate threat)
    - runbooks/hosting-cutover.md (§Day-14 Cleanup — the row in the cleanup ledger points at this section)
  </read_first>
  <action>
**Step A — Cleanup ledger updates.**

Read 03-PREFLIGHT.md `## Cutover Log` section and extract `cutover_date`. Compute `day_14_deletion_date = cutover_date + 14 days` (use the actual calendar arithmetic; if cutover_date is `2026-05-08` the deletion date is `2026-05-22`).

> **ROLLBACK branch:** If 03-PREFLIGHT.md `## Cutover Log` records `cutover_complete: rolled-back` (set by 03-05 Task 3 on rollback), do NOT compute a real `day_14_deletion_date`. Instead, write the cleanup ledger row's "Re-evaluation date" cell as the literal string `TBD — cutover rolled back; update when cutover succeeds`. The deletion is gated on a successful future cutover; until then the GH-Pages substrate must remain available indefinitely as the production fallback. Re-open this row in the next phase 3 retry plan.

Open `runbooks/phase-4-cleanup-ledger.md`. Locate the §Out-of-band soft-fail entries table (line 53-58). Append two new rows AFTER the existing rows:

```markdown
| Phase 3 GH-Pages rollback substrate (gh-pages branch + any Pages workflow file)            | D-03 — 14-day rollback retention; the substrate is dormant (Pages disabled in repo settings) but re-deployable | <day_14_deletion_date> | Delete `gh-pages` branch (`git push origin --delete gh-pages`) + any Pages workflow file in `.github/workflows/`; commit `chore(03): delete GH-Pages rollback substrate (day 14 — cutover_date + 14)`; close this row. T-3-4 mitigation. |
| Phase 3 — meta-CSP regression guard                                                         | Phase 3 confirmed `index.html` has no `<meta http-equiv="Content-Security-Policy">` tag (T-3-7). Phase 4's modular split touches `index.html`; without a CI guard the tag could be re-introduced silently and conflict with the header CSP. | Phase 4 (CODE-01) | Add a CI test (`tests/index-html-meta-csp.test.js`) that fails if `index.html` contains `http-equiv="Content-Security-Policy"`. Mirror the schema-test pattern from `tests/firebase-config.test.js`. Close this row when Phase 4 lands the test. |
```

Substitute `<day_14_deletion_date>` with the computed date (e.g. `2026-05-22`).

**Step B — Branch protection runbook update.**

Open `runbooks/branch-protection-bootstrap.md`. Locate the required-status-checks list (the runbook documents the GitHub repo Settings → Branches → Branch protection rules → Require status checks to pass before merging configuration). The list will contain entries like `Lint`, `Typecheck`, `Test`, `Security Audit`, `Build` from Phase 1.

Append two new entries to the list:

```markdown
- `Deploy to Firebase Hosting` (added Phase 3 — D-14; the `name:` field of the `deploy` job in `.github/workflows/ci.yml`. Pitfall A pattern: this name was registered in GitHub's check registry by the first green deploy run on <first-deploy-run-date>; safe to add to required-status-checks now.)
- `Deploy PR Preview Channel` (added Phase 3 — D-14; the `name:` field of the `preview` job. Pitfall A pattern: registered by the first green PR preview run.)
```

Look up the actual first-green-deploy-run date by:
1. Read 03-PREFLIGHT.md `## Cutover Log` for cutover_date (the cutover prerequisites required at least one green deploy before that date).
2. Or run `gh run list --workflow=ci.yml --branch=main --status=success --limit=10 --json databaseId,createdAt,name | head` and pick the earliest run where the `deploy` job appears.

Document the run ID in the runbook entry above for traceability — replace `<first-deploy-run-date>` with the actual ISO date.

If `runbooks/branch-protection-bootstrap.md` does NOT exist (Phase 1 might have used a different filename — Phase 1 D-12 references `branch-protection-bootstrap.md` per Phase 1 SUMMARY, but verify): create the file with this minimal structure:

```markdown
# Branch Protection Bootstrap Runbook

> Pitfall A pattern: required-status-checks must be added AFTER the first green run of each check.

## Required Status Checks (apply to `main`)

The following CI job names must be added to the branch protection rule on `main`:

- `Lint` (Phase 1 D-12, registered in run #25317482833)
- `Typecheck` (Phase 1 D-12)
- `Test` (Phase 1 D-12)
- `Security Audit` (Phase 1 D-12)
- `Build` (Phase 1 D-12)
- `Deploy to Firebase Hosting` (added Phase 3 — D-14; the `name:` field of the `deploy` job in `.github/workflows/ci.yml`. Registered by first green deploy run on <first-deploy-run-date> — run #<run-id>.)
- `Deploy PR Preview Channel` (added Phase 3 — D-14; registered by first green preview run.)

## Apply via GitHub UI

1. GitHub repo → Settings → Branches → Branch protection rules → Edit rule for `main`.
2. Under "Require status checks to pass before merging", add each check name above.
3. Save.

## Apply via gh CLI (optional)

`gh api -X PUT /repos/lukebadiali/base-layers-diagnostic/branches/main/protection ...` (full JSON omitted; use the GitHub API docs for branch protection — the relevant key is `required_status_checks.contexts`).
```

**Step C — Commit Step A and Step B atomically.**

`feat(03-06): cleanup ledger + branch protection close-out (HOST-01, HOST-08)`. Body lists the two ledger rows + the two required-status-checks added.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const ledger=fs.readFileSync('runbooks/phase-4-cleanup-ledger.md','utf8');const bp=fs.readFileSync('runbooks/branch-protection-bootstrap.md','utf8');const checks=[['ledger_GH_Pages',ledger.includes('Phase 3 GH-Pages rollback substrate')],['ledger_meta_csp',ledger.includes('Phase 3 — meta-CSP regression guard')||ledger.includes('Phase 3 - meta-CSP regression guard')||ledger.includes('Phase 3 meta-CSP regression guard')],['bp_deploy',bp.includes('Deploy to Firebase Hosting')],['bp_preview',bp.includes('Deploy PR Preview Channel')],['bp_pitfall_a',/Pitfall A/.test(bp)],['ledger_t34',ledger.includes('T-3-4')||ledger.includes('day 14')]];const failed=checks.filter(([,v])=>!v).map(([k])=>k);if(failed.length){console.error('FAILED',failed);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `runbooks/phase-4-cleanup-ledger.md` contains the row text `Phase 3 GH-Pages rollback substrate`.
    - `runbooks/phase-4-cleanup-ledger.md` row references `T-3-4` mitigation (substring `T-3-4` OR `day 14`).
    - `runbooks/phase-4-cleanup-ledger.md` row's "Re-evaluation date" column matches EITHER a concrete ISO date `YYYY-MM-DD` (computed as cutover_date + 14 days when `cutover_complete: yes`) OR the literal string `TBD — cutover rolled back` (when `cutover_complete: rolled-back`). Verify with `grep -E '(20[0-9]{2}-[0-9]{2}-[0-9]{2}|TBD — cutover rolled back)' runbooks/phase-4-cleanup-ledger.md`.
    - `runbooks/phase-4-cleanup-ledger.md` contains a row with text `Phase 3 — meta-CSP regression guard` (or hyphen variant).
    - `runbooks/phase-4-cleanup-ledger.md` meta-CSP row's "Hardening target" column references `tests/index-html-meta-csp.test.js` (the proposed test file name).
    - `runbooks/branch-protection-bootstrap.md` exists.
    - `runbooks/branch-protection-bootstrap.md` contains substrings `Deploy to Firebase Hosting` AND `Deploy PR Preview Channel`.
    - `runbooks/branch-protection-bootstrap.md` references `Pitfall A`.
    - Commit message mentions both ledger and branch-protection updates.
  </acceptance_criteria>
  <done>Two ledger rows + two branch-protection entries committed atomically; ISO date computed from cutover_date.</done>
</task>

<task type="auto">
  <name>Task 2: SECURITY.md polish — backfill commit SHAs + add §Phase 3 Audit Index</name>
  <read_first>
    - SECURITY.md (full file — locate the three Phase 3 sections: ## § HTTP Security Headers, ## § Content Security Policy (Report-Only), ## § Hosting & Deployment; each has a `${commit-sha-of-Task-1}` or `${commit-sha-of-this-task}` placeholder)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-02-SUMMARY.md (the SUMMARY from Plan 02 — it records the commit SHAs for §HTTP Security Headers + §CSP atomic commits)
    - .planning/phases/03-hosting-cutover-baseline-security-headers/03-04-SUMMARY.md (the SUMMARY from Plan 04 — records the commit SHA for §Hosting & Deployment)
    - .planning/research/SUMMARY.md §"Compliance Mapping Cheat-Sheet" (the canonical citation list — confirm framework version strings match)
  </read_first>
  <action>
**Step A — Look up commit SHAs.**

Run `git log --all --oneline -- SECURITY.md | head -20` to surface the recent commits touching SECURITY.md. Identify:
- The commit that landed §HTTP Security Headers + §Content Security Policy (Report-Only) (atomic with firebase.json from 03-02-PLAN.md Task 1) — search for `feat(03-02): land firebase.json hosting headers + CSP report-only` or grep the commit message via `git log --grep='03-02.*hosting headers'`.
- The commit that landed §Hosting & Deployment (atomic with ci.yml deploy job from 03-04-PLAN.md Task 1) — search for `feat(03-04): CI deploy + preview jobs`.

Capture the short SHA (7 chars) of each.

If the SUMMARY files (03-02-SUMMARY.md / 03-04-SUMMARY.md) record the SHAs, use those — they were captured at execution time and are authoritative. Otherwise use the git log search.

**Step B — Backfill SECURITY.md placeholders.**

Open SECURITY.md. Find each occurrence of `${commit-sha-of-Task-1}` (in §HTTP Security Headers and §Content Security Policy (Report-Only)) and replace with the 03-02 commit SHA. Find each occurrence of `${commit-sha-of-this-task}` (in §Hosting & Deployment) and replace with the 03-04 commit SHA. The expected substrings post-replace look like:

```
- Header configuration: `firebase.json` `hosting.headers[0]` (source: `**`) — Phase 3 Plan 02 commit a1b2c3d
```

(With `a1b2c3d` being the actual short SHA.)

**Step C — Add §Phase 3 Audit Index sub-block.**

After the §Hosting & Deployment section, add a new sub-block:

```markdown
## § Phase 3 Audit Index

This is a one-stop pointer for an auditor walking Phase 3's controls. Each row maps a framework citation to (a) the SECURITY.md section that documents it, (b) the artefact that implements it, and (c) the test / smoke / runbook step that verifies it.

| Framework | Citation | Phase 3 Section | Implemented in | Verified by |
|-----------|----------|-----------------|----------------|-------------|
| OWASP ASVS L2 v5.0 | V14.4 — HTTP Security Headers | § HTTP Security Headers | `firebase.json` `hosting.headers[0]` | `tests/firebase-config.test.js` (commit-time) + ci.yml `deploy` job "Assert security headers" step (deploy-time) + securityheaders.com manual smoke per `runbooks/hosting-cutover.md` Step 6 (cutover-time) |
| OWASP ASVS L2 v5.0 | V14.4 — Content Security Policy | § Content Security Policy (Report-Only) | `firebase.json` `Content-Security-Policy-Report-Only` header value + dual `Reporting-Endpoints` / `report-uri` / `report-to csp-endpoint` + `functions/src/csp/cspReportSink.ts` | `tests/firebase-config.test.js` (header presence) + `functions/test/csp/*.test.ts` (filter/dedup/normalise unit tests) + 03-PREFLIGHT.md ## Smoke Result (Wave 4 synthetic E2E) |
| OWASP ASVS L2 v5.0 | V14.7 — Build & Deploy Pipeline | § Hosting & Deployment | `.github/workflows/ci.yml` `deploy` + `preview` jobs via OIDC WIF | First-green-CI-run + post-deploy header assertion + `runbooks/firebase-oidc-bootstrap.md` |
| ISO/IEC 27001:2022 | A.13.1.3 — Segregation in networks (header-enforced same-origin boundary) | § HTTP Security Headers | `Cross-Origin-Opener-Policy: same-origin` + `Cross-Origin-Embedder-Policy: credentialless` + `Cross-Origin-Resource-Policy: same-origin` | `tests/firebase-config.test.js` `it.each` over header keys |
| ISO/IEC 27001:2022 | A.13.1 — Network security management | § Content Security Policy (Report-Only) | CSP-RO + dual reporting | Synthetic smoke + soak window |
| ISO/IEC 27001:2022 | A.5.7 — Threat intelligence (cloud services governance) | § Hosting & Deployment | OIDC WIF (no long-lived JSON keys) + per-PR preview channels with 7d expiry | `runbooks/firebase-oidc-bootstrap.md` + `runbooks/hosting-cutover.md` |
| SOC 2 | CC6.6 — Logical access security boundaries | § HTTP Security Headers + § CSP (Report-Only) | Header-set + CSP enforcement substrate | Schema test + post-deploy assertion + securityheaders.com rating ≥ A |
| SOC 2 | CC8.1 — Change management | § Hosting & Deployment | OIDC-authenticated CI deploy from main + per-PR preview channels + 14-day GH-Pages rollback retention | `.github/workflows/ci.yml` + `runbooks/hosting-cutover.md` + `runbooks/phase-4-cleanup-ledger.md` Phase 3 row |
| GDPR | Art. 32(1)(b) — Confidentiality of processing systems and services | § Content Security Policy (Report-Only) | CSP-RO + same-origin report sink (no cross-origin data leak channel) | Synthetic smoke confirms reports stay same-origin; SECURITY.md documents un-authed exemption rationale |

**Cross-phase references this index will plug into:**

- Phase 10 (CSP enforcement / HOST-06 / HOST-07) — drops `'unsafe-inline'` from `style-src`, submits HSTS preload; updates §CSP and §HTTP Security Headers
- Phase 11 (Evidence Pack / DOC-09) — `docs/CONTROL_MATRIX.md` row-per-control walks this index; screenshots for `docs/evidence/`
- Phase 12 (Audit Walkthrough) — `SECURITY_AUDIT_REPORT.md` Pass / Partial / N/A entries cite this index

**Forward-looking concerns recorded in `runbooks/phase-4-cleanup-ledger.md`:**

- T-3-4 mitigation: GH-Pages rollback substrate deleted on cutover_date + 14 days
- T-3-meta-csp-conflict: Phase 4 must add `tests/index-html-meta-csp.test.js` to prevent regression
```

**Step D — Commit atomically.**

`docs(03-06): SECURITY.md commit-SHA backfill + Phase 3 Audit Index (DOC-10 close-out)`. Body explains the polish pass + lists the index rows.

If 03-02-SUMMARY.md or 03-04-SUMMARY.md don't exist yet (because plans haven't executed), this task should still proceed using `git log --grep` to find the SHAs. The acceptance criteria below verify the placeholders are gone, regardless of whether the SUMMARYs exist.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('SECURITY.md','utf8');const checks=[['noPlaceholderTask1',!s.includes('${commit-sha-of-Task-1}')],['noPlaceholderThisTask',!s.includes('${commit-sha-of-this-task}')],['auditIndex',s.includes('## § Phase 3 Audit Index')],['indexHasASVS144',/V14\\.4.*HTTP Security Headers|V14\\.4.*Content Security Policy/.test(s)],['indexHasASVS147',/V14\\.7.*Build & Deploy/.test(s)],['indexHasISOA1313',/A\\.13\\.1\\.3/.test(s)],['indexHasA57',/A\\.5\\.7/.test(s)],['indexHasCC66',/CC6\\.6/.test(s)],['indexHasCC81',/CC8\\.1/.test(s)],['indexHasGDPR32',/Art\\. 32/.test(s)],['indexHasT34',s.includes('T-3-4')||s.includes('GH-Pages rollback substrate deleted')],['indexHasMetaCSP',s.includes('meta-csp')||s.includes('meta-CSP')||s.includes('http-equiv')||s.includes('index-html-meta-csp.test.js')]];const failed=checks.filter(([,v])=>!v).map(([k])=>k);if(failed.length){console.error('FAILED',failed);process.exit(1)}console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md does NOT contain the substring `${commit-sha-of-Task-1}`.
    - SECURITY.md does NOT contain the substring `${commit-sha-of-this-task}`.
    - SECURITY.md contains a 7-character (or longer) hex SHA where each placeholder used to be (verify by surrounding context — `Phase 3 Plan 02 commit ` followed by 7+ hex chars; same for Plan 04).
    - SECURITY.md contains heading `## § Phase 3 Audit Index`.
    - SECURITY.md Audit Index lists ALL of: V14.4, V14.7, A.13.1.3 (or A.13.1), A.5.7, CC6.6, CC8.1, Art. 32.
    - SECURITY.md Audit Index references the cross-phase plug-ins (Phase 10, Phase 11, Phase 12).
    - SECURITY.md Audit Index references both forward-looking concerns (T-3-4 day-14 + T-3-meta-csp-conflict).
    - Commit message identifies the polish pass + the audit-index addition.
  </acceptance_criteria>
  <done>SECURITY.md placeholders backfilled; §Phase 3 Audit Index added; verify command exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Apply branch protection updates in GitHub UI</name>
  <read_first>
    - runbooks/branch-protection-bootstrap.md (just updated in Task 1 — read the §Required Status Checks list)
    - .github/workflows/ci.yml (confirm the deploy job `name: Deploy to Firebase Hosting` and preview job `name: Deploy PR Preview Channel` strings — these MUST match the runbook entries verbatim)
  </read_first>
  <what-built>
- 03-04-PLAN.md added `deploy` and `preview` jobs to ci.yml; the first push of that commit triggered them; first green run is now in GitHub's check registry.
- Task 1 above updated `runbooks/branch-protection-bootstrap.md` listing the new check names.
- This checkpoint applies the runbook update via the GitHub UI — adds `Deploy to Firebase Hosting` and `Deploy PR Preview Channel` to the required-status-checks for `main`.
- Pitfall A is satisfied: the names exist in the registry (because at least one run completed); GitHub will accept them now.
  </what-built>
  <how-to-verify>
1. Open https://github.com/lukebadiali/base-layers-diagnostic/settings/branches.
2. Edit the existing branch protection rule for `main`.
3. Under "Require status checks to pass before merging", search for and add:
   - `Deploy to Firebase Hosting`
   - `Deploy PR Preview Channel`
4. The existing required checks (`Lint`, `Typecheck`, `Test`, `Security Audit`, `Build`) remain unchanged.
5. Save.
6. Confirm the rule now lists 7 required checks (5 existing + 2 new).
7. Reply with confirmation (`branch-protection-applied: yes`) or with `branch-protection-skipped: <reason>` if for some reason the names are not yet showing in the search dropdown (Pitfall A timing — GitHub sometimes lags 5-10 minutes after first run; retry).
  </how-to-verify>
  <resume-signal>Reply with one of:
- `branch-protection-applied: yes` — Plan 06 wraps up; Phase 3 close-out complete.
- `branch-protection-skipped: <reason>` — runbook is updated, applying the rule deferred to a later session; record reason in the runbook and ROADMAP.
  </resume-signal>
  <acceptance_criteria>
    - User has typed `branch-protection-applied: yes` OR `branch-protection-skipped: <reason>`.
    - If `applied`: nothing further to commit; Phase 3 close-out is complete.
    - If `skipped`: a one-line note added to `runbooks/branch-protection-bootstrap.md` recording the deferral reason and a new ROADMAP annotation `Phase 3 close-out deferred: branch protection apply` so future-session orchestrator picks it up.
  </acceptance_criteria>
  <done>User confirms branch protection applied OR deferral logged; Phase 3 close-out logged either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → GitHub repo settings | Manual UI session; operator applies branch protection per the runbook. |
| Operator → calendar | Day-14 deletion is a calendar-driven follow-up; the cleanup ledger is the durable reminder. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-4 | Tampering / Availability | Stale GH-Pages serving substrate left rollback-deployable for 14 days could be re-enabled accidentally (or by attacker if repo write access is compromised) | mitigate (calendar-driven) | Cleanup ledger row Phase 3 records the explicit deletion date (cutover_date + 14d). The dormant gh-pages branch + Pages workflow are visible in repo so any accidental re-enable is auditable; deletion on day 14 closes the window. T-1-02 (gitleaks pre-commit) AND T-1-04 (CI gitleaks) are already in place from Phase 1 — even if the dormant branch were re-activated, secrets cannot land. |
| T-3-meta-csp-conflict | Tampering | Phase 4's modular split could re-introduce a `<meta http-equiv="Content-Security-Policy">` tag in `index.html` that conflicts with header CSP | mitigate (forward-looking; cleanup ledger row points at Phase 4 work) | Cleanup ledger row Phase 3 — meta-CSP regression guard explicitly tasks Phase 4 with adding `tests/index-html-meta-csp.test.js`. The hardening target is documented; Phase 4 closes the row by landing the test. |
| T-3-bp-pitfall-a | Availability | Adding `deploy` / `preview` to required-status-checks BEFORE the first green run would block the next PR merge with "expected check 'Deploy ...' has not been reported" — regression to the Phase 1 D-12 problem | mitigate | Pitfall A pattern explicitly followed: branch protection update lands AFTER the first green run from 03-04 has registered the name in GitHub's check registry. The runbook update happens in this plan; the actual UI apply (Task 3) is a checkpoint. |
| T-3-audit-index-stale | Information Disclosure (low) | The Phase 3 Audit Index could go stale if Phase 10 changes Phase 3 controls without updating the index | accept | The audit index points to Phase 10 / Phase 11 / Phase 12 as the cross-phase plug-ins. Phase 10's planning has the explicit task to update `## § Phase 3 Audit Index` when CSP enforcement lands. The forward-looking concerns in the index (T-3-4 day-14, T-3-meta-csp-conflict) make the index self-checking — if those rows are still open, the index is current; if both are closed, the index needs a Phase 10 / Phase 11 update commit. |
</threat_model>

<verification>
- `runbooks/phase-4-cleanup-ledger.md` has the two new Phase 3 rows.
- `runbooks/branch-protection-bootstrap.md` lists the two new required-status-checks.
- SECURITY.md has no `${commit-sha-...}` placeholders left.
- SECURITY.md has the §Phase 3 Audit Index sub-block.
- Branch protection applied (or deferral logged).
</verification>

<success_criteria>
- Phase 3 close-out housekeeping complete: cleanup ledger informs Phase 4 of two follow-ups; branch protection runbook is current; SECURITY.md is auditor-ready.
- T-3-4 has a calendar-driven mitigation tracked.
- T-3-meta-csp-conflict has a Phase 4 forward-task tracked.
- DOC-10 incremental owner-handoff to Phase 11: the audit index is the canonical Phase 3 entry-point for `docs/CONTROL_MATRIX.md` row-per-control walks.
- ROADMAP.md still shows Phase 3 plans 1-6 status; Phase 3 success criteria from the roadmap (#1-#5) are all closed: hosting cuts over, headers + CSP shipped, CI deploys, securityheaders rates ≥ A.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hosting-cutover-baseline-security-headers/03-06-SUMMARY.md` with:
- Cleanup ledger rows added (with Re-evaluation dates)
- Branch protection runbook updates (check names added; first-green-run date recorded)
- Branch protection apply state (applied | deferred — with reason)
- SECURITY.md polish: SHA backfill (3 placeholders → 2 unique commit SHAs), §Phase 3 Audit Index added (9 framework citation rows)
- Phase 3 close-out checklist:
  - [ ] HOST-01 (hosting cuts over) — closed Plan 05
  - [ ] HOST-02 (custom domain SSL) — closed Plan 05
  - [ ] HOST-03 (security headers) — closed Plan 02
  - [ ] HOST-04 (CSP report-only) — closed Plan 02
  - [ ] HOST-05 (csp-violations endpoint) — closed Plan 03 (build) + Plan 05 (deployed + smoke)
  - [ ] HOST-08 (CI deploy + preview channels + securityheaders ≥ A) — closed Plan 04 (CI) + Plan 05 (manual smoke)
  - [ ] FN-10 (csp-violations endpoint paired with HOST-05) — closed Plan 03 + 05
  - [ ] DOC-10 (incremental SECURITY.md updates) — closed Plans 02 + 04 + 06
- Phase 3 forward concerns to Phase 4 / Phase 10 / Phase 11 / Phase 12 (all recorded in cleanup ledger or audit index)
</output>
