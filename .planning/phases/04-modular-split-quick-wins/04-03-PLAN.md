---
phase: 04-modular-split-quick-wins
plan: 03
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - src/data/orgs.js
  - src/data/users.js
  - src/data/roadmaps.js
  - src/data/funnels.js
  - src/data/funnel-comments.js
  - src/data/allowlist.js
  - src/data/responses.js
  - src/data/comments.js
  - src/data/actions.js
  - src/data/documents.js
  - src/data/messages.js
  - src/data/audit-events.js
  - src/cloud/audit.js
  - src/cloud/soft-delete.js
  - src/cloud/gdpr.js
  - src/cloud/claims-admin.js
  - src/cloud/retry.js
  - src/observability/sentry.js
  - src/observability/audit-events.js
  - tests/data/orgs.test.js
  - tests/data/users.test.js
  - tests/data/roadmaps.test.js
  - tests/data/funnels.test.js
  - tests/data/funnel-comments.test.js
  - tests/data/allowlist.test.js
  - tests/data/responses.test.js
  - tests/data/comments.test.js
  - tests/data/actions.test.js
  - tests/data/documents.test.js
  - tests/data/messages.test.js
  - tests/data/audit-events.test.js
  - tests/cloud/audit.test.js
  - tests/cloud/soft-delete.test.js
  - tests/cloud/gdpr.test.js
  - tests/cloud/claims-admin.test.js
  - tests/cloud/retry.test.js
  - tests/observability/sentry.test.js
  - tests/observability/audit-events.test.js
  - eslint.config.js
  - app.js
  - runbooks/phase-4-cleanup-ledger.md
autonomous: true
requirements:
  - CODE-01
  - CODE-02
  - DOC-10
must_haves:
  truths:
    - "All 12 data/*.js wrappers exist (orgs/users/roadmaps/funnels/funnel-comments/allowlist as full owners; responses/comments/actions/documents/messages/audit-events as Phase-5-rewrite-target pass-throughs)"
    - "Each data/*.js exports Promise CRUD (get/list/save/delete) + subscribe* helpers per D-10"
    - "All 5 cloud/*.js stubs exist (audit/soft-delete/gdpr/claims-admin/retry — empty no-op seams Phase 7/8 fills)"
    - "Both observability/*.js stubs exist (sentry — Phase 9; audit-events — Phase 7/9 constants table)"
    - "ESLint Wave 3 flip: data/* may only import from src/firebase/db.js + src/firebase/storage.js + src/util/* + src/domain/* — direct firebase/firestore|storage SDK paths blocked"
    - "tests/mocks/firebase.js mock factory retargets to src/firebase/db.js path; existing tests/data/cloud-sync.test.js + tests/data/migration.test.js pattern preserved as the analog"
    - "app.js IIFE imports the 6 full-owner data wrappers; the 6 pass-through wrappers are imported by Wave 4 views as they extract"
    - "Phase 2 snapshot baselines (dashboard/diagnostic/report) produce zero diff"
    - "runbooks/phase-4-cleanup-ledger.md gains 13 forward-tracking rows: 6 Phase-5 pass-through closure rows (D-09) + 5 Phase-7/8 cloud/* body rows (D-11) + 2 Phase-7/9 observability/* body rows (D-11)"
  artifacts:
    - path: "src/data/orgs.js"
      provides: "Full CRUD + subscribeOrgs (D-09 owner per D-10 Promise CRUD + subscribe shape)"
      exports: ["getOrg", "listOrgs", "saveOrg", "deleteOrg", "subscribeOrgs"]
    - path: "src/data/users.js"
      provides: "Full CRUD + subscribeUsers"
      exports: ["getUser", "listUsers", "saveUser", "deleteUser", "subscribeUsers"]
    - path: "src/data/roadmaps.js"
      provides: "Full CRUD + subscribeRoadmaps"
      exports: ["getRoadmap", "listRoadmaps", "saveRoadmap", "deleteRoadmap", "subscribeRoadmaps"]
    - path: "src/data/funnels.js"
      provides: "Full CRUD + subscribeFunnels"
      exports: ["getFunnel", "listFunnels", "saveFunnel", "deleteFunnel", "subscribeFunnels"]
    - path: "src/data/funnel-comments.js"
      provides: "Full CRUD + subscribeFunnelComments"
      exports: ["listFunnelComments", "addFunnelComment", "deleteFunnelComment", "subscribeFunnelComments"]
    - path: "src/data/allowlist.js"
      provides: "Read-only allowlist client (admin only — Phase 6 wires Email/Password against this)"
      exports: ["getAllowlistEntry", "listAllowlist"]
    - path: "src/data/responses.js"
      provides: "Pass-through to data/orgs nested-map (Phase 5 DATA-01 rewrites body to subcollection access)"
      exports: ["listResponses", "saveResponse", "deleteResponse"]
    - path: "src/data/comments.js"
      provides: "Pass-through (Phase 5 DATA-01)"
      exports: ["listComments", "addComment", "deleteComment"]
    - path: "src/data/actions.js"
      provides: "Pass-through (Phase 5 DATA-01)"
      exports: ["listActions", "saveAction", "deleteAction"]
    - path: "src/data/documents.js"
      provides: "Pass-through + Storage (Phase 5 DATA-01 + Phase 7 callable)"
      exports: ["listDocuments", "saveDocument", "deleteDocument"]
    - path: "src/data/messages.js"
      provides: "Pass-through + onSnapshot (Phase 5 DATA-01)"
      exports: ["listMessages", "addMessage", "subscribeMessages"]
    - path: "src/data/audit-events.js"
      provides: "Pass-through to cloud/audit.js (Phase 7 wires real audit log)"
      exports: ["recordAuditEvent"]
    - path: "src/cloud/audit.js"
      provides: "Phase 7 (FN-04) seam — empty no-op stub"
      exports: ["writeAuditEvent"]
    - path: "src/cloud/soft-delete.js"
      provides: "Phase 8 (LIFE-04) seam"
      exports: ["softDelete", "restoreSoftDeleted"]
    - path: "src/cloud/gdpr.js"
      provides: "Phase 8 (GDPR-01/02) seam"
      exports: ["exportUser", "eraseUser"]
    - path: "src/cloud/claims-admin.js"
      provides: "Phase 6 (AUTH-07) seam"
      exports: ["setClaims"]
    - path: "src/cloud/retry.js"
      provides: "Phase 7 (FN-09) shared retry helper seam"
      exports: ["withRetry"]
    - path: "src/observability/sentry.js"
      provides: "Phase 9 (OBS-01) seam"
      exports: ["captureError", "addBreadcrumb"]
    - path: "src/observability/audit-events.js"
      provides: "Phase 7/9 constants table seam"
      exports: ["AUDIT_EVENTS", "emitAuditEvent"]
  key_links:
    - from: "src/data/orgs.js"
      to: "src/firebase/db.js"
      via: "import { db, collection, doc, ... } from '../firebase/db.js'"
      pattern: "from.*\\.\\./firebase/db\\.js"
    - from: "src/data/comments.js (pass-through)"
      to: "src/data/orgs.js"
      via: "import { getOrg, saveOrg } from './orgs.js'"
      pattern: "from.*\\./orgs\\.js"
    - from: "tests/data/*.test.js"
      to: "src/firebase/db.js (mocked)"
      via: "vi.mock('../../src/firebase/db.js', () => makeFirestoreMock(...))"
      pattern: "vi\\.mock.*src/firebase/db"
---

<objective>
Wave 3 — Land all 12 data/* per-collection wrappers (D-09: 6 full owners + 6 Phase-5-rewrite-target pass-throughs), all 5 cloud/* empty stub seams (D-11: Phase 7/8 fills), both observability/* empty stub seams (D-11: Phase 7/9 fills), retarget the firebase mock factory's vi.mock path, flip ESLint data/* boundary to error.

Purpose: Establish the data tier boundary that Wave 4 view extractions consume. Ship the complete ARCHITECTURE.md §2 directory tree (cloud/* + observability/* land as documented no-op exports per D-11 — Phase 7/8/9 fill bodies WITHOUT changing the import surface). Phase 5 (DATA-01..06) replaces the 6 pass-through bodies with subcollection access; the API surface stays stable across the cutover so views/* never re-extract their consumption pattern (D-09).

Output: 12 data/*.js wrappers (with paired tests, retargeted firebase mock), 5 cloud/*.js stubs, 2 observability/*.js stubs (each with minimal "exports the expected no-op function" smoke tests per Pattern Map "Test-file pairing" rule), Wave 3 ESLint flip (data/* SDK direct imports blocked outside firebase/db.js + firebase/storage.js), 13 forward-tracking cleanup-ledger rows added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modular-split-quick-wins/04-CONTEXT.md
@.planning/phases/04-modular-split-quick-wins/04-PATTERNS.md
@.planning/phases/04-modular-split-quick-wins/04-01-SUMMARY.md
@.planning/phases/04-modular-split-quick-wins/04-02-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@runbooks/phase-4-cleanup-ledger.md
@CLAUDE.md

<interfaces>
Source-of-truth analogs for each data/* wrapper. Per PATTERNS.md "Wave 3" section.

From src/data/cloud-sync.js (full owner analog — Pattern D DI Promise wrapper, // @ts-check, JSDoc):
- Module preamble pattern (lines 1-22)
- Pattern-D DI bundle pattern
- async function shape with JSDoc

From src/data/migration.js (pass-through analog — minimal DI surface):
- Module preamble pattern
- Single-DI param shape

From src/firebase/db.js (Wave 1 product) — what data/* imports:
```
import { db, collection, doc, setDoc, getDoc, getDocs, addDoc, updateDoc, deleteDoc,
         query, where, orderBy, onSnapshot, serverTimestamp, limit } from "../firebase/db.js";
```

From src/firebase/check.js (no-op stub analog — for cloud/* + observability/*):
```
// Phase X (REQ-Y) replaces the body with: <documented next-phase implementation>
// Phase 4 (D-11): no-op stub. The exported function exists so consumers can import the signature today.
/** @param {*} _input */
export async function fn(_input) { /* Phase X body lands here */ }
```

From tests/mocks/firebase.js — already-designed-to-retarget factory. Wave 3 changes vi.mock targets:
- Old (Phase 2): `vi.mock("firebase/firestore", () => makeFirestoreMock(...))`
- New (Wave 3+): `vi.mock("../../src/firebase/db.js", () => makeFirestoreMock(...))`

From tests/data/cloud-sync.test.js (analog for tests/data/orgs.test.js etc.):
- in-memory store + DI deps factory pattern (lines 1-122)

From eslint.config.js (Wave 3 flip — add data/* boundary):
```
{
  files: ["src/data/**/*.js"],
  rules: {
    "no-restricted-imports": [
      "error",
      {
        patterns: [
          { group: ["firebase/firestore", "firebase/storage", "firebase/auth", "firebase/app-check", "firebase/functions"],
            message: "data/* must access SDK only through src/firebase/db.js + src/firebase/storage.js." },
        ],
      },
    ],
  },
},
```

D-09 mapping — full owners vs pass-throughs:
- Full owners (own their full read/write logic): orgs, users, roadmaps, funnels, funnel-comments, allowlist
- Pass-throughs (delegate to data/orgs.js's nested-map shape; Phase 5 DATA-01 rewrites bodies): responses, comments, actions, documents, messages, audit-events

D-11 mapping — cloud/* + observability/* stubs and which downstream phase fills them:
- src/cloud/audit.js → Phase 7 (FN-04 / AUDIT-01)
- src/cloud/soft-delete.js → Phase 8 (LIFE-04)
- src/cloud/gdpr.js → Phase 8 (GDPR-01/02)
- src/cloud/claims-admin.js → Phase 6 (AUTH-07)
- src/cloud/retry.js → Phase 7 (FN-09)
- src/observability/sentry.js → Phase 9 (OBS-01)
- src/observability/audit-events.js → Phase 7 (AUDIT-02 constants table) + Phase 9 (AUDIT-05 view wiring)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the 6 full-owner data/*.js wrappers (orgs/users/roadmaps/funnels/funnel-comments/allowlist) + paired tests with retargeted firebase mock</name>
  <files>src/data/orgs.js, src/data/users.js, src/data/roadmaps.js, src/data/funnels.js, src/data/funnel-comments.js, src/data/allowlist.js, tests/data/orgs.test.js, tests/data/users.test.js, tests/data/roadmaps.test.js, tests/data/funnels.test.js, tests/data/funnel-comments.test.js, tests/data/allowlist.test.js, app.js</files>
  <read_first>
    - src/data/cloud-sync.js (FULL READ — full Pattern D DI Promise wrapper analog; module preamble; JSDoc)
    - src/data/migration.js (FULL READ — single-DI Promise shape analog)
    - tests/data/cloud-sync.test.js (FULL READ lines 1-122 — in-memory store + DI deps factory pattern; copy this for tests/data/orgs.test.js)
    - tests/mocks/firebase.js (FULL READ — confirm makeFirestoreMock export shape; verify forward-compat note about retargeting)
    - app.js (grep for the IIFE-resident `loadOrg`, `loadOrgMetas`, `loadUsers`, `findUser`, `upsertUser`, `loadSettings`, `saveSettings`, `loadRoadmaps`, `loadFunnels`, `loadFunnelComments`, `loadAllowlist` functions — these become the body of each data/*.js wrapper. Capture the read/write semantics for D-12 faithful extraction.)
    - app.js (grep for `cloudFetchAllOrgs`, `cloudFetchAllUsers`, `cloudPushOrg`, `cloudPushUser` — the Firestore-touching versions inside the IIFE; data/orgs.js + data/users.js own these now)
    - .planning/phases/04-modular-split-quick-wins/04-PATTERNS.md "Wave 3: src/data/orgs.js" + "Wave 3: tests/data/orgs.test.js" sections (paste-ready code)
    - .planning/research/ARCHITECTURE.md §4 (Firestore data model — confirms the current orgs/{id} monolithic doc shape; Phase 5 rewrites)
  </read_first>
  <behavior>
    - Test 1 (tests/data/orgs.test.js): getOrg("o1") returns the seeded {id:"o1", name:"Acme"} when present; null when absent
    - Test 2 (tests/data/orgs.test.js): listOrgs returns all seeded orgs as an array
    - Test 3 (tests/data/orgs.test.js): saveOrg(org) calls setDoc with serverTimestamp() in updatedAt field
    - Test 4 (tests/data/orgs.test.js): deleteOrg(id) removes the doc
    - Test 5 (tests/data/orgs.test.js): subscribeOrgs({onChange, onError}) receives an initial snapshot with the seeded orgs and returns an unsubscribe function
    - Tests 6-30 (other 5 wrappers): mirror the same 5-test pattern (get/list/save/delete/subscribe), seeded via the makeFirestoreMock factory's `seed` option.
  </behavior>
  <action>
1. Create `src/data/orgs.js` per PATTERNS.md "Wave 3: src/data/orgs.js" lines 692-757 (full paste-ready code). Module preamble: `Phase 4 (D-09 / D-10 / D-12): full CRUD owner per D-09. Phase 5 (DATA-01..06) rewrites this body to use subcollection access (orgs/{orgId}/{collection}) — the Promise-CRUD-+-subscribe API surface stays stable across the cutover.` Imports `db, collection, doc, setDoc, getDoc, getDocs, deleteDoc, onSnapshot, serverTimestamp` from `../firebase/db.js`. Exports `getOrg`, `listOrgs`, `saveOrg`, `deleteOrg`, `subscribeOrgs` with the JSDoc-typed signatures from PATTERNS.md.

2. Create `src/data/users.js` mirroring orgs.js with the IIFE's user-shape semantics. Body byte-identical with app.js's `loadUsers` / `findUser` / `upsertUser` (per D-12 faithful extraction). Pattern:
```
import { db, collection, doc, setDoc, getDoc, getDocs, deleteDoc, onSnapshot } from "../firebase/db.js";
export async function getUser(userId) { /* getDoc(doc(db, "users", userId)) */ }
export async function listUsers() { /* getDocs(collection(db, "users")) */ }
export async function saveUser(user) { /* setDoc(doc(db, "users", user.id), user, { merge: true }) */ }
export async function deleteUser(userId) { /* deleteDoc(doc(db, "users", userId)) */ }
export function subscribeUsers({ onChange, onError }) { /* onSnapshot(collection(db, "users"), ...) */ }
```

3. Create `src/data/roadmaps.js` — per ARCHITECTURE.md §4 the collection is `roadmaps/{orgId}` (one roadmap doc per org). Pattern:
```
export async function getRoadmap(orgId) { /* getDoc(doc(db, "roadmaps", orgId)) */ }
export async function listRoadmaps() { /* getDocs(collection(db, "roadmaps")) */ }
export async function saveRoadmap(orgId, roadmap) { /* setDoc(doc(db, "roadmaps", orgId), roadmap, { merge: true }) */ }
export async function deleteRoadmap(orgId) { /* deleteDoc(doc(db, "roadmaps", orgId)) */ }
export function subscribeRoadmaps({ onChange, onError }) { /* onSnapshot(...) */ }
```

4. Create `src/data/funnels.js` — collection `funnels/{orgId}`. Mirrors roadmaps.js shape.

5. Create `src/data/funnel-comments.js` — collection `funnelComments/{commentId}` — flat collection with orgId field (per ARCHITECTURE.md §4). Pattern:
```
export async function listFunnelComments(orgId) { /* getDocs(query(collection(db, "funnelComments"), where("orgId", "==", orgId))) */ }
export async function addFunnelComment(orgId, comment) { /* addDoc(collection(db, "funnelComments"), { ...comment, orgId, createdAt: serverTimestamp() }) */ }
export async function deleteFunnelComment(commentId) { /* deleteDoc(doc(db, "funnelComments", commentId)) */ }
export function subscribeFunnelComments(orgId, { onChange, onError }) { /* onSnapshot(query(...), ...) */ }
```

6. Create `src/data/allowlist.js` — collection `internalAllowlist/{lowercasedEmail}` — read-only client (admin only; Phase 6 wires Email/Password against this). Pattern:
```
export async function getAllowlistEntry(email) { /* getDoc(doc(db, "internalAllowlist", email.toLowerCase())) */ }
export async function listAllowlist() { /* getDocs(collection(db, "internalAllowlist")) */ }
```
No write functions — admin-only writes go through Phase 7 callable.

7. For each of the 6 wrappers above, create a paired `tests/data/<wrapper>.test.js` mirroring the tests/data/cloud-sync.test.js pattern. Use the retargeted vi.mock path:
```
import { describe, it, expect, vi } from "vitest";
import { makeFirestoreMock } from "../mocks/firebase.js";

vi.mock("../../src/firebase/db.js", () => makeFirestoreMock({
  seed: { "orgs/o1": { id: "o1", name: "Acme" } },
}));

import { getOrg, listOrgs, saveOrg, deleteOrg, subscribeOrgs } from "../../src/data/orgs.js";

describe("data/orgs.js", () => {
  it("getOrg returns the seeded doc", async () => {
    const o = await getOrg("o1");
    expect(o).toEqual({ id: "o1", name: "Acme" });
  });
  // ... 4 more tests for list/save/delete/subscribe
});
```

8. Update `tests/mocks/firebase.js` if needed — verify makeFirestoreMock returns an object compatible with the src/firebase/db.js export shape (db + collection + doc + setDoc + getDoc + getDocs + addDoc + updateDoc + deleteDoc + query + where + orderBy + onSnapshot + serverTimestamp + limit). If the factory was authored with a forward-compat signature (per Phase 2 D-11), no change needed — just retarget the vi.mock path. If the factory's seed format expects a different doc-id shape, extend it minimally.

9. Update `app.js` IIFE — at file scope add:
```
import { getOrg, listOrgs, saveOrg, deleteOrg, subscribeOrgs } from "./src/data/orgs.js";
import { getUser, listUsers, saveUser, deleteUser, subscribeUsers } from "./src/data/users.js";
import { getRoadmap, listRoadmaps, saveRoadmap, deleteRoadmap, subscribeRoadmaps } from "./src/data/roadmaps.js";
import { getFunnel, listFunnels, saveFunnel, deleteFunnel, subscribeFunnels } from "./src/data/funnels.js";
import { listFunnelComments, addFunnelComment, deleteFunnelComment, subscribeFunnelComments } from "./src/data/funnel-comments.js";
import { getAllowlistEntry, listAllowlist } from "./src/data/allowlist.js";
```
Then DELETE the IIFE-resident `loadOrg` / `loadOrgMetas` / `loadUsers` / `findUser` / `upsertUser` / `loadRoadmaps` / `loadFunnels` / `loadFunnelComments` / `loadAllowlist` / `cloudFetchAllOrgs` / `cloudFetchAllUsers` / `cloudPushOrg` / `cloudPushUser` definitions. The IIFE's call sites that previously called these now call the imported equivalents — usually a 1:1 rename. If any local call-site uses a different signature (e.g., `loadOrg(orgId)` returns `Promise<Org|null>` while `getOrg(orgId)` returns the same shape), no rename needed. Otherwise add a thin local alias inside the IIFE — `const loadOrg = getOrg;`.

10. The src/data/cloud-sync.js file from Phase 2 stays AS-IS — it's already extracted; data/orgs.js does NOT subsume it. The Phase 2 D-05 byte-identical extraction stands; cloud-sync.js will be rewritten in Phase 5 (H8 fix). Add a forward-tracking ledger row noting that data/cloud-sync.js's `cloudFetchAllOrgs` / `cloudFetchAllUsers` / `cloudPushOrg` / `cloudPushUser` DI surface collapses to direct imports from data/orgs.js + data/users.js once Wave 3 lands — leave the closure to Phase 5 per D-12 faithful-extraction discipline.
  </action>
  <verify>
    <automated>npm run test -- tests/data/orgs.test.js tests/data/users.test.js tests/data/roadmaps.test.js tests/data/funnels.test.js tests/data/funnel-comments.test.js tests/data/allowlist.test.js &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 6 src/data/{orgs,users,roadmaps,funnels,funnel-comments,allowlist}.js exist with `// @ts-check`
    - All 6 tests/data/{orgs,users,roadmaps,funnels,funnel-comments,allowlist}.test.js exist and use `vi.mock("../../src/firebase/db.js", ...)`
    - `npm run test -- tests/data/` exits 0 (existing migration + cloud-sync tests + 6 new files all green)
    - `grep -c "from.*\\.\\./firebase/db\\.js" src/data/orgs.js src/data/users.js src/data/roadmaps.js src/data/funnels.js src/data/funnel-comments.js src/data/allowlist.js` returns at least 6 (each imports from db.js)
    - `grep -c "subscribe" src/data/orgs.js src/data/users.js src/data/roadmaps.js src/data/funnels.js src/data/funnel-comments.js` returns at least 5 (each subscribe* helper exported)
    - `grep -c "loadOrg\\|loadUsers\\|loadRoadmaps\\|loadFunnels\\|loadAllowlist" app.js` returns 0 (IIFE definitions removed; call sites use imports or local aliases)
    - `git diff tests/__snapshots__/views/dashboard.html` is empty
    - `git diff tests/__snapshots__/views/diagnostic.html` is empty
    - `git diff tests/__snapshots__/views/report.html` is empty
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>6 full-owner data/* wrappers + paired tests created; firebase mock retargeted; app.js IIFE imports replace IIFE-resident helpers; snapshot baselines unchanged; D-10 Promise CRUD + subscribe* contract enforced uniformly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the 6 pass-through data/*.js wrappers (responses/comments/actions/documents/messages/audit-events) + paired tests + forward-tracking cleanup-ledger rows for Phase 5</name>
  <files>src/data/responses.js, src/data/comments.js, src/data/actions.js, src/data/documents.js, src/data/messages.js, src/data/audit-events.js, tests/data/responses.test.js, tests/data/comments.test.js, tests/data/actions.test.js, tests/data/documents.test.js, tests/data/messages.test.js, tests/data/audit-events.test.js, runbooks/phase-4-cleanup-ledger.md</files>
  <read_first>
    - .planning/phases/04-modular-split-quick-wins/04-PATTERNS.md "Wave 3: pass-through pattern (D-09)" + the data/comments.js paste-ready code (lines 763-798)
    - app.js (grep for the IIFE-resident `commentsFor` / `actionsFor` / `responsesFor` / `documentsFor` / `messagesFor` functions — pass-throughs preserve these read patterns)
    - src/data/orgs.js (just-created — pass-throughs delegate here)
    - src/data/cloud-sync.js (uses serverTimestamp; pass-throughs may also)
    - .planning/phases/04-modular-split-quick-wins/04-CONTEXT.md D-09 mapping (orgs.js owns nested-map; pass-throughs read+write through getOrg/saveOrg)
    - runbooks/phase-4-cleanup-ledger.md (informational rows table — find existing rows or section to append the 6 forward-tracking rows)
  </read_first>
  <behavior>
    - Test 1 (tests/data/comments.test.js): listComments("o1", "p1") returns the comments array from the seeded org's nested-map (org.comments.p1)
    - Test 2 (tests/data/comments.test.js): addComment("o1", "p1", {body:"hi"}) calls saveOrg with the org's comments map updated
    - Test 3 (tests/data/comments.test.js): listComments returns [] when no comments for that pillar
    - Tests 4-15: similar shape for responses/actions/documents/messages/audit-events
  </behavior>
  <action>
1. Create `src/data/comments.js` per PATTERNS.md "Wave 3: pass-through pattern" lines 763-798 (full paste-ready code):
```
// src/data/comments.js
// @ts-check
// Phase 4 (D-09 / D-12): pass-through to data/orgs.js's nested-map shape.
// Phase 5 (DATA-01..06) replaces this body with subcollection access
// (orgs/{orgId}/comments/{commentId}); the API surface stays stable.
//
// Cleanup-ledger row: "Phase 5 replaces body with subcollection access;
// data/comments.js API stable" — closes at Phase 5.
import { getOrg, saveOrg } from "./orgs.js";

/** @param {string} orgId @param {number|string} pillarId @returns {Promise<Array<any>>} */
export async function listComments(orgId, pillarId) {
  const org = await getOrg(orgId);
  return ((org?.comments || {})[pillarId] || []).slice();
}

/** @param {string} orgId @param {number|string} pillarId @param {*} comment @returns {Promise<void>} */
export async function addComment(orgId, pillarId, comment) {
  const org = await getOrg(orgId);
  if (!org) return;
  org.comments = org.comments || {};
  org.comments[pillarId] = org.comments[pillarId] || [];
  org.comments[pillarId].push(comment);
  await saveOrg(org);
}

/** @param {string} orgId @param {number|string} pillarId @param {string} commentId @returns {Promise<void>} */
export async function deleteComment(orgId, pillarId, commentId) {
  const org = await getOrg(orgId);
  if (!org) return;
  const arr = (org.comments?.[pillarId] || []);
  const next = arr.filter((c) => c.id !== commentId);
  if (next.length === arr.length) return;
  org.comments[pillarId] = next;
  await saveOrg(org);
}
```

2. Create `src/data/responses.js` mirroring the same shape — pass-through to orgs.js. The IIFE shape today is `org.responses[roundId][userId][pillarId][idx]` (4-deep nested map). Per D-12 faithful extraction, preserve that shape verbatim:
```
import { getOrg, saveOrg } from "./orgs.js";

export async function listResponses(orgId, roundId, userId, pillarId) {
  const org = await getOrg(orgId);
  return ((org?.responses?.[roundId]?.[userId]?.[pillarId]) || []).slice();
}

export async function saveResponse(orgId, roundId, userId, pillarId, idx, value) {
  const org = await getOrg(orgId);
  if (!org) return;
  org.responses = org.responses || {};
  org.responses[roundId] = org.responses[roundId] || {};
  org.responses[roundId][userId] = org.responses[roundId][userId] || {};
  org.responses[roundId][userId][pillarId] = org.responses[roundId][userId][pillarId] || [];
  org.responses[roundId][userId][pillarId][idx] = value;
  await saveOrg(org);
}

export async function deleteResponse(orgId, roundId, userId, pillarId) {
  const org = await getOrg(orgId);
  if (!org) return;
  if (org.responses?.[roundId]?.[userId]) delete org.responses[roundId][userId][pillarId];
  await saveOrg(org);
}
```

3. Create `src/data/actions.js` — pass-through. The IIFE shape is `org.actions[actionId]` (flat map under org). Pattern:
```
import { getOrg, saveOrg } from "./orgs.js";

export async function listActions(orgId) {
  const org = await getOrg(orgId);
  return Object.values(org?.actions || {});
}

export async function saveAction(orgId, action) {
  const org = await getOrg(orgId);
  if (!org) return;
  org.actions = org.actions || {};
  org.actions[action.id] = action;
  await saveOrg(org);
}

export async function deleteAction(orgId, actionId) {
  const org = await getOrg(orgId);
  if (!org) return;
  if (org.actions) delete org.actions[actionId];
  await saveOrg(org);
}
```

4. Create `src/data/documents.js` — pass-through PLUS Storage. The Storage path is `orgs/{orgId}/documents/{docId}/{filename}` (per ARCHITECTURE.md §4). Pattern:
```
import { getOrg, saveOrg } from "./orgs.js";
import { storage, ref, uploadBytesResumable, getDownloadURL, deleteObject } from "../firebase/storage.js";
import { uid } from "../util/ids.js";

export async function listDocuments(orgId) {
  const org = await getOrg(orgId);
  return Object.values(org?.documents || {});
}

/**
 * Wave 4 documents view calls validateUpload(file) FIRST (D-15 trust boundary).
 * data/documents.js does NOT re-validate — trusts the contract.
 * @param {string} orgId @param {File} file @param {string} sanitisedName @param {*} meta @returns {Promise<{ id: string, downloadURL: string }>}
 */
export async function saveDocument(orgId, file, sanitisedName, meta = {}) {
  const docId = uid("d_");
  const path = `orgs/${orgId}/documents/${docId}/${sanitisedName}`;
  const task = uploadBytesResumable(ref(storage, path), file);
  await task;
  const downloadURL = await getDownloadURL(task.snapshot.ref);
  const org = await getOrg(orgId);
  if (org) {
    org.documents = org.documents || {};
    org.documents[docId] = { id: docId, name: sanitisedName, path, downloadURL, ...meta };
    await saveOrg(org);
  }
  return { id: docId, downloadURL };
}

export async function deleteDocument(orgId, docId) {
  const org = await getOrg(orgId);
  if (!org?.documents?.[docId]) return;
  const doc = org.documents[docId];
  await deleteObject(ref(storage, doc.path));
  delete org.documents[docId];
  await saveOrg(org);
}
```

5. Create `src/data/messages.js` — pass-through PLUS onSnapshot. The IIFE shape today is `org.messages[messageId]` (flat map under org); chat view uses onSnapshot on the org doc and filters in-memory. Phase 5 moves this to `orgs/{orgId}/messages/{messageId}` subcollection. Pattern:
```
import { getOrg, saveOrg } from "./orgs.js";
import { db, doc, onSnapshot } from "../firebase/db.js";
import { uid } from "../util/ids.js";

export async function listMessages(orgId) {
  const org = await getOrg(orgId);
  return Object.values(org?.messages || {}).sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0));
}

export async function addMessage(orgId, message) {
  const org = await getOrg(orgId);
  if (!org) return;
  const id = message.id || uid("m_");
  org.messages = org.messages || {};
  org.messages[id] = { ...message, id };
  await saveOrg(org);
  return id;
}

/** @returns {() => void} */
export function subscribeMessages(orgId, { onChange, onError }) {
  return onSnapshot(
    doc(db, "orgs", orgId),
    (snap) => onChange(Object.values(snap.data()?.messages || {}).sort((a, b) => (a.createdAt || 0) - (b.createdAt || 0))),
    onError,
  );
}
```

6. Create `src/data/audit-events.js` — pass-through to cloud/audit.js. Phase 7 wires the real audit log. Pattern:
```
import { writeAuditEvent } from "../cloud/audit.js";

/**
 * Phase 4 (D-11): pass-through to cloud/audit.js (which is a stub until Phase 7).
 * Calls are no-ops today; Phase 7 (FN-04 / AUDIT-01) lands the real callable.
 * @param {{ event: string, payload?: any }} input @returns {Promise<void>}
 */
export async function recordAuditEvent(input) {
  await writeAuditEvent(input);
}
```

7. Create paired tests for each pass-through. tests/data/comments.test.js example:
```
import { describe, it, expect, vi } from "vitest";
import { makeFirestoreMock } from "../mocks/firebase.js";

vi.mock("../../src/firebase/db.js", () => makeFirestoreMock({
  seed: { "orgs/o1": { id: "o1", comments: { p1: [{ id: "c1", body: "hi" }] } } },
}));

import { listComments, addComment, deleteComment } from "../../src/data/comments.js";

describe("data/comments.js (D-09 pass-through)", () => {
  it("listComments returns seeded comments for the pillar", async () => {
    const cs = await listComments("o1", "p1");
    expect(cs).toEqual([{ id: "c1", body: "hi" }]);
  });
  it("listComments returns [] when no comments for that pillar", async () => {
    const cs = await listComments("o1", "p999");
    expect(cs).toEqual([]);
  });
  it("addComment appends to the org's comments map", async () => {
    await addComment("o1", "p1", { id: "c2", body: "hello" });
    const cs = await listComments("o1", "p1");
    expect(cs.find(c => c.id === "c2")).toBeTruthy();
  });
});
```

Mirror this for responses/actions/documents/messages/audit-events. For documents, the test must mock src/firebase/storage.js (uploadBytesResumable returns a thenable that resolves, getDownloadURL returns a sentinel URL). For messages, the test verifies subscribeMessages composes correctly (snapshot callback receives the messages array).

8. Append 6 forward-tracking rows to `runbooks/phase-4-cleanup-ledger.md` (Phase 2 — extracted leaf modules — informational table or a new "Phase 4 forward-tracking" section):

```
## Phase 4 — forward-tracking rows (Phase 5 / Phase 7 / Phase 8 / Phase 9 closures)

| Module                               | Forward closure                                                                                                                                | Closure phase     |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| src/data/responses.js                | Phase 5 (DATA-01) replaces pass-through body with subcollection access (orgs/{orgId}/responses/{respId}); API stable                          | Phase 5           |
| src/data/comments.js                 | Phase 5 (DATA-01) replaces pass-through body with subcollection access (orgs/{orgId}/comments/{cmtId}); API stable                            | Phase 5           |
| src/data/actions.js                  | Phase 5 (DATA-01) replaces pass-through body with subcollection access (orgs/{orgId}/actions/{actId}); API stable                             | Phase 5           |
| src/data/documents.js                | Phase 5 (DATA-01) replaces pass-through body with subcollection access (orgs/{orgId}/documents/{docId}); API stable                           | Phase 5           |
| src/data/messages.js                 | Phase 5 (DATA-01) replaces pass-through body with subcollection access (orgs/{orgId}/messages/{msgId}) + readStates/{userId}; API stable      | Phase 5           |
| src/data/audit-events.js             | Phase 7 (FN-04 / AUDIT-01) wires recordAuditEvent → cloud/audit.js → real auditWrite callable                                                  | Phase 7           |
```
  </action>
  <verify>
    <automated>npm run test -- tests/data/ &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - All 6 src/data/{responses,comments,actions,documents,messages,audit-events}.js exist with `// @ts-check`
    - `grep -c 'from "./orgs.js"' src/data/responses.js src/data/comments.js src/data/actions.js src/data/documents.js src/data/messages.js` returns at least 5 (each pass-through delegates to orgs.js — except audit-events which delegates to cloud/audit.js)
    - `grep -c 'from "../cloud/audit.js"' src/data/audit-events.js` returns 1
    - All 6 tests/data/{responses,comments,actions,documents,messages,audit-events}.test.js exist
    - `npm run test -- tests/data/` exits 0
    - `grep -c "Phase 5 (DATA-01) replaces pass-through" runbooks/phase-4-cleanup-ledger.md` returns at least 5
    - `grep -c "Phase 7 (FN-04 / AUDIT-01)" runbooks/phase-4-cleanup-ledger.md` returns at least 1
    - `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>6 pass-through data/* wrappers + paired tests created; Phase 5 rewrite-target API surface frozen (views/* never re-extract); 6 forward-tracking ledger rows added; snapshot baselines unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 5 cloud/*.js stubs + 2 observability/*.js stubs + paired smoke tests + Wave 3 ESLint flip + 7 forward-tracking ledger rows</name>
  <files>src/cloud/audit.js, src/cloud/soft-delete.js, src/cloud/gdpr.js, src/cloud/claims-admin.js, src/cloud/retry.js, src/observability/sentry.js, src/observability/audit-events.js, tests/cloud/audit.test.js, tests/cloud/soft-delete.test.js, tests/cloud/gdpr.test.js, tests/cloud/claims-admin.test.js, tests/cloud/retry.test.js, tests/observability/sentry.test.js, tests/observability/audit-events.test.js, eslint.config.js, runbooks/phase-4-cleanup-ledger.md</files>
  <read_first>
    - src/firebase/check.js (FULL READ — Wave 1's no-op stub canonical pattern; copy the comment template)
    - src/util/hash.js (Phase-X-deletes-this comment-template style)
    - .planning/phases/04-modular-split-quick-wins/04-PATTERNS.md "Wave 3: src/cloud/audit.js + soft-delete.js + gdpr.js + claims-admin.js + retry.js" + "Wave 3: src/observability/sentry.js + audit-events.js" sections
    - .planning/research/ARCHITECTURE.md §3 (Cloud Functions enumeration — names of callables Phase 7 wires)
    - eslint.config.js (Wave 1 + Wave 2 entries already present)
  </read_first>
  <behavior>
    - Test 1 per stub: the exported function is typeof "function" and resolves to undefined (no-op)
    - Test 2 per stub: importing the module does not throw
    - Test 3 (audit-events.js): AUDIT_EVENTS is exported as Object.freeze({}) — mutable-rejection check (frozen)
  </behavior>
  <action>
1. Create `src/cloud/audit.js` per PATTERNS.md "Wave 3: src/cloud/audit.js" lines 805-825 (paste-ready):
```
// src/cloud/audit.js
// @ts-check
// Phase 4 (D-11): empty stub seam. Phase 7 (FN-04 / AUDIT-01) replaces the body
// with real httpsCallable wiring through src/firebase/functions.js (the auditWrite
// callable). API surface defined here so that src/data/audit-events.js can
// import the signature today. Cleanup-ledger forward-tracking row: "Phase 7
// wires src/cloud/audit.js body" — closes at Phase 7.

/** @param {{ event: string, payload?: any }} _input @returns {Promise<void>} */
export async function writeAuditEvent(_input) { /* Phase 7 body lands here (FN-04) */ }
```

2. Create `src/cloud/soft-delete.js`:
```
// @ts-check
// Phase 8 (LIFE-04) replaces the body with httpsCallable("softDelete") + ("restoreSoftDeleted").
/** @param {{ type: string, id: string }} _input @returns {Promise<void>} */
export async function softDelete(_input) { /* Phase 8 body */ }
/** @param {{ type: string, id: string }} _input @returns {Promise<void>} */
export async function restoreSoftDeleted(_input) { /* Phase 8 body */ }
```

3. Create `src/cloud/gdpr.js`:
```
// @ts-check
// Phase 8 (GDPR-01/02) replaces the body with httpsCallable("gdprExportUser") + ("gdprEraseUser").
/** @param {{ userId: string }} _input @returns {Promise<{ downloadURL: string }>} */
export async function exportUser(_input) { /* Phase 8 body */ return { downloadURL: "" }; }
/** @param {{ userId: string }} _input @returns {Promise<void>} */
export async function eraseUser(_input) { /* Phase 8 body */ }
```

4. Create `src/cloud/claims-admin.js`:
```
// @ts-check
// Phase 6 (AUTH-07) replaces the body with httpsCallable("setClaims").
/** @param {{ uid: string, role?: string, orgId?: string }} _input @returns {Promise<void>} */
export async function setClaims(_input) { /* Phase 6 body */ }
```

5. Create `src/cloud/retry.js`:
```
// @ts-check
// Phase 7 (FN-09) replaces the body with exponential-backoff + 429-aware retry.
/**
 * @template T
 * @param {() => Promise<T>} fn
 * @param {{ retries?: number, baseMs?: number }} [opts]
 * @returns {Promise<T>}
 */
export async function withRetry(fn, _opts = {}) { return fn(); /* Phase 7 body */ }
```

6. Create `src/observability/sentry.js`:
```
// @ts-check
// Phase 9 (OBS-01) replaces the body with @sentry/browser init + captureException + addBreadcrumb.
/** @param {Error} _err @param {*} [_context] */
export function captureError(_err, _context) { /* Phase 9 body */ }
/** @param {{ category: string, message: string, data?: any }} _crumb */
export function addBreadcrumb(_crumb) { /* Phase 9 body */ }
```

7. Create `src/observability/audit-events.js` per PATTERNS.md "Wave 3: src/observability/audit-events.js" lines 836-857 (paste-ready):
```
// @ts-check
// Phase 4 (D-11): NEW constants table. Phase 7 (AUDIT-02) populates the table
// with the canonical event-name set; Phase 9 (AUDIT-05) wires emitAuditEvent
// calls in views/*.

/** @type {Readonly<Record<string, string>>} */
export const AUDIT_EVENTS = Object.freeze({});

/** @param {keyof typeof AUDIT_EVENTS|string} _event @param {any} [_payload] */
export function emitAuditEvent(_event, _payload) { /* Phase 9 body */ }
```

8. Create paired smoke tests for each of the 7 stubs. tests/cloud/audit.test.js example:
```
import { describe, it, expect } from "vitest";
import { writeAuditEvent } from "../../src/cloud/audit.js";

describe("cloud/audit.js (Phase 4 D-11 stub)", () => {
  it("writeAuditEvent is a function", () => { expect(typeof writeAuditEvent).toBe("function"); });
  it("writeAuditEvent resolves without throwing (no-op)", async () => {
    await expect(writeAuditEvent({ event: "test" })).resolves.toBeUndefined();
  });
});
```

Mirror this minimal "exports the expected no-op function" pattern for soft-delete/gdpr/claims-admin/retry/sentry/audit-events. For audit-events.js add:
```
it("AUDIT_EVENTS is a frozen object", () => {
  expect(Object.isFrozen(AUDIT_EVENTS)).toBe(true);
});
```

The cloud/* + observability/* directories are EXCLUDED from coverage thresholds in Phase 4 per D-21 — these smoke tests exist to satisfy the Pattern-Map "test-file pairing" rule, not to gate coverage.

9. Add Wave 3 ESLint flip to `eslint.config.js` per PATTERNS.md "Wave 3 flip — data/* limited to firebase/db.js + firebase/storage.js":
```
// Phase 4 Wave 3 (D-04): data/* must access SDK only through src/firebase/db.js +
// src/firebase/storage.js — direct firebase/firestore|storage|auth|app-check|functions
// imports blocked.
{
  files: ["src/data/**/*.js"],
  rules: {
    "no-restricted-imports": [
      "error",
      {
        patterns: [
          {
            group: ["firebase/firestore", "firebase/storage", "firebase/auth", "firebase/app-check", "firebase/functions"],
            message: "data/* must access SDK only through src/firebase/db.js + src/firebase/storage.js (Wave 3 D-04).",
          },
        ],
      },
    ],
  },
},
```

This is the third per-files-scoped entry (after Wave 1's `**/*.js` ignores firebase/** + Wave 2's domain/* boundary). Verify by running `npm run lint` after the edit.

10. Append 7 forward-tracking rows to `runbooks/phase-4-cleanup-ledger.md` (in the same "Phase 4 — forward-tracking rows" section added in Task 2):

```
| src/cloud/audit.js                   | Phase 7 (FN-04 / AUDIT-01) replaces body with httpsCallable("auditWrite")                                                                       | Phase 7           |
| src/cloud/soft-delete.js             | Phase 8 (LIFE-04) replaces body with httpsCallable("softDelete") + ("restoreSoftDeleted")                                                       | Phase 8           |
| src/cloud/gdpr.js                    | Phase 8 (GDPR-01/02) replaces body with httpsCallable("gdprExportUser") + ("gdprEraseUser")                                                     | Phase 8           |
| src/cloud/claims-admin.js            | Phase 6 (AUTH-07) replaces body with httpsCallable("setClaims")                                                                                | Phase 6           |
| src/cloud/retry.js                   | Phase 7 (FN-09) replaces body with exponential-backoff + 429-aware retry helper                                                                | Phase 7           |
| src/observability/sentry.js          | Phase 9 (OBS-01) replaces body with @sentry/browser init + captureException + addBreadcrumb                                                    | Phase 9           |
| src/observability/audit-events.js    | Phase 7 (AUDIT-02) populates AUDIT_EVENTS constants table; Phase 9 (AUDIT-05) wires emitAuditEvent calls in views/*                            | Phase 7 / Phase 9 |
```

11. Update the existing "no-restricted-imports warn→error" tracking row — add Wave 3 checkbox: `[x] Wave 3: data/* → only firebase/db.js + firebase/storage.js (closes direct SDK imports outside firebase/**).`

12. Final verify: `npm run test && npm run typecheck && npm run lint && npm run build` exits 0.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 5 src/cloud/{audit,soft-delete,gdpr,claims-admin,retry}.js exist with `// @ts-check`
    - Both src/observability/{sentry,audit-events}.js exist with `// @ts-check`
    - All 7 paired tests/cloud/* + tests/observability/* exist
    - `grep -c "Object.isFrozen(AUDIT_EVENTS)" tests/observability/audit-events.test.js` returns at least 1
    - `grep -c 'files: \\["src/data/\\*\\*/\\*.js"\\]' eslint.config.js` returns 1 (Wave 3 entry added)
    - `grep -c "data/\\* must access SDK only through" eslint.config.js` returns 1
    - `grep -c "Phase 7 (FN-04 / AUDIT-01)" runbooks/phase-4-cleanup-ledger.md` returns at least 2 (cloud/audit.js row + data/audit-events.js row from Task 2)
    - `grep -c "Phase 8 (LIFE-04)\\|Phase 8 (GDPR-01/02)" runbooks/phase-4-cleanup-ledger.md` matches at least 2
    - `grep -c "Phase 9 (OBS-01)" runbooks/phase-4-cleanup-ledger.md` matches at least 1
    - `grep -c "Wave 3: data/\\*" runbooks/phase-4-cleanup-ledger.md` returns at least 1
    - `npm run test` exits 0 (full suite)
    - `npm run lint` exits 0 (Wave 3 ESLint flip dormant since data/* uses firebase/db.js + firebase/storage.js — not direct SDK paths)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
    - `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty
  </acceptance_criteria>
  <done>5 cloud/* + 2 observability/* stubs + paired smoke tests created; Wave 3 ESLint flip live; 7 forward-tracking ledger rows added; ARCHITECTURE.md §2 directory tree fully landed (every directory documented with no-op exports where bodies come in later phases); full test+typecheck+lint+build green; snapshot baselines unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Firestore (data/* tier) | data/*.js is the sole consumer of src/firebase/db.js; views/* never call SDK directly (D-04 lint enforcement) |
| client → Storage (data/documents.js) | uploadBytesResumable wired through src/firebase/storage.js; views call validateUpload(file) FIRST (Wave 4) |
| client → Cloud Functions (cloud/* stubs) | Empty seams today; Phase 7 wires real callables with App Check + Zod validation + Sentry capture |
| audit log integrity | data/audit-events.js → cloud/audit.js → (Phase 7) → auditWrite Cloud Function (Admin SDK) — clients never write directly to auditLog/* (RULES-03 enforces server-only writes Phase 5) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-3-1 | Tampering | data/* → firebase/* boundary | mitigate | Wave 3 ESLint flip: src/data/** blocked from importing firebase/firestore|storage|auth|app-check|functions directly. Lint hard-fails CI. |
| T-4-3-2 | Spoofing | Phase 5 collision (views re-extract data API) | mitigate | D-09 pass-through pattern: views/* see a stable data/comments.js / responses.js / etc. API in both Phase 4 and Phase 5. Phase 5 only rewrites bodies; views/* never re-extract their consumption pattern. |
| T-4-3-3 | Tampering | Stub bodies misused as if they were real (e.g., Phase 4 code calls writeAuditEvent expecting it to actually log) | accept | Phase 4 IS a pure-refactor phase per ROADMAP "Phase 4: Pure-Refactor Phase"; backend behaviour is unchanged. cloud/* stubs are explicit no-ops; downstream phases (6/7/8/9) fill bodies. The audit-narrative line for SOC2 is "the seam exists; Phase 7 wires the body" — defensible and accurate. |
| T-4-3-4 | Information Disclosure | Storage path injection via data/documents.js | mitigate | saveDocument receives sanitisedName from validateUpload (D-15 trust boundary at ui/upload.js); path constructed as `orgs/{orgId}/documents/{docId}/{sanitisedName}` — orgId from caller, docId from uid() (CSPRNG), sanitisedName already passed `replace(/[^\w.\- ]/g, "_")`. Phase 5 storage.rules adds the actual server-side enforcement. |
| T-4-3-5 | Repudiation | data/audit-events.js no-ops (audit gap during Phase 4-6) | accept | Phase 4 ships before audit log infrastructure (Phase 7) — there is no audit log to write to yet. The data/audit-events.js + cloud/audit.js seams exist so views/* can call recordAuditEvent today and the call lands at the real callable Phase 7. The audit gap is intentional and time-bounded. |
| T-4-3-6 | Elevation of Privilege | data/allowlist.js write surface | mitigate | data/allowlist.js exports get* + list* only — no write surface. Admin writes go through Phase 7 callable. internalAllowlist/* writes are denied to clients by Phase 5 storage.rules (RULES-03). |
</threat_model>

<verification>
1. `npm run test` exits 0 (existing 149+ tests + 12 new tests/data + 7 new tests/cloud + 2 new tests/observability all green)
2. `npm run typecheck` exits 0 (all new src/data/*.js + src/cloud/*.js + src/observability/*.js have // @ts-check)
3. `npm run lint` exits 0 (Wave 3 ESLint flip — data/* SDK direct imports blocked; rule dormant since data/* correctly uses src/firebase/db.js + src/firebase/storage.js)
4. `npm run build` exits 0
5. `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty (D-12 faithful extraction holds)
6. ARCHITECTURE.md §2 directory tree fully landed: src/firebase/ + src/data/ + src/domain/ + src/auth/ + src/cloud/ + src/observability/ + src/ui/ all populated
7. tests/mocks/firebase.js retargeted to src/firebase/db.js path (verified by grep across tests/data/*.test.js)
8. Cleanup-ledger Phase 4 forward-tracking section gains 13 rows (6 Phase-5 pass-through + 5 Phase-7/8 cloud/* + 2 Phase-7/9 observability/*)
9. ESLint Wave 3 flip live; out-of-band-soft-fail "no-restricted-imports" tracking row gains a Wave 3 checkbox
</verification>

<success_criteria>
- 12 src/data/*.js wrappers exist (6 owners + 6 pass-throughs)
- 5 src/cloud/*.js stubs exist (audit/soft-delete/gdpr/claims-admin/retry — empty no-op seams)
- 2 src/observability/*.js stubs exist (sentry — Phase 9; audit-events — Phase 7/9 constants table frozen)
- 19 paired test files exist; tests/data/*.test.js retargets vi.mock to src/firebase/db.js path
- Wave 3 ESLint flip live: data/* SDK direct imports blocked outside firebase/db.js + firebase/storage.js
- 13 forward-tracking rows added to cleanup-ledger documenting Phase-5/6/7/8/9 closures
- app.js IIFE imports the 6 full-owner data wrappers; pass-throughs imported by Wave 4 views
- Phase 2 snapshot baselines unchanged
- D-09 pass-through pattern keeps views/* consumption stable across Phase 4 → Phase 5 cutover (ZERO views/* re-extraction in Phase 5)
- npm run test/typecheck/lint/build all green
</success_criteria>

<output>
After completion, create `.planning/phases/04-modular-split-quick-wins/04-03-SUMMARY.md` documenting:
- The 12 data/* module shapes (per-collection + pass-through pattern)
- The 7 stub module shapes (cloud/* + observability/*)
- The retargeted vi.mock paths in tests/data/*.test.js
- The Wave 3 ESLint flip diff
- The 13 forward-tracking ledger rows added (which phase closes which row)
- The full ARCHITECTURE.md §2 directory tree state (which directories now exist; which are still empty)
- app.js line count diff (continuing from Wave 2; the IIFE thins further as data helpers move out)
- Snapshot baseline zero-diff verification
</output>
