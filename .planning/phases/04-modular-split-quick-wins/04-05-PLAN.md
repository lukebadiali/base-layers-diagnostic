---
phase: 04-modular-split-quick-wins
plan: 05
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - src/state.js
  - src/router.js
  - src/main.js
  - tests/state.test.js
  - tests/router.test.js
  - tests/main.test.js
  - tests/views/dashboard.test.js
  - tests/views/diagnostic.test.js
  - tests/views/report.test.js
  - index.html
  - app.js
  - runbooks/phase-4-cleanup-ledger.md
autonomous: true
requirements:
  - CODE-01
  - DOC-10
must_haves:
  truths:
    - "src/state.js exists with the in-memory state singleton extracted byte-identical from app.js per D-02"
    - "src/router.js exists with setRoute + render dispatcher; imports the 12 views from src/views/*"
    - "src/main.js exists with init() + DOMContentLoaded auto-start; imports src/firebase/app.js FIRST (D-06 critical)"
    - "index.html script tag flipped from `./app.js` to `./src/main.js` in the same atomic terminal commit (D-03)"
    - "app.js DELETED entirely (D-03)"
    - "runbooks/phase-4-cleanup-ledger.md row at app.js:1 (`// @ts-nocheck`) closed (file deleted)"
    - "Phase 2 snapshot baselines (dashboard/diagnostic/report) produce zero diff — pre/post cutover (D-12 verification gate)"
    - "tests/views/{dashboard,diagnostic,report}.test.js retargeted from app.js import to src/main.js (or src/views/* directly per Phase 2 D-08 toMatchFileSnapshot pattern)"
    - "Phase 2 D-04 bridge comment in index.html removed"
    - "All `// @ts-nocheck` annotations gone from the codebase (firebase-init.js's was closed Wave 1; app.js's closes here)"
  artifacts:
    - path: "src/state.js"
      provides: "In-memory state singleton (mode/route/orgId/pillarId/chart/userMenuOpen/authTab/authError/expandedPillars/chatMessages/chatSubscription/chatSubscribedFor)"
      exports: ["state"]
    - path: "src/router.js"
      provides: "setRoute(route) + render() dispatcher; calls per-route renderX(state, deps) imports from src/views/*"
      exports: ["setRoute", "render"]
    - path: "src/main.js"
      provides: "init() + DOMContentLoaded auto-start; imports src/firebase/app.js FIRST"
      exports: []
    - path: "index.html"
      provides: "Production shell — script src='./src/main.js' (NOT app.js)"
      contains: "src/main.js"
  key_links:
    - from: "src/main.js"
      to: "src/firebase/app.js"
      via: "FIRST import (per D-06 critical ordering — initializeApp + initAppCheck must run before any data/views code)"
      pattern: "import.*firebase/app"
    - from: "src/main.js"
      to: "src/router.js"
      via: "import { render } from './router.js'"
      pattern: "from.*\\./router\\.js"
    - from: "src/router.js"
      to: "all 12 src/views/*.js"
      via: "import { renderX } from './views/X.js'"
      pattern: "from.*\\./views/"
    - from: "index.html"
      to: "src/main.js"
      via: "<script type=\"module\" src=\"./src/main.js?v=51\">"
      pattern: "src/main\\.js"
---

<objective>
Wave 5 — Extract state.js + router.js + main.js (D-02 — LAST, after all 12 views), DELETE app.js entirely + flip index.html script tag from `./app.js` to `./src/main.js` in a single atomic terminal commit (D-03), retarget the 3 view-snapshot tests to src/views/* (Phase 2 D-08 baselines must hold), close the final IIFE-related cleanup-ledger rows.

Purpose: This is the load-bearing cutover commit. Wave 5's verification gate is `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` showing zero diff — pre/post the app.js deletion + main.js boot path. If snapshots diff, the dispatcher shape changed mid-flight; revert and re-extract per D-12. The dispatcher signature (`renderRoute(main, user, org)` → calls per-route renderer with state injected) was preserved across Waves 1–4 by keeping app.js's IIFE alive as the dispatcher; Wave 5 replicates that shape in src/router.js and main.js owns init().

Output: 3 new modules (state.js + router.js + main.js); paired tests; index.html flipped; app.js DELETED; 3 view-snapshot tests retargeted; cleanup-ledger Suppressions table reaches its terminal state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modular-split-quick-wins/04-CONTEXT.md
@.planning/phases/04-modular-split-quick-wins/04-PATTERNS.md
@.planning/phases/04-modular-split-quick-wins/04-01-SUMMARY.md
@.planning/phases/04-modular-split-quick-wins/04-04-SUMMARY.md
@.planning/research/ARCHITECTURE.md
@runbooks/phase-4-cleanup-ledger.md
@CLAUDE.md

<interfaces>
Source-of-truth analogs per PATTERNS.md "Wave 5: src/state.js + src/router.js + src/main.js" lines 949-1041.

state.js — verbatim from app.js:563-577:
```
export const state = {
  mode: localStorage.getItem("baselayers:mode") || "internal",
  route: "dashboard",
  orgId: null,
  pillarId: null,
  chart: null,
  userMenuOpen: false,
  authTab: "signin",
  authError: null,
  expandedPillars: new Set(),
  chatMessages: [],
  chatSubscription: null,
  chatSubscribedFor: null,
};
```

router.js — extract setRoute (app.js:618-622) + render (app.js:627-666) + renderRoute (app.js:668-689):
```
import { state } from "./state.js";
import { h } from "./ui/dom.js";
import { renderTopbar, renderFooter } from "./ui/chrome.js";
import { renderAuth } from "./views/auth.js";
import { renderDashboard } from "./views/dashboard.js";
// ... 10 more view imports

export function setRoute(route) { state.route = route; render(); }
export function render() { /* body byte-identical with app.js:627-666 */ }
export function renderRoute(main, user, org) { /* body byte-identical with app.js:668-689 */ }
```

main.js — extract init() + DOMContentLoaded (app.js:5466-5494):
```
// CRITICAL D-06: src/firebase/app.js MUST be the FIRST import — initializeApp +
// initAppCheck run before any data/* or views/* code touches the SDK.
import "./firebase/app.js";
// ...other imports
import { migrateV1IfNeeded, clearOldScaleResponsesIfNeeded } from "./data/migration.js";
import { syncFromCloud } from "./data/cloud-sync.js";
import { render } from "./router.js";

function init() {
  // body byte-identical with app.js:5466-5494
}

if (document.readyState === "loading") document.addEventListener("DOMContentLoaded", init);
else init();
```

index.html — current state (after Wave 1 + Wave 2 edits):
- `<script type="module" src="./src/firebase/app.js?v=51">` (Wave 1)
- `<script type="module" src="./src/ui/charts.js?v=51">` (Wave 1, if not already collapsed)
- `<script src="data/pillars.js?v=51">` (legacy IIFE; Claude's discretion to move to src/data/pillars.js or import from src/main.js)
- `<script type="module" src="./app.js?v=51">` (← FLIPS THIS WAVE TO src/main.js)

Wave 5 final state (atomic commit):
- `<script type="module" src="./src/main.js?v=52">` (the only application bootstrap script)
- The src/main.js's `import "./firebase/app.js"` first line takes care of SDK init (replaces the standalone `<script src="./src/firebase/app.js">` tag)
- The src/main.js imports src/ui/charts.js (or chart usage falls naturally through views which import from src/ui/charts.js)
- data/pillars.js stays as a separate `<script>` tag (legacy global) OR move to src/data/pillars.js + import — Claude's discretion D-12
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create src/state.js + src/router.js + src/main.js + paired tests (the IIFE survivors extracted to ESM)</name>
  <files>src/state.js, src/router.js, src/main.js, tests/state.test.js, tests/router.test.js, tests/main.test.js</files>
  <read_first>
    - app.js (full state object at line 563-577 — read for verbatim extraction)
    - app.js (setRoute at line 618-622; render at line 627-666; renderRoute at line 668-689 — verify exact line ranges since prior waves shifted lines significantly)
    - app.js (init function at line 5466-5494 — verify exact range)
    - app.js (FULL READ — confirm only state + dispatcher + init remain after Waves 1-4 extraction; the IIFE should be ~200 lines now, down from 5,289)
    - .planning/phases/04-modular-split-quick-wins/04-PATTERNS.md "Wave 5" section (paste-ready code blocks for state.js + router.js + main.js)
    - src/firebase/app.js (Wave 1 — confirm import-side-effect pattern works for D-06 ordering)
    - src/data/{migration,cloud-sync}.js (Phase 2 — main.js calls these on init)
    - src/data/pillars.js OR root-level data/pillars.js (Claude's discretion D-12 — leave at root vs move to src/data/)
    - All 12 src/views/*.js (Wave 4 — router imports these)
    - src/ui/{dom,chrome}.js (router imports these)
  </read_first>
  <behavior>
    - Test 1 (tests/state.test.js): state object exports the 12 fields with the expected default values
    - Test 2 (tests/state.test.js): localStorage.getItem("baselayers:mode") is honoured for the mode field
    - Test 3 (tests/router.test.js): setRoute("dashboard") sets state.route AND calls render()
    - Test 4 (tests/router.test.js): render() finds the #app element + calls renderRoute() with the current route
    - Test 5 (tests/router.test.js): renderRoute dispatches to renderDashboard for state.route === "dashboard" (mock all view imports)
    - Test 6 (tests/main.test.js): init() runs migrateV1IfNeeded + syncFromCloud + render() in the documented order
  </behavior>
  <action>
1. Create `src/state.js` byte-identical from app.js:563-577 per PATTERNS.md "Wave 5" lines 949-975 (paste-ready):
```
// src/state.js
// @ts-check
// Phase 4 (D-02 / D-12): byte-identical extraction from app.js:563-577. The
// IIFE no longer captures `state` — it imports from here. Phase 6 (AUTH-14)
// will tighten state.session shape when Firebase Auth replaces the local-
// allowlist substrate.

/** @type {*} */
export const state = {
  mode: localStorage.getItem("baselayers:mode") || "internal",
  route: "dashboard",
  orgId: null,
  pillarId: null,
  chart: null,
  userMenuOpen: false,
  authTab: "signin",
  authError: null,
  expandedPillars: new Set(),
  chatMessages: [],
  chatSubscription: null,
  chatSubscribedFor: null,
};
```

2. Create `src/router.js` byte-identical extraction of setRoute + render + renderRoute from app.js:618-689 per PATTERNS.md "Wave 5" lines 977-1003. Add imports for all 12 views + ui/dom + ui/chrome:
```
// src/router.js
// @ts-check
// Phase 4 (D-02 / D-12): byte-identical extraction from app.js:618-689.
// renderRoute imports each view from src/views/* (Wave 4 extracted them).

import { state } from "./state.js";
import { h, $, $$ } from "./ui/dom.js";
import { renderTopbar, renderFooter } from "./ui/chrome.js";
import { renderAuth } from "./views/auth.js";
import { renderDashboard } from "./views/dashboard.js";
import { renderDiagnostic } from "./views/diagnostic.js";
import { renderPillar } from "./views/pillar.js";
import { renderActions } from "./views/actions.js";
import { renderEngagement } from "./views/engagement.js";
import { renderReport } from "./views/report.js";
import { renderAdmin } from "./views/admin.js";
import { renderDocuments } from "./views/documents.js";
import { renderChat } from "./views/chat.js";
import { renderRoadmap } from "./views/roadmap.js";
import { renderFunnel } from "./views/funnel.js";

/** @param {string} route */
export function setRoute(route) {
  state.route = route;
  render();
}

export function render() {
  // body byte-identical with app.js:627-666
  // [Executor: paste app.js:627-666 body here verbatim, replacing closure-captured
  //  references with imports]
}

/** @param {HTMLElement} main @param {*} user @param {*} org @returns {void} */
export function renderRoute(main, user, org) {
  // body byte-identical with app.js:668-689
  // [Executor: paste app.js:668-689 body here verbatim, dispatching by state.route
  //  to the appropriate imported render* function]
}
```

3. Create `src/main.js` byte-identical extraction of init() + DOMContentLoaded auto-start from app.js:5466-5494 per PATTERNS.md "Wave 5" lines 1009-1029:
```
// src/main.js
// @ts-check
// Phase 4 (D-06 critical): src/firebase/app.js MUST be imported FIRST so that
// initializeApp + initAppCheck run before any data/* or views/* code touches
// the SDK. Phase 7 (FN-04) wires the App Check body without re-ordering this.
import "./firebase/app.js";
import "./firebase/auth.js"; // side-effect: onAuthStateChanged dispatch + signInAnonymously bridge
import "./firebase/db.js"; // side-effect: window.FB.* bridge for any IIFE-residual code paths (Phase 6 deletes)
import "./firebase/storage.js"; // side-effect: window.FB.* bridge
import "./ui/charts.js"; // side-effect: window.Chart bridge

import { migrateV1IfNeeded, clearOldScaleResponsesIfNeeded } from "./data/migration.js";
import { syncFromCloud } from "./data/cloud-sync.js";
import { render } from "./router.js";
import { state } from "./state.js";

// Plus any other state-setup imports the IIFE init() calls today

function init() {
  // body byte-identical with app.js:5466-5494
  // [Executor: paste app.js:5466-5494 body here verbatim, replacing closure-captured
  //  references with imports. The init body typically:
  //  - calls migrateV1IfNeeded() → side-effect on localStorage / Firestore
  //  - calls clearOldScaleResponsesIfNeeded()
  //  - calls syncFromCloud()
  //  - listens for "firebase-ready" event
  //  - calls render()]
}

if (document.readyState === "loading") {
  document.addEventListener("DOMContentLoaded", init);
} else {
  init();
}
```

4. Create paired tests:

`tests/state.test.js`:
```
import { describe, it, expect } from "vitest";
import { state } from "../src/state.js";

describe("src/state.js", () => {
  it("exports state object with expected default fields", () => {
    expect(state).toMatchObject({
      route: "dashboard",
      orgId: null,
      pillarId: null,
      chart: null,
      userMenuOpen: false,
      authTab: "signin",
      authError: null,
      chatMessages: [],
      chatSubscription: null,
      chatSubscribedFor: null,
    });
  });
  it("expandedPillars is a Set", () => {
    expect(state.expandedPillars).toBeInstanceOf(Set);
  });
  it("mode reads from localStorage", () => {
    // The default-value-from-localStorage behaviour was tested at module load;
    // this is a smoke check
    expect(typeof state.mode).toBe("string");
  });
});
```

`tests/router.test.js` — mock all view imports, verify dispatcher logic:
```
import { describe, it, expect, vi, beforeEach } from "vitest";

vi.mock("../src/views/dashboard.js", () => ({ renderDashboard: vi.fn(() => document.createElement("div")) }));
vi.mock("../src/views/auth.js", () => ({ renderAuth: vi.fn(() => document.createElement("div")) }));
// ... mock all 12 views
vi.mock("../src/firebase/app.js", () => ({ app: {}, auth: {}, db: {}, storage: {}, functions: {} }));

import { setRoute } from "../src/router.js";
import { state } from "../src/state.js";

describe("src/router.js", () => {
  beforeEach(() => {
    document.body.innerHTML = '<div id="app"></div>';
    state.route = "dashboard";
  });
  it("setRoute(route) sets state.route and triggers render", () => {
    setRoute("admin");
    expect(state.route).toBe("admin");
  });
  // ... more dispatcher tests
});
```

`tests/main.test.js` — mock side-effect imports + verify init order:
```
import { describe, it, expect, vi } from "vitest";

const initOrder = [];
vi.mock("../src/data/migration.js", () => ({
  migrateV1IfNeeded: vi.fn(() => initOrder.push("migrate")),
  clearOldScaleResponsesIfNeeded: vi.fn(() => initOrder.push("clear")),
}));
vi.mock("../src/data/cloud-sync.js", () => ({
  syncFromCloud: vi.fn(() => { initOrder.push("sync"); return Promise.resolve(); }),
}));
vi.mock("../src/router.js", () => ({ render: vi.fn(() => initOrder.push("render")) }));
vi.mock("../src/firebase/app.js", () => ({ app: {}, auth: {}, db: {}, storage: {}, functions: {} }));
vi.mock("../src/firebase/auth.js", () => ({}));
vi.mock("../src/firebase/db.js", () => ({}));
vi.mock("../src/firebase/storage.js", () => ({}));
vi.mock("../src/ui/charts.js", () => ({}));

describe("src/main.js init", () => {
  it("imports run in the documented order (firebase first, then data, then render)", async () => {
    document.body.innerHTML = '<div id="app"></div>';
    await import("../src/main.js");
    // Allow the async DOMContentLoaded handler / immediate init to run
    await new Promise(r => setTimeout(r, 0));
    expect(initOrder).toContain("migrate");
    expect(initOrder).toContain("sync");
    expect(initOrder).toContain("render");
  });
});
```

5. Verify the new modules import-resolve cleanly: `npm run typecheck` exits 0; `npm run test -- tests/state.test.js tests/router.test.js tests/main.test.js` exits 0. At this point app.js is STILL alive — the bridge tags in index.html still load it. Task 2 does the cutover.
  </action>
  <verify>
    <automated>npm run test -- tests/state.test.js tests/router.test.js tests/main.test.js &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/state.js, src/router.js, src/main.js exist with `// @ts-check`
    - tests/state.test.js, tests/router.test.js, tests/main.test.js exist
    - `grep -c "import" src/main.js | head -1` shows the firebase/app.js import is the FIRST import line (D-06 critical)
    - `grep -c "renderDashboard\\|renderAuth\\|renderDiagnostic\\|renderPillar\\|renderActions\\|renderEngagement\\|renderReport\\|renderAdmin\\|renderDocuments\\|renderChat\\|renderRoadmap\\|renderFunnel" src/router.js` returns at least 12 (all view imports present)
    - `grep -c "DOMContentLoaded" src/main.js` returns at least 1
    - `npm run test -- tests/state.test.js tests/router.test.js tests/main.test.js` exits 0
    - `npm run typecheck` exits 0
    - app.js still exists and runs (Task 2 cuts over)
    - `git diff tests/__snapshots__/views/dashboard.html` is empty
  </acceptance_criteria>
  <done>state.js + router.js + main.js extracted byte-identical with paired tests; D-06 ordering preserved (firebase/app.js imported FIRST in main.js); all 12 view imports wired into router.js; app.js still alive (bridge — Task 2 cuts over).</done>
</task>

<task type="auto">
  <name>Task 2: Atomic terminal cutover commit — index.html script flip + app.js DELETION + retarget 3 view-snapshot tests + close final cleanup-ledger rows + remove window.FB bridge code</name>
  <files>index.html, app.js (DELETE), tests/views/dashboard.test.js, tests/views/diagnostic.test.js, tests/views/report.test.js, src/firebase/auth.js, src/firebase/db.js, src/firebase/storage.js, src/ui/charts.js, runbooks/phase-4-cleanup-ledger.md</files>
  <read_first>
    - index.html (FULL READ — current state has multiple script tags from Wave 1 + Wave 2 + Wave 3 + Wave 4 bridges)
    - app.js (FULL READ — should be ~150-300 lines now, mostly stub IIFE wrapper around the imports + init shim from Waves 1-4)
    - tests/views/dashboard.test.js (current state — likely imports app.js; Wave 5 retargets to src/main.js OR src/views/dashboard.js directly per Phase 2 D-08 toMatchFileSnapshot pattern)
    - tests/views/diagnostic.test.js + tests/views/report.test.js (similar)
    - tests/__snapshots__/views/{dashboard,diagnostic,report}.html (Phase 2 D-08 baselines — must hold zero diff after cutover)
    - src/firebase/auth.js (Wave 1 — has `window.FB = window.FB || {}` bridge for app.js IIFE; this code can come out now that app.js is dying)
    - src/firebase/db.js (Wave 1 — has window.FB bridge code)
    - src/firebase/storage.js (Wave 1 — has window.FB bridge code)
    - src/ui/charts.js (Wave 1 — has window.Chart bridge for app.js)
    - runbooks/phase-4-cleanup-ledger.md (final state — only the app.js:1 @ts-nocheck row should remain in Suppressions table; this task closes it by deleting the file)
  </read_first>
  <action>
This task lands as a SINGLE atomic terminal commit per D-03. The commit message follows Phase 1 D-25 atomic-commit pattern:
```
refactor(04-05): app.js dies — flip index.html to src/main.js (D-03 cutover)
```

**STEP 1: Update `index.html`** to its terminal Wave 5 state. The single application bootstrap is `src/main.js`; data/pillars.js stays as a separate `<script>` tag (legacy global, used by domain logic — Claude's discretion D-12 leaves it OR move to src/data/ — recommend: leave at root, import from src/main.js OR keep `<script>` tag for backwards compat with domain references). The Wave 1 + Wave 2 bridge `<script type="module">` tags for src/firebase/app.js + src/ui/charts.js + src/firebase/auth.js etc. are NO LONGER NEEDED — src/main.js imports them all transitively.

Final index.html `<body>` script section:
```
<!-- Phase 4 Wave 5 (D-03): app.js died; src/main.js is the single bootstrap -->
<script src="data/pillars.js?v=52"></script>
<script type="module" src="./src/main.js?v=52"></script>
```

(Bumped version to ?v=52 to bust any cache. Removed: the standalone src/firebase/app.js script tag, src/ui/charts.js script tag, app.js script tag.)

If Phase 2 D-04 had a "bridge comment" anywhere in index.html (e.g., `<!-- Phase 2: type="module" — Phase 4 may flip src -->`) DELETE it now.

**STEP 2: DELETE `app.js`** (`git rm app.js`). The atomic commit lands the index.html flip + app.js deletion together. NOT TWO COMMITS — ONE COMMIT (D-03 explicit).

**STEP 3: Remove the `window.FB.*` bridge code** from src/firebase/auth.js + src/firebase/db.js + src/firebase/storage.js + src/ui/charts.js. These bridges existed only to keep app.js's IIFE working during Waves 1-4. With app.js gone, the bridges are dead code. Per D-12 faithful extraction: leaving them creates unused state on `window`; removing them tightens the surface. Diff:

In src/firebase/auth.js, delete:
```
if (typeof window !== "undefined") {
  window.FB = window.FB || {};
  window.FB.currentUser = u;
}
```
(Inside the onAuthStateChanged callback — the dispatchEvent line stays for any downstream-phase listeners.)

In src/firebase/db.js, delete the bottom `if (typeof window !== "undefined") { window.FB = ...; window.FB.firestore = ...; }` block.

In src/firebase/storage.js, delete the bottom `if (typeof window !== "undefined") { window.FB = ...; window.FB.storageOps = ...; }` block.

In src/ui/charts.js, delete the bottom `if (typeof window !== "undefined") { window.Chart = Chart; }` block.

**STEP 4: Retarget the 3 view-snapshot tests.** The current tests/views/{dashboard,diagnostic,report}.test.js (from Phase 2) imported app.js to drive rendering and snapshot the DOM. Per Phase 2 D-13 (toMatchFileSnapshot pattern) they assert against tests/__snapshots__/views/{dashboard,diagnostic,report}.html. Wave 5 retargets these tests to import from src/main.js OR directly from src/views/{dashboard,diagnostic,report}.js + src/state.js.

Recommended retarget pattern (cleaner than booting main.js for tests):
```
// tests/views/dashboard.test.js (Wave 5 retarget)
import { describe, it, expect, beforeEach } from "vitest";
import { renderDashboard } from "../../src/views/dashboard.js";
import { state } from "../../src/state.js";

describe("renderDashboard — snapshot baseline (Phase 2 D-08)", () => {
  beforeEach(() => {
    document.body.innerHTML = "";
    state.orgId = "<seed-org-id-from-tests/fixtures/snapshot-org.json>";
    // mock data layer per Phase 2 D-11 reusable factory
  });
  it("matches the Phase 2 baseline at tests/__snapshots__/views/dashboard.html", async () => {
    // Use the same fixture as Phase 2 D-10's snapshot-org.json
    const el = renderDashboard(state, { /* deps */ });
    document.body.appendChild(el);
    await expect(document.body.innerHTML).toMatchFileSnapshot("../__snapshots__/views/dashboard.html");
  });
});
```

CRITICAL: the test must seed the state + data layer with the EXACT same fixture Phase 2 used (likely `tests/fixtures/snapshot-org.json`). The toMatchFileSnapshot path stays at `tests/__snapshots__/views/dashboard.html` — Phase 2 D-08 / D-13 contract. Read tests/views/dashboard.test.js current state to identify the fixture seeding pattern; replicate.

If the existing tests pass with `git diff tests/__snapshots__/views/dashboard.html` zero before retargeting, the retargeted test must also produce zero diff. If it DOES diff, the byte-identical extraction was incomplete — revert and re-extract.

**STEP 5: Run the snapshot baseline verification gate (D-03):**
```
npm run test -- tests/views/dashboard.test.js tests/views/diagnostic.test.js tests/views/report.test.js
git diff tests/__snapshots__/views/dashboard.html  # MUST be empty
git diff tests/__snapshots__/views/diagnostic.html # MUST be empty
git diff tests/__snapshots__/views/report.html      # MUST be empty
```

If ANY of these diffs are non-empty: REVERT the cutover commit, debug per-extraction, retry. The snapshot baseline is the rendered-DOM contract; non-zero diff = D-12 violation = extraction was not byte-identical somewhere in Waves 1-4.

**STEP 6: Close the FINAL cleanup-ledger rows.** In `runbooks/phase-4-cleanup-ledger.md`:
- DELETE the row at app.js:1 (`// @ts-nocheck`) — file deleted
- DELETE any remaining rows at app.js:N — file deleted (every row whose source file is app.js closes by virtue of the file deletion; verify the Suppressions table is now zero rows for app.js entries)

The Suppressions table at end of Wave 5 may still have:
- Out-of-band-soft-fail rows (OSV-Scanner soft-fail, gitleaks-action Node 20, INTERNAL_PASSWORD_HASH for Phase 6, no-restricted-imports tracking row with checkboxes for Waves 1-4 closed and Wave 6 still pending) — these close in Wave 6 or out of phase.

**STEP 7: Run the full test+typecheck+lint+build gate:**
```
npm run test
npm run typecheck
npm run lint
npm run build
```
All exit 0. The Wave 5 ESLint state holds Wave 1-4 boundaries; Wave 6 final-hardens. Build must produce dist/ without app.js references.

**STEP 8: Commit message** (single atomic terminal commit per D-03):
```
refactor(04-05): app.js dies — flip index.html to src/main.js (D-03 cutover)

- DELETE app.js (5,289 → 0 lines; closes runbooks/phase-4-cleanup-ledger.md
  row at app.js:1 // @ts-nocheck)
- index.html: <script src="./app.js"> → <script src="./src/main.js"> + remove
  Wave 1-4 bridge tags (firebase/app.js, ui/charts.js — main.js imports
  transitively now)
- src/firebase/{auth,db,storage}.js + src/ui/charts.js: remove window.FB / 
  window.Chart bridge code (was alive only to keep app.js IIFE working during
  Waves 1-4)
- tests/views/{dashboard,diagnostic,report}.test.js: retarget from app.js
  import to src/views/* + src/state.js (Phase 2 D-08 baseline preserved —
  zero diff verified)
- runbooks/phase-4-cleanup-ledger.md: final IIFE-residual rows closed

Closes CODE-01 (modular split — boundary complete; ARCHITECTURE.md §2 directory
tree fully populated; views/* dispatch through router.js + main.js).

Verification:
- npm run test: 149+ tests, all green
- npm run typecheck: clean (no @ts-nocheck remains anywhere)
- npm run lint: Wave 1-4 ESLint flips enforced
- npm run build: dist/ produced without app.js references
- git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html: ZERO
  (D-12 faithful-extraction discipline holds across the cutover)

DOC-10 increment: SECURITY.md update lands separately in Wave 6 alongside
the final § Code Quality + Module Boundaries paragraph (D-19 Wave 6 mapping —
modular-split + lint-enforced-boundaries narrative + cleanup-ledger zero-out).
```
  </action>
  <verify>
    <automated>test ! -f app.js &amp;&amp; grep "src/main.js" index.html &amp;&amp; ! grep "app.js" index.html &amp;&amp; npm run test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; git diff --exit-code tests/__snapshots__/views/dashboard.html tests/__snapshots__/views/diagnostic.html tests/__snapshots__/views/report.html</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f app.js` (file deleted)
    - `grep -c "src/main.js" index.html` returns 1 (and only 1 — the canonical bootstrap)
    - `grep -c "\\./app\\.js" index.html` returns 0
    - `grep -c "src/firebase/app.js" index.html` returns 0 (Wave 1 bridge tag removed; main.js imports it transitively)
    - `grep -c "src/ui/charts.js" index.html` returns 0 (Wave 1 bridge tag removed; main.js imports it transitively)
    - `grep -c "window.FB" src/firebase/auth.js src/firebase/db.js src/firebase/storage.js` returns 0 (bridge code removed)
    - `grep -c "window.Chart" src/ui/charts.js` returns 0
    - `grep -c "@ts-nocheck" src/ app.js index.html 2>/dev/null` returns 0 (every @ts-nocheck eliminated; firebase-init.js + app.js both deleted)
    - `grep -c "app.js:" runbooks/phase-4-cleanup-ledger.md` returns 0 (every app.js: line-reference row closed)
    - `git diff tests/__snapshots__/views/dashboard.html` is empty
    - `git diff tests/__snapshots__/views/diagnostic.html` is empty
    - `git diff tests/__snapshots__/views/report.html` is empty
    - `npm run test` exits 0 (149+ tests + Wave 1-5 new tests all green)
    - `npm run typecheck` exits 0 (zero @ts-nocheck remaining anywhere)
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `find dist -name "app-*.js"` matches (Vite still produces a main bundle, just sourced from src/main.js)
  </acceptance_criteria>
  <done>app.js DELETED; index.html flipped to src/main.js as the single bootstrap; window.FB/window.Chart bridge code removed from firebase/* + ui/charts.js; 3 view-snapshot tests retargeted with zero baseline diff; cleanup-ledger Suppressions table cleaned of all app.js: rows; CODE-01 modular-split boundary COMPLETE; full test/typecheck/lint/build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → boot path | src/main.js is the single application bootstrap; src/firebase/app.js (initializeApp + initAppCheck) MUST run FIRST per D-06; the boot path is now linear + auditable |
| client → IIFE residue | Wave 5 eliminates the IIFE entirely; window.FB / window.Chart bridge code removed; no more closure-captured globals |
| client → snapshot-baseline contract | Phase 2 D-08 view-snapshot baselines are the rendered-DOM regression contract; Wave 5's verification gate is `git diff` showing zero — non-zero diff = D-12 byte-identical violation in any prior wave |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-5-1 | Tampering | boot-order race (App Check before Firestore/Storage/Auth) | mitigate | D-06 critical: src/main.js's `import "./firebase/app.js"` is the FIRST import; app.js exports run synchronously at module-graph load; getAuth/getFirestore/getStorage/getFunctions all run AFTER initAppCheck. Phase 7 (FN-04) wires the App Check body into the existing slot — zero adapter-shape change. |
| T-4-5-2 | Spoofing | rendered-DOM drift across cutover (visual regression) | mitigate | tests/__snapshots__/views/{dashboard,diagnostic,report}.html zero-diff verification IS the cutover gate (D-03). CI fails on any baseline diff; D-12 faithful-extraction discipline enforced across all Waves 1-5. |
| T-4-5-3 | Information Disclosure | window.FB residue post-cutover | mitigate | Bridge code removed from firebase/* + ui/charts.js. After cutover, no application code reads/writes window.FB or window.Chart. ESLint no-restricted-globals could be added in Wave 6 (recommended) to harden. |
| T-4-5-4 | Repudiation | snapshot baseline tampering (someone updates the baseline to mask a regression) | mitigate | Phase 2 D-15 snapshot-presence backstop in CI uses `test -s tests/__snapshots__/views/*.html`; baseline updates must be documented per D-12 atomic-commit + reviewable in PR. Phase 11 evidence pack will preserve audit-trail. |
| T-4-5-5 | DoS | main.js fails to boot (import resolution error) | mitigate | Vite production build at `npm run build` exits 0 only if all imports resolve; CI hard-fail (Phase 1 D-09). The dist/main-*.js bundle is what's deployed; module-graph errors surface at build, not at runtime. |
</threat_model>

<verification>
1. `npm run test` exits 0 (149+ tests + new state/router/main tests + retargeted view-snapshot tests all green)
2. `npm run typecheck` exits 0 (every // @ts-nocheck annotation gone — firebase-init.js was deleted Wave 1; app.js deleted Wave 5)
3. `npm run lint` exits 0
4. `npm run build` exits 0; dist/main-*.js produced (no app.js references)
5. `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty (THE cutover gate per D-03)
6. `test ! -f app.js` (file deleted)
7. index.html script section contains exactly `<script src="data/pillars.js?v=52">` + `<script type="module" src="./src/main.js?v=52">` (no app.js, no Wave 1-4 bridge tags)
8. window.FB / window.Chart bridge code removed from firebase/* + ui/charts.js
9. Cleanup-ledger Suppressions table contains zero rows for app.js source lines
10. CODE-01 (modular split — boundary complete) is now CLOSED across the codebase
</verification>

<success_criteria>
- src/state.js + src/router.js + src/main.js exist with byte-identical extractions per D-02 / D-12
- main.js imports src/firebase/app.js as its FIRST line (D-06 ordering preserved)
- index.html flipped from `./app.js` to `./src/main.js` in atomic terminal commit (D-03)
- app.js DELETED entirely
- window.FB / window.Chart bridge code removed from firebase/* + ui/charts.js
- 3 view-snapshot tests retargeted to src/views/* + src/state.js with zero baseline diff (Phase 2 D-08 contract preserved)
- Cleanup-ledger rows for app.js:* all closed (Suppressions table no longer references app.js)
- CODE-01 (modular split boundary) COMPLETE
- npm run test/typecheck/lint/build all green
- ZERO `@ts-nocheck` annotations remain anywhere in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/04-modular-split-quick-wins/04-05-SUMMARY.md` documenting:
- The state.js + router.js + main.js extracted shapes (with the exact app.js line ranges they came from)
- The single atomic terminal commit's diff summary (commit SHA, files changed, lines + / -)
- The before/after index.html comparison
- The window.FB bridge code removal diff (which files, which lines)
- The 3 view-snapshot test retarget patterns + the zero-diff verification artefact
- The cleanup-ledger final state (Suppressions table size before vs after Wave 5)
- The CODE-01 closure verification (boundary complete; ARCHITECTURE.md §2 directory tree fully populated)
- npm run test/typecheck/lint/build outputs (counts, durations, zero failures)
- Wave 6's pending work (per-directory coverage thresholds + final ESLint hardening + CODE-11 + CODE-13 + cleanup-ledger zero-out + SECURITY.md final paragraph)
</output>
