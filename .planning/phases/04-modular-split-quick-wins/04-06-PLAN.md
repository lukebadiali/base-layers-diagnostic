---
phase: 04-modular-split-quick-wins
plan: 06
type: execute
wave: 6
depends_on:
  - "04-05"
files_modified:
  - vite.config.js
  - eslint.config.js
  - src/util/ids.js
  - src/data/migration.js
  - src/ui/modal.js
  - tests/util/ids.test.js
  - tests/data/migration.test.js
  - runbooks/phase-4-cleanup-ledger.md
  - SECURITY.md
autonomous: false
requirements:
  - CODE-02
  - CODE-11
  - CODE-13
  - DOC-10
must_haves:
  truths:
    - "vite.config.js test.coverage.thresholds extended per D-21 (data/** raised to 95%; ui/** 100%; views/** 80%; state.js/router.js/main.js 90%; firebase/+cloud/+observability/ excluded)"
    - "Coverage exclude block updated: app.js + firebase-init.js rows REMOVED (both deleted by Wave 5); src/firebase/** + src/cloud/** + src/observability/** ADDED to exclude"
    - "ESLint final hardening: every per-wave no-restricted-imports rule from Waves 1-4 is at error; Wave 6 verifies zero `\"warn\"` strings remain for the no-restricted-imports group"
    - "src/util/ids.js formatWhen uses Math.floor for monotonic-decreasing labels (CODE-11 closes L4)"
    - "src/data/migration.js dead v1-migration code path REMOVED (CODE-13 closes L2; verified via flag/code-path analysis BEFORE removal)"
    - "src/ui/modal.js root.innerHTML='' replaced with root.replaceChildren() (forward-tracked from Wave 2)"
    - "runbooks/phase-4-cleanup-ledger.md Suppressions table has ZERO unchecked rows (D-17 phase-close gate)"
    - "SECURITY.md § Code Quality + Module Boundaries gains the modular-split + lint-enforced-boundaries paragraph (D-19 Wave 6 final mapping)"
    - "CHECKPOINT: human-verify reads the SUMMARY + cleanup-ledger zero-out + verifies test/typecheck/lint/build all green"
  artifacts:
    - path: "vite.config.js"
      provides: "Per-directory coverage thresholds extended per D-21"
      contains: "src/ui/\\*\\*"
    - path: "eslint.config.js"
      provides: "All Wave 1-4 boundaries at error; final hardening verified"
    - path: "src/util/ids.js"
      provides: "formatWhen uses Math.floor (CODE-11 closes L4)"
    - path: "src/data/migration.js"
      provides: "Dead v1-migration code REMOVED (CODE-13 closes L2)"
    - path: "runbooks/phase-4-cleanup-ledger.md"
      provides: "Suppressions table empty (D-17 phase-close gate)"
    - path: "SECURITY.md"
      provides: "§ Code Quality + Module Boundaries final paragraph (D-19)"
  key_links:
    - from: "vite.config.js"
      to: "all per-directory thresholds"
      via: "test.coverage.thresholds object"
      pattern: "thresholds:"
    - from: "src/util/ids.js formatWhen"
      to: "Math.floor"
      via: "CODE-11 swap"
      pattern: "Math\\.floor"
---

<objective>
Wave 6 — Cleanup. Extend per-directory coverage thresholds (D-21), final ESLint hardening (every boundary now error), close CODE-11 (formatWhen Math.floor), close CODE-13 (dead v1-migration code removal — gated on flag/code-path verification), close the Wave-2-forward-tracked src/ui/modal.js innerHTML→replaceChildren swap, drain the cleanup ledger to zero rows in the Suppressions table (D-17 phase-close gate), append the final SECURITY.md DOC-10 paragraph (§ Code Quality + Module Boundaries — modular-split + lint-enforced-boundaries narrative per D-19 Wave 6 mapping). Final task is a human-verify checkpoint that confirms phase close.

Purpose: Phase 4 close gate. D-17 mandates zero rows in the Suppressions table; D-21 mandates extended per-directory coverage thresholds; D-19 mandates the final SECURITY.md paragraph. The pre-deletion verification for CODE-13 (dead v1-migration) is a load-bearing safety check — verify v1Active flag is false on all live data BEFORE removing the migration path; do not delete reactively.

Output: Updated vite.config.js + eslint.config.js + src/util/ids.js + src/data/migration.js + src/ui/modal.js; cleanup-ledger drained; SECURITY.md final paragraph; phase-close human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modular-split-quick-wins/04-CONTEXT.md
@.planning/phases/04-modular-split-quick-wins/04-PATTERNS.md
@.planning/phases/04-modular-split-quick-wins/04-05-SUMMARY.md
@runbooks/phase-4-cleanup-ledger.md
@CLAUDE.md

<interfaces>
Coverage thresholds per D-21 (PATTERNS.md "Wave 6: vite.config.js" lines 1063-1081):
```
thresholds: {
  // Global threshold still NOT set — firebase/, cloud/, observability/ are
  // excluded by design (Phase 4 D-21).
  "src/domain/**":        { lines: 100, branches: 100, functions: 100, statements: 100 },
  "src/util/**":          { lines: 100, branches: 100, functions: 100, statements: 100 },
  "src/auth/**":          { lines: 95,  branches: 95,  functions: 95,  statements: 95 },
  "src/data/**":          { lines: 95,  branches: 95,  functions: 95,  statements: 95 }, // raised from 90
  "src/ui/**":            { lines: 100, branches: 100, functions: 100, statements: 100 }, // NEW
  "src/views/**":         { lines: 80,  branches: 80,  functions: 80,  statements: 80 },  // NEW
  "src/state.js":         { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  "src/router.js":        { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  "src/main.js":          { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  // src/firebase/**, src/cloud/**, src/observability/** EXCLUDED in Phase 4 per D-21.
},
```

Coverage exclude block — REMOVE app.js + firebase-init.js (deleted by Wave 5); ADD src/firebase/** + src/cloud/** + src/observability/**:
```
exclude: [
  "tests/**",
  "data/pillars.js",         // legacy global; not source-of-truth
  "vite.config.js",
  "eslint.config.js",
  "**/_generators/**",
  "node_modules/**",
  "dist/**",
  "coverage/**",
  "src/firebase/**",         // NEW per D-21 (adapter — exercised through data/* tests)
  "src/cloud/**",             // NEW per D-21 (Phase 4 stubs; Phase 7/8 will set thresholds)
  "src/observability/**",     // NEW per D-21 (Phase 4 stubs; Phase 9 will set thresholds)
],
```

CODE-11 — formatWhen Math.floor swap. Read src/util/ids.js current formatWhen body — find the duration/age calculations using Math.round or implicit truncation; replace with Math.floor for monotonic-decreasing labels (e.g., "5 minutes ago" → "4 minutes ago" never drifts backwards then forwards).

CODE-13 — Dead v1-migration code in src/data/migration.js. Pre-deletion verification:
1. Confirm via Firestore data audit: `loadUsers().length > 0` returns true on production (means v1→v2 migration already happened on every user); v1-localStorage path is dead.
2. Confirm via flag: `migrateV1IfNeeded` early-return condition (Phase 2 D-05 / D-12 preserves the `if (loadUsers().length > 0) return;` early return). Identify the legacy v1-localStorage migration body that's now dead — the part that runs only when no users exist.
3. CHECKPOINT before delete: human verifies the pre-deletion check passes.

src/ui/modal.js innerHTML→replaceChildren — forward-tracked from Wave 2 ("Note for planner" in PATTERNS.md). The two sites at app.js:552 + app.js:559 (now src/ui/modal.js) where `root.innerHTML = ""` clears the modal root. Replace with `root.replaceChildren()` — semantically identical for clearing children, but consistent with CODE-05 sweep.

Final ESLint hardening — verify no `"warn"` strings remain for `no-restricted-imports` rules. Wave 6 also adds (recommended optional hardening per Wave 5 threat model):
```
// Wave 6 (recommended): block window.FB / window.Chart access (Wave 5 cleanup)
{
  files: ["src/**/*.js"],
  rules: {
    "no-restricted-globals": [
      "error",
      { name: "FB", message: "window.FB removed in Phase 4 Wave 5 — use src/firebase/* imports." },
    ],
  },
},
```

D-19 Wave 6 SECURITY.md paragraph — § Code Quality + Module Boundaries — modular-split + lint-enforced-boundaries narrative; OWASP ASVS V14.2 + ISO 27001:2022 A.8.28 + SOC2 CC8.1 cited.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CODE-13 pre-deletion verification + CODE-13 dead-code removal + CODE-11 formatWhen Math.floor + Wave 2 forward-tracked modal.js innerHTML→replaceChildren</name>
  <files>src/util/ids.js, src/data/migration.js, src/ui/modal.js, tests/util/ids.test.js, tests/data/migration.test.js, tests/ui/modal.test.js, runbooks/phase-4-cleanup-ledger.md</files>
  <read_first>
    - src/util/ids.js (FULL READ — find formatWhen body; identify the truncation site)
    - src/data/migration.js (FULL READ — find migrateV1IfNeeded + clearOldScaleResponsesIfNeeded; identify the v1-localStorage code path that's now dead)
    - src/ui/modal.js (FULL READ — find the two `root.innerHTML = ""` lines)
    - tests/util/ids.test.js (FULL READ — confirm formatWhen tests; CODE-11 may shift test expectations)
    - tests/data/migration.test.js (FULL READ — confirm migration tests; CODE-13 deletion may make some tests dead — coordinate)
    - runbooks/phase-4-cleanup-ledger.md (forward-tracking rows for Wave 6 closures)
    - .planning/codebase/CONCERNS.md §L2 (dead v1-migration code) + §L4 (formatWhen monotonic)
  </read_first>
  <behavior>
    - Test 1 (tests/util/ids.test.js — CODE-11): formatWhen with a 90-second age returns "1 minute ago" (Math.floor(90/60) = 1), not "2 minutes ago" (Math.round). Add monotonicity assertion.
    - Test 2 (tests/util/ids.test.js): for ages crossing the minute boundary (60s → 119s), formatWhen output stays "1 minute ago" (never drifts forward then back).
    - Test 3 (tests/data/migration.test.js): migrateV1IfNeeded with no localStorage v1 data + existing users in Firestore returns early (no-op). The dead-code branch we removed was reachable only when v1Active was true at startup; production has v1Active=false, so the branch is unreachable.
    - Test 4 (tests/ui/modal.test.js): existing modal mount + close tests still pass (replaceChildren is semantically equivalent to innerHTML='' for clearing children).
  </behavior>
  <action>
**STEP 1: CODE-11 — src/util/ids.js formatWhen Math.floor swap.**

Read `src/util/ids.js` formatWhen body. Identify each `Math.round` (or implicit Math.round via division then string conversion) for duration/age calculations. Pattern (typical):
```
// Before
const mins = Math.round((Date.now() - then) / 60000);

// After (CODE-11 — monotonic decreasing labels per L4)
const mins = Math.floor((Date.now() - then) / 60000);
```

Apply Math.floor consistently across all duration calculations: seconds, minutes, hours, days. Rationale: Math.round can flip "2 minutes ago" between displays as 90s passes 1.5min boundary; Math.floor produces monotonic decreasing labels ("1 minute ago" stays until 120s elapses).

Update `tests/util/ids.test.js` with the new expected values + a monotonicity assertion:
```
it("formatWhen output is monotonic decreasing across the minute boundary (CODE-11)", () => {
  const t0 = Date.now() - 90 * 1000;  // 90s old
  expect(formatWhen(t0)).toBe("1 minute ago"); // Math.floor(90/60) = 1
  const t1 = Date.now() - 119 * 1000; // 119s old
  expect(formatWhen(t1)).toBe("1 minute ago"); // Math.floor(119/60) = 1
});
```

**STEP 2: CODE-13 PRE-DELETION verification (load-bearing).**

Before removing any code from src/data/migration.js, confirm v1Active is false everywhere. The pre-deletion gate has two parts:

(a) Read the existing migrateV1IfNeeded body:
```
export function migrateV1IfNeeded({ loadUsers, ... }) {
  if (loadUsers().length > 0) return; // EARLY RETURN — v1→v2 already happened
  // ... v1-localStorage migration body that's dead in production
}
```
The dead code is the body AFTER the early return that runs only when `loadUsers().length === 0`. With Firestore audit confirming users exist on production, this branch is unreachable.

(b) Verify by reading scout output OR by adding a runtime check in main.js (Wave 5) that logs to console when migrateV1IfNeeded executes the dead body — run for one full session, confirm zero invocations. If unable to run a live verification, rely on (a) — the early-return guard is documented in Phase 2 cleanup-ledger D-05 row.

Document the verification result in this task's SUMMARY: "v1-migration body unreachable on production — verified via Firestore loadUsers().length > 0 + Phase 2 D-05 early-return guard. Removal safe."

If verification FAILS (i.e., v1Active is unexpectedly true on production), DO NOT remove the code; defer CODE-13 to a follow-up phase and document in the cleanup ledger as "deferred — production has live v1 data, requires migration completion first."

**STEP 3: CODE-13 — Remove dead v1-migration code (gated on STEP 2 pass).**

In `src/data/migration.js`, after the `if (loadUsers().length > 0) return;` early return, delete the dead body. Keep the early return + the function signature (so callers still work). The result:
```
export function migrateV1IfNeeded({ loadUsers, ... }) {
  if (loadUsers().length > 0) return;
  // CODE-13 (Phase 4 Wave 6): dead v1-migration body removed after pre-deletion
  // verification confirmed v1Active=false on all production data. Closes L2.
}
```

Alternatively, if `migrateV1IfNeeded` is now entirely a no-op early-return + comment, you can delete the entire function and update callers to delete their call. Phase 1 D-25 atomic-commit pattern: this commit also updates callers (likely src/main.js) to remove the call.

Update `tests/data/migration.test.js` — DELETE tests that exercised the dead v1-migration body (they were the regression baseline for behaviour we're now removing). Keep tests for `clearOldScaleResponsesIfNeeded` (which is a different function and not dead). Document the test deletion: "Removed 5 tests covering the dead v1-localStorage migration path; kept 4 tests for clearOldScaleResponsesIfNeeded which remains live (CODE-13 closure; src/data/migration.js coverage threshold remains achievable per D-21)."

**STEP 4: src/ui/modal.js — Wave 2 forward-tracked CODE-05 closure.**

Find the two `root.innerHTML = ""` lines in src/ui/modal.js (originally app.js:552 + app.js:559). Replace each with `root.replaceChildren()`:
```
// Before
root.innerHTML = "";

// After (CODE-05 / Wave 6 forward-tracked from Wave 2)
root.replaceChildren();
```

Verify tests/ui/modal.test.js still passes (the modal mount + close behaviour is preserved — replaceChildren() removes all children of root, semantically identical to innerHTML="" for the clearing use case).

**STEP 5: Close cleanup-ledger forward-tracking rows for Wave 6.**

In `runbooks/phase-4-cleanup-ledger.md`, close:
- "Wave 4 candidate: src/ui/modal.js — replace root.innerHTML='' with root.replaceChildren() once Wave 4 fully sweeps innerHTML sites" — DELETE this row
- Any CODE-11 row (if explicitly tracked) — DELETE
- Any CODE-13 row — DELETE

Also close the Phase 2 JSDoc-was-`any` rows that are now resolvable now that all extractions are complete:
- `src/data/migration.js` `migrated` `any` — depending on whether removal of v1-migration code makes the type tightening trivial, either tighten now OR document as "no further work required" per D-12 discipline

**STEP 6: Verify all tests + typecheck + lint + build:**
```
npm run test
npm run typecheck
npm run lint
npm run build
```
All exit 0. Snapshot baselines unchanged.
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Math.floor" src/util/ids.js` returns at least 1 (CODE-11)
    - `grep -c "Math.round" src/util/ids.js` returns 0 in formatWhen body (CODE-11 — verify by reading the function)
    - `grep -c "innerHTML *= *\"\"" src/ui/modal.js` returns 0 (CODE-05 forward-tracked closure)
    - `grep -c "replaceChildren" src/ui/modal.js` returns at least 2 (replaces both sites)
    - `grep -c "v1.*localStorage\\|v1Active\\|migrateV1" src/data/migration.js | head -1` shows the dead-code body removed (only the early-return + comment + function signature remain)
    - `grep -c "CODE-13" runbooks/phase-4-cleanup-ledger.md` returns 0 (row deleted)
    - `grep -c "modal.js — replace root.innerHTML" runbooks/phase-4-cleanup-ledger.md` returns 0 (row deleted)
    - `npm run test` exits 0
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>CODE-11 (formatWhen Math.floor) closed; CODE-13 (dead v1-migration code) closed gated on pre-deletion verification; src/ui/modal.js innerHTML→replaceChildren forward-tracked closure landed; relevant cleanup-ledger rows deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Extend per-directory coverage thresholds (D-21) + final ESLint hardening + window.FB/Chart no-restricted-globals + verify zero unchecked rows in Suppressions table + SECURITY.md DOC-10 final paragraph</name>
  <files>vite.config.js, eslint.config.js, runbooks/phase-4-cleanup-ledger.md, SECURITY.md</files>
  <read_first>
    - vite.config.js (current state — coverage.thresholds at lines 50-57; coverage.exclude at lines 38-49)
    - eslint.config.js (current state — Wave 1-4 entries from prior plans)
    - runbooks/phase-4-cleanup-ledger.md (current state — Suppressions table should have zero rows after Task 1; out-of-band-soft-fail table may still have rows for Phase 6/7/8/9 closures)
    - SECURITY.md (current state — find § Code Quality + Module Boundaries section OR create it after the existing Phase 4 Wave 1/2/4 paragraphs)
    - .planning/research/SUMMARY.md "Compliance Mapping Cheat-Sheet" (citations)
    - .planning/phases/04-modular-split-quick-wins/04-PATTERNS.md "Wave 6: vite.config.js (D-21 thresholds)" + "Waves 1–6: eslint.config.js (D-04 per-wave warn→error flips)" sections (paste-ready code)
  </read_first>
  <action>
**STEP 1: Extend `vite.config.js` test.coverage.thresholds per D-21.**

Read current vite.config.js lines 50-57. Replace the thresholds object with the D-21 extended state per PATTERNS.md "Wave 6: vite.config.js" lines 1063-1081 (paste-ready):
```
thresholds: {
  // Global threshold still NOT set — firebase/, cloud/, observability/ are
  // excluded by design (Phase 4 D-21).
  "src/domain/**":        { lines: 100, branches: 100, functions: 100, statements: 100 },
  "src/util/**":          { lines: 100, branches: 100, functions: 100, statements: 100 },
  "src/auth/**":          { lines: 95,  branches: 95,  functions: 95,  statements: 95 },
  "src/data/**":          { lines: 95,  branches: 95,  functions: 95,  statements: 95 }, // raised from 90
  "src/ui/**":            { lines: 100, branches: 100, functions: 100, statements: 100 }, // NEW
  "src/views/**":         { lines: 80,  branches: 80,  functions: 80,  statements: 80 },  // NEW
  "src/state.js":         { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  "src/router.js":        { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  "src/main.js":          { lines: 90,  branches: 90,  functions: 90,  statements: 90 },  // NEW
  // src/firebase/**, src/cloud/**, src/observability/** EXCLUDED in Phase 4 per D-21.
},
```

**STEP 2: Update `vite.config.js` coverage.exclude block.**

Per PATTERNS.md "Wave 6: vite.config.js" — REMOVE app.js + firebase-init.js rows (both deleted by Wave 5); ADD src/firebase/** + src/cloud/** + src/observability/** to exclude:
```
exclude: [
  "tests/**",
  "data/pillars.js",
  "vite.config.js",
  "eslint.config.js",
  "**/_generators/**",
  "node_modules/**",
  "dist/**",
  "coverage/**",
  "src/firebase/**",         // NEW per D-21 (adapter excluded — exercised through data/* tests)
  "src/cloud/**",             // NEW per D-21 (Phase 4 stubs; Phase 7/8 sets thresholds)
  "src/observability/**",     // NEW per D-21 (Phase 4 stubs; Phase 9 sets thresholds)
],
```

Run `npm run test:coverage` (or `npm run test -- --coverage`) and confirm:
- domain/util/ui all hit 100%
- auth/data hit 95%+
- views hit 80%+
- state.js/router.js/main.js hit 90%+
- firebase/cloud/observability are absent from the report (excluded)

If any threshold misses, EITHER add tests to bring coverage up (preferred) OR document the miss with rationale in CONTRIBUTING.md per Phase 2 D-17 + cleanup-ledger forward-tracking. Phase 4 close is gated on thresholds passing.

**STEP 3: Final ESLint hardening in `eslint.config.js`.**

Verify every Wave 1-4 entry uses `"error"` (not `"warn"`) for the no-restricted-imports group. Wave 6 final-check command:
```
grep -nE 'no-restricted-imports[^\\n]*"warn"' eslint.config.js
```
Expected: zero matches. If any survive, flip them.

ADD (recommended hardening per Wave 5 threat model): no-restricted-globals to block window.FB / window.Chart access:
```
// Wave 6: window.FB / window.Chart removed Wave 5; block reintroduction
{
  files: ["src/**/*.js"],
  rules: {
    "no-restricted-globals": [
      "error",
      {
        name: "FB",
        message: "window.FB was removed in Phase 4 Wave 5 — use src/firebase/* imports.",
      },
      {
        name: "Chart",
        message: "Direct Chart global removed in Phase 4 Wave 5 — use createChart() from src/ui/charts.js.",
      },
    ],
  },
},
```

Verify by running `npm run lint` after the edit; should pass since src/ doesn't reference window.FB or Chart any more.

**STEP 4: Drain the cleanup-ledger Suppressions table to zero (D-17 phase-close gate).**

Run the regeneration commands from runbooks/phase-4-cleanup-ledger.md "How to regenerate this table":
```
git grep -nE "eslint-disable-next-line" -- src/ index.html
git grep -nE "@ts-(nocheck|ignore|expect-error)" -- src/ index.html
```

Expected: zero output (every Wave 1-5 closure removed app.js + firebase-init.js entirely; src/* uses // @ts-check + JSDoc, no @ts-nocheck or @ts-ignore; src/ files have no eslint-disable comments because Wave 1-5 extractions removed them all).

If output is non-zero, DELETE the corresponding rows in the Suppressions table that were already closed by Waves 1-5 BUT also document any genuinely-still-needed survivor in a "Persistent suppressions" subsection with rationale + reflect in SECURITY.md so audit narrative is honest.

CRITICAL: D-17 mandates ZERO rows. If any rows remain, this is a phase-close blocker — fix or document-as-persistent (latter is rare and requires explicit justification).

Out-of-band-soft-fail table — per Phase 4 close mapping:
- OSV-Scanner soft-fail row: re-evaluate at 2026-06-03 (calendar-driven; orthogonal to phase close)
- `no-restricted-imports` warn→error tracking row: ALL waves now closed (Waves 1-4 closed the per-boundary flips; Wave 6 verified zero `"warn"` strings) — DELETE the tracking row entirely OR mark all checkboxes done + leave for the Phase 11 audit narrative
- `INTERNAL_PASSWORD_HASH` row: closes at Phase 6 AUTH-14 — leave open
- gitleaks-action Node 20 row: closes via Dependabot — leave open
- Phase 3 GH-Pages rollback substrate row: closes at cutover_date+14 calendar — leave open until calendar tick fires
- Phase 3 — meta-CSP regression guard row: closed Wave 1 (D-18 lands tests/index-html-meta-csp.test.js) — DELETE if not already deleted
- Phase 4 forward-tracking section (D-09 pass-throughs / D-11 stubs): leave (these close in Phase 5/6/7/8/9 — they ARE the audit-narrative substrate)

**STEP 5: Append SECURITY.md DOC-10 final paragraph (D-19 Wave 6 mapping).**

Find or create § Code Quality + Module Boundaries section. Append:

```
### Phase 4 Wave 6 — Modular split + lint-enforced module boundaries (CODE-01 + CODE-02)

Phase 4 decomposed the prior 5,289-line `app.js` IIFE into a per-feature ES module layout per `.planning/research/ARCHITECTURE.md` §2:

- `src/firebase/*` — sole import surface for the Firebase SDK (`@firebase/app|auth|firestore|storage|functions|app-check`)
- `src/data/*` — per-collection wrappers (12 collections; 6 owners + 6 Phase-5-rewrite-target pass-throughs); accesses SDK only through `src/firebase/db.js` + `src/firebase/storage.js`
- `src/domain/*` — pure logic with zero Firebase imports
- `src/auth/*` — session-layer logic on top of `src/firebase/auth.js` (Phase 6 replaces with real Email/Password + custom claims; current state is the local-allowlist substrate)
- `src/cloud/*` — empty stub seams Phase 6/7/8 fill (callable Cloud Functions clients)
- `src/views/*` — 12 per-route render functions; may import data/domain/auth/ui/cloud — never firebase/* directly
- `src/ui/*` — DOM helpers (dom/modal/toast/format/chrome/charts/upload); pure DOM, no business logic
- `src/observability/*` — empty stub seams Phase 7/9 fill (Sentry init + audit-event constants + emit)
- `src/state.js` + `src/router.js` + `src/main.js` — boot infrastructure; main.js imports src/firebase/app.js as its FIRST import to guarantee initializeApp + initAppCheck run before any data/* or views/* code touches the SDK

These boundaries are lint-enforced via ESLint `no-restricted-imports` rules in `eslint.config.js`:
- `**/*.js` (excluding src/firebase/**) cannot import `firebase/firestore|storage|auth|app-check|functions` directly
- `src/domain/**` cannot import any `firebase/*` path
- `src/data/**` cannot import `firebase/*` directly (only through the adapter)
- `src/views/**` cannot import `firebase/*` directly (only through data/, domain/, auth/, ui/, cloud/)

Wave 6 also adds a `no-restricted-globals` rule blocking `window.FB` and direct `Chart` global reads (the IIFE-era bridge globals removed in Wave 5). Per-directory coverage thresholds are tiered per `vite.config.js`: domain/util/ui at 100%, auth/data at 95%, views at 80%, state/router/main at 90%; firebase/cloud/observability are excluded by design (adapter exercised through data/* tests; cloud + observability stubs filled in Phase 7/8/9).

Quick wins folded across Waves 1-6 close the corresponding CONCERNS.md findings:
- CODE-03 (Math.random → crypto.randomUUID) — closes H5; ESLint security/detect-pseudoRandomBytes blocks reintroduction
- CODE-04 (delete `html:` branch in `h()` + permanent XSS regression test) — closes C4; ESLint no-unsanitized blocks reintroduction
- CODE-05 (17 innerHTML= → replaceChildren) — closes M2
- CODE-06 (inline-style sweep) — closes M5; precondition for Phase 10 HOST-07 strict-CSP enforcement
- CODE-07 (alert → toast notify) — closes M3
- CODE-08 (renderConversation shared helper) — closes M8 (chat/funnel duplication)
- CODE-09 (client-side upload validation) — closes H6 client-side; trust boundary at Phase 5 storage.rules + Phase 7 callable
- CODE-10 (tab-title unread badge memoisation) — closes M9
- CODE-11 (formatWhen Math.floor) — closes L4
- CODE-12 (rel="noopener noreferrer" on download links) — closes L3 / CWE-1021
- CODE-13 (dead v1-migration code removal — gated on pre-deletion verification of v1Active=false on all production data) — closes L2

The CSP allowlist tightening (Wave 1 — `cdn.jsdelivr.net`, `fonts.googleapis.com`, `fonts.gstatic.com` dropped via Chart.js npm import + Google Fonts self-host) shrinks Phase 10's strict-CSP enforcement work to a single-knob flip (drop `'unsafe-inline'` from `style-src`).

Phase 4 cleanup-ledger Suppressions table reached its terminal state (D-17 phase-close gate): zero rows. The forward-tracking section retains rows for Phase 5 (D-09 pass-through bodies → subcollection rewrites), Phase 6 (AUTH-14 deletes src/auth/state-machine.js + INTERNAL_PASSWORD_HASH; AUTH-07 fills src/cloud/claims-admin.js), Phase 7 (FN-04 fills src/firebase/check.js + src/cloud/audit.js + src/cloud/retry.js + src/observability/audit-events.js constants), Phase 8 (LIFE-04/GDPR-01 fills src/cloud/soft-delete.js + src/cloud/gdpr.js), and Phase 9 (OBS-01 fills src/observability/sentry.js; AUDIT-05 wires emitAuditEvent in views/*) — these are the audit-narrative substrate, not leftover suppressions.

Citations: OWASP ASVS L2 v5.0 — V14.2 (Dependencies — npm migration), V14.7 (Build & Deploy — lint-enforced boundaries), V5.3 (Output Encoding / XSS prevention — CODE-04 layered with CODE-05/06); ISO/IEC 27001:2022 Annex A — A.8.28 (Secure coding — modular boundary enforcement), A.8.24 (Use of cryptography — CSPRNG via CODE-03); SOC 2 — CC8.1 (Change management — atomic per-requirement commits), CC6.1 (Logical access — boundary enforcement via lint); GDPR Art. 32(1)(b) (Confidentiality of processing — XSS + upload validation), 32(1)(d) (Testing/evaluating effectiveness — coverage thresholds + permanent regression tests).
```

**STEP 6: Final verification — run the complete gate one more time:**
```
npm run test         # all tests green
npm run typecheck    # zero @ts-nocheck/@ts-ignore
npm run lint         # zero violations across Wave 1-4 boundaries + Wave 6 globals
npm run build        # dist/ produced cleanly
```

Plus:
- `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty
- Cleanup-ledger Suppressions table is empty (D-17 gate)
- D-21 thresholds pass in npm run test:coverage
  </action>
  <verify>
    <automated>npm run test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run test -- --coverage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "src/ui/\\*\\*" vite.config.js` returns 1 (D-21 ui/** threshold added)
    - `grep -c "src/views/\\*\\*" vite.config.js` returns 1
    - `grep -c '"src/data/\\*\\*".*95' vite.config.js` matches at least 1 (raised from 90)
    - `grep -c "src/firebase/\\*\\*" vite.config.js` returns 1 (in exclude block)
    - `grep -c "src/cloud/\\*\\*" vite.config.js` returns 1 (in exclude block)
    - `grep -c "src/observability/\\*\\*" vite.config.js` returns 1 (in exclude block)
    - `grep -c "app.js\\|firebase-init.js" vite.config.js` returns 0 (deleted-files rows removed from exclude)
    - `grep -nE 'no-restricted-imports[^\\n]*"warn"' eslint.config.js` returns no match (every boundary at error)
    - `grep -c "no-restricted-globals" eslint.config.js` returns at least 1 (window.FB/Chart guard)
    - `grep -cE "^\\| \\[ \\]" runbooks/phase-4-cleanup-ledger.md` returns 0 (zero unchecked rows in Suppressions table — D-17 gate)
    - Suppressions table contains zero rows in the table body (count by `git grep -nE "eslint-disable-next-line" -- src/` returning zero output)
    - `grep -c "Phase 4 Wave 6 — Modular split + lint-enforced module boundaries" SECURITY.md` returns 1
    - `grep -c "OWASP ASVS L2" SECURITY.md` matches at least 1
    - `grep -c "ISO/IEC 27001:2022 Annex A" SECURITY.md` matches at least 1
    - `grep -c "SOC 2" SECURITY.md` matches at least 1
    - `npm run test` exits 0 (full suite)
    - `npm run typecheck` exits 0 (zero @ts-nocheck/@ts-ignore anywhere)
    - `npm run lint` exits 0
    - `npm run build` exits 0
    - `npm run test -- --coverage` exits 0 (D-21 per-directory thresholds all met)
  </acceptance_criteria>
  <done>D-21 per-directory coverage thresholds extended; final ESLint hardening (zero `"warn"` strings remain for no-restricted-imports group); window.FB/Chart no-restricted-globals added; Suppressions table drained to zero rows (D-17 phase-close gate cleared); SECURITY.md § Code Quality + Module Boundaries final paragraph appended; npm run test/typecheck/lint/build/coverage all green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 4 close — human-verify checkpoint</name>
  <files>(checkpoint task — operator-driven verification; no files modified by Claude in this task)</files>
  <action>This is a human-verify checkpoint. Claude does NOT execute this task; the operator runs the steps in &lt;how-to-verify&gt; and types the resume signal. The &lt;what-built&gt; / &lt;how-to-verify&gt; / &lt;resume-signal&gt; structure below is the operator-facing prompt.</action>
  <verify>
    <automated>echo "checkpoint task — operator verifies per how-to-verify"</automated>
  </verify>
  <done>Operator types "approved" per &lt;resume-signal&gt;. STATE.md is updated to mark Phase 4 complete and ROADMAP.md Phase 4 row is checked.</done>
  <what-built>
Phase 4 modular split is complete. All 6 waves landed:
- Wave 1: firebase/ adapter (6 submodules) + Chart.js npm + Google Fonts self-host + CSP allowlist tightening (3 CDN entries dropped) + CODE-03 (crypto.randomUUID) + meta-CSP regression test (D-18 closes Phase 3 cleanup-ledger row)
- Wave 2: ui/* helpers (6 modules) + CODE-04 (html: branch DELETED + permanent XSS regression fixture) + ESLint domain/* boundary
- Wave 3: 12 data/* wrappers (6 owners + 6 pass-throughs) + 5 cloud/* stubs + 2 observability/* stubs + ESLint data/* boundary + 13 forward-tracking ledger rows
- Wave 4: 12 views/*.js + _shared/render-conversation.js + per-view CODE-05/06/07/08/09/10/12 quick wins folded + ESLint views/* boundary + SECURITY.md § Data Handling paragraph
- Wave 5: state.js + router.js + main.js + atomic terminal cutover (app.js DELETED, index.html flipped to ./src/main.js) + 3 view-snapshot tests retargeted with zero baseline diff
- Wave 6: D-21 per-directory coverage thresholds + final ESLint hardening + CODE-11/13 + Wave-2-forward-tracked modal.js innerHTML→replaceChildren + cleanup-ledger zero-out + SECURITY.md § Code Quality + Module Boundaries final paragraph

The codebase now consists of: src/firebase/ + src/data/ + src/domain/ + src/auth/ + src/cloud/ + src/views/ + src/ui/ + src/observability/ + src/state.js + src/router.js + src/main.js. domain/* has zero Firebase imports (lint-enforced). All 13 CODE-* requirements + DOC-10 incremental are closed.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/04-modular-split-quick-wins/04-06-SUMMARY.md` and read the cleanup-ledger zero-out verification + the per-CODE-* closure summary.

2. Confirm cleanup-ledger Suppressions table is empty:
   ```
   cat runbooks/phase-4-cleanup-ledger.md | head -50
   ```
   Visual check: the Suppressions table should have a header row and a "(empty)" or comment line indicating zero rows.

3. Run the full test+typecheck+lint+build+coverage gate:
   ```
   npm run test && npm run typecheck && npm run lint && npm run build && npm run test -- --coverage
   ```
   All exit 0.

4. Verify snapshot baselines are unchanged:
   ```
   git diff tests/__snapshots__/views/dashboard.html
   git diff tests/__snapshots__/views/diagnostic.html
   git diff tests/__snapshots__/views/report.html
   ```
   All return empty output.

5. Visual smoke check of the application — open http://localhost:5178 (after `npm run dev`) and click through:
   - Sign in (auth view)
   - Dashboard (radar chart renders)
   - Diagnostic (pillar list)
   - One pillar (responses)
   - Report (donut chart)
   - Documents (upload a small PDF; should validate + upload)
   - Chat (open a conversation; verify renderConversation rendering)
   - Roadmap + Funnel
   Each view should render visually identical to pre-Phase-4 (D-12 faithful extraction). If anything visibly different, check git diff on snapshot baseline files and on styles.css.

6. Verify `index.html` final state:
   ```
   cat index.html
   ```
   Contains exactly the new `<script type="module" src="./src/main.js?v=52">` (and `<script src="data/pillars.js?v=52">`); no app.js reference; no firebase-init.js reference.

7. Verify ARCHITECTURE.md §2 directory tree fully populated:
   ```
   ls src/firebase/ src/data/ src/domain/ src/auth/ src/cloud/ src/views/ src/ui/ src/observability/
   ls src/state.js src/router.js src/main.js
   test ! -f app.js
   test ! -f firebase-init.js
   ```
   All commands succeed.

8. Verify SECURITY.md has all 4 Phase 4 paragraphs (Wave 1 § HTTP Security Headers, Wave 2 § Build & Supply Chain, Wave 4 § Data Handling, Wave 6 § Code Quality + Module Boundaries):
   ```
   grep -c "Phase 4 Wave [1246]" SECURITY.md
   ```
   Returns 4.

9. Confirm no `@ts-nocheck` survives anywhere:
   ```
   grep -rE "@ts-nocheck" src/ tests/ index.html 2>/dev/null
   ```
   Returns nothing.
  </how-to-verify>
  <resume-signal>
Type "approved" to mark Phase 4 closed and unblock Phase 5 planning. Or describe issues — common discrepancies include:
- Snapshot baseline non-zero diff (visual regression — investigate which extraction in Waves 1-5 was not byte-identical)
- Coverage threshold miss (under D-21 — add tests OR document with rationale)
- Lint violation (Wave 1-4 boundary breach — re-extract the offending file)
- Cleanup-ledger non-zero rows (D-17 gate breach — close or document-as-persistent)
- A view rendering visibly different (D-12 violation — diff against snapshot baseline)
- Build error (Vite module-graph error — likely a missing export or wrong import path)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit narrative integrity | Cleanup-ledger zero-out is the audit-trail evidence that every Phase 4 suppression closed; D-17 phase-close gate enforces this for SOC2 CC8.1 (change management) |
| coverage threshold enforcement | D-21 per-directory thresholds + Phase 1 D-09 CI hard-fail policy = any coverage drop fails CI; defends against silent dilution of test coverage |
| lint-enforced module boundaries | Wave 1-4 ESLint flips at error + Wave 6 final hardening = lint hard-fails CI on any future regression to the IIFE-era pattern |
| Phase-close completeness | The human-verify checkpoint is the operator-in-the-loop check that the SUMMARY accurately reflects code state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-6-1 | Repudiation | undocumented persistent suppressions | mitigate | D-17 mandates "if a suppression was actually still needed — document under Persistent suppressions with rationale, and reflect in SECURITY.md so the audit narrative is honest." Wave 6 verifies zero unchecked rows; persistent rows (if any) are explicit + rationalised. |
| T-4-6-2 | Tampering | coverage threshold relaxation to mask drops | mitigate | Phase 2 D-17 + D-18 (CONTRIBUTING.md governance) + Phase 1 D-09 CI hard-fail policy block silent threshold drops. Any threshold change requires a documented PR with rationale. |
| T-4-6-3 | Tampering | reintroducing window.FB / Chart globals | mitigate | Wave 6 ESLint no-restricted-globals: error on `FB` + `Chart` from any src/ file; CI hard-fails. |
| T-4-6-4 | Information Disclosure | dead v1-migration code reactivation | mitigate | CODE-13 deletion gated on pre-deletion verification (loadUsers().length > 0 confirms v1Active=false). Pre-deletion check documented in SUMMARY for audit narrative. |
| T-4-6-5 | Spoofing | snapshot baseline tampered with | mitigate | git diff verification at every wave + Phase 2 D-15 snapshot-presence backstop in CI (test -s on baseline files). PR review surfaces baseline updates. |
</threat_model>

<verification>
1. `npm run test` exits 0
2. `npm run typecheck` exits 0 (zero @ts-nocheck anywhere)
3. `npm run lint` exits 0 (Wave 1-4 boundaries + Wave 6 globals all enforced at error)
4. `npm run build` exits 0
5. `npm run test -- --coverage` exits 0 (D-21 per-directory thresholds met)
6. `git diff tests/__snapshots__/views/{dashboard,diagnostic,report}.html` is empty
7. Cleanup-ledger Suppressions table is empty (D-17 phase-close gate cleared)
8. SECURITY.md has 4 Phase 4 paragraphs (Wave 1/2/4/6)
9. ARCHITECTURE.md §2 directory tree fully populated; app.js + firebase-init.js both deleted
10. CODE-01 through CODE-13 all closed; DOC-10 incrementally appended at every wave
11. Human-verify checkpoint approved
</verification>

<success_criteria>
- vite.config.js extended per D-21: data/** raised to 95%; ui/** 100%; views/** 80%; state.js/router.js/main.js 90%; firebase/+cloud/+observability/ excluded
- vite.config.js exclude block updated: app.js + firebase-init.js rows removed; src/firebase/** + src/cloud/** + src/observability/** added
- eslint.config.js: zero `"warn"` strings remain for no-restricted-imports group; no-restricted-globals blocks window.FB + Chart
- src/util/ids.js formatWhen uses Math.floor (CODE-11)
- src/data/migration.js dead v1-migration code REMOVED (CODE-13; gated on pre-deletion verification)
- src/ui/modal.js root.innerHTML='' replaced with root.replaceChildren() (Wave 2 forward-tracked closure)
- runbooks/phase-4-cleanup-ledger.md Suppressions table has ZERO rows (D-17 phase-close gate)
- SECURITY.md § Code Quality + Module Boundaries final paragraph appended (D-19 Wave 6 mapping; OWASP ASVS V14.2 + ISO 27001:2022 A.8.28 + SOC2 CC8.1 cited)
- All 14 requirements (CODE-01..13 + DOC-10) closed
- npm run test/typecheck/lint/build/coverage all green
- Human-verify checkpoint approved → Phase 4 closes; ROADMAP updated; Phase 5 planning unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/04-modular-split-quick-wins/04-06-SUMMARY.md` documenting:
- The vite.config.js threshold + exclude diff
- The eslint.config.js final hardening diff
- The CODE-11 + CODE-13 + modal.js forward-tracked closures
- The cleanup-ledger zero-out evidence (count: 16 → 0 rows in Suppressions table)
- The SECURITY.md § Code Quality + Module Boundaries final paragraph appended
- The full Phase 4 CODE-* closure index (CODE-01 through CODE-13: each row → wave + commit SHA + verification)
- The full Phase 4 DOC-10 increment index (4 SECURITY.md paragraphs: Wave 1/2/4/6)
- npm run test/typecheck/lint/build/coverage final outputs
- The human-verify checkpoint approval timestamp + any noted observations

Then update STATE.md (`progress.completed_phases: 4`; `progress.percent: 33`) and ROADMAP.md (Phase 4 row → checked + Completed date) per Phase 1 D-25 atomic-commit pattern. The Phase 5 planning prerequisites are met: data/* boundaries are in place; views/* consume the stable D-09 pass-through API; subcollection rewrite (DATA-01..06) replaces pass-through bodies without touching views/*.
</output>
