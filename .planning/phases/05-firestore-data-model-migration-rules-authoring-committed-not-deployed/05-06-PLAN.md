---
phase: 05-firestore-data-model-migration-rules-authoring-committed-not-deployed
plan: 06
type: execute
wave: 6
depends_on:
  - "05-05"
files_modified:
  - SECURITY.md
  - runbooks/phase-4-cleanup-ledger.md
  - runbooks/phase-5-cleanup-ledger.md
autonomous: false
requirements:
  - DOC-10
  - RULES-06
must_haves:
  truths:
    - "SECURITY.md gains 4 new sections per D-20: §Firestore Data Model + §Firestore Security Rules - Authored, Not Yet Deployed + §Storage Rules + §Phase 5 Audit Index"
    - "§Firestore Data Model section documents subcollection layout + migration narrative + per-doc idempotency markers (D-02) + the D-05 deviation from Pitfall 1's staging recommendation with the audit-narrative line verbatim"
    - "§Firestore Security Rules section documents predicate library (D-14) + scope (D-17) + RULES-06 commit-not-deploy framing + table-driven test approach"
    - "§Storage Rules section documents size + MIME + path-scope summary + ui/upload.js allowlist single-source-of-truth"
    - "§Phase 5 Audit Index appends a framework citation table per Phase 3 D-15 / Phase 4 D-25 pattern; rows cite OWASP ASVS V4.x + ISO 27001:2022 A.5.x + SOC2 CC6 + GDPR Art 32"
    - "RULES-06 verification gate executed: git log --grep='firebase deploy --only firestore:rules' phase-04..HEAD returns empty (no deploy command in any Phase 5 commit)"
    - "Cleanup-ledger zero-out: 6 Phase 4 carryover rows CLOSED (5 data/* body rewrites in Wave 3 + 1 cloud-sync rewrite in Wave 4); 4 new rows queued for Phase 6/7 (RULES-07 production deploy, AUTH-15 legacy field deletion, FN-09 rateLimits predicate, FN-01..09 server-side writers for auditLog/softDeleted)"
    - "New runbooks/phase-5-cleanup-ledger.md created mirroring runbooks/phase-4-cleanup-ledger.md - tracks the 4 forward-tracking rows for Phase 6/7 + carries over the still-open Phase 4 4.1 main.js-body-migration items"
    - "Commit-SHA backfill at phase close per Phase 3 + Phase 4 precedent"
    - "Phase-close human-verify checkpoint: operator confirms test/typecheck/lint/build all green; npm run test:rules green; 05-PREFLIGHT.md PENDING-USER markers all resolved (or explicit UAT carry-forward documented)"
  artifacts:
    - path: "SECURITY.md"
      provides: "DOC-10 incremental update: 4 new sections + Phase 5 audit index (D-20)"
      contains: "Firestore Data Model"
    - path: "runbooks/phase-5-cleanup-ledger.md"
      provides: "Phase 5 cleanup ledger - zero rows in Phase 5 Suppressions table; 4 forward-tracking rows for Phase 6/7"
    - path: "runbooks/phase-4-cleanup-ledger.md"
      provides: "Updated with Phase 5 closure entries (Wave 3 5 wrappers + Wave 4 H8 fix)"
  key_links:
    - from: "SECURITY.md §Firestore Data Model"
      to: "D-05 staging deviation"
      via: "audit-narrative line: 'Migration is idempotent (per-doc markers) and bracketed by a manual gcloud firestore export taken immediately before the run; rollback is gcloud firestore import of that export. Rules unit-tested against the local emulator before the script runs.'"
      pattern: "idempotent.*manual.*gcloud firestore export"
    - from: "SECURITY.md §Firestore Security Rules"
      to: "RULES-06 commit-not-deploy framing"
      via: "the rules deploy gate is held for Phase 6 (RULES-07) when the Auth migration is live"
      pattern: "committed.*not.*deployed|deploy gate held for Phase 6"
    - from: "SECURITY.md §Phase 5 Audit Index"
      to: "OWASP ASVS V4 / V5 + ISO 27001 A.8 + SOC2 CC6 + GDPR Art 32"
      via: "framework citation table"
      pattern: "ASVS"
    - from: "RULES-06 verification gate"
      to: "git log scan"
      via: "git log --grep='firebase deploy --only firestore:rules' phase-04..HEAD"
      pattern: "firebase deploy --only firestore:rules"
---

<objective>
Wave 6 - Cleanup + DOC-10 + RULES-06 verification gate. Append 4 new sections to SECURITY.md per D-20 (Firestore Data Model + Firestore Security Rules + Storage Rules + Phase 5 Audit Index). Execute the RULES-06 verification gate via git log scan asserting zero `firebase deploy --only firestore:rules` invocations across the phase. Close 6 Phase 4 carryover cleanup-ledger rows (already closed across Waves 3+4 - this wave verifies the closure + appends the Phase 5 audit index entry). Queue 4 new forward-tracking rows for Phase 6/7. Final task is a human-verify checkpoint per Phase 4 Wave 6 precedent.

Purpose: D-08 Wave 6 + D-19 + D-20 + D-21 + RULES-06. Phase-close gate. Compliance bar = "credible, not certified" - the SECURITY.md sections + Audit Index are the artefacts a vendor reviewer reads to answer "how do you secure data at rest + access?"; the answer is "subcollections + claims-based rules + emulator-tested + deploy gate held for Auth migration". Mirrors Phase 3 D-15 (commit-SHA backfill at phase close) + Phase 4 Wave 6 (cleanup-ledger zero-out + SECURITY.md final paragraph + human-verify).

Output: Updated SECURITY.md with 4 new sections + audit index; Phase 4 cleanup-ledger fully closed for Phase 5 carryover; new Phase 5 cleanup-ledger queueing 4 Phase 6/7 forward-trackers; RULES-06 verification gate executed; phase-close human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-CONTEXT.md
@.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-RESEARCH.md
@.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-PATTERNS.md
@.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-VALIDATION.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/codebase/CONCERNS.md
@CLAUDE.md
@SECURITY.md
@runbooks/phase-4-cleanup-ledger.md
@firestore.rules
@storage.rules
@firebase.json

<interfaces>
<!-- SECURITY.md - 4 new sections to append (D-20)

Append AFTER the existing Phase 4 § Code Quality + Module Boundaries paragraph (per Phase 3 + Phase 4 precedent: each phase appends; previous sections preserved verbatim). Include framework citations per Phase 3 D-15 + Phase 4 D-25 pattern. -->

```markdown
## § Firestore Data Model

**Control:** Production Firestore data lives under the subcollection-based shape
specified in `.planning/research/ARCHITECTURE.md` §4 - `orgs/{orgId}/responses/{respId}`,
`orgs/{orgId}/comments/{cmtId}`, `orgs/{orgId}/actions/{actId}`, `orgs/{orgId}/documents/{docId}`,
`orgs/{orgId}/messages/{msgId}`, `orgs/{orgId}/readStates/{userId}` - replacing the prior
monolithic nested-map shape (`orgs/{orgId}.responses[roundId][userId][pillarId][idx]` etc.) that
was approaching the 1 MiB doc-size cliff and exhibited last-writer-wins entanglement (CONCERNS H8).

The migration was executed via a one-shot Node script (`scripts/migrate-subcollections/run.js`)
using the firebase-admin SDK from an internal operator workstation. The script supports a
`--dry-run` flag whose output (`scripts/migrate-subcollections/dry-run-output.log`) is committed
as audit evidence. Per-doc idempotency markers under `migrations/{stepId}/items/{docId}` make
the script re-runnable - DONE markers skip; PENDING markers re-process partial-runs.

**Audit-narrative line (D-05 staging deviation):** "Migration is idempotent (per-doc markers) and
bracketed by a manual `gcloud firestore export` taken immediately before the run; rollback is
`gcloud firestore import` of that export. Rules unit-tested against the local emulator before
the script runs." This deviates from `.planning/research/PITFALLS.md` Pitfall 1's "use a
staging Firebase project" recommendation explicitly. Justification: the project was between
client engagements with no live writes; the per-doc markers + pre-migration export + emulator
rules tests provide equivalent safety with substantially lower operational cost than a permanent
staging project (duplicate IAM, OIDC pool, App Check site keys, billing).

The CONCERNS.md H7 (clock skew on unread tracking) and H8 (last-writer-wins cloud sync) were
folded into Phase 5 Wave 4 as TWO ATOMIC COMMITS per `.planning/research/PITFALLS.md` §Pitfall 20
(the bundled-fix risk): Commit A rewrote `src/domain/unread.js` to read server-time Timestamp
values via duck-typed `toMillis()` (DATA-07); Commit B rewrote `src/data/cloud-sync.js` to
remove the parent-doc syncer entirely + delegate per-subcollection listening to wrappers.

**Evidence:**
- Subcollection layout: `.planning/research/ARCHITECTURE.md` §4
- Migration script: `scripts/migrate-subcollections/run.js` (Wave 2)
- Idempotency marker pattern: `scripts/migrate-subcollections/process-doc.js` (D-02 / Pitfall 10)
- Pre/post assertion harness: `scripts/migrate-subcollections/assertions.js` (DATA-06)
- Pre-migration export procedure: `runbooks/phase5-subcollection-migration.md` §3.1 (D-04)
- Migration runbook: `runbooks/phase5-subcollection-migration.md`
- Operator cutover log: `.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-PREFLIGHT.md`
- H7 fix commit: `feat(05-04): H7 fix - server-clock readStates comparator (DATA-07)`
- H8 fix commit: `feat(05-04): H8 fix - per-subcollection listeners replace parent-doc sync (D-13)`

**Framework citations:**
- OWASP ASVS L2 V4.1.5 (data-model authorization aligned with rules engine)
- ISO/IEC 27001:2022 A.8.10 (information deletion - the legacy nested-map fields stay during Phase 5 as the rollback substrate; a Phase 6+ cleanup wave deletes them)
- SOC 2 CC6.1 (logical access - subcollection-scoped match blocks make access decisions explicit per resource)
- GDPR Art. 32(1)(b) (ongoing confidentiality + integrity - subcollection migration enables per-resource access controls without 1 MiB doc-size cliff)

## § Firestore Security Rules - Authored, Not Yet Deployed

**Control:** `firestore.rules` and `storage.rules` are committed at the repo root. They are
**NOT** deployed to production in Phase 5 (`firebase deploy --only firestore:rules` is not
invoked anywhere in this phase). Sequencing Non-Negotiable #2: rules deploy in lockstep with
the Phase 6 Auth migration when Email/Password sign-in + custom claims + MFA all go live; per
`.planning/research/PITFALLS.md` §Pitfall 1, deploying tightened rules before claims-issuing
Auth is a production lockout pattern.

The rules predicate library (D-14) reads top-to-bottom as English: a reviewer can trace each
`allow` rule back to a named helper (`isAuthed()`, `isInternal()`, `isAdmin()`, `inOrg(o)`,
`notDeleted(r)`, `isOwnAuthor(r)`, `immutable(field)`, `mutableOnly(fields)`) declared once at
the top of the file. `isAuthed()` includes both `email_verified == true` AND
`sign_in_provider != "anonymous"` per `.planning/research/PITFALLS.md` §Pitfall 2 (anonymous
tokens trivially satisfy `request.auth != null`).

Every `allow update` path uses `mutableOnly([...])` field whitelist + `immutable("orgId")` /
`immutable("createdAt")` per D-15 (Pitfall 3 closure). Mass-assignment attacks (a client adding
`role: "admin"` to its own user doc) deny by virtue of the whitelist.

The collection scope per D-17:
- `orgs/{orgId}` parent doc (read: inOrg + notDeleted; create/update: isInternal; delete: false)
- 6 subcollections under `orgs/{orgId}` (responses, comments, actions, documents, messages, readStates)
- `users/{uid}`, `internalAllowlist/{email}`, `auditLog/{eventId}`, `softDeleted/{type}/items/{id}`,
  `rateLimits/{uid}/buckets/{windowStart}` (server-only via Cloud Functions; clients cannot write),
  `roadmaps/{orgId}`, `funnels/{orgId}`, `funnelComments/{id}`

The audit log rules (`auditLog/{eventId}: allow read: if isAdmin(); allow write: if false`)
explicitly prevent the audited user from reading their own audit records (per
`.planning/research/PITFALLS.md` §Pitfall 17 / AUDIT-07).

A table-driven `@firebase/rules-unit-testing` v5 matrix at `tests/rules/firestore.test.js`
iterates 5 roles (anonymous / client_orgA / client_orgB / internal / admin) x 16 collection
paths x 4 ops (read / create / update / delete) with explicit allow/deny expectations per
cell. Cross-cutting suites: `tests/rules/tenant-jump.test.js` (every collection enumerated
under a tenant-jump scenario per RULES-04), `tests/rules/soft-delete-predicate.test.js`,
`tests/rules/server-only-deny-all.test.js`. The CI `test-rules` job boots Firestore + Storage
emulators via `firebase emulators:exec` and runs the matrix on every PR (TEST-08).

**Evidence:**
- Rules file: `firestore.rules` (Wave 1)
- Storage rules: `storage.rules` (Wave 1)
- firebase.json declarations: `firebase.json` `firestore.rules` + `storage.rules` paths declared (Wave 1)
- Predicate library: `firestore.rules` lines 1-30 (D-14)
- Mutation whitelist enforcement: every `allow update` path
- Test matrix: `tests/rules/firestore.test.js` (Wave 1)
- Cross-cutting test suites: `tests/rules/tenant-jump.test.js`, `tests/rules/soft-delete-predicate.test.js`, `tests/rules/server-only-deny-all.test.js`
- CI test-rules job: `.github/workflows/ci.yml` `test-rules` job (Wave 1 + 1 SHA-pinned setup-java action per TOOL-09)
- Phase 5 RULES-06 verification gate (this Wave 6): `git log --grep="firebase deploy --only firestore:rules" phase-04..HEAD` returns empty

**Framework citations:**
- OWASP ASVS L2 V4.1 (general access control), V4.2 (operation level access control), V4.3 (other access control considerations)
- ISO/IEC 27001:2022 A.5.15 (access control), A.5.18 (access rights), A.8.3 (information access restriction)
- SOC 2 CC6.1 (logical access), CC6.2 (new/modified access), CC6.3 (access modification)
- GDPR Art. 32(1)(b) (ongoing confidentiality), Art. 32(2) (level of security appropriate to risk)

## § Storage Rules

**Control:** `storage.rules` enforces three constraints on uploads to
`orgs/{orgId}/documents/{docId}/{filename}`:
1. **Size cap:** `request.resource.size <= 25 * 1024 * 1024` (25 MiB) - matches
   `src/ui/upload.js` `MAX_BYTES` exactly. Closes CONCERNS.md H6 server-side (the client-side
   sanitisation in Phase 4 was the UX-feedback layer; the storage rule is the trust boundary).
2. **MIME allowlist:** `request.resource.contentType.matches('application/pdf|image/jpeg|...')`
   - mirrors the canonical `ALLOWED_MIME_TYPES` constant exported from `src/ui/upload.js`. The
   client-side magic-byte sniff (Phase 4 `validateUpload`) cross-checks the declared content-type
   against the actual file header; the storage rule enforces the declared type at upload time.
3. **Path scope:** the `match /orgs/{orgId}/documents/{docId}/{filename}` block constrains writes
   to the documented prefix; a global `match /{allPaths=**} { allow read, write: if false }`
   denies everything else (defense in depth).

The MIME allowlist is deliberately duplicated between `src/ui/upload.js` and `storage.rules`
because the rules language does not support `import`. A cleanup-ledger row tracks drift if
`ui/upload.js`'s allowlist ever changes - any modification must sync `storage.rules` in the
same commit.

**Evidence:**
- Storage rules: `storage.rules` (Wave 1)
- Single source of truth: `src/ui/upload.js` `ALLOWED_MIME_TYPES` + `MAX_BYTES` (Phase 4 D-15/D-16)
- Cleanup-ledger drift tracker: `runbooks/phase-5-cleanup-ledger.md`
- Storage matrix tests: `tests/rules/storage.test.js` (Wave 1)
- Closure of CONCERNS.md H6: client-side validation (Phase 4 CODE-09) + server-side rule (Phase 5 RULES-05)

**Framework citations:**
- OWASP ASVS L2 V12.1 (file upload), V13.1 (web service security)
- ISO/IEC 27001:2022 A.8.24 (use of cryptography - covers content-type integrity), A.8.7 (protection against malware - MIME allowlist limits attack surface)
- SOC 2 CC6.7 (boundary protection)
- GDPR Art. 32(1)(b) (integrity)

## § Phase 5 Audit Index

| Audit row | Control | Code path | Test | Framework |
|-----------|---------|-----------|------|-----------|
| Subcollection data model | DATA-01..04 | `src/data/responses,comments,actions,documents,messages,read-states.js` | `tests/data/*.test.js` | ASVS V4.1.5 / ISO A.8.10 / SOC2 CC6.1 / GDPR Art 32(1)(b) |
| Migration script (one-shot) | DATA-04 / D-01 | `scripts/migrate-subcollections/run.js` | `tests/scripts/migrate-subcollections/*.test.js` | ASVS V14.5.1 (deployment) / SOC2 CC8.1 |
| Idempotency markers | DATA-05 / D-02 / Pitfall 10 | `scripts/migrate-subcollections/process-doc.js` | `tests/scripts/migrate-subcollections/idempotency.test.js` | SOC2 CC7.4 (system operations - resilience) |
| Pre/post assertion harness | DATA-06 | `scripts/migrate-subcollections/assertions.js` | `tests/scripts/migrate-subcollections/assertions.test.js` | SOC2 CC7.4 |
| Inline legacy fields | DATA-02 / D-03 / Pitfall 5 | `scripts/migrate-subcollections/builders.js` (legacyAppUserId / legacyAuthorId) | `tests/scripts/migrate-subcollections/builders.test.js` | (substrate for Phase 6 AUTH-15 backfill) |
| H7 server-clock comparator | DATA-07 | `src/domain/unread.js` | `tests/domain/unread.test.js` (5-min skew test) | ASVS V8.3.1 / GDPR Art 32(1)(b) |
| H8 per-subcollection listeners | (D-13) | `src/data/cloud-sync.js` | `tests/data/cloud-sync.test.js` | SOC2 CC7.2 (system operations - data integrity) |
| Helper-rich predicate library | RULES-01 / D-14 / Pitfall 2 | `firestore.rules` lines 1-30 | `tests/rules/firestore.test.js` | ASVS V4.1.1 (claims-based access control) |
| Mutation whitelist | RULES-02 / D-15 / Pitfall 3 | `firestore.rules` every `allow update` | `tests/rules/firestore.test.js` (mass-assignment cells) | ASVS V4.2.1 / SOC2 CC6.1 |
| Server-only collections | RULES-03 / D-17 | `firestore.rules` auditLog/softDeleted/rateLimits blocks | `tests/rules/server-only-deny-all.test.js` | ASVS V13.1.5 / Pitfall 17 |
| Tenant isolation | RULES-04 | `firestore.rules` `inOrg(orgId)` predicate | `tests/rules/tenant-jump.test.js` | ASVS V4.3.2 / SOC2 CC6.1 |
| Storage size + MIME + path | RULES-05 | `storage.rules` | `tests/rules/storage.test.js` | ASVS V12.1 / ISO A.8.7 / GDPR Art 32(1)(b) |
| Rules deploy gate held | RULES-06 / Pitfall 1 | (no `firebase deploy --only firestore:rules` invocation) | git log scan in this Wave 6 | (audit-narrative integrity) |
| AUDIT-07 audited user cannot read | RULES-03 / Pitfall 17 | `firestore.rules` auditLog `allow read: if isAdmin()` | `tests/rules/server-only-deny-all.test.js` | ASVS V13.1.5 |
| Migration runbook + rollback | (procedural) | `runbooks/phase5-subcollection-migration.md` | (operator-execution + 05-PREFLIGHT.md Cutover Log) | SOC2 CC9.1 (risk mitigation) / ISO A.5.30 (ICT readiness for business continuity) |

Each row's evidence + test path is a direct verification of the control. A reviewer reading
this index plus the `firestore.rules` and `storage.rules` files plus the `tests/rules/` matrix
output answers the "how do you secure data at rest + access?" portion of a vendor questionnaire
without referencing additional documentation.
```

<!-- runbooks/phase-5-cleanup-ledger.md (NEW - mirrors phase-4-cleanup-ledger.md structure)

Phase 4 cleanup-ledger has Suppressions table + Out-of-band soft-fail entries + Phase 4 carryover. Phase 5 cleanup-ledger:
- Suppressions table at Phase 5 close: ZERO rows (no new suppressions introduced in Phase 5)
- Phase 5 forward-tracking rows: 4 entries queued for Phase 6/7
- Phase 4 carryover rows that REMAIN unclosed: main.js-body-migration sub-wave items (Phase 4 4.1) - NOT in Phase 5 scope per CONTEXT.md `<specifics>` (Wave 4 Dev #1 carries the IIFE forward) -->

```markdown
# Phase 5 Cleanup Ledger

> Phase 5 (Firestore Data Model Migration + Rules Authoring) cleanup tracker.
> Phase entry state: zero new suppressions; 6 Phase 4 carryover rows closed
> across Waves 3+4; 4 new forward-tracking rows queued for Phase 6/7.
> Mirrors `runbooks/phase-4-cleanup-ledger.md` structure.

## Suppressions

(Zero rows - Phase 5 introduced no new `eslint-disable-next-line` or `@ts-nocheck`.
The 12 Phase 4 carryover rows in main.js-body-migration sub-wave (Phase 4 4.1) remain
open in `runbooks/phase-4-cleanup-ledger.md`'s carryover section; those are Phase 4 4.1
sub-wave's responsibility, not Phase 5's.)

## Phase 5 forward-tracking (Phase 6 + Phase 7 closures)

| Carryover item | File / line | Rationale | Closes when |
|---|---|---|---|
| `firestore.rules` + `storage.rules` deploy to production | (deploy via firebase-tools) | Phase 5 Sequencing Non-Negotiable #2: deploy gate held until Phase 6 Auth migration is live + claims propagate | Phase 6 (RULES-07) production deploy step |
| `legacyAppUserId` + `legacyAuthorId` inline fields on migrated docs | `orgs/{orgId}/responses/{respId}` + `orgs/{orgId}/comments/{cmtId}` + `orgs/{orgId}/messages/{msgId}` etc. | D-03 inline legacy fields are the substrate for Phase 6's `legacyAppUserId -> firebaseUid` backfill (AUTH-15 bootstrap migration) | Phase 6 (AUTH-15) backfill completes; cleanup wave deletes the legacy fields |
| `rateLimits/{uid}/buckets/{windowStart}` deny-block | `firestore.rules` `match /rateLimits/...` | D-17 Phase 5 ships `allow read, write: if false`; Phase 7 FN-09 replaces with `request.time` predicate | Phase 7 (FN-09) replaces the body |
| Server-side writers for `auditLog/`, `softDeleted/` | (Cloud Functions in `functions/`) | D-17 Phase 5 ships `allow write: if false` for clients; Phase 7 (FN-01..09 / AUDIT-01..04) adds the Cloud Function writers | Phase 7 (FN-01..09) ships server-side writers |
| `legacyAppUserId` migrations parked in `migrations/{stepId}/items/{docId}` | (migration markers under `migrations/...`) | D-02 markers are post-cutover audit evidence; Phase 8 cleanup-wave deletes them after backups confirm migration outcome | Phase 8 (BACKUP-07 restore-drill or BACKUP-01 daily-export rotation) |
| Old nested-map fields on parent `orgs/{orgId}` docs (.responses, .comments, .actions, .documents, .messages, .readStates) | (parent docs) | NOT deleted by Phase 5 - intentional rollback substrate for the Phase 5 cutover | Phase 6+ cleanup wave deletes parent-doc nested-map fields once Phase 6 Auth migration validated + Phase 7 audit-log infra running |
| Deprecated legacy `markPillarRead` shim in `src/domain/unread.js` | `src/domain/unread.js` | Wave 4 Commit A keeps the shim with the OLD signature for IIFE consumers in src/main.js until Phase 4 4.1 main.js-body-migration sub-wave deletes IIFE callers | Phase 4 4.1 main.js-body-migration sub-wave |
| Deprecated legacy 9-prop `syncFromCloud` shim in `src/data/cloud-sync.js` | `src/data/cloud-sync.js` (parameter-detector branch) | Wave 4 Commit B parameter-detector branch returns no-op + deprecation warn for IIFE consumers | Phase 4 4.1 main.js-body-migration sub-wave |
| Storage rules MIME allowlist drift tracker | `storage.rules` `validMime()` vs `src/ui/upload.js` `ALLOWED_MIME_TYPES` | Rules language doesn't support `import`; allowlist is duplicated; any change to `ui/upload.js` must sync `storage.rules` in the same commit | (cross-file drift; ongoing - close only when both sources of truth converge into a single source via build-step generation) |

## Phase 4 carryover (Phase 4 4.1 main.js-body-migration items - DO NOT CLOSE in Phase 5)

The 12 src/main.js IIFE-resident `eslint-disable-next-line` + `@ts-nocheck` rows tracked in
`runbooks/phase-4-cleanup-ledger.md` "Wave 6 -> main.js-body-migration carryover" section
remain open. Phase 5 Wave 4 ADDED 2 deprecation shims (markPillarRead in domain/unread.js +
legacy 9-prop syncFromCloud in data/cloud-sync.js) that ALSO close in Phase 4 4.1 - those are
tracked in this ledger's "Phase 5 forward-tracking" table above as the relevant rows.

Phase 5 takes no action on Phase 4 4.1 carryover items.

## How to regenerate Phase 5 closure status

```sh
# Verify zero rules deploy commands in Phase 5 commits
git log --grep="firebase deploy --only firestore:rules" phase-04..HEAD

# Verify migration script ran (operator-supplied)
test -f scripts/migrate-subcollections/dry-run-output.log
test -f scripts/migrate-subcollections/real-run-output.log  # exists only after operator-execution

# Verify rules + storage rules committed
test -f firestore.rules
test -f storage.rules

# Verify SECURITY.md DOC-10 sections present
grep -c "## § Firestore Data Model" SECURITY.md  # 1
grep -c "## § Firestore Security Rules" SECURITY.md  # 1
grep -c "## § Storage Rules" SECURITY.md  # 1
grep -c "## § Phase 5 Audit Index" SECURITY.md  # 1
```
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 5 commits -> RULES-06 deploy gate | Verification grep ensures no rules deploy command leaked into Phase 5 commits |
| SECURITY.md -> reviewer | Audit-narrative artefact; reviewer reads sections to answer security questionnaire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-w6-deploy-leak | Tampering / Trust-boundary cutover | Phase 5 commits invoking `firebase deploy --only firestore:rules` | mitigate | Wave 6 verification gate via `git log --grep` returns empty across phase-04..HEAD; if non-empty, the offending commit is reverted before phase close (RULES-06) |
| T-5-w6-doc-drift | Repudiation | SECURITY.md sections drift from actual implementation | mitigate | Audit Index table cross-references every claim to a code path + test + framework citation; commit-SHA backfill at phase close pins the evidence to a specific commit |
| T-5-w6-cleanup-ledger-overflow | Audit-narrative integrity | cleanup-ledger Suppressions table accumulates open rows | mitigate | Phase 5 introduces zero new suppressions; the 4 forward-tracking rows are explicit (Phase 6 RULES-07 / AUTH-15 backfill / Phase 7 FN-09 / Phase 7 server-side writers); the operational ledger is honest |
| T-5-w6-uat-deferred-execution | Availability / Traceability | Wave 5 operator execution deferred to UAT | mitigate | If 05-PREFLIGHT.md Cutover Log has unresolved PENDING-USER markers, Wave 6 surfaces them in 05-HUMAN-UAT.md per Phase 3 + Phase 4 precedent so they can't silently slip into Phase 6 |
| T-5-w6-checkpoint-bypass | Audit | phase-close human-verify skipped | mitigate | Final task is `checkpoint:human-verify gate=blocking` per Phase 4 Wave 6 precedent; operator confirms test/typecheck/lint/build green + 05-PREFLIGHT.md resolved before phase signoff |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: SECURITY.md DOC-10 update + RULES-06 verification gate + Phase 5 cleanup-ledger</name>
  <files>SECURITY.md, runbooks/phase-4-cleanup-ledger.md, runbooks/phase-5-cleanup-ledger.md</files>
  <read_first>
    - SECURITY.md (FULL READ - find existing § sections from Phase 1-4 to confirm append point + heading style; the new sections go AFTER the existing Phase 4 § Code Quality + Module Boundaries section)
    - runbooks/phase-4-cleanup-ledger.md (FULL READ - confirm 6 Phase 4 D-09 + D-13 cleanup-ledger rows are CLOSED across Wave 3 + Wave 4 entries; if any are missing the closure, surface as deviation)
    - .planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-CONTEXT.md (D-19, D-20, D-21)
    - .planning/research/SUMMARY.md "Compliance Mapping Cheat-Sheet" (citations format + framework references)
    - .planning/research/PITFALLS.md §Pitfall 1 (the audit-narrative line for D-05 staging deviation references this)
    - firestore.rules (verify the rules referenced by SECURITY.md sections actually contain the predicates cited)
    - storage.rules
    - 05-PREFLIGHT.md (operator's Cutover Log; if PENDING-USER markers remain, surface them in 05-HUMAN-UAT.md)
  </read_first>
  <action>
**STEP 1: Append the 4 new sections to SECURITY.md.**

Read SECURITY.md to confirm append location (after Phase 4 § Code Quality + Module Boundaries). Paste the 4 sections from `<interfaces>` block above:
1. § Firestore Data Model
2. § Firestore Security Rules - Authored, Not Yet Deployed
3. § Storage Rules
4. § Phase 5 Audit Index

CRITICAL: Each section's "Evidence" subsection MUST cite real artefacts. After pasting:
- Replace any placeholder commit SHAs with actual SHAs (resolve via `git log --oneline -- <file>`). Phase 3 + Phase 4 precedent: commit-SHA backfill at phase close ensures evidence is pinned to specific commits.
- Verify each cited file path exists (`firestore.rules`, `storage.rules`, `scripts/migrate-subcollections/run.js`, etc.).
- Verify the predicate names cited (`isAuthed()`, `isInternal()`, etc.) match the actual `firestore.rules` content.

**STEP 2: Execute RULES-06 verification gate.**

```bash
# Find the merge base (commit where Phase 4 ended + Phase 5 began)
PHASE4_END=$(git log --oneline | grep -m1 "phase-04\|Phase 4 COMPLETE\|04-06" | awk '{print $1}')
echo "Phase 4 end: $PHASE4_END"

# Search for any firebase deploy --only firestore:rules invocation in Phase 5 commits
git log --grep="firebase deploy --only firestore:rules" "$PHASE4_END..HEAD" --all
```

Expected output: EMPTY. If non-empty, the offending commit MUST be reverted before phase close - RULES-06 is a load-bearing gate (Sequencing Non-Negotiable #2).

Also grep the source for any committed deploy command:

```bash
git grep "firebase deploy --only firestore:rules" -- '*.md' '*.yml' '*.json' '*.js' '*.sh'
```

Expected output: EMPTY.

If both grep returns are empty, the gate PASSES; document the result in SECURITY.md "§ Phase 5 Audit Index" row "Rules deploy gate held" + commit-SHA backfill the evidence row.

**STEP 3: Create runbooks/phase-5-cleanup-ledger.md.**

Paste the cleanup-ledger from `<interfaces>` block above. The 4 forward-tracking rows (Phase 6/7 closures) + a section noting Phase 4 4.1 carryover items remain open in `runbooks/phase-4-cleanup-ledger.md`.

After creation, verify:
- Suppressions table is empty (Phase 5 introduced zero new suppressions)
- 4+ forward-tracking rows present (Phase 6 RULES-07 + Phase 6 AUTH-15 + Phase 7 FN-09 + Phase 7 server-side writers + storage MIME drift tracker + 2 deprecation shims for Phase 4 4.1)
- Cross-reference to phase-4-cleanup-ledger.md is explicit

**STEP 4: Confirm Phase 4 cleanup-ledger updates from prior waves.**

Check `runbooks/phase-4-cleanup-ledger.md` already has the 2026-05-08 closure entries from:
- Wave 3 (Plan 05-03) - 5 wrapper closures + 1 read-states.js entry (added in 05-03 Task 3)
- Wave 4 Commit B (Plan 05-04) - cloud-sync.js H8 fix closure entry

If any of these closure entries are missing, surface in the SUMMARY as a deviation (the prior wave's plan was supposed to add them; verify each was committed). Add any missing entries here as a backstop, but flag in SUMMARY.

**STEP 5: 05-HUMAN-UAT.md - check for unresolved Wave 5 markers.**

Read `.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-PREFLIGHT.md`. If any PENDING-USER markers remain unresolved (i.e. Wave 5 operator execution was deferred), create `.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-HUMAN-UAT.md` per Phase 3 + Phase 4 precedent:

```markdown
# Phase 5 Human UAT - Operator-deferred items

**Created:** 2026-05-08 (Phase 5 Wave 6 close)

## Pending operator-execution items

### 1. Production migration cutover (Wave 5 Plan 05-05 Task 2)

**Trigger:** runbooks/phase5-subcollection-migration.md Section 3 (Cutover Steps)
**Owner:** Hugh
**Pre-condition:** Phase 5 PR merged to main; rules + migration script in place
**Steps:** Follow runbooks/phase5-subcollection-migration.md Sections 1-7
**Output:** 05-PREFLIGHT.md Cutover Log fully populated; dry-run-output.log + real-run-output.log committed

(Add additional rows for any other PENDING-USER items in 05-PREFLIGHT.md)
```

If 05-PREFLIGHT.md has all markers resolved, skip this step (no UAT needed).

**STEP 6: Commit-SHA backfill for SECURITY.md evidence.**

For each "Evidence" bullet in the new SECURITY.md sections that says "(Wave N)" or cites a commit:

```bash
# Resolve commits per file
git log --oneline -- firestore.rules | head -3
git log --oneline -- storage.rules | head -3
git log --oneline -- scripts/migrate-subcollections/run.js | head -3
git log --oneline -- src/domain/unread.js | head -3  # H7 fix commit
git log --oneline -- src/data/cloud-sync.js | head -3  # H8 fix commit
```

Update the Evidence bullets to include the short SHA + date (e.g. "Wave 1 - commit `abc1234`"). Mirrors Phase 3 + Phase 4 commit-SHA-backfill pattern for audit narrative pinning.

**STEP 7: Commit Wave 6 documentation.**

```bash
git add SECURITY.md runbooks/phase-5-cleanup-ledger.md runbooks/phase-4-cleanup-ledger.md
[ -f .planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-HUMAN-UAT.md ] && git add .planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-HUMAN-UAT.md
git commit -m "docs(05-06): wave 6 SECURITY.md DOC-10 + RULES-06 gate + cleanup-ledger zero-out"
```
  </action>
  <verify>
    <automated>grep -c "## § Firestore Data Model" SECURITY.md &amp;&amp; grep -c "## § Firestore Security Rules" SECURITY.md &amp;&amp; grep -c "## § Storage Rules" SECURITY.md &amp;&amp; grep -c "## § Phase 5 Audit Index" SECURITY.md &amp;&amp; test -f runbooks/phase-5-cleanup-ledger.md &amp;&amp; bash -c "! git log --grep='firebase deploy --only firestore:rules' --all | grep -q ."</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## § Firestore Data Model" SECURITY.md` returns at least 1
    - `grep -c "## § Firestore Security Rules - Authored, Not Yet Deployed" SECURITY.md` returns at least 1
    - `grep -c "## § Storage Rules" SECURITY.md` returns at least 1
    - `grep -c "## § Phase 5 Audit Index" SECURITY.md` returns at least 1
    - `grep -c "DATA-01\|RULES-01\|RULES-06\|DATA-07" SECURITY.md` returns at least 4 (audit index references)
    - `grep -c "ASVS\|ISO/IEC 27001\|SOC 2 CC\|GDPR Art" SECURITY.md` returns at least 8 (framework citations across 4 sections)
    - `grep -c "idempotent.*manual.*gcloud firestore export\|gcloud firestore import" SECURITY.md` returns at least 2 (D-05 audit-narrative line + rollback procedure)
    - `grep -c "deploy gate held\|committed.*not.*deployed\|Phase 6.*RULES-07" SECURITY.md` returns at least 1 (RULES-06 framing)
    - `test -f runbooks/phase-5-cleanup-ledger.md` exits 0
    - `grep -c "Phase 5 forward-tracking" runbooks/phase-5-cleanup-ledger.md` returns at least 1
    - `grep -c "Phase 6.*RULES-07\|Phase 6.*AUTH-15\|Phase 7.*FN-09" runbooks/phase-5-cleanup-ledger.md` returns at least 3 (Phase 6/7 forward-trackers documented)
    - `git log --grep="firebase deploy --only firestore:rules" --all | wc -l` returns 0 (RULES-06 gate)
    - `git grep -l "firebase deploy --only firestore:rules" -- '*.md' '*.yml' '*.json' '*.js' '*.sh'` returns empty
    - `grep -c "2026-05-08.*Phase 5 Wave 3\|2026-05-08.*Phase 5 Wave 4" runbooks/phase-4-cleanup-ledger.md` returns at least 6 (5 wrapper closures + H8 closure from prior waves)
  </acceptance_criteria>
  <done>SECURITY.md DOC-10 update committed (4 sections + audit index); RULES-06 verification gate passed (no deploy command anywhere in Phase 5 commits); Phase 5 cleanup-ledger created with 4+ forward-tracking rows; Phase 4 cleanup-ledger closures verified intact; commit-SHA backfill applied; UAT created if Wave 5 operator-execution deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2 (CHECKPOINT): Phase 5 close gate - operator confirms full suite green + cleanup-ledger zero-out + 05-PREFLIGHT.md resolved</name>
  <what-built>
    - 6 PLAN.md files executed across 6 sequential waves (rules infra + migration script + data/* rewrites + H7/H8 fixes + production migration runbook + cleanup)
    - firestore.rules + storage.rules committed (NOT deployed - RULES-06 / Sequencing Non-Negotiable #2)
    - Production migration script with --dry-run + per-doc idempotency markers + pre/post assertion harness
    - 5 src/data/* wrappers rewritten to subcollection access; 1 new src/data/read-states.js wrapper
    - H7 fix in src/domain/unread.js (server-clock comparator); H8 fix in src/data/cloud-sync.js (per-subcollection listeners)
    - SECURITY.md gains 4 new sections + Phase 5 Audit Index
    - runbooks/phase-5-cleanup-ledger.md created; 6 Phase 4 carryover rows closed; 4+ Phase 6/7 forward-trackers queued
    - CI test-rules job wired with SHA-pinned setup-java; full matrix green on every PR
  </what-built>
  <how-to-verify>
    1. **Test/typecheck/lint/build all green:**
       ```bash
       npm test                  # full unit + integration suite
       npm run test:rules        # rules-unit-testing matrix via emulator
       npm run test:migrate      # migration script unit tests
       npm run typecheck         # tsc strict checkJs
       npm run lint              # ESLint with all per-wave boundary rules at error
       npm run build             # vite production build
       ```
       All exit 0.

    2. **Snapshot baselines zero-diff:**
       ```bash
       test -s tests/__snapshots__/views/dashboard.html
       test -s tests/__snapshots__/views/diagnostic.html
       test -s tests/__snapshots__/views/report.html
       npm test -- --reporter=basic  # any snapshot drift surfaces here
       ```
       No snapshot file diffs.

    3. **RULES-06 verification gate:**
       ```bash
       git log --grep='firebase deploy --only firestore:rules' --all | wc -l  # expects 0
       git grep -l 'firebase deploy --only firestore:rules' -- '*.md' '*.yml' '*.json' '*.js' '*.sh'  # expects empty
       ```

    4. **05-PREFLIGHT.md Cutover Log resolved (if Wave 5 ran live):**
       Open `.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-PREFLIGHT.md`. Every PENDING-USER marker in "Pre-flight verifications" + "Cutover Log" should have an actual operator-supplied value, OR be explicitly carried forward to `05-HUMAN-UAT.md` with rationale.

    5. **SECURITY.md DOC-10 sections present:**
       Open `SECURITY.md`. Confirm the 4 new sections exist:
       - § Firestore Data Model
       - § Firestore Security Rules - Authored, Not Yet Deployed
       - § Storage Rules
       - § Phase 5 Audit Index

    6. **Cleanup-ledger zero-out (Phase 5 ledger):**
       Open `runbooks/phase-5-cleanup-ledger.md`. Suppressions table should have ZERO rows. Forward-tracking table should have 4+ entries (Phase 6 RULES-07 + Phase 6 AUTH-15 + Phase 7 FN-09 + Phase 7 server-side writers).

    7. **Cleanup-ledger Phase 4 closures verified:**
       Open `runbooks/phase-4-cleanup-ledger.md`. Find the 2026-05-08 closure entries dated Phase 5 Wave 3 + Wave 4. There should be at least 6 (5 data/* wrapper closures + 1 cloud-sync H8 closure).

    8. **EXPECTED OUTCOME:** All 8 verification points GREEN. Phase 5 is complete.
  </how-to-verify>
  <resume-signal>
    After all 8 verification points pass, type "approved" and Claude will:
    - Update `.planning/STATE.md` to mark Phase 5 COMPLETE
    - Append a Phase 5 close entry to STATE.md "Session Continuity"
    - Update ROADMAP.md "Progress Table" to show 5/12 phases complete
    - Confirm phase signoff

    If any verification point FAILS, type "fail: <description>" and Claude will diagnose + propose remediation.

    If 05-PREFLIGHT.md has unresolved Wave 5 markers because the operator deferred live execution, type "deferred-uat" and Claude will confirm 05-HUMAN-UAT.md captures the carry-forward + signs off Phase 5 as "code-complete; operator-execution deferred to UAT" per Phase 3 + Phase 4 precedent.
  </resume-signal>
</task>

</tasks>

<verification>
- npm test, npm run test:rules, npm run test:migrate, npm run typecheck, npm run lint, npm run build all exit 0
- Snapshot baselines zero diff
- `git log --grep='firebase deploy --only firestore:rules' --all` returns empty (RULES-06)
- SECURITY.md has 4 new sections (§Firestore Data Model + §Firestore Security Rules + §Storage Rules + §Phase 5 Audit Index)
- runbooks/phase-5-cleanup-ledger.md exists; Suppressions table empty; 4+ forward-tracking rows for Phase 6/7
- runbooks/phase-4-cleanup-ledger.md has 2026-05-08 closure entries from Phase 5 Waves 3+4
- 05-PREFLIGHT.md fully resolved OR 05-HUMAN-UAT.md captures carry-forward
- Phase-close human-verify checkpoint resolved (approved or deferred-uat)
</verification>

<success_criteria>
1. SECURITY.md gains 4 new sections per D-20: §Firestore Data Model + §Firestore Security Rules - Authored, Not Yet Deployed + §Storage Rules + §Phase 5 Audit Index
2. Each new section has Evidence + Framework citations subsections per Phase 3 D-15 + Phase 4 D-25 pattern
3. Audit Index table cross-references every Phase 5 control to a code path + test + framework citation (OWASP ASVS L2 / ISO 27001:2022 A.x / SOC2 CC / GDPR Art)
4. RULES-06 verification gate executed and PASSED: `git log --grep='firebase deploy --only firestore:rules' --all` returns empty
5. runbooks/phase-5-cleanup-ledger.md created with empty Suppressions table + 4+ Phase 6/7 forward-tracking rows
6. runbooks/phase-4-cleanup-ledger.md has 6 Phase 5 closure entries (5 data/* wrappers + H8 cloud-sync) dated 2026-05-08
7. Commit-SHA backfill applied to SECURITY.md Evidence rows per Phase 3 + Phase 4 precedent
8. 05-PREFLIGHT.md operator markers all resolved OR 05-HUMAN-UAT.md captures Wave 5 deferred-execution carry-forward
9. Phase-close human-verify checkpoint passes (test/typecheck/lint/build green; cleanup-ledger zero-out; SECURITY.md sections present)
10. Phase 5 marked COMPLETE in STATE.md after operator approves the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/05-firestore-data-model-migration-rules-authoring-committed-not-deployed/05-06-SUMMARY.md` capturing: 4 SECURITY.md sections appended (with framework citation summary table); RULES-06 verification gate result (empty -> pass); Phase 5 cleanup-ledger summary (zero suppressions + 4+ forward-trackers); Phase 4 cleanup-ledger closure status (6 entries verified); 05-PREFLIGHT.md status (resolved | deferred-uat); commit-SHA backfill summary; phase-close checkpoint outcome.
</output>
