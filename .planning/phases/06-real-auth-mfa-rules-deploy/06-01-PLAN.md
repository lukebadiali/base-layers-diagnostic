---
phase: 06-real-auth-mfa-rules-deploy
plan: 01
plan_id: 06-01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md
  - functions/src/auth/.keep
requirements: [AUTH-02, AUTH-04]
requirements_addressed: [AUTH-02, AUTH-04]
autonomous: true
must_haves:
  truths:
    - "Firestore region for bedeveloped-base-layers is verified and matches europe-west2 (or escalation captured)"
    - "Identity Platform upgrade is in place (passwordPolicy + TOTP MFA + org-wide MFA enforcement available)"
    - "passwordPolicy is enabled with minLength >= 12 and HIBP leaked-password check enabled"
    - "firebase.json firestore + storage rule declarations are present and correct (verify-and-leave)"
    - "functions/src/auth/ subdirectory exists for Wave 2 to populate"
  artifacts:
    - path: ".planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md"
      provides: "Pre-flight verification log capturing gcloud + Console output for AUTH-02, AUTH-04, D-09 (Firestore region), and firebase.json declarations"
      contains: "## Firestore Region, ## Identity Platform Upgrade, ## passwordPolicy, ## firebase.json declarations, ## functions/src/auth scaffolding"
    - path: "functions/src/auth/.keep"
      provides: "Empty placeholder so the subdirectory exists in git for Wave 2"
  key_links:
    - from: ".planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md"
      to: "Wave 2 (06-02)"
      via: "deviation gate — escalates to user before Wave 2 if Firestore region != europe-west2"
      pattern: "## Firestore Region.*verified|## Firestore Region.*ESCALATE"
---

<objective>
Wave 1 — Pre-flight verifications. Run the four AUTH-02 + AUTH-04 + D-09 + firebase.json checks BEFORE any code lands, document each result in `06-PREFLIGHT.md`, and create the empty `functions/src/auth/` subdirectory that Wave 2 will populate. This is the load-bearing gate that closes the STATE.md "Firestore region not yet verified" outstanding todo and establishes the deployment substrate Wave 2 lands on.

Purpose: prove the platform can run Phase 6 BEFORE writing handlers. Identity Platform must be upgraded (else passwordPolicy + TOTP APIs are absent); Firestore must be in europe-west2 (else cspReportSink + new auth functions live in a different region than the database, violating D-09's UK data-residency story); passwordPolicy must be enabled (else AUTH-04 is unimplementable); firebase.json must already declare firestore.rules + storage.rules (per pattern mapper this is already present at lines 50-56 — verify-and-leave gate, NOT additive).

Output: `06-PREFLIGHT.md` with all four verification results captured + `functions/src/auth/.keep` placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-PATTERNS.md
@CLAUDE.md
@firebase.json

<interfaces>
<!-- Existing firebase.json (lines 50-56) — already declares firestore + storage rules. Wave 1 task collapses to verify-and-leave per pattern mapper. -->

From firebase.json (lines 50-56):
```json
"firestore": {
  "rules": "firestore.rules",
  "indexes": "firestore.indexes.json"
},
"storage": {
  "rules": "storage.rules"
}
```

From functions/src/csp/cspReportSink.ts (line 35):
```typescript
{ region: "europe-west2" },
```
This is the region the new functions/src/auth/* handlers must match (D-09).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Firestore region + Identity Platform + passwordPolicy via gcloud + Firebase Console; capture results in 06-PREFLIGHT.md</name>
  <files>.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md</files>
  <read_first>
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md (D-09 + Folded Todos paragraph 1; D-21 for AUTH-13/passwordPolicy verification scope)
    - .planning/STATE.md ("Outstanding Todos / Open Questions for Future Phases" — confirms region verification is the load-bearing missing piece)
    - .planning/REQUIREMENTS.md AUTH-02 + AUTH-04 rows (lines 92-94)
    - runbooks/hosting-cutover.md "Cutover Log" pattern (lines 305-326) — same key/value/timestamp shape
  </read_first>
  <action>
Create `.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md` with five top-level sections (`## Firestore Region`, `## Identity Platform Upgrade`, `## passwordPolicy`, `## firebase.json declarations`, `## functions/src/auth scaffolding`). Treat this as a verification log; each section has a verify command, expected outcome, captured timestamp, and decision (PASS / ESCALATE).

For `## Firestore Region` (D-09 / closes STATE.md outstanding todo):
1. Run `gcloud firestore databases describe --database="(default)" --project=bedeveloped-base-layers --format="value(locationId,type)"`. Capture stdout verbatim under a fenced shell block.
2. Expected: `europe-west2 FIRESTORE_NATIVE`. If `locationId != europe-west2`, write `### Decision: ESCALATE — Firestore region is <X>; D-09 requires europe-west2 to co-locate with cspReportSink + new auth functions; do NOT proceed to Wave 2 until user resolves.` and STOP. If matches, write `### Decision: PASS — region verified at <ISO timestamp>`.

For `## Identity Platform Upgrade` (AUTH-02):
1. Operator-run check (this is a Console verification — record the URL the operator must visit and the visual indicator). URL: `https://console.firebase.google.com/project/bedeveloped-base-layers/authentication/providers`. Visual indicator: Identity Platform settings panel is present (the page shows "Identity Platform" branding + "MFA" tab is present in the left nav).
2. Capture: paste the operator's confirmation block: `verified_at: <ISO timestamp>; identity_platform_upgrade: present; mfa_tab_visible: yes; password_policy_tab_visible: yes`.
3. If absent, write `### Decision: ESCALATE — Identity Platform upgrade required for AUTH-02 (passwordPolicy + TOTP). Operator must run "Upgrade to Identity Platform" in Console before proceeding.` and STOP.

For `## passwordPolicy` (AUTH-04):
1. Operator-run check via Console at `https://console.firebase.google.com/project/bedeveloped-base-layers/authentication/settings`. Look for "Password policy" section.
2. Verify minimum length >= 12 chars AND "Check passwords against compromised credentials" (HIBP) is enabled.
3. Alternative gcloud check: `gcloud identity-toolkit projects describe bedeveloped-base-layers --format="value(passwordPolicyConfig)"` — capture verbatim if available.
4. Capture: `min_length: <int>; hibp_enabled: <yes|no>; verified_at: <ISO timestamp>`.
5. If min_length < 12 or HIBP disabled, write `### Decision: ESCALATE — passwordPolicy below AUTH-04 threshold. Operator must enable in Console.` and STOP.

For `## firebase.json declarations` (verify-and-leave per pattern mapper):
1. Read `firebase.json` lines 50-56 and confirm the file contains exactly these blocks: `"firestore": { "rules": "firestore.rules", "indexes": "firestore.indexes.json" }` AND `"storage": { "rules": "storage.rules" }`.
2. Run: `node -e "const j=require('./firebase.json'); console.log(JSON.stringify({fs: j.firestore, st: j.storage}, null, 2))"` and paste stdout.
3. Confirm `firestore.rules` and `storage.rules` files exist at repo root: `ls firestore.rules storage.rules firestore.indexes.json` — paste stdout.
4. Decision: PASS if all three files exist and declarations match; otherwise note exact discrepancy. Pattern mapper says this should be a no-op verify-and-leave.

End with a `## Wave 1 Status` section: PASS-or-ESCALATE summary across the 4 sections + commit SHA of this preflight + the timestamp Wave 2 unblocks.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const txt=fs.readFileSync('.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md','utf8');const sections=['## Firestore Region','## Identity Platform Upgrade','## passwordPolicy','## firebase.json declarations','## functions/src/auth scaffolding','## Wave 1 Status'];for(const s of sections){if(!txt.includes(s)){console.error('MISSING:',s);process.exit(1)}}console.log('06-PREFLIGHT.md sections present')"</automated>
  </verify>
  <done>
    - `.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md` exists
    - All 5 named sections (Firestore Region, Identity Platform Upgrade, passwordPolicy, firebase.json declarations, functions/src/auth scaffolding) present, each with a Decision line (PASS or ESCALATE)
    - `## Wave 1 Status` section ends with PASS or explicit ESCALATE summary; if any section escalates, Wave 2 is blocked
    - `gcloud firestore databases describe` stdout captured verbatim under `## Firestore Region`
    - Identity Platform Console verification block + URL captured under `## Identity Platform Upgrade`
    - passwordPolicy minLength + HIBP captured under `## passwordPolicy`
    - firebase.json declarations confirmed (Wave 1 task is a no-op verify-and-leave per pattern mapper)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create functions/src/auth/.keep placeholder so the subdirectory exists in git for Wave 2 to populate</name>
  <files>functions/src/auth/.keep</files>
  <read_first>
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md (D-10 — source layout `functions/src/auth/{beforeUserCreated,beforeUserSignedIn,setClaims,claim-builder}.ts`)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-PATTERNS.md (Pattern A — mirror of `functions/src/csp/*` layout)
    - functions/src/csp/cspReportSink.ts (existing analog)
  </read_first>
  <action>
Create the file `functions/src/auth/.keep` with content `# Phase 6 Wave 1: subdirectory placeholder. Wave 2 (06-02) populates with beforeUserCreated.ts, beforeUserSignedIn.ts, setClaims.ts, and claim-builder.ts per CONTEXT.md D-10.\n`. The `.keep` convention forces git to track an otherwise-empty directory; Wave 2 will commit alongside its first .ts file landing.

This task is one file, one line of content. Do NOT add any TS source — that is Wave 2's job per D-01 wave shape. Do NOT modify `functions/src/index.ts` — that is also Wave 2's job (re-exports come with the handlers). Do NOT modify `functions/package.json` — Phase 3 already pinned firebase-admin@13.8.0 + firebase-functions@7.2.5 (verified in functions/package.json lines 16-17).

Update the `## functions/src/auth scaffolding` section of `06-PREFLIGHT.md` from Task 1 with: `subdirectory_created: yes; placeholder_path: functions/src/auth/.keep; verified_at: <ISO timestamp>`.
  </action>
  <verify>
    <automated>test -f functions/src/auth/.keep && grep -q "Phase 6" functions/src/auth/.keep && echo "placeholder OK"</automated>
  </verify>
  <done>
    - `functions/src/auth/.keep` exists with one-line comment naming Phase 6 Wave 1 + Wave 2 hand-off
    - `06-PREFLIGHT.md ## functions/src/auth scaffolding` section has the `subdirectory_created: yes` block
    - `git status` shows `functions/src/auth/.keep` is a new tracked file (untracked is fine — operator commits at end of plan)
    - Zero `.ts` source files added (Wave 2 owns those)
    - `functions/src/index.ts` UNCHANGED (Wave 2 owns re-exports)
    - `functions/package.json` UNCHANGED (Phase 3 pinned dependencies stand)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> Firebase Console | operator's authenticated browser session is the only path that can verify Identity Platform upgrade + passwordPolicy state |
| operator -> gcloud | gcloud CLI is the only programmatic path that can read Firestore databases.describe; operator's gcloud auth list is the trust root |
| repo -> production | this plan does NOT touch production (read-only verifications only); the trust boundary moves in Wave 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Information-Disclosure | gcloud output capture | accept | gcloud output is the same data the Console shows; storing it in `.planning/` is consistent with prior preflights (e.g., 03-PREFLIGHT.md) |
| T-06-01-02 | Tampering | preflight result fabrication | mitigate | every section requires a verbatim command stdout block; reviewer can re-run the same gcloud command to confirm |
| T-06-01-03 | EoP | Wave 2 starts before preflight passes | mitigate | Wave 2 plan's depends_on=[06-01] enforces ordering; `## Wave 1 Status` ESCALATE outcome is the structural block |
| T-06-01-04 | EoP | Firestore region mismatch causes Pitfall 1 cascade in Wave 5 | mitigate | D-09 verification gate pre-Wave-2; ESCALATE branch halts the entire phase, not just Wave 1 |
</threat_model>

<verification>
- All 4 verification sections present in 06-PREFLIGHT.md, each with a Decision line
- `## Wave 1 Status` summary is PASS or explicitly ESCALATE
- `functions/src/auth/.keep` exists and is tracked by git
- No production state changed during this wave (read-only verifications + one placeholder file)
- `firebase.json` UNCHANGED (verify-and-leave per pattern mapper)
</verification>

<success_criteria>
- 06-PREFLIGHT.md captures all 4 verifications with timestamps and PASS or ESCALATE decisions
- Firestore region confirmed `europe-west2` (or escalation logged with the escalation Decision and a freeze on Wave 2)
- Identity Platform upgrade visible in Console; passwordPolicy minLength >= 12 + HIBP enabled
- `functions/src/auth/.keep` exists; subdirectory tracked in git
- Zero changes to `firebase.json`, `functions/src/index.ts`, `functions/package.json`, or any production code
</success_criteria>

<output>
After completion, create `.planning/phases/06-real-auth-mfa-rules-deploy/06-01-SUMMARY.md` capturing the 4 verification outcomes + the timestamps + the commit SHA + the decision on Wave 2 unblock.
</output>
