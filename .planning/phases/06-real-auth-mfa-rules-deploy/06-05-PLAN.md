---
phase: 06-real-auth-mfa-rules-deploy
plan: 05
plan_id: 06-05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - runbooks/phase6-rules-rollback-rehearsal.md
  - .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md
  - src/main.js
  - src/firebase/auth.js
  - src/auth/state-machine.js
  - tests/auth/state-machine.test.js
  - tests/fixtures/auth-passwords.js
  - .gitleaks.toml
requirements: [AUTH-01, AUTH-03, AUTH-05, AUTH-06, AUTH-07, AUTH-08, AUTH-10, AUTH-11, AUTH-12, AUTH-13, AUTH-14, AUTH-15, RULES-07]
requirements_addressed: [AUTH-01, AUTH-03, AUTH-05, AUTH-06, AUTH-07, AUTH-08, AUTH-10, AUTH-11, AUTH-12, AUTH-13, AUTH-14, AUTH-15, RULES-07]
autonomous: false
must_haves:
  truths:
    - "runbooks/phase6-rules-rollback-rehearsal.md exists with rehearsal evidence captured during the live pre-cutover rehearsal step"
    - "5-minute rules-rollback rehearsal completed end-to-end against the live Firebase project; evidence proves total_seconds < 300"
    - "All 3 auth-blocking + callable Cloud Functions deployed to bedeveloped-base-layers in europe-west2"
    - "internalAllowlist/{lowercaseEmail} docs exist for both luke and george with role=admin"
    - "Both admins Console-created with emailVerified=true; signed in successfully against OLD anon-OK rules; ID-token claims confirmed role=admin"
    - "firestore.rules + storage.rules deployed to production exactly once during this phase against bedeveloped-base-layers (gates RULES-07)"
    - "Anonymous Auth Console-disabled in Firebase Console at cutover"
    - "AUTH-14 deletion commit landed atomically with rules deploy: INTERNAL_PASSWORD_HASH + INTERNAL_ALLOWED_EMAILS + state-machine.js + state-machine.test.js + auth-passwords.js + signInAnonymously source + firebase-ready bridge + .gitleaks.toml C2 rule all deleted"
    - "Both admins enrolled TOTP MFA same-session"
    - "AUTH-10 drill executed live: each admin took a turn locked-out; both Tier-2 un-enrol procedures completed end-to-end with evidence captured"
    - "SC#4 clock-skew exercise executed: ±5-minute clock skew did not change unread badge counts (closes 05-HUMAN-UAT.md test #2)"
    - "06-PREFLIGHT.md ## Cutover Log block populated with all 12-step evidence"
  artifacts:
    - path: "runbooks/phase6-rules-rollback-rehearsal.md"
      provides: "Pre-cutover rehearsal evidence — timing of revert + redeploy + revert-the-revert against live project"
      contains: "rehearsal_date|rehearsal_sha_before|rehearsal_revert_sha|total_seconds_revert_to_redeploy_green|within_5_min_target"
    - path: ".planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md"
      provides: "Updated with ## Cutover Log block populated by all 12-step evidence (functions deploy + bootstrap + claims-verify + rules deploy + anon-disable + AUTH-14 deletion + MFA enrol + AUTH-10 drill + SC#4)"
      contains: "## Cutover Log|cutover_complete: yes|rules_deploy_sha|auth14_deletion_sha"
    - path: "src/main.js"
      provides: "AUTH-14 deletion mods: lines 464 (verifyInternalPassword wrapper) + 625-633 (constants block) + 3156 (Allowed emails UI string) + 4115 (firebase-ready listener) all removed"
      contains: "no INTERNAL_PASSWORD_HASH|no INTERNAL_ALLOWED_EMAILS|no firebase-ready"
    - path: "src/firebase/auth.js"
      provides: "AUTH-14 deletion: signInAnonymously import + re-export + onAuthStateChanged firebase-ready bridge block + signInAnonymously call all removed"
      contains: "no signInAnonymously|no firebase-ready"
    - path: ".gitleaks.toml"
      provides: "AUTH-14 deletion: C2 SHA-256 hex regression rule [[rules]] block removed"
      contains: "no sha256-hex-literal-regression"
  key_links:
    - from: "src/firebase/auth.js (post-cutover)"
      to: "src/views/auth.js"
      via: "deps.signInEmailPassword (Wave 3 wired); replaces the firebase-ready bridge"
      pattern: "signInEmailPassword"
    - from: "firestore.rules + storage.rules (deployed)"
      to: "request.auth.token.role from beforeUserCreated"
      via: "claims set in beforeUserCreated -> Auth ID token -> rules predicate"
      pattern: "request\\.auth\\.token\\.role"
    - from: "runbooks/phase6-cutover.md"
      to: "06-PREFLIGHT.md ## Cutover Log"
      via: "Step 12 — operator populates the log from the 12-step evidence"
      pattern: "## Cutover Log"
---

<objective>
Wave 5 — Production cutover. Operator-supervised single session executing all 12 steps of `runbooks/phase6-cutover.md` end-to-end against `bedeveloped-base-layers`. This wave is `autonomous: false` per D-02 — Claude does NOT run the live deploy commands; the operator runs them with Claude pair-programming the verification + capture loop. The 4 checkpoint tasks below stage Claude's role: (1) author the rules-rollback rehearsal runbook DURING the live rehearsal step, (2) supervise the deploy + bootstrap + claims-verify + rules deploy + anon-disable sequence (3) compose + commit the AUTH-14 atomic deletion commit (4) supervise + capture the AUTH-10 drill + SC#4 clock-skew exercise + populate the Cutover Log.

Purpose: this is the load-bearing milestone-defining cutover phase per ROADMAP.md. Pitfall 1 (rules deploy before claims propagate causes lockout) is structurally mitigated by D-11 ordering (functions FIRST → bootstrap → claims-verify → rules deploy → anon-disable). Pitfall 19 (compliance theatre) is closed by AUTH-10 drill evidence + rules-rollback rehearsal evidence — both live, both same-day, both committed.

Output: rules-rollback rehearsal runbook + populated 06-PREFLIGHT.md ## Cutover Log + AUTH-14 deletion commit on main + RULES-07 production deploy + Anonymous Auth disabled in Console + AUTH-10 drill evidence + SC#4 closure.

This plan has FOUR checkpoint tasks. Each pauses for the operator and resumes when the operator confirms the gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-PATTERNS.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-01-SUMMARY.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-02-SUMMARY.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-03-SUMMARY.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-04-SUMMARY.md
@runbooks/phase6-cutover.md
@runbooks/phase6-bootstrap.md
@runbooks/phase6-mfa-recovery-drill.md
@runbooks/hosting-cutover.md
@scripts/seed-internal-allowlist/run.js
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-cutover rules-rollback rehearsal — operator runs revert + redeploy + revert-the-revert against live project; Claude authors runbooks/phase6-rules-rollback-rehearsal.md as evidence</name>
  <files>runbooks/phase6-rules-rollback-rehearsal.md</files>
  <read_first>
    - runbooks/phase6-cutover.md Step 1 (the procedure being executed)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-PATTERNS.md "phase6-rules-rollback-rehearsal.md" section + Pattern F (lines 774-826)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md D-12 (5-minute rollback substrate)
    - runbooks/hosting-cutover.md DNS Revert Procedure (lines 334-380 — same shape)
    - .planning/research/PITFALLS.md §Pitfall 19 ("claim only what was rehearsed")
  </read_first>
  <what-built>
Claude authored `runbooks/phase6-rules-rollback-rehearsal.md` skeleton with the 6-step rehearsal procedure + Rehearsal Evidence block. The operator runs the 6 steps live; Claude captures timing into the Evidence block as each step completes. The runbook is BOTH the procedure document AND the evidence artefact — committed-as-rehearsed.

The live rehearsal performs the full revert-and-redeploy cycle against the live `bedeveloped-base-layers` project:
1. Confirm CI deploy job is green on current main.
2. Note SHA at HEAD.
3. `git revert <sha> --no-edit && git push` -> capture revert SHA + push timestamp.
4. Watch CI deploy job; capture timestamp when green.
5. Verify rules reverted in Firebase Console.
6. `git revert <revert-sha> --no-edit && git push` (revert-the-revert); watch CI green; verify rules restored.

Total elapsed time from Step 3 to Step 4 green is the SC#4 evidence; D-12 target < 5 minutes.
  </what-built>
  <how-to-verify>
**Operator runs:** Follow `runbooks/phase6-cutover.md` Step 1 exactly. After each sub-step, paste the result into the corresponding section of `runbooks/phase6-rules-rollback-rehearsal.md` (Claude prompts for each value).

**Operator confirms:**

1. CI deploy job green on current main? Paste `gh run list --workflow=ci.yml --branch=main --status=success --limit=1 --json databaseId,createdAt,headSha` output.
2. HEAD SHA captured? Paste `git rev-parse HEAD` output.
3. Revert pushed? Paste new revert SHA + push timestamp.
4. CI deploy green after revert? Paste timestamp + `gh run watch` exit status.
5. Firebase Console -> Firestore -> Rules: confirm Console "Last deployed" timestamp matches the CI green timestamp. Paste screenshot or Console-quoted value.
6. Revert-the-revert pushed? CI green again? Rules restored to current state in Console?

**Total elapsed time** Step 3 -> Step 4 green: paste integer seconds. Confirm < 300 (D-12 target).

**If rehearsal exceeded 300 seconds:** Operator escalates — Phase 3 CI deploy job is the rollback substrate; if it can't redeploy in <5 min, the milestone has a SC#4 risk. Capture in `## Rehearsal Evidence` block: `within_5_min_target: no` + `gaps_observed:` describing what slow path was found.

**Claude's role:** prompt the operator for each value; populate the Rehearsal Evidence block in real-time; commit when complete.

The runbook content (skeleton Claude authors before the rehearsal):

```markdown
# Phase 6 Rules-Rollback Rehearsal Runbook

> Phase 6 Wave 5 deliverable, authored DURING the live rehearsal step (D-12). Captures the 5-minute rollback substrate (`git revert + Phase 3 CI deploy job redeploys parent commit's rules`) end-to-end against the live `bedeveloped-base-layers` project. Pre-cutover rehearsal — runs BEFORE Wave 5 Step 2 (functions deploy) so the rollback path is proven before the deploy that might need it.

## Rehearsal Procedure

### Step 1: Confirm CI deploy job is green on current main (~1 min)

\`\`\`sh
gh run list --workflow=ci.yml --branch=main --status=success --limit=1 --json databaseId,createdAt,headSha
\`\`\`

### Step 2: Note SHA at HEAD (~1 min)

\`\`\`sh
git rev-parse HEAD     # capture as $REHEARSAL_SHA_BEFORE
\`\`\`

### Step 3: git revert + push (~30 sec)

\`\`\`sh
git revert $REHEARSAL_SHA_BEFORE --no-edit
git push
\`\`\`

Capture: `$REHEARSAL_REVERT_SHA` + `$PUSH_TIMESTAMP_AT`.

### Step 4: Watch CI deploy job; capture green timestamp (~3 min)

\`\`\`sh
gh run watch $(gh run list --branch=main --limit=1 --json databaseId -q '.[0].databaseId')
\`\`\`

Capture: `$DEPLOY_GREEN_AT`.

### Step 5: Verify rules reverted in Firebase Console (~1 min)

Visit https://console.firebase.google.com/project/bedeveloped-base-layers/firestore/rules. Confirm "Last deployed" timestamp matches `$DEPLOY_GREEN_AT` (within ~30 seconds for Console refresh latency).

### Step 6: Re-deploy current rules (~2 min)

\`\`\`sh
git revert $REHEARSAL_REVERT_SHA --no-edit   # revert-the-revert
git push
\`\`\`

Watch CI green: `$REDEPLOY_GREEN_AT`.

## Rehearsal Evidence

\`\`\`
rehearsal_date: <YYYY-MM-DD HH:MM TZ>
rehearsal_sha_before: <sha>
rehearsal_revert_sha: <sha>
push_timestamp_at: <YYYY-MM-DDTHH:MM:SSZ>
deploy_green_at: <YYYY-MM-DDTHH:MM:SSZ>
total_seconds_revert_to_redeploy_green: <int>
within_5_min_target: <yes | no>
revert_the_revert_sha: <sha>
redeploy_green_at: <YYYY-MM-DDTHH:MM:SSZ>
notes: <freeform — any deviation, slowdown, gap>
gaps_observed: <freeform>
\`\`\`

## Citations

- Phase 6 D-12 (5-min rollback substrate via Phase 3 CI deploy job)
- Pitfall 19 ("claim only what was rehearsed" — this rehearsal IS the SC#4 audit artefact)
- Phase 3 ROADMAP success criterion #4 ("rollback procedure is documented in runbooks/ and verified within 5 minutes against staging")
- runbooks/hosting-cutover.md DNS Revert Procedure (Phase 3 precedent — same shape)
```

**Resume signal once operator pastes evidence:** "rehearsal-complete" + the populated Rehearsal Evidence block.
  </how-to-verify>
  <resume-signal>"rehearsal-complete" — operator pastes the populated `## Rehearsal Evidence` block (rehearsal_date, both SHAs, push_timestamp_at, deploy_green_at, total_seconds_revert_to_redeploy_green, within_5_min_target, revert_the_revert_sha, redeploy_green_at). Claude commits runbooks/phase6-rules-rollback-rehearsal.md with the populated evidence, then proceeds to Task 2.</resume-signal>
  <action>
Claude:
1. Read `runbooks/phase6-cutover.md` Step 1 + the rehearsal pattern from 06-PATTERNS.md.
2. Author `runbooks/phase6-rules-rollback-rehearsal.md` with the 6-step Procedure + the Rehearsal Evidence skeleton block (verbatim above).
3. Pause for operator. Operator runs the 6 steps live and pastes evidence values back to Claude.
4. Claude updates the Rehearsal Evidence block with the operator-provided values.
5. Claude commits: `git add runbooks/phase6-rules-rollback-rehearsal.md && git commit -m "feat(06-05): rules-rollback rehearsal evidence (D-12 / SC#4)"`. Push.
6. Verify: `grep -c "<sha>\|<int>" runbooks/phase6-rules-rollback-rehearsal.md` should be 0 (all template placeholders replaced).

**If rehearsal exceeded 5 minutes:** Document in `gaps_observed:`. The within_5_min_target field is `no`. Operator escalates to user — milestone SC#4 has a documented gap; decision required: accept the gap (compliance-credible-not-certified posture allows this if rationale is captured) or fix the CI substrate before continuing cutover. Do NOT proceed to Task 2 without explicit user decision.

**If rehearsal Step 4 (CI green) failed:** Rollback substrate is broken. ABORT cutover. Do NOT proceed. Escalate to user.
  </action>
  <verify>
    <automated>test -f runbooks/phase6-rules-rollback-rehearsal.md && grep -q "rehearsal_date:" runbooks/phase6-rules-rollback-rehearsal.md && grep -q "total_seconds_revert_to_redeploy_green:" runbooks/phase6-rules-rollback-rehearsal.md && ! grep -q "<sha>\|<int>" runbooks/phase6-rules-rollback-rehearsal.md && echo "OK"</automated>
  </verify>
  <done>
    - `runbooks/phase6-rules-rollback-rehearsal.md` exists with all 6 procedure steps + populated Rehearsal Evidence block
    - All template placeholders (`<sha>`, `<int>`, `<YYYY-MM-DD ...>`, `<HH:MM:SS ...>`) replaced with real values
    - `total_seconds_revert_to_redeploy_green` is an integer; if >= 300, `gaps_observed` documents the deviation
    - `within_5_min_target` is `yes` or `no` (explicit)
    - File committed to main with conventional commit message
    - CI is green on the redeploy commit (rules now in their pre-rehearsal production state)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Functions deploy + seed-internal-allowlist + Console-create admins + claims-verify + rules deploy + Anonymous Auth disable (Steps 2-7 of cutover runbook)</name>
  <files>.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md</files>
  <read_first>
    - runbooks/phase6-cutover.md Steps 2-7
    - runbooks/phase6-bootstrap.md (followed during Step 4)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md D-09 + D-11 (load-bearing deploy ordering — Pitfall 1 mitigation gate)
    - 06-PREFLIGHT.md (Wave 1 output — populating ## Cutover Log here)
  </read_first>
  <what-built>
Operator executes Steps 2-7 of `runbooks/phase6-cutover.md` against the live `bedeveloped-base-layers` project; Claude captures evidence into 06-PREFLIGHT.md ## Cutover Log block as each step completes.

Step 2 — Functions deploy: 3 handlers in europe-west2; cold-start measured.
Step 3 — Seed script run: 2 internalAllowlist docs created.
Step 4 — Console-create both admins: emailVerified=true; uids captured.
Step 5 — Claims verification: both admins' ID tokens carry `claims.role === "admin"`. ABORT GATE — if either fails, do not proceed.
Step 6 — Rules deploy: firestore.rules + storage.rules deployed via either CI merge or `firebase deploy --only firestore:rules,storage:rules`.
Step 7 — Anonymous Auth Console-disabled.

This is the Pitfall 1 mitigation gate — claims MUST be confirmed before rules tighten. If Step 5 fails, the runbook's ABORT path triggers: roll back functions deploy + investigate.
  </what-built>
  <how-to-verify>
**Operator confirms after each step:**

**Step 2 verify:**
1. `firebase functions:list --project bedeveloped-base-layers | grep -E "beforeUserCreated|beforeUserSignedIn|setClaims"` returns 3 lines (paste output).
2. All 3 lines show `europe-west2`.
3. Cold-start probe: trigger a test Auth event (create + immediately delete a throwaway test user); paste `gcloud functions logs read beforeUserCreatedHandler --region=europe-west2 --limit=10 --project=bedeveloped-base-layers --format=json` showing the cold-start latency.
4. Capture `functions_deploy_p99_cold_start_ms:` (estimate from log entries — single cold-start sample is fine for the milestone, p99 is a Phase 7 concern).

**Step 3 verify:**
Operator runs `cd functions && node ../scripts/seed-internal-allowlist/run.js --verify` and pastes stdout. Expect 4 `[OK]` lines (2 writes + 2 verifies).

**Step 4 verify (per admin, both Luke + George):**
- Console "Add user" succeeded; uid captured.
- Admin SDK `updateUser({emailVerified: true})` exit code 0.
- Re-fetch confirms emailVerified === true.

**Step 5 verify (per admin):**
- Admin signs in at https://baselayers.bedeveloped.com (or default URL pre-cutover).
- DevTools console: `firebase.auth().currentUser.getIdTokenResult(true).then(r => console.log(JSON.stringify(r.claims)))`.
- Paste claims output. Confirm `claims.role === "admin"`.

**ABORT GATE:** If Step 5 fails for either admin, operator must:
- `firebase functions:delete beforeUserCreatedHandler beforeUserSignedInHandler setClaims --project bedeveloped-base-layers --force`
- Investigate root cause (most likely: case mismatch on internalAllowlist docId, or beforeUserCreated returning `void` instead of `{customClaims: {...}}`).
- Re-attempt Step 2 once root cause fixed.
- Do NOT proceed to Step 6 until both admins' claims verify.

**Step 6 verify:**
- `firebase deploy --only firestore:rules,storage:rules --project bedeveloped-base-layers` exit code 0.
- Firebase Console -> Firestore -> Rules: "Last deployed" timestamp is within last 60 seconds of operator action.
- Firebase Console -> Storage -> Rules: same.
- `git log --grep="firebase deploy --only firestore:rules"` shows exactly one entry on the cutover SHA chain (RULES-07 verification gate).
- Paste rules_deploy_sha + Console timestamp.

**Step 7 verify:**
- Firebase Console -> Authentication -> Sign-in method: Anonymous provider shows "Disabled" status.
- Paste anon_auth_console_disabled_at timestamp (when Save was clicked).

**Claude's role:** read each operator paste; append the values to 06-PREFLIGHT.md ## Cutover Log block. Do NOT continue past the ABORT GATE in Step 5 if either admin's claims missing.
  </how-to-verify>
  <resume-signal>"step-2-7-complete" — operator pastes:
- `functions_deploy_p99_cold_start_ms:` (int)
- `seed_script_output:` (4-line stdout)
- `bootstrap_log_luke_uid:` + `bootstrap_log_george_uid:` (UIDs)
- `bootstrap_log_*_emailVerified_set_at:` timestamps
- `admin_signin_claims_verified:` (yes for both admins, with claim payloads)
- `rules_deploy_sha:` + `rules_deploy_console_timestamp:`
- `anon_auth_console_disabled_at:` (timestamp)

Claude appends these to 06-PREFLIGHT.md ## Cutover Log + commits + proceeds to Task 3.

**ABORT signal:** "abort" — operator's claims-verify failed. Claude documents the failure in 06-PREFLIGHT.md ## Cutover Log under `cutover_aborted_at_step:` + `abort_reason:` + halts the entire wave. User must intervene.</resume-signal>
  <action>
Claude:
1. Append `## Cutover Log` skeleton to 06-PREFLIGHT.md (use the skeleton from runbooks/phase6-cutover.md).
2. Pause for operator. Operator runs Steps 2-7 of phase6-cutover.md; pastes evidence one step at a time.
3. Claude populates 06-PREFLIGHT.md ## Cutover Log with each step's evidence as it arrives.
4. After Step 7 evidence captured, Claude commits: `git add .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && git commit -m "docs(06-05): cutover steps 2-7 evidence (functions deploy + bootstrap + claims-verify + rules deploy + anon-disable)"`. Push.
5. Verify: `## Cutover Log` block has 11+ populated keys (functions_deploy + seed_script + bootstrap luke + bootstrap george + admin_signin + rules_deploy_sha + rules_deploy_console_timestamp + anon_auth_console_disabled_at).

**Critical:** Do NOT proceed to Task 3 if `cutover_aborted_at_step:` is set. The AUTH-14 deletion commit (Task 3) is atomic with the rules deploy — if rules deploy didn't happen (or rolled back), AUTH-14 deletion does NOT happen this session.

**If Step 6 used Path A (CI deploy):** the rules_deploy_sha is the cutover-commit SHA from Task 3 (which lands BEFORE Step 6 in that path). Update task ordering accordingly: Task 3 lands the cutover commit first; CI deploy job runs Step 6 automatically; this task captures the post-CI-green evidence. Operator picks the path; runbook documents both options.
  </action>
  <verify>
    <automated>grep -q "## Cutover Log" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "rules_deploy_sha:" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "anon_auth_console_disabled_at:" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "admin_signin_claims_verified:" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && firebase functions:list --project bedeveloped-base-layers 2>&1 | grep -E "beforeUserCreated|beforeUserSignedIn|setClaims" | wc -l | grep -q "^3$" && echo "OK"</automated>
  </verify>
  <done>
    - `firebase functions:list` shows all 3 handlers in europe-west2
    - `internalAllowlist/luke@bedeveloped.com` + `internalAllowlist/george@bedeveloped.com` exist in Firestore with `role: "admin"`
    - Both admins exist in Firebase Auth with emailVerified=true; UIDs captured in 06-PREFLIGHT.md ## Cutover Log
    - Both admins signed in successfully against pre-strict rules; claims `{role: "admin"}` confirmed in ID tokens
    - `firestore.rules` + `storage.rules` deployed; Console "Last deployed" timestamps captured
    - Anonymous Auth shows "Disabled" in Firebase Console; timestamp captured
    - 06-PREFLIGHT.md ## Cutover Log has 11+ populated keys for Steps 2-7
    - 0 admin claim verification failures (Pitfall 1 mitigation gate passed)
    - File committed to main; CI green on the commit
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: AUTH-14 atomic deletion commit — operator + Claude jointly compose the cutover commit deleting INTERNAL_PASSWORD_HASH + INTERNAL_ALLOWED_EMAILS + state-machine.js + state-machine.test.js + auth-passwords.js + signInAnonymously source + firebase-ready bridge + .gitleaks.toml C2 rule</name>
  <files>src/main.js, src/firebase/auth.js, src/auth/state-machine.js (DELETED), tests/auth/state-machine.test.js (DELETED), tests/fixtures/auth-passwords.js (DELETED), .gitleaks.toml</files>
  <read_first>
    - runbooks/phase6-cutover.md Step 8 (the 7-substep deletion procedure)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md D-04 (atomic deletion sites enumerated) + D-03 (firebase-ready bridge retirement)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-PATTERNS.md "src/main.js" + "src/firebase/auth.js" + ".gitleaks.toml" sections
    - src/main.js (current — verify lines 464, 625-633, 3156, 4115 still exist as captured in pattern mapper)
    - src/firebase/auth.js (current — verify the firebase-ready bridge at lines 20-30 + signInAnonymously call at line 31 still exist)
    - src/auth/state-machine.js + tests/auth/state-machine.test.js + tests/fixtures/auth-passwords.js (the 3 files being deleted)
    - .gitleaks.toml (verify C2 rule at lines 8-14 still exists)
  </read_first>
  <what-built>
Single atomic commit lands the AUTH-14 deletion across 8 file changes:

**Deletions (3 files):**
- `src/auth/state-machine.js`
- `tests/auth/state-machine.test.js`
- `tests/fixtures/auth-passwords.js`

**Modifications (4 files):**
- `src/main.js` — remove line 464 (verifyInternalPassword wrapper) + lines 625-633 (constants block) + line 3156 (Allowed emails UI string — also remove the surrounding `settingsCard.appendChild(h(...))` block since the dead reference is in a UI element) + line 4115 (window.addEventListener("firebase-ready", ...) listener block — this is a multi-line block ending with `});` ~line 4131)
- `src/firebase/auth.js` — remove `signInAnonymously` from the import on line 5 + remove `signInAnonymously` from the export on line 8 + remove the entire `onAuthStateChanged(auth, (u) => { ... })` block at lines 20-30 + remove the `signInAnonymously(auth).catch(...)` call on line 31
- `.gitleaks.toml` — remove the `[[rules]] id = "sha256-hex-literal-regression"` block at lines 8-14 (KEEP the [allowlist] block at lines 16-37 untouched)

**Test invariant:** The Phase 2 TEST-07 baseline (state-machine.test.js + auth-passwords.js fixtures) is consumed-as-designed when its replacement ships — this is exactly that moment. The tests were designed to be deleted alongside the production code.

**Atomic commit message:**

```
feat(06-05): AUTH-14 atomic deletion + signInAnonymously source removal + firebase-ready bridge retirement (D-04)

Deletes the C1 (Anonymous Auth substrate) + C2 (hardcoded INTERNAL_PASSWORD_HASH)
substrates atomically with the RULES-07 production deploy. No dead-code window:
the password hash is removed from source in the same commit that disabled
Anonymous Auth in the Console (Step 7) and deployed the strict rules (Step 6).

Deletions:
- src/auth/state-machine.js (Phase 2 TEST-07 deletion baseline)
- tests/auth/state-machine.test.js (consumed-as-designed)
- tests/fixtures/auth-passwords.js (sole consumer was state-machine.test.js)

Source mods:
- src/main.js: 4 deletion sites (verifyInternalPassword wrapper L464; constants
  block L625-633; "Allowed emails:" UI string L3156; firebase-ready listener L4115)
- src/firebase/auth.js: signInAnonymously import + re-export + onAuthStateChanged
  firebase-ready bridge block + signInAnonymously call
- .gitleaks.toml: removed sha256-hex-literal-regression custom rule (C2 target gone)

Closes:
- AUTH-01 (Anonymous Auth disabled)
- AUTH-14 (INTERNAL_PASSWORD_HASH + INTERNAL_ALLOWED_EMAILS deleted)
- C1 (anonymous auth substrate)
- C2 (hardcoded password hash)
- Phase 4 D-?? cleanup-ledger row (window.FB.currentUser + firebase-ready bridge retirement)
- Phase 5 D-21 cleanup-ledger row (state-machine.js deletion + INTERNAL_PASSWORD_HASH-shape gitleaks rule retirement)

Cutover SHA: $REHEARSED_SHA pre-Step-1 of 06-05; full 12-step evidence in
06-PREFLIGHT.md ## Cutover Log.
```
  </what-built>
  <how-to-verify>
**Pre-deletion verify (operator runs):**

```bash
# Confirm all 4 deletion sites + the 3 deletion target files exist as expected
grep -n "verifyInternalPassword" src/main.js                # expect line 464
grep -n "INTERNAL_ALLOWED_EMAILS\|INTERNAL_PASSWORD_HASH" src/main.js  # expect lines 628-629, 632, 3156
grep -n "firebase-ready" src/main.js                        # expect line 4115
grep -n "signInAnonymously\|firebase-ready" src/firebase/auth.js  # expect lines 5, 8, 11, 27, 31
ls src/auth/state-machine.js tests/auth/state-machine.test.js tests/fixtures/auth-passwords.js  # all 3 must exist
grep -n "sha256-hex-literal-regression" .gitleaks.toml      # expect lines 8-14
```

**Post-deletion verify (after Claude composes the commit):**

```bash
# All 7 deletion sites must be empty
grep -rn "INTERNAL_PASSWORD_HASH" src/ tests/             # expect 0 matches
grep -rn "INTERNAL_ALLOWED_EMAILS" src/ tests/            # expect 0 matches
grep -rn "verifyInternalPassword" src/ tests/             # expect 0 matches
grep -rn "signInAnonymously" src/                         # expect 0 matches
grep -rn "firebase-ready" src/                            # expect 0 matches
test ! -f src/auth/state-machine.js                       # exit 0
test ! -f tests/auth/state-machine.test.js                # exit 0
test ! -f tests/fixtures/auth-passwords.js                # exit 0
grep "sha256-hex-literal-regression" .gitleaks.toml       # expect 0 matches

# Quality gates
npm test 2>&1 | tail -10                                  # all tests pass (excluding deleted state-machine.test.js)
npm run lint 2>&1 | tail -5                               # exit 0
npm run typecheck 2>&1 | tail -5                          # exit 0
cd functions && npm test 2>&1 | tail -10                  # functions tests pass
```

**Operator confirms:**
1. All 9 grep verifications return 0 matches.
2. `npm test` green (test count drops by ~3 from state-machine deletion — confirm the only red is "module not found" for the deleted test file, which Vitest skips automatically since the file is gone).
3. `npm run lint && npm run typecheck` green.
4. `cd functions && npm test` green.
5. Anonymous Auth still shows "Disabled" in Firebase Console (Task 2 outcome).
6. Both admins can still sign in (now via the post-Wave-3 Email/Password flow); claims still carry `role: "admin"`.

**If `npm test` shows unexpected failures:** Investigate. Most likely cause: some other module in `src/` or `tests/` referenced one of the deleted symbols (e.g., the `auth.verifyInternalPassword` wrapper at main.js:464 was called from elsewhere). Fix by either (a) removing the call site or (b) replacing with a no-op.

**If grep finds residual matches:** Fix by editing the offending file; re-stage; amend the commit (since this is the same atomic commit). Do NOT split into a follow-up commit — D-04 says atomic.

**Claude's role:** compose the commit by executing the 7 sub-steps of phase6-cutover.md Step 8 verbatim; verify all 9 greps + quality gates; commit with the message above (with `$REHEARSED_SHA` substituted from Task 1 evidence). Push to main.
  </how-to-verify>
  <resume-signal>"auth14-deletion-complete" — operator confirms all 9 greps return 0 + npm test/lint/typecheck green + commit pushed to main + CI green on the commit. Operator pastes:
- `auth14_deletion_sha:` (the commit SHA)
- `auth14_grep_verifications:` (paste of the 9 grep outputs, all empty)
- `npm_test_after_deletion:` (test count + status)

Claude appends these to 06-PREFLIGHT.md ## Cutover Log + proceeds to Task 4.</resume-signal>
  <action>
Claude:
1. Run all pre-deletion grep verifications. If any line numbers diverge from the pattern mapper (e.g., line 464 isn't `verifyInternalPassword`), STOP and ask operator (file may have shifted since pattern mapper).
2. Execute the 7-substep deletion sequence from runbooks/phase6-cutover.md Step 8:
   - rm -f src/auth/state-machine.js tests/auth/state-machine.test.js tests/fixtures/auth-passwords.js
   - Edit src/main.js to remove the verifyInternalPassword wrapper. WARNING-FIX: line numbers may have shifted; use anchor-based removal. The line to remove is exactly: `  const verifyInternalPassword = (pass) => auth.verifyInternalPassword(pass, { INTERNAL_PASSWORD_HASH });`. Remove that single line (it is a one-line const declaration; verified via `awk 'NR>=460 && NR<=475' src/main.js` at planning time). Also remove the orphaned comment lines immediately above it (`// Phase 2 Wave 4 (D-05): wrappers for auth state-machine (Pattern E).` + `// Bodies extracted to src/auth/state-machine.js. Phase 6 (AUTH-14) deletes the whole module.`). Post-edit grep MUST pass: `grep -n "verifyInternalPassword" src/main.js` returns 0 matches AND `grep -n "Phase 2 Wave 4 (D-05): wrappers for auth state-machine" src/main.js` returns 0 matches.
   - Edit src/main.js to remove lines 625-633 (the entire `// ---------- Hardcoded internal credentials ----------` block down to `}` of isAllowedInternalEmail)
   - Edit src/main.js to remove line 3156 (the dead `INTERNAL_ALLOWED_EMAILS.join(", ")` reference; also remove the surrounding `settingsCard.appendChild(h("div", {style:...}, "Allowed emails: " + ...))` block — find the matching outer `appendChild` call and remove the entire block since the UI element no longer makes sense)
   - Edit src/main.js to remove the entire window.addEventListener("firebase-ready", async () => { ... }) block starting at line 4115 (multi-line block; find the closing `});`).
   - **BLOCKER-FIX 1 cross-plan from 06-03 Task 3:** Add the user.appClaims + user.appEnrolledFactors hydration block to main.js's onAuthStateChanged callback. Firebase JS SDK `user` does NOT expose `idTokenClaims` directly + `multiFactor` is a function (not a property) — without this hydration the router's MFA gate would silently see undefined. Concrete shape (insert AT the existing onAuthStateChanged call site or wherever sign-in flow routes; replaces the firebase-ready bridge as the primary auth-state observer):
     ```js
     // Phase 6 Wave 5 (BLOCKER-FIX 1): hydrate appClaims + appEnrolledFactors
     // BEFORE invoking renderRoute. Required because Firebase user object
     // doesn't expose idTokenClaims directly + multiFactor is a function call.
     import { onAuthStateChanged, multiFactor } from "./firebase/auth.js";
     // ...
     onAuthStateChanged(auth, async (user) => {
       if (user) {
         const tokenResult = await user.getIdTokenResult();
         user.appClaims = tokenResult.claims;
         user.appEnrolledFactors = multiFactor(user).enrolledFactors;
         // BLOCKER-FIX 3 cross-plan: detect MFA recovery flow from query param
         // OR email-link sign-in completion; main.js sets state.route = "forgot-mfa"
         // which the router's auth-state ladder picks up.
         if (typeof window !== "undefined") {
           const params = new URLSearchParams(window.location.search);
           if (params.get("mfaRecovery") === "1") state.route = "forgot-mfa";
         }
       }
       renderRoute(main, user, org, deps);
     });
     ```
     Verify: `grep -n "user.appClaims" src/main.js` returns at least 1 match; `grep -n "user.appEnrolledFactors" src/main.js` returns at least 1 match; `grep -n "firebase-ready" src/main.js` returns 0 matches.
   - Edit src/firebase/auth.js: replace the file content with the post-cutover version (see below)
   - Edit .gitleaks.toml to remove the `[[rules]] id = "sha256-hex-literal-regression"` block at lines 8-14 (DO NOT touch the [allowlist] block at lines 16-37)

3. Post-cutover `src/firebase/auth.js` content (Wave 3's filled bodies + Wave 5's deletions, final form):

```javascript
// src/firebase/auth.js
// @ts-check
// Phase 6 Wave 5 cutover: real Email/Password auth replaces signInAnonymously
// substrate. AUTH-12 unified-error wrapper at this chokepoint per D-13.
// Anonymous Auth disabled in Firebase Console at cutover (D-03 + Step 7).
import {
  onAuthStateChanged,
  signInWithEmailAndPassword,
  signOut as fbSignOut,
  multiFactor as fbMultiFactor,
  updatePassword as fbUpdatePassword,
  sendSignInLinkToEmail as fbSendSignInLinkToEmail,
  isSignInWithEmailLink as fbIsSignInWithEmailLink,
  signInWithEmailLink as fbSignInWithEmailLink,
} from "firebase/auth";
import { auth } from "./app.js";

export { auth, onAuthStateChanged };

// Phase 6 (AUTH-12 / D-13): single-chokepoint unified-error wrapper.
export class SignInError extends Error {
  constructor() {
    super("Email or password incorrect");
    this.name = "SignInError";
  }
}

const AUTH_CRED_ERROR_CODES = new Set([
  "auth/user-not-found",
  "auth/wrong-password",
  "auth/invalid-credential",
  "auth/too-many-requests",
  "auth/user-disabled",
  "auth/invalid-email",
  "auth/missing-password",
  "auth/missing-email",
]);

/** @param {string} email @param {string} password */
export async function signInEmailPassword(email, password) {
  try {
    return await signInWithEmailAndPassword(auth, email, password);
  } catch (err) {
    const code = (err && /** @type {*} */ (err).code) || "";
    if (AUTH_CRED_ERROR_CODES.has(code)) {
      throw new SignInError();
    }
    throw err;
  }
}

/** @returns {Promise<void>} */
export async function signOut() {
  await fbSignOut(auth);
}

/** @param {*} user */
export function multiFactor(user) {
  return fbMultiFactor(user);
}

// BLOCKER-FIX 2 carry-forward from Wave 3: updatePassword consumed by renderFirstRun.
/** @param {string} newPassword */
export async function updatePassword(newPassword) {
  const user = auth.currentUser;
  if (!user) throw new SignInError();
  try {
    await fbUpdatePassword(user, newPassword);
  } catch (err) {
    const code = (err && /** @type {*} */ (err).code) || "";
    if (code === "auth/weak-password" || code === "auth/requires-recent-login") {
      throw new Error(code === "auth/weak-password"
        ? "Password does not meet policy (min 12 chars, no leaked passwords)"
        : "Please sign in again to change your password");
    }
    throw err;
  }
}

// BLOCKER-FIX 3 carry-forward from Wave 3: Tier-1 user-side MFA recovery surface.
/** @param {string} email @param {{url: string, handleCodeInApp: boolean}} settings */
export async function sendSignInLinkToEmail(email, settings) {
  return fbSendSignInLinkToEmail(auth, email, settings);
}
/** @param {string} url */
export function isSignInWithEmailLink(url) {
  return fbIsSignInWithEmailLink(auth, url);
}
/** @param {string} email @param {string} url */
export async function signInWithEmailLink(email, url) {
  return fbSignInWithEmailLink(auth, email, url);
}
```

4. Run all 9 post-deletion greps + quality gates (npm test, npm run lint, npm run typecheck, cd functions && npm test). All must pass.
5. Compose the commit with the message above (substituting the actual rules_deploy_sha or rehearsal SHA from Task 1).
6. `git add -A && git commit -m "..."` (HEREDOC for the multi-line message).
7. `git push`. Watch CI: `gh run watch`. Confirm CI green.
8. Pause for operator confirmation. Operator pastes the 3 evidence items.
9. Claude appends `auth14_deletion_sha:` + verification evidence to 06-PREFLIGHT.md ## Cutover Log. Commit + push.

**If quality gates fail:** Investigate the failure; fix; amend the same commit (NOT a follow-up commit — D-04 atomicity). Push --force-with-lease only on the cutover commit, no main-branch force pushes.

**If main.js dead-reference removal touches IIFE closure variables (loadOrg, currentUser, etc.):** Be surgical. Do NOT migrate IIFE bodies to Pattern D in this commit (Phase 4 sub-wave 4.1 owns that). The deletion is targeted at the 4 specific sites only.
  </action>
  <verify>
    <automated>grep -rn "INTERNAL_PASSWORD_HASH" src/ tests/ 2>&1 | wc -l | grep -q "^0$" && grep -rn "INTERNAL_ALLOWED_EMAILS" src/ tests/ 2>&1 | wc -l | grep -q "^0$" && grep -rn "signInAnonymously" src/ 2>&1 | wc -l | grep -q "^0$" && grep -rn "firebase-ready" src/ 2>&1 | wc -l | grep -q "^0$" && test ! -f src/auth/state-machine.js && test ! -f tests/auth/state-machine.test.js && test ! -f tests/fixtures/auth-passwords.js && ! grep -q "sha256-hex-literal-regression" .gitleaks.toml && npm test 2>&1 | tail -5 && npm run lint 2>&1 | tail -3 && npm run typecheck 2>&1 | tail -3 && echo "AUTH-14 deletion verified"</automated>
  </verify>
  <done>
    - 3 files DELETED: state-machine.js + state-machine.test.js + auth-passwords.js
    - 4 files MODIFIED: src/main.js (4 deletion sites) + src/firebase/auth.js (4 deletion sites — import name, re-export, dispatchEvent block, call) + .gitleaks.toml (1 [[rules]] block) + (.gitleaks.toml [allowlist] block UNCHANGED)
    - 9 grep verifications all return 0 matches
    - `npm test && npm run lint && npm run typecheck` all green
    - `cd functions && npm test` green
    - Atomic single-commit pushed to main with the prescribed message
    - CI green on the commit
    - Both admins can still sign in (post-cutover Email/Password flow)
    - Anonymous Auth still shows "Disabled" in Firebase Console
    - 06-PREFLIGHT.md ## Cutover Log has `auth14_deletion_sha:` populated
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: MFA enrolment + AUTH-10 live drill (Round 1 + Round 2) + SC#4 clock-skew exercise + final 06-PREFLIGHT.md ## Cutover Log population</name>
  <files>runbooks/phase6-mfa-recovery-drill.md, .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md</files>
  <read_first>
    - runbooks/phase6-cutover.md Steps 9-12
    - runbooks/phase6-mfa-recovery-drill.md (Wave 4 deliverable — Round 1 + Round 2 evidence templates ready for population)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-CONTEXT.md D-08 (AUTH-10 drill same-day-as-cutover) + D-19 (SC#4 clock-skew exercise)
    - .planning/research/PITFALLS.md §Pitfall 7 (MFA enrolment lockout) + §Pitfall 19 (claim only what was rehearsed)
  </read_first>
  <what-built>
Operator runs Steps 9-12 of phase6-cutover.md against the live project; Claude captures evidence into:
- runbooks/phase6-mfa-recovery-drill.md: `## Drill Evidence — Round 1` and `## Drill Evidence — Round 2` blocks fully populated with timing, factorIds, exit codes, and any gaps.
- 06-PREFLIGHT.md ## Cutover Log: final 5+ keys (luke_mfa_enrolled_at, george_mfa_enrolled_at, mfa_drill_round_*_total_seconds, sc4_clock_skew_passed, cutover_complete: yes).

**Step 9 — MFA enrolment:** Both admins sign in; router routes to renderMfaEnrol; each scans QR + enters code + submits.

**Step 10 — AUTH-10 drill (Tier-2 procedure):**
- Round 1: Luke is "locked out". George operates. OOB voice/video identity verification → list factors → un-enrol → Luke signs in → re-enrols TOTP. Capture all timing.
- Round 2: George is "locked out". Luke operates. Same procedure, swapped roles.

**Step 11 — SC#4 clock-skew exercise:**
- One admin spins up a test org via the admin panel.
- Sends 2 chat messages.
- Marks them read.
- DevTools clock override ±5 minutes.
- Reload; observe unread badge.
- Expected: badge unchanged (server-clock-vs-server-clock comparator from Phase 5 D-14 / DATA-07).
- Closes 05-HUMAN-UAT.md test #2.

**Step 12 — Cutover Log finalisation:**
- Append all remaining keys to 06-PREFLIGHT.md ## Cutover Log.
- `cutover_complete: yes` is the closing key.
- Commit + push.
  </what-built>
  <how-to-verify>
**Step 9 verify (per admin):**
- Admin signs in at production URL.
- Router routes to renderMfaEnrol view (per D-16).
- Admin scans QR with TOTP authenticator.
- Admin enters 6-digit code in form.
- Form submits successfully; admin is signed in fully.
- Capture `luke_mfa_enrolled_at:` + `george_mfa_enrolled_at:` timestamps.

**Step 10 Round 1 verify (Luke locked out, George operating):**
- OOB identity verification: voice/video call; capture channel + verified_at.
- List enrolled factors via canonical Admin SDK path: see `runbooks/phase6-mfa-recovery-drill.md` Tier-2 procedure step 2 (`admin.auth().getUser(<uid>)` + JSON-stringify u.multiFactor). Capture factorIds + ran_at. (BLOCKER-FIX 4: `firebase auth:multifactor:list` CLI is NOT used — unverified at milestone pin.)
- Un-enrol via canonical Admin SDK path (BLOCKER-FIX 4): `cd functions && node ../scripts/admin-mfa-unenroll/run.js --uid <luke uid>`. Capture exit code + ran_at + the BEFORE/AFTER stdout. (The `firebase auth:multifactor:unenroll` CLI subcommand is NOT used per BLOCKER-FIX 4 — Admin SDK turn-key script is canonical.)
- Luke signs in (without MFA gate now); capture ms_to_signin_success.
- Luke re-enrols TOTP via renderMfaEnrol; capture ms_to_enrol_success.
- Total drill seconds: target < 900 (15 min), excluding the OOB identity verification time which is operator-paced.
- Capture all values in runbooks/phase6-mfa-recovery-drill.md `## Drill Evidence — Round 1`.

**Step 10 Round 2 verify (George locked out, Luke operating):** Same procedure as Round 1, swapped roles. Capture all values in `## Drill Evidence — Round 2`.

**AUTH-10 closure criteria (per the drill runbook):**
- [ ] Round 1 + Round 2 both completed end-to-end
- [ ] Both step_3 exit_code = 0
- [ ] Both step_4 sign-ins succeeded
- [ ] gaps_observed: either empty (no gaps) OR explicitly accepted in SECURITY.md (Wave 6 captures any accepted gaps)

**Step 11 verify:**
- Admin spins up test org "Test Org for SC#4" in admin panel.
- Sends 2 chat messages.
- Reads both (badge goes 2 → 0).
- DevTools sensors: override clock to current_time + 5 minutes; refresh page; observe badge (still 0 expected).
- DevTools sensors: override clock to current_time - 5 minutes; refresh page; observe badge (still 0 expected).
- Capture `sc4_clock_skew_passed: yes` if both probes show no badge change. Capture `sc4_clock_skew_passed: no` + investigation notes if badge changes.

**Step 12 verify:**
- 06-PREFLIGHT.md ## Cutover Log has all 12-step evidence keys populated.
- `cutover_complete: yes` is set.
- File committed; push; CI green.

**Claude's role:** prompt operator for each value; append to the right runbook (drill rounds → mfa-recovery-drill.md; SC#4 → 06-PREFLIGHT.md). Final commit closes the wave.
  </how-to-verify>
  <resume-signal>"step-9-12-complete" — operator pastes:
- `luke_mfa_enrolled_at:` + `george_mfa_enrolled_at:` (timestamps)
- Round 1 evidence block (8 fields)
- Round 2 evidence block (8 fields)
- `mfa_drill_round_1_total_seconds:` + `mfa_drill_round_2_total_seconds:` (ints)
- `sc4_clock_skew_passed: yes|no` + any investigation notes
- Final `cutover_complete: yes` confirmation

Claude appends all evidence to runbooks/phase6-mfa-recovery-drill.md (Rounds 1 + 2) AND 06-PREFLIGHT.md ## Cutover Log + commits + pushes. Wave 5 closes.</resume-signal>
  <action>
Claude:
1. Pause for operator. Operator runs Steps 9-12 live; pastes evidence as each step completes.
2. After Step 9: append luke_mfa_enrolled_at + george_mfa_enrolled_at to 06-PREFLIGHT.md ## Cutover Log.
3. After Round 1: populate runbooks/phase6-mfa-recovery-drill.md `## Drill Evidence — Round 1` block with all 8 fields (drill_date, locked_out_uid, locked_out_email, operator_uid, step_1..step_5, total_drill_seconds, gaps_observed).
4. After Round 2: populate `## Drill Evidence — Round 2` block.
5. After Step 11: append sc4_clock_skew_passed to 06-PREFLIGHT.md ## Cutover Log.
6. After Step 12: append cutover_complete: yes to 06-PREFLIGHT.md ## Cutover Log + mfa_drill_round_*_total_seconds + mfa_drill_evidence_path: runbooks/phase6-mfa-recovery-drill.md.
7. Commit: `git add runbooks/phase6-mfa-recovery-drill.md .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && git commit -m "feat(06-05): cutover complete — MFA enrolment + AUTH-10 drill (Round 1 + Round 2) + SC#4 clock-skew passed"`. Push.
8. Watch CI green; confirm.

**If AUTH-10 drill encounters a gap (e.g., factorId list is empty, Admin SDK call unexpectedly fails):**
- Document in Round 1 or Round 2 `gaps_observed:` field.
- Operator decides: (a) close gap real-time (find Admin SDK alternative, retry) or (b) accept gap (escalate; SECURITY.md Wave 6 captures the accepted gap with rationale).
- Do NOT mark AUTH-10 closed if either round fails to complete.

**If SC#4 clock-skew exercise reveals a real bug** (e.g., badge count changes when clock skewed):
- Document in 06-PREFLIGHT.md ## Cutover Log: `sc4_clock_skew_passed: no` + investigation notes.
- Phase 5 H7 fix (DATA-07) is supposed to prevent this — investigate whether the test org's data path goes through `src/domain/unread.js` server-clock comparator or some other path.
- Escalate; do NOT mark cutover complete.

**If everything passes cleanly:** Wave 5 closes. Wave 6 (cleanup + DOC-10 SECURITY.md update + RULES-07 verification gate) unblocks.
  </action>
  <verify>
    <automated>grep -q "drill_date:" runbooks/phase6-mfa-recovery-drill.md && grep -A 1 "Round 1" runbooks/phase6-mfa-recovery-drill.md | grep -q "drill_date" && grep -A 1 "Round 2" runbooks/phase6-mfa-recovery-drill.md | grep -q "drill_date" && grep -q "cutover_complete: yes" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "sc4_clock_skew_passed" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "mfa_drill_round_1_total_seconds" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && grep -q "mfa_drill_round_2_total_seconds" .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md && echo "OK"</automated>
  </verify>
  <done>
    - Both admins MFA-enrolled (timestamps captured); MFA shows "TOTP" enrolled in Firebase Console for both UIDs
    - runbooks/phase6-mfa-recovery-drill.md `## Drill Evidence — Round 1` populated with all 8+ fields (no template placeholders remaining); step_3 exit_code = 0; step_4 sign-in succeeded
    - runbooks/phase6-mfa-recovery-drill.md `## Drill Evidence — Round 2` populated with all 8+ fields (no template placeholders remaining); step_3 exit_code = 0; step_4 sign-in succeeded
    - AUTH-10 closure criteria all checked off (in mfa-recovery-drill.md `## AUTH-10 Closure Criteria`)
    - 06-PREFLIGHT.md ## Cutover Log has `cutover_complete: yes` + sc4_clock_skew_passed + all 12-step evidence keys
    - runbooks/phase6-mfa-recovery-drill.md committed to main
    - 06-PREFLIGHT.md committed to main
    - CI green on the close-out commit
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> production Firebase project | live deploys, live Console mutations, live Admin SDK invocations cross this boundary; trust comes from operator's authenticated session |
| operator OOB -> admin recipients | temp credential delivery + AUTH-10 identity verification happen outside the system |
| repo HEAD -> CI deploy job -> Firebase | the rollback path; rules-rollback rehearsal proves this loop closed |
| pre-cutover state -> post-cutover state | atomic transition: must end with claims live + rules deployed + anon disabled + AUTH-14 source removed; partial transitions are the failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | EoP | Pitfall 1 — strict rules deployed before claims propagate locks all users out | mitigate | D-11 deploy ordering + Step 5 ABORT GATE: rules deploy CANNOT proceed unless both admins' ID tokens carry role=admin; runbook explicitly documents the abort path |
| T-06-05-02 | EoP | Pitfall 19 — claim a 5-min rollback that wasn't actually tested | mitigate | Step 1 rules-rollback rehearsal runs the full revert + redeploy + revert-the-revert cycle against the live project pre-cutover; runbook captures total_seconds; rehearsal IS the SC#4 evidence |
| T-06-05-03 | EoP | Pitfall 7 — admin permanently locked out from MFA loss | mitigate | AUTH-10 Tier-1 (email-link self-service) + Tier-2 (drilled live in Step 10 Round 1 + Round 2); both admins prove they can recover the other |
| T-06-05-04 | Information-Disclosure | AUTH-14 deletion misses one site → INTERNAL_PASSWORD_HASH lingers in source | mitigate | Step 8 enumerates 9 grep verifications; commit cannot be considered done until all 9 return 0 matches |
| T-06-05-05 | Tampering | Anonymous Auth Console-disable not actually clicked → bridge stays alive in production | mitigate | Step 7 verifies Console "Disabled" status; combined with the source-removal in the AUTH-14 commit (Step 8) — both must hold |
| T-06-05-06 | Repudiation | Cutover happens but no auditable record exists | mitigate | 06-PREFLIGHT.md ## Cutover Log has 18+ populated keys; runbook drill has 16+ populated fields across both rounds; rehearsal runbook has 9 populated fields — full audit trail in repo |
| T-06-05-07 | DoS | Cold-start exceeds 7s deadline on first auth event after deploy | mitigate | minInstances:1 from Phase 6 Wave 2 + Step 2 cold-start measurement; if p99 ≤ 4s fails, escalate before Step 5 |
| T-06-05-08 | EoP | Operator skips Step 1 rehearsal under time pressure → discovers rollback broken if cutover fails | mitigate | Step 1 is the FIRST cutover step; runbook makes the rehearsal load-bearing; Wave 5 plan task ordering puts Task 1 before Task 2 |
| T-06-05-09 | Spoofing | Anonymous user signs in via residual `signInAnonymously` call between Step 6 (rules deploy) and Step 8 (source removal) | mitigate | Step 7 disables Anonymous Auth in Console BEFORE Step 8; even if source remained for a few seconds, Console-disable rejects the call server-side; AUTH-14 source removal then closes the audit-narrative window |
| T-06-05-10 | Information-Disclosure | OOB credential delivery channel compromised | accept | D-06 leaves channel operator-defined; SECURITY.md Wave 6 captures audit-narrative tradeoff (compliance-credible-not-certified posture) |
| T-06-05-11 | EoP | Bootstrap admin claims_set fails silently → Step 5 ABORT GATE catches it | mitigate | Step 5's getIdTokenResult().claims.role === "admin" is the explicit verification; ABORT path documented |
| T-06-05-12 | Tampering | Cutover commit force-pushed by accident overwrites main | mitigate | --force-with-lease only used on cutover commit + only by operator; CLAUDE.md sequencing non-negotiables + Conventional Commits convention; Phase 3 branch protection in place |
</threat_model>

<verification>
- runbooks/phase6-rules-rollback-rehearsal.md exists with populated Rehearsal Evidence (total_seconds < 300 OR explicit gap)
- All 3 auth-blocking + callable Cloud Functions deployed to bedeveloped-base-layers in europe-west2
- internalAllowlist/{lukeEmail,georgeEmail} docs exist with role=admin
- Both admins Console-created with emailVerified=true; signed in successfully; ID-token claims confirm role=admin
- firestore.rules + storage.rules deployed to production exactly once during this phase (Wave 6 verifies this gate)
- Anonymous Auth Console-disabled in Firebase Console
- AUTH-14 deletion commit landed on main with 9-grep verification (all 0 matches)
- Both admins MFA-enrolled (TOTP)
- AUTH-10 drill Round 1 + Round 2 both completed end-to-end with evidence captured
- SC#4 clock-skew exercise passed (badge count unchanged with ±5-min skew)
- 06-PREFLIGHT.md ## Cutover Log has cutover_complete: yes + 18+ evidence keys
- CI green on all cutover commits
</verification>

<success_criteria>
- 13 of 13 phase requirements addressed by the live cutover (AUTH-01 anon-disabled, AUTH-03 sign-in works, AUTH-05 beforeUserCreated live, AUTH-06 beforeUserSignedIn live, AUTH-07 setClaims callable + claims-admin.js wired, AUTH-08 TOTP MFA enrolled, AUTH-10 drill complete, AUTH-11 email-verify enforced both rules + client, AUTH-12 unified-error live, AUTH-13 Firebase progressive-delay defaults documented in Wave 6, AUTH-14 source removal complete, AUTH-15 bootstrap migration complete, RULES-07 production deploy gated to exactly one)
- 4 checkpoint tasks completed in order (rehearsal → functions/bootstrap/rules → AUTH-14 deletion → MFA/drill/SC#4)
- ALL Pitfall 1 mitigations honoured (functions FIRST → bootstrap → claims-verify → rules deploy → anon-disable)
- ALL Pitfall 19 mitigations honoured (rehearsal evidence + drill evidence both committed)
- 06-PREFLIGHT.md ## Cutover Log + runbooks/phase6-mfa-recovery-drill.md drill rounds both fully populated
- Production state: real Firebase Auth identity for both admins + custom claims live + MFA enrolled + rules enforced + Anonymous Auth disabled + AUTH-14 source deleted
</success_criteria>

<output>
After completion, create `.planning/phases/06-real-auth-mfa-rules-deploy/06-05-SUMMARY.md` capturing:
- All 4 checkpoint outcomes
- The cutover SHA + AUTH-14 deletion SHA + rehearsal SHAs
- The 11 of 13 requirement closures landed live (AUTH-13 doc-only + AUTH-09 supersession defer to Wave 6)
- Confirmation that RULES-07 deploy ran exactly once (Wave 6 verifies the gate)
- Confirmation that the Phase 5 D-21 carry-forward rows are now closeable in Wave 6
</output>
