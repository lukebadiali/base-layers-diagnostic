---
phase: 06.1
plan: 03
type: execute
wave: 3
depends_on:
  - 06.1-01
  - 06.1-02
files_modified:
  - src/main.js
  - src/auth/state-machine.js
  - tests/auth/state-machine.test.js
  - tests/fixtures/auth-passwords.js
  - scripts/strip-legacy-user-passwords/run.js
  - scripts/strip-legacy-user-passwords/README.md
  - SECURITY.md
  - REQUIREMENTS.md
  - .planning/ROADMAP.md
  - runbooks/phase6-mfa-recovery-drill.md
  - runbooks/06.1-app-check-backoff.md
  - runbooks/phase-06.1-cleanup-ledger.md
  - .planning/STATE.md
autonomous: false
requirements:
  - AUTH-16
  - AUTH-17
  - AUTH-18
requirements_addressed:
  - AUTH-16
  - AUTH-17
  - AUTH-18
threat_model_refs:
  - T-06.1-02
  - T-06.1-04
  - T-06.1-07
user_setup:
  - service: gcloud
    why: "Operator runs scripts/strip-legacy-user-passwords/run.js via Application Default Credentials (Phase 6 D-20 / Pitfall 13 pattern)."
    env_vars: []
    dashboard_config:
      - task: "Run `gcloud auth application-default login` with an account holding Cloud Datastore User role on bedeveloped-base-layers."
        location: "Operator's local shell."

must_haves:
  truths:
    - "src/auth/state-machine.js, tests/auth/state-machine.test.js, and tests/fixtures/auth-passwords.js are deleted in a single atomic commit (per CONTEXT D-02 — no dead-code window)."
    - "setUserPassword function and the legacy client sign-in branch (state.authTab === \"client\") and openChangePasswordModal are deleted from src/main.js in the same atomic commit."
    - "Wrapper imports for verifyOrgClientPassphrase and verifyUserPassword at src/main.js:525-527 are deleted; only the `currentUser` wrapper survives (still used by other surfaces)."
    - "After cutover: zero references to state-machine.js / verifyOrgClientPassphrase / verifyUserPassword / setUserPassword in src/ + tests/."
    - "scripts/strip-legacy-user-passwords/run.js exists + supports --dry-run / --verify / --help flags."
    - "SECURITY.md gains a `## § Client Authentication` section and a `## § Phase 06.1 Audit Index` table mapping AUTH-16/17/18 to controls + code paths + tests."
    - "REQUIREMENTS.md gains 3 new rows (AUTH-16, AUTH-17, AUTH-18) + 3 Traceability table entries marked Validated 2026-05-22 / Phase 06.1."
    - ".planning/ROADMAP.md § Phase 06.1 replaces `[Urgent work - to be planned]` placeholder with real goal + 7 success criteria + REQ-IDs list + 3 plans."
    - "runbooks/phase6-mfa-recovery-drill.md gains a § Client users row extending Tier-2 admin un-enrol to cover client-user lockout."
    - "runbooks/06.1-app-check-backoff.md captures the DevTools Clear-site-data workaround + the underlying HANDOFF.md follow-up #3 forward-tracking row."
    - "runbooks/phase-06.1-cleanup-ledger.md has `phase_06.1_active_rows: 0` zero-out gate; closes Phase 6 HANDOFF.md follow-up #9; queues v2 client-MFA-hardening + setOrgClientPassphrase-callable-migration forward-tracking rows."
    - ".planning/STATE.md is updated: progress fields reconciled (HANDOFF.md follow-up #9 closed), current-position narrative updated."
  artifacts:
    - path: "scripts/strip-legacy-user-passwords/run.js"
      provides: "Admin SDK ADC one-shot strip script (defensive — expected count 0 per HANDOFF.md)."
      contains: "FieldValue.delete()"
    - path: "scripts/strip-legacy-user-passwords/README.md"
      provides: "Operator docs for the strip script (Why / Prerequisites / Usage / Expected outcome / Audit narrative)."
      min_lines: 50
    - path: "SECURITY.md"
      provides: "§ Client Authentication section + § Phase 06.1 Audit Index citation table."
      contains: "## § Phase 06.1 Audit Index"
    - path: "REQUIREMENTS.md"
      provides: "AUTH-16, AUTH-17, AUTH-18 rows + Traceability entries."
      contains: "**AUTH-18**"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 06.1 entry populated with real goal + success criteria + plans."
      contains: "AUTH-16, AUTH-17, AUTH-18"
    - path: "runbooks/phase6-mfa-recovery-drill.md"
      provides: "Tier-2 admin un-enrol row extended to cover client users."
      contains: "Client users"
    - path: "runbooks/06.1-app-check-backoff.md"
      provides: "Operator runbook for App Check 22h backoff workaround."
      min_lines: 25
    - path: "runbooks/phase-06.1-cleanup-ledger.md"
      provides: "Zero-out gate + closure of Phase 6 follow-up #9 + forward-tracking rows for v2 client-MFA + setOrgClientPassphrase-callable migration."
      contains: "phase_06.1_active_rows: 0"
  key_links:
    - from: "Atomic cutover commit"
      to: "HANDOFF.md follow-up #9"
      via: "src/auth/state-machine.js deletion completes Phase 6 D-04's claimed-but-never-executed deletion"
      pattern: "follow-up #9|HANDOFF"
    - from: "SECURITY.md § Phase 06.1 Audit Index"
      to: "REQUIREMENTS.md AUTH-16/17/18"
      via: "citation table maps requirement IDs to code paths + tests + framework"
      pattern: "AUTH-1[678]"
    - from: ".planning/ROADMAP.md § Phase 06.1"
      to: "REQUIREMENTS.md"
      via: "REQ-IDs listed in Requirements field"
      pattern: "AUTH-16, AUTH-17, AUTH-18"
---

<objective>
Land the atomic cutover commit deleting the legacy client-auth substrate (state-machine.js + its tests + fixture + setUserPassword + the client sign-in branch + openChangePasswordModal + the 2 wrapper imports), ship the operator strip script + README, author the DOC-10 SECURITY.md additions + Phase 06.1 Audit Index, update REQUIREMENTS.md with 3 new rows, replace the ROADMAP placeholder with the real Phase 06.1 entry, extend the MFA recovery runbook for client users, author the App Check backoff runbook, zero out the Phase 06.1 cleanup ledger, and reconcile STATE.md.

Purpose: This is the close-out wave for Phase 06.1. The atomic cutover commit (CONTEXT D-02) is the load-bearing audit-narrative event — no dead-code window, no half-deletions. HANDOFF.md follow-up #9 (the Phase 6 verification gap where state-machine.js was claimed-deleted but never removed) closes here. The DOC-10 + REQUIREMENTS bookkeeping makes the work questionnaire-credible.

Output: 1 atomic deletion commit (multiple files in one commit) + 1 operator strip script + multiple docs additions. Ends with a `/gsd-verify-work 06.1` operator checkpoint.
</objective>

<execution_context>
@C:\Users\hughd\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\hughd\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.md
@.planning/REQUIREMENTS.md
@SECURITY.md
@.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-CONTEXT.md
@.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-RESEARCH.md
@.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-PATTERNS.md
@.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-01-PLAN.md
@.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-02-PLAN.md
@scripts/seed-internal-allowlist/run.js
@scripts/seed-internal-allowlist/README.md
@runbooks/phase6-mfa-recovery-drill.md
@CLAUDE.md

<interfaces>
src/main.js deletion targets (verified line ranges via grep):
- L522-528: 3 wrapper-related lines. REMOVE the `verifyOrgClientPassphrase` declaration (lines 525-526) and the `verifyUserPassword` declaration (line 527). KEEP `legacyCurrentUser` (line 528) + `currentUser` (line 531).
- L710-716: `setUserPassword` function — DELETE wholesale.
- L1184-1303: the `if (state.authTab === "client") { ... }` branch inside sign-in renderer — DELETE wholesale; the `else` (L1304+, internal-admin branch) becomes unconditional.
- L5042-5089: `openChangePasswordModal(user)` — DELETE wholesale.
- L920: any `openChangePasswordModal,` reference in dispatch tables — REMOVE.

Cutover commit message (CONTEXT D-02 + PATTERNS § Pattern S9 verbatim):
```
feat(06.1)!: wave 3 cutover — delete legacy client auth substrate; closes HANDOFF #9

Deletes src/auth/state-machine.js + tests/auth/state-machine.test.js +
tests/fixtures/auth-passwords.js + setUserPassword + the legacy client sign-in
branch + openChangePasswordModal + 2 wrapper imports in src/main.js. Closes
Phase 6 HANDOFF.md follow-up #9 (state-machine.js claimed-deleted but never
removed). Replaces with Firebase Auth Email/Password + inviteClient callable
(landed Wave 2). No dead-code window.

REQ-IDs: AUTH-16, AUTH-17, AUTH-18.

BREAKING CHANGE: removes the legacy passphrase + per-user passwordHash client
sign-in path. All client users must now authenticate via Firebase Auth Email/
Password (after admin invites them via the inviteClient callable + they
complete renderFirstRun forced password change).
```

scripts/seed-internal-allowlist/run.js (verbatim template — 138 lines): shebang + @ts-check + ADC init + DRY_RUN/VERIFY/HELP argv + main() async + tail `.catch((err) => { console.error("[FAIL]", err); exit(1); })`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atomic cutover commit — delete all legacy client-auth substrate</name>
  <files>
    src/main.js,
    src/auth/state-machine.js,
    tests/auth/state-machine.test.js,
    tests/fixtures/auth-passwords.js
  </files>
  <read_first>
    - src/main.js lines 520-535 (wrapper-import block) + lines 696-720 (setUserPassword + setOrgClientPassphrase + orgHasClientPassphrase) + lines 1180-1310 (client sign-in branch) + lines 5040-5090 (openChangePasswordModal) + lines 915-925 (dispatch-table references)
    - src/auth/state-machine.js (the module being deleted)
    - tests/auth/state-machine.test.js (the test being deleted)
    - tests/fixtures/auth-passwords.js (the fixture being deleted)
    - .planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-CONTEXT.md § D-16 (verbatim deletion list + PRESERVE list)
    - .planning/HANDOFF.md (follow-up #9 closure narrative)
  </read_first>
  <action>
    Step 1 — Pre-flight grep verification (no edits yet):
    1a. `grep -rn "from \"\\./auth/state-machine\\|from \"\\./\\./auth/state-machine" src/ tests/` — list all importers. Expected non-empty (Wave 3 about to remove wrapper imports).
    1b. `grep -rn "verifyOrgClientPassphrase\|verifyUserPassword\|setUserPassword" src/ tests/` — confirm each call site is either in the deletion zones (1184-1303 + 5042-5089 + 710-716 + 525-527), or in state-machine.js itself, or in state-machine.test.js. If ANY call site is OUTSIDE these zones, STOP and report.
    1c. `grep -rn "passwordHash" src/` — production code OUTSIDE the deletion zones should not write or read `users/{uid}.passwordHash` after Wave 3.
    1d. `grep -rn "openChangePasswordModal" src/` — confirm references are only in src/main.js (definition + any dispatch-table call). External views (src/views/*) do NOT call it.

    If ANY grep step surfaces unexpected references, halt and write a deviation note. Otherwise proceed.

    Step 2 — Delete the 3 standalone files:
    - `git rm src/auth/state-machine.js`
    - `git rm tests/auth/state-machine.test.js`
    - `git rm tests/fixtures/auth-passwords.js`

    Step 3 — Surgery on src/main.js (single file, multiple ranges):

    3a. Lines 522-528 wrapper-import block: REMOVE the `verifyOrgClientPassphrase` declaration (lines 525-526) and the `verifyUserPassword` declaration (line 527). Update surrounding comment at lines 520-524 to:

    ```javascript
    // Phase 06.1 (AUTH-17 / D-16): verifyOrgClientPassphrase + verifyUserPassword
    // wrappers DELETED alongside src/auth/state-machine.js. Client sign-in now
    // routes through Firebase Auth signInEmailPassword (src/firebase/auth.js)
    // identically to internal admins; AUTH-12 chokepoint handles error mapping.
    // The legacyCurrentUser + currentUser pair below survives — still used by
    // other surfaces hydrating from localStorage during the IIFE→modular
    // migration window (Phase 4 sub-wave 4.1 carry-forward).
    ```

    Also REMOVE the `state-machine.js` import from the imports block (planner verifies exact import shape via grep `from.*state-machine`; surgically remove only the state-machine import).

    3b. Lines 710-716: DELETE the entire `setUserPassword` function definition wholesale.

    3c. Lines 1184-1303: DELETE the entire `if (state.authTab === "client") { ... }` branch wholesale. The surrounding code at L1182 (`container.appendChild(tabs)`) stays; the trailing `} else {` (L1304) loses its `if` partner — replace with: `// Phase 06.1 (AUTH-17): client sign-in branch deleted — clients now sign in via Firebase Auth identical to internal admins.` followed by the internal-admin body de-indented one level.

    Also: if `state.authTab` is no longer consumed anywhere (drop the tabs UI substrate above lines ~1160-1182 — planner verifies via grep `state.authTab`). If only used for switching internal/client tabs that no longer exist, REMOVE the tabs entirely.

    3d. Lines 5042-5089: DELETE the entire `openChangePasswordModal(user)` function wholesale.

    3e. Line ~920 + anywhere else: if a dispatch table or menu has `openChangePasswordModal,` as an entry, REMOVE that line. `grep -c openChangePasswordModal src/main.js` must return 0 after surgery.

    Step 4 — Post-deletion verification:
    4a. `npm test -- --run` — must exit 0. TEST-07 / state-machine cases removed; suite shrinks by the test count in the deleted file (was ~14 cases per Phase 2 baseline).
    4b. `cd functions && npm test -- --run && cd ..` — must exit 0.
    4c. `npm run lint` — must exit 0.
    4d. `npm run typecheck` (or `tsc --noEmit`) — must exit 0.
    4e. `npm run build` — must exit 0; clean dist/.
    4f. Final grep gates:
        - `grep -rn "state-machine\\b\|verifyOrgClientPassphrase\|verifyUserPassword\|setUserPassword" src/ tests/` returns ZERO hits (.planning/ + runbooks/ may still reference for narrative — exclude those).
        - `grep -rn "openChangePasswordModal" src/` returns ZERO hits.
        - `git status` shows: deleted state-machine.js + state-machine.test.js + auth-passwords.js; modified src/main.js. No other source files touched.

    Step 5 — Atomic commit (Task 1 only — docs + script land in Tasks 2-5 as separate commits but the load-bearing DELETION + src/main.js surgery are ONE commit):

    Verify staged set via `git status` is exactly {D state-machine.js, D state-machine.test.js, D auth-passwords.js, M src/main.js}. Commit with the verbatim message from <interfaces>.
  </action>
  <verify>
    <automated>npm test -- --run &amp;&amp; (cd functions &amp;&amp; npm test -- --run) &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/auth/state-machine.js` returns false (file deleted).
    - `test -f tests/auth/state-machine.test.js` returns false.
    - `test -f tests/fixtures/auth-passwords.js` returns false.
    - `grep -c "function setUserPassword" src/main.js` returns 0.
    - `grep -c "function openChangePasswordModal" src/main.js` returns 0.
    - `grep -c "state.authTab === \"client\"" src/main.js` returns 0.
    - `grep -c "verifyOrgClientPassphrase\|verifyUserPassword" src/main.js` returns 0.
    - `grep -rn "from \"\\./auth/state-machine" src/ tests/` returns 0 hits.
    - `grep -c "setOrgClientPassphrase\|orgHasClientPassphrase" src/main.js` returns ≥ 2 (PRESERVE list intact; Wave 1 length-floor preserved).
    - Full test suites green; build clean; lint clean.
    - Commit message contains `closes HANDOFF #9`, `BREAKING CHANGE`, and `AUTH-16, AUTH-17, AUTH-18`.
    - `git log -1 --name-status` shows only D + M markers consistent with the deletion list — no unintended changes.
  </acceptance_criteria>
  <done>Atomic cutover commit landed; all tests green; build clean; HANDOFF #9 closed at source level.</done>
</task>

<task type="auto">
  <name>Task 2: Author strip script + README + App Check backoff runbook</name>
  <files>
    scripts/strip-legacy-user-passwords/run.js,
    scripts/strip-legacy-user-passwords/README.md,
    runbooks/06.1-app-check-backoff.md
  </files>
  <read_first>
    - scripts/seed-internal-allowlist/run.js (verbatim 138-line template)
    - scripts/seed-internal-allowlist/README.md (verbatim 70-line template)
    - .planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-RESEARCH.md § 9 (verbatim strip loop) + § 4 (App Check 22h backoff)
    - .planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-PATTERNS.md § strip-legacy-user-passwords variations (lines 684-708)
    - .planning/HANDOFF.md (follow-up #3 — App Check backoff)
  </read_first>
  <action>
    Step 1: Create directory `scripts/strip-legacy-user-passwords/`.

    Step 2: Author `scripts/strip-legacy-user-passwords/run.js` — copy `scripts/seed-internal-allowlist/run.js` verbatim, then apply variations per PATTERNS § 684-708 + RESEARCH § 9:
    - Replace header docstring: cite Phase 06.1 / AUTH-18 / D-17 / Pitfall 13.
    - Replace hardcoded `ALLOWLIST` array with dynamic Firestore query: `const snap = await db.collection("users").get();`.
    - Strip loop per RESEARCH § 9 verbatim — gate the `update()` on `doc.get("passwordHash") !== undefined`; log `[DRY-RUN]` vs `[OK]`; count strips.
    - Final log: `\n[OK] Scan complete. ${stripped} doc(s) stripped (expected 0 per HANDOFF.md — no live client users).`
    - `--verify` mode: re-read each previously-stripped doc and assert `doc.get("passwordHash") === undefined`. Log `[VERIFY] users/${id}.passwordHash absent ✓`.
    - Preserve `main().catch((err) => { console.error("[FAIL]", err); exit(1); })` tail.

    Step 3: Author `scripts/strip-legacy-user-passwords/README.md` — copy seed README verbatim then variations per PATTERNS § lines 720-758:
    - Title: `# scripts/strip-legacy-user-passwords`.
    - § Why: cite Phase 06.1 / AUTH-18 / D-17; expected count 0 per HANDOFF.md.
    - § Prerequisites: same ADC + Cloud Datastore User role requirements as seed script.
    - § Usage: `cd functions && node ../scripts/strip-legacy-user-passwords/run.js [--dry-run] [--verify]`.
    - § Expected outcome: field removed via `FieldValue.delete()`; idempotent re-run safe; expected `0 doc(s) stripped`.
    - § When this runs: Wave 3 of Phase 06.1; after the cutover commit lands; before `/gsd-verify-work 06.1`.
    - § Audit narrative: HANDOFF.md follow-up #9 closure + SECURITY.md § Client Authentication cross-reference.

    Step 4: Author `runbooks/06.1-app-check-backoff.md` with the structure from <action> of the prior version: Symptom + Root cause (per RESEARCH § 4) + Workaround (4-step DevTools Clear-site-data) + Verification + Out of scope (HANDOFF.md #3 forward-tracking). Cite `runbooks/phase-06.1-cleanup-ledger.md F-06.1-01` for the forward-tracking row.
  </action>
  <verify>
    <automated>test -f scripts/strip-legacy-user-passwords/run.js &amp;&amp; node --check scripts/strip-legacy-user-passwords/run.js &amp;&amp; test -s scripts/strip-legacy-user-passwords/README.md &amp;&amp; test -s runbooks/06.1-app-check-backoff.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/strip-legacy-user-passwords/run.js` returns true.
    - `head -1 scripts/strip-legacy-user-passwords/run.js` shows `#!/usr/bin/env node`.
    - `grep -c "FieldValue.delete()" scripts/strip-legacy-user-passwords/run.js` returns ≥ 1.
    - `grep -c "applicationDefault()" scripts/strip-legacy-user-passwords/run.js` returns 1.
    - `grep -c "--dry-run\|--verify\|--help" scripts/strip-legacy-user-passwords/run.js` returns ≥ 3.
    - `grep -c "expected 0\|expected count = 0" scripts/strip-legacy-user-passwords/run.js scripts/strip-legacy-user-passwords/README.md` returns ≥ 1.
    - `node --check scripts/strip-legacy-user-passwords/run.js` exits 0.
    - `wc -l scripts/strip-legacy-user-passwords/README.md` returns ≥ 50.
    - `grep -c "AUTH-18\|HANDOFF" scripts/strip-legacy-user-passwords/README.md` returns ≥ 2.
    - `test -f runbooks/06.1-app-check-backoff.md` returns true.
    - `grep -c "Clear site data\|appCheck/throttled" runbooks/06.1-app-check-backoff.md` returns ≥ 2.
    - `wc -l runbooks/06.1-app-check-backoff.md` returns ≥ 25.
  </acceptance_criteria>
  <done>Strip script + README + App Check backoff runbook all authored and syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 3: DOC-10 SECURITY.md additions + REQUIREMENTS.md updates + ROADMAP.md Phase 06.1 fill-in</name>
  <files>
    SECURITY.md,
    REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - SECURITY.md (sample § Multi-Factor Authentication structure + § Phase 6 Audit Index pattern + ToC structure)
    - REQUIREMENTS.md (§ Authentication & MFA AUTH-01..15 row format + AUTH-09 supersession-annotation + Traceability table column shape)
    - .planning/ROADMAP.md (§ Phase 6 entry — analog goal + success criteria + plans format; § Phase 06.1 placeholder to replace)
    - .planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-CONTEXT.md § D-19 (3 new REQ-IDs + verbatim definitions) + § D-18 (HANDOFF #9 closure) + § D-16 (PRESERVE list)
  </read_first>
  <action>
    Step 1 — SECURITY.md additions:

    1a. Add a new top-level section `## § Client Authentication` BEFORE the existing `## § Multi-Factor Authentication`. Content (planner authors prose; structure below):

    - **Owner + Framework** line: cite Phase 06.1 (AUTH-16, AUTH-17, AUTH-18) + OWASP ASVS L2 v5.0 V2.1 + V2.2 + ISO 27001:2022 A.5.16 + A.8.5 + SOC2 CC6.1 + GDPR Art. 32(1)(b).
    - **Provisioning model** subsection: cite `inviteClient` callable at `functions/src/auth/inviteClient.ts`; input contract `{email, name, orgId, orgPassphrase, confirmReset?}`; verifies passphrase against `orgs/{orgId}.clientPassphraseHash`; calls `auth.createUser({email, password: orgPassphrase, emailVerified: true, displayName})`; sets claims `{role:"client", orgId, firstRun:true}`; emits `auth.client.invite`.
    - **Bootstrap-credential mechanism**: per-org `clientPassphraseHash` (SHA-256) is the initial Firebase Auth password; plaintext transient only (admin browser + HTTPS + callable memory); never persisted server-side beyond hash.
    - **First-run forced password change**: `firstRun: true` custom claim routes to `renderFirstRun`; chained `setClaims({uid, role, orgId})` drops `firstRun` because `SetClaimsSchema` deliberately excludes it; `setCustomUserClaims` replaces entire claim set; next ID-token refresh routes to dashboard.
    - **Cross-org refusal**: refuses if existing user has `claims.role==="client"` and `claims.orgId !== requested`; throws `failed-precondition` with `details.code: "auth/cross-org-invite-rejected"`; emits `auth.client.invite.rejected.cross-org`; AUTH-V2-02 deferred.
    - **Trust-on-invite email verification**: `emailVerified: true` set server-side at createUser; admin-vouched, not periodically re-verified; documented as substrate-honest deviation from click-to-verify; credible-not-certified bar accepts for low-privilege `role:"client"`.
    - **MFA for client users**: optional opt-in only; existing `renderMfaEnrol` + `beginTotpEnrollment` + `enrollTotp` + `unenrollAllMfa` substrate exposed via Security panel; Tier-1 email-link recovery inherits; Tier-2 admin Admin SDK un-enrol extended to clients in `runbooks/phase6-mfa-recovery-drill.md § Client users`; mandatory MFA queued for v2 hardening (forward-tracked).
    - **Org passphrase length policy**: ≥12 chars enforced in `src/auth/passphrase-policy.js validateOrgPassphrase`; load-bearing per RESEARCH § Critical Pinned Fact 1.1 (Admin SDK bypasses Identity Platform passwordPolicy).
    - **Legacy substrate deletion**: cite Phase 06.1 Wave 3 cutover commit (will fill SHA after Task 1 lands); list of deleted artefacts; `scripts/strip-legacy-user-passwords/run.js` for the `passwordHash` field strip; closes HANDOFF.md follow-up #9.

    1b. Add `## § Phase 06.1 Audit Index` section AFTER `## § Phase 6 Audit Index` and BEFORE Phase 7 audit index. 4-row citation table:

    ```markdown
    | Requirement | Control | Code Path(s) | Test / Evidence | Framework |
    |-------------|---------|--------------|-----------------|-----------|
    | AUTH-16 | Admin-initiated client invite via `inviteClient` callable; verifies org passphrase + sets {role:"client", orgId, firstRun:true} claims + emailVerified:true | `functions/src/auth/inviteClient.ts`, `functions/src/auth/invite-builder.ts`, `src/cloud/invite-admin.js`, `functions/src/util/hash.ts` | `functions/test/auth/inviteClient.unit.test.ts` (9 cases), `functions/test/auth/inviteClient.integration.test.ts` (6+ cases), `functions/test/util/hash-parity.test.ts` | ASVS L2 v5.0 V2.1.1 + V2.2.1; ISO 27001:2022 A.5.16; SOC2 CC6.1 |
    | AUTH-17 | Clients sign in via Firebase Auth Email/Password; legacy state-machine.js + setUserPassword + sign-in branch + openChangePasswordModal deleted atomically; closes HANDOFF.md follow-up #9 | `src/firebase/auth.js#signInEmailPassword` + Phase 06.1 Wave 3 cutover commit `<SHA from Task 1>` | `tests/firebase/auth-audit-emit.test.js`, atomic cutover commit in git log, `grep -r state-machine src/` returns 0 | ASVS L2 v5.0 V2.1.1 + V2.1.5; ISO 27001:2022 A.5.16 + A.8.5; SOC2 CC6.1 |
    | AUTH-18 | Admin SDK strip script removes legacy `users/{uid}.passwordHash` field | `scripts/strip-legacy-user-passwords/run.js`, `scripts/strip-legacy-user-passwords/README.md` | Operator execution log (post-cutover; expected count 0 per HANDOFF.md) | ASVS L2 v5.0 V14.3.3 (Configuration files); ISO 27001:2022 A.8.10 (Information deletion) |
    | DOC-10 | This § + § Client Authentication + Phase 06.1 Audit Index | `SECURITY.md` | Self | (cross-cutting) |
    ```

    Append a **Substrate-honest disclosure** paragraph after the table noting: App Check 22h backoff is operationally mitigated by `runbooks/06.1-app-check-backoff.md`; underlying reCAPTCHA Enterprise origin fix is HANDOFF.md #3 forward-tracked. Cross-org client membership (AUTH-V2-02) and mandatory client MFA are explicit out-of-scope items.

    1c. Update SECURITY.md ToC at top of file — add 2 anchor entries: one for `§ Client Authentication`, one for `§ Phase 06.1 Audit Index`. Match the existing anchor format (e.g. `- [§ Client Authentication](#-client-authentication)`).

    1d. Append a brief cross-reference inside § Multi-Factor Authentication's AUTH-09 supersession block: "Phase 06.1 extends opt-in MFA to client users; see § Client Authentication > MFA for client users."

    Step 2 — REQUIREMENTS.md updates:

    2a. Add 3 new rows in § Authentication & MFA section AFTER AUTH-15 row + AUTH-09 supersession block:

    ```markdown
    - [ ] **AUTH-16**: `inviteClient` admin-only callable Cloud Function exists; creates Firebase Auth users with `{role: "client", orgId, firstRun: true}` claims + `emailVerified: true`; verifies admin-provided org passphrase against `org.clientPassphraseHash` before creation; refuses cross-org invites with explicit `auth/cross-org-invite-rejected` HttpsError code. Server emits 4 audit-event types (`auth.client.invite`, `auth.client.invite.resend`, `auth.client.invite.rejected.cross-org`, `auth.client.invite.rejected.passphrase-invalid`). Closes via Phase 06.1.
    - [ ] **AUTH-17**: Client users sign in via Firebase Auth Email/Password (`signInEmailPassword` chokepoint at `src/firebase/auth.js`); legacy `src/auth/state-machine.js` + `setUserPassword` + `users/{uid}.passwordHash` field source-writers + client-side sign-in branch + `openChangePasswordModal` atomically deleted in a single Phase 06.1 cutover commit. Closes Phase 6 HANDOFF.md follow-up #9.
    - [ ] **AUTH-18**: Admin SDK one-shot script (`scripts/strip-legacy-user-passwords/run.js`) strips `passwordHash` from existing user docs in production; documented in `scripts/strip-legacy-user-passwords/README.md`; operator-paced execution per Phase 6 D-20 ADC pattern. Expected count 0 per HANDOFF.md; defensive substrate.
    ```

    2b. Add 3 rows to the Traceability table (planner samples one existing row to match column shape — typical structure: `| REQ-ID | Description | Phase | Status |`):

    ```markdown
    | AUTH-16 | inviteClient admin-only callable | Phase 06.1 | Validated 2026-05-22 |
    | AUTH-17 | Client Email/Password sign-in + legacy deletion | Phase 06.1 | Validated 2026-05-22 |
    | AUTH-18 | Admin SDK strip script | Phase 06.1 | code+docs complete; operator strip execution deferred |
    ```

    Step 3 — .planning/ROADMAP.md Phase 06.1 fill-in:

    3a. Locate the existing 10-line Phase 06.1 placeholder block:

    ```
    ### Phase 06.1: Client auth completion — Firebase Auth + inviteClient callable for client users (INSERTED)

    **Goal:** [Urgent work - to be planned]
    **Requirements**: TBD
    **Depends on:** Phase 6
    **Plans:** 0 plans

    Plans:
    - [ ] TBD (run /gsd-plan-phase 06.1 to break down)
    ```

    Replace atomically with:

    ```markdown
    ### Phase 06.1: Client auth completion — Firebase Auth + inviteClient callable for client users (INSERTED)

    **Goal**: Migrate client users from the legacy passphrase + per-user `passwordHash` substrate to real Firebase Auth identities; preserve the per-org passphrase as the admin-set bootstrap initial credential; ship an admin-only `inviteClient` Cloud Function that creates Firebase Auth users with `{role: "client", orgId, firstRun: true}` custom claims + `emailVerified: true`; reuse the existing `renderFirstRun` + `updatePassword` + `setClaims`-refresh substrate verbatim (zero new client-side first-run code); delete the legacy substrate atomically with the cutover; close Phase 6 HANDOFF.md follow-up #9.

    **Depends on**: Phase 6 (Firebase Auth + Identity Platform + custom claims + `signInEmailPassword` chokepoint + `renderFirstRun` view), Phase 7 (Cloud Functions Pattern A: App Check + Zod + idempotency + Sentry + minimal-IAM SA + audit-event substrate).

    **Requirements**: AUTH-16, AUTH-17, AUTH-18, DOC-10 (incremental).

    **Success Criteria** (what must be TRUE):
      1. Admin can invite a client via the "+ Invite client" modal — providing the client's email, name, org, and the org's current passphrase. The callable creates a Firebase Auth user with `password = orgPassphrase`, `emailVerified: true`, and custom claims `{role: "client", orgId, firstRun: true}`; emits an `auth.client.invite` audit event.
      2. Re-inviting an existing same-org client without `confirmReset` returns `{existed: true, hasFirstRun}` without mutating; with `confirmReset: true` the callable resets their password + re-flips first-run + emits `auth.client.invite.resend`.
      3. Inviting an email already bound to a DIFFERENT org claim fails with `auth/cross-org-invite-rejected` + emits `auth.client.invite.rejected.cross-org`. Cross-org client membership is deferred (AUTH-V2-02).
      4. `setOrgClientPassphrase` enforces ≥12-character length before hashing (research finding A1 — Admin SDK bypasses Identity Platform passwordPolicy); the Set passphrase modal placeholder + error message reflect the 12-char floor.
      5. The legacy substrate is atomically deleted in a single Phase 06.1 cutover commit: `src/auth/state-machine.js` + `tests/auth/state-machine.test.js` + `tests/fixtures/auth-passwords.js` + `setUserPassword` + the legacy client sign-in branch in `src/main.js` + `openChangePasswordModal` + 2 wrapper imports.
      6. `scripts/strip-legacy-user-passwords/run.js` exists + operator-runnable via ADC; expected count 0 per HANDOFF.md (defensive substrate).
      7. SECURITY.md gains § Client Authentication + § Phase 06.1 Audit Index; REQUIREMENTS.md gains AUTH-16/17/18 + Traceability entries; `runbooks/phase6-mfa-recovery-drill.md` extends Tier-2 to cover client users.

    **Plans**: 3 plans
    Plans:
    - [ ] 06.1-01-PLAN.md — Wave 1: pre-flight (App Check status checkpoint) + setOrgClientPassphrase ≥12 length floor + invite-builder.ts pure-logic helpers + inviteClient.ts skeleton + 3 new AUTH-12 error classes + 4 new audit-event enum entries (AUTH-16, AUTH-17)
    - [ ] 06.1-02-PLAN.md — Wave 2: inviteClient body + server-side hashString parity helper + barrel re-export + src/cloud/invite-admin.js wrapper + Invite modal rewire + Instructions modal copy revision + Vitest unit + firebase-functions-test integration tests (AUTH-16, AUTH-17)
    - [ ] 06.1-03-PLAN.md — Wave 3: atomic cutover commit (legacy substrate deletion) + scripts/strip-legacy-user-passwords + DOC-10 SECURITY.md additions + REQUIREMENTS.md AUTH-16/17/18 + ROADMAP.md fill-in + MFA recovery runbook extension + App Check backoff runbook + Phase 06.1 cleanup-ledger zero-out + STATE.md reconciliation + /gsd-verify-work 06.1 checkpoint (AUTH-16, AUTH-17, AUTH-18)
    **UI hint**: yes (admin's Invite modal carries a new orgPassphrase input)
    ```

    Step 4: Commit `docs(06.1): wave 3 — SECURITY.md § Client Authentication + Phase 06.1 Audit Index + REQUIREMENTS.md AUTH-16/17/18 + ROADMAP.md fill-in`.
  </action>
  <verify>
    <automated>grep -c "Client Authentication" SECURITY.md &amp;&amp; grep -c "Phase 06.1 Audit Index" SECURITY.md &amp;&amp; grep -c "AUTH-18" REQUIREMENTS.md &amp;&amp; grep -c "Validated 2026-05-22" REQUIREMENTS.md &amp;&amp; ! grep -q "Urgent work - to be planned" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## § Client Authentication" SECURITY.md` returns 1.
    - `grep -c "## § Phase 06.1 Audit Index" SECURITY.md` returns 1.
    - `grep -c "AUTH-16" SECURITY.md` returns ≥ 1 (in audit index).
    - `grep -c "AUTH-17" SECURITY.md` returns ≥ 1.
    - `grep -c "AUTH-18" SECURITY.md` returns ≥ 1.
    - `grep -c "AUTH-16\|AUTH-17\|AUTH-18" REQUIREMENTS.md` returns ≥ 6 (3 row definitions + 3 Traceability entries).
    - `grep -c "Validated 2026-05-22\|code+docs complete" REQUIREMENTS.md` returns ≥ 3 (3 Traceability status cells).
    - `grep -q "Urgent work - to be planned" .planning/ROADMAP.md` exits 1 (string absent).
    - `grep -c "AUTH-16, AUTH-17, AUTH-18" .planning/ROADMAP.md` returns ≥ 1 (Phase 06.1 Requirements line).
    - `grep -c "06.1-01-PLAN.md\|06.1-02-PLAN.md\|06.1-03-PLAN.md" .planning/ROADMAP.md` returns ≥ 3.
    - Pitfall 19 substrate-honest check: `grep -E "(?<!non-)(?<!not\\s)(compliant|certified)" SECURITY.md` per the negation-tolerant pattern Phase 11 uses → 0 violations outside code spans (preserves "credible, not certified" canonical phrasing). Note: this check should be run via the same regex pattern Phase 11 tests use; if a separate doc-shape test exists at `tests/security-md-*-shape.test.js`, run it.
  </acceptance_criteria>
  <done>SECURITY.md + REQUIREMENTS.md + ROADMAP.md all updated; Pitfall 19 forbidden-words clean.</done>
</task>

<task type="auto">
  <name>Task 4: MFA recovery runbook extension + Phase 06.1 cleanup-ledger zero-out + STATE.md reconciliation</name>
  <files>
    runbooks/phase6-mfa-recovery-drill.md,
    runbooks/phase-06.1-cleanup-ledger.md,
    .planning/STATE.md
  </files>
  <read_first>
    - runbooks/phase6-mfa-recovery-drill.md (full file — sample Tier-2 admin un-enrol structure)
    - runbooks/phase-11-cleanup-ledger.md (analog cleanup-ledger structure with zero-out frontmatter gate)
    - .planning/STATE.md (current shape — frontmatter + narrative pattern)
    - .planning/HANDOFF.md (follow-up #9 — closure record-keeping)
    - .planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-CONTEXT.md § Deferred Ideas (forward-tracking row sources)
  </read_first>
  <action>
    Step 1: Append `## § Client users` subsection to `runbooks/phase6-mfa-recovery-drill.md`. Place after the existing Tier-2 admin un-enrol section. Content sketch:

    ```markdown
    ### § Client users (Phase 06.1 extension)

    **Scope:** A client user has MFA enrolled (opt-in per Phase 06.1 D-10) and has lost their TOTP device PLUS their email-link recovery substrate.

    **Tier-1 (user-side, expected path):** Same as internal admins — `sendSignInLinkToEmail` to verified email → re-authenticate via link → user un-enrols + re-enrols TOTP.

    **Tier-2 (operator-side, fallback for catastrophic loss):**
    1. Out-of-band identity verification (voice/video call with admin who originally invited the client; verify client's name + org context).
    2. Operator runs Admin SDK `auth.updateUser(uid, { multiFactor: { enrolledFactors: [] } })` (or `firebase auth:multifactor:unenroll`) to clear all enrolled factors.
    3. Operator re-invokes `inviteClient(email, name, orgId, orgPassphrase, confirmReset: true)` via an in-app admin action — this resets the client's password back to the current org passphrase + re-flips `firstRun: true` + emits `auth.client.invite.resend`.
    4. Client signs in with the current org passphrase + sets a new personal password via `renderFirstRun`. They can then opt-in to MFA again via the Security panel.

    **Audit trail:** `auth.mfa.unenrol` (admin-initiated) + `auth.client.invite.resend` (admin-initiated) + `auth.signin.success` (client) + `auth.password.change` (client) — 4 events captured in `auditLog/`.

    **Drill cadence:** Combined with the internal-admin AUTH-10 drill; the next paired-review session exercises both internal + client recovery paths.
    ```

    Step 2: Author `runbooks/phase-06.1-cleanup-ledger.md`. Frontmatter + body structure (mirrors `runbooks/phase-11-cleanup-ledger.md`):

    ```markdown
    ---
    phase: 06.1
    phase_06.1_active_rows: 0
    closed_at: 2026-05-22
    ---

    # Phase 06.1 Cleanup Ledger

    **Authored:** 2026-05-22 (Wave 3 close).
    **Status:** zero-out gate active. Phase 06.1 closes HANDOFF.md follow-up #9 (Phase 6 verification gap); no in-phase active rows remain.

    ## Closed rows (in-phase)

    | Row | Closure event | Commit SHA |
    |-----|---------------|------------|
    | C-06.1-01 | Wave 1 ≥12-char passphrase floor enforced at `setOrgClientPassphrase` chokepoint | (Wave 1 commit SHA) |
    | C-06.1-02 | Wave 1 inviteClient skeleton + 4 new audit-event enum entries + 3 AUTH-12 error classes | (Wave 1 commit SHA) |
    | C-06.1-03 | Wave 2 inviteClient body + server-side hash parity helper + barrel export | (Wave 2 commit SHA) |
    | C-06.1-04 | Wave 2 src/cloud/invite-admin.js wrapper + 3 typed-error mapping | (Wave 2 commit SHA) |
    | C-06.1-05 | Wave 2 Invite modal rewire + Instructions modal copy revision + integration test | (Wave 2 commit SHA) |
    | C-06.1-06 | Wave 3 atomic cutover commit (legacy substrate deletion); closes HANDOFF #9 | (Wave 3 Task 1 commit SHA) |
    | C-06.1-07 | Wave 3 scripts/strip-legacy-user-passwords/run.js + README + App Check backoff runbook | (Wave 3 Task 2 commit SHA) |
    | C-06.1-08 | Wave 3 SECURITY.md § Client Authentication + § Phase 06.1 Audit Index + REQUIREMENTS.md + ROADMAP.md | (Wave 3 Task 3 commit SHA) |

    ## Cross-phase closures

    - **Phase 6 HANDOFF.md follow-up #9** — closed by C-06.1-06 (state-machine.js deletion landed atomically).
    - **Phase 6 ROADMAP `[ ]` cell for AUTH-14 verification** — overstated completeness in Phase 6/7; Phase 06.1 actually executes the substrate the Phase 6 D-04 audit-narrative claimed.

    ## Carry-forward operator-deferred rows

    | Row | Owner | Trigger |
    |-----|-------|---------|
    | C-06.1-09 (operator) | Operator strip-script execution | `node scripts/strip-legacy-user-passwords/run.js --dry-run` → review → real run via `/gsd-verify-work 06.1` checkpoint (Task 5). Expected count 0 per HANDOFF.md. |

    ## Forward-tracking rows (queued for future phases)

    | Row | Owner | Trigger / Phase |
    |-----|-------|-----------------|
    | F-06.1-01 | reCAPTCHA Enterprise origin config fix (HANDOFF.md follow-up #3) — underlying App Check 22h backoff root cause | Phase 7 substrate / operator session whenever backoff next bites |
    | F-06.1-02 | v2 client-MFA-hardening — promote opt-in MFA to mandatory for `role:"client"`; deferred per CONTEXT.md Deferred Ideas | v2 milestone |
    | F-06.1-03 | `setOrgClientPassphrase` migration from full-org-write to callable Cloud Function | HANDOFF.md follow-up #1 broader cleanup phase |
    | F-06.1-04 | Server-side validation of `orgPassphrase` length INSIDE the `inviteClient` callable (defense-in-depth on top of Wave 1's setOrgClientPassphrase chokepoint) | Concurrent with F-06.1-03 |
    | F-06.1-05 | Cross-org client membership (AUTH-V2-02) — SCIM provisioning + shared-email support | v2 milestone |
    | F-06.1-06 | SSO/OIDC for enterprise client orgs (AUTH-V2-01) | v2 milestone |
    | F-06.1-07 | Hardware-key (YubiKey) second factor for clients + internal | v2 milestone |
    | F-06.1-08 | Transactional email replacing `mailto:` invite UX (NOTIF-V2-01) | v2 milestone |
    | F-06.1-09 | Anomaly rule 5 — invite-rejected burst detection (Phase 9 OBS-05 extension; currently no rule paged on 4 new audit-event types per RESEARCH § 5) | Future hardening phase |

    ## Verification gate

    `phase_06.1_active_rows: 0` in frontmatter is the zero-out invariant. Cleanup-ledger close == phase close.
    ```

    Step 3: Reconcile `.planning/STATE.md`:
    - Update `last_updated` to current ISO timestamp.
    - Bump `total_phases` if needed (Phase 06.1 is an inserted decimal — verify whether STATE counts it).
    - Add a new entry to the leading narrative paragraph documenting Phase 06.1 close: cite the 3 PLAN files + the closure of HANDOFF.md follow-up #9 + the AUTH-16/17/18 landing.
    - Move the previous "Prior context (preserved)" entry from `Phase 12 Plan 12-03 COMPLETE` to be the second narrative entry; Phase 06.1 narrative goes first.

    Step 4: Commit `docs(06.1): wave 3 — MFA runbook client-user extension + cleanup-ledger zero-out + STATE.md reconciliation`.
  </action>
  <verify>
    <automated>grep -c "Client users\|phase 06.1 extension" runbooks/phase6-mfa-recovery-drill.md &amp;&amp; grep -c "phase_06.1_active_rows: 0" runbooks/phase-06.1-cleanup-ledger.md &amp;&amp; grep -c "HANDOFF.md follow-up #9\|HANDOFF #9" runbooks/phase-06.1-cleanup-ledger.md &amp;&amp; grep -c "Phase 06.1" .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Client users\|Phase 06.1 extension" runbooks/phase6-mfa-recovery-drill.md` returns ≥ 1.
    - `grep -c "inviteClient.*confirmReset: true" runbooks/phase6-mfa-recovery-drill.md` returns ≥ 1.
    - `test -f runbooks/phase-06.1-cleanup-ledger.md` returns true.
    - `grep -c "phase_06.1_active_rows: 0" runbooks/phase-06.1-cleanup-ledger.md` returns 1.
    - `grep -c "HANDOFF.md follow-up #9\|HANDOFF #9" runbooks/phase-06.1-cleanup-ledger.md` returns ≥ 1.
    - `grep -c "F-06.1-" runbooks/phase-06.1-cleanup-ledger.md` returns ≥ 9 (9 forward-tracking rows queued).
    - `grep -c "Phase 06.1" .planning/STATE.md` returns ≥ 1 (narrative entry added).
    - `grep -c "follow-up #9\|HANDOFF #9" .planning/STATE.md` returns ≥ 1 (closure documented).
  </acceptance_criteria>
  <done>MFA runbook extended; cleanup-ledger zero-out gate set; STATE.md reflects Phase 06.1 close.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: /gsd-verify-work 06.1 operator checkpoint — strip-script execution + end-to-end invite UAT</name>
  <what-built>All Wave 1-3 substrate landed in code + tests + docs. The autonomous portion of Phase 06.1 is complete. Final operator session: (a) run the strip script against production (expected count 0); (b) end-to-end invite a UAT client via the new modal; (c) verify the client can sign in via Firebase Auth + complete renderFirstRun + land on dashboard; (d) check audit-log rows landed for at least one invite outcome.</what-built>
  <how-to-verify>
    **Pre-checks (autonomous portion already done; verify):**
    1. `git log --oneline -10` shows the 4+ Phase 06.1 commits (Wave 1 RED + GREEN, Wave 2 commits, Wave 3 cutover + scripts + docs + ledger).
    2. `cat runbooks/phase-06.1-cleanup-ledger.md | grep "phase_06.1_active_rows: 0"` returns 1.
    3. `grep -c "AUTH-16\|AUTH-17\|AUTH-18" REQUIREMENTS.md` returns ≥ 6.
    4. `grep -q "Urgent work - to be planned" .planning/ROADMAP.md` exits 1 (string absent).
    5. `grep -c "Phase 06.1" SECURITY.md` returns ≥ 2 (§ Client Authentication + Audit Index).

    **Operator actions:**

    6. `gcloud auth application-default login` (with an account holding Cloud Datastore User role on bedeveloped-base-layers).
    7. `cd functions && node ../scripts/strip-legacy-user-passwords/run.js --dry-run`. Confirm output reports `Scan complete. 0 doc(s) stripped (expected 0 per HANDOFF.md — no live client users)` OR documents the unexpected count.
    8. If dry-run shows 0: optionally re-run without `--dry-run` to confirm idempotent behaviour (still 0). If dry-run shows 1+ doc with `passwordHash`: pause, investigate (which user has it? when was it set? HANDOFF.md says "no live client users"); document the deviation; THEN run without `--dry-run` if appropriate.

    **End-to-end invite UAT (per docs/UAT.md Step 5 scheme):**

    9. Operator (logged in as admin via the new Firebase Auth path) opens the admin panel.
    10. Verify "+ Invite client" modal shows the new orgPassphrase input field.
    11. Test 1 — Happy invite: type a UAT email (e.g., hugh+uat-alpha@assume-ai.com), name, choose an org, type the org's current passphrase (≥12 chars). Click Create account. Modal opens openInviteInstructionsModal with revised copy (no "share separately" wording).
    12. Test 2 — Bad passphrase: re-invite a fresh email but mistype the passphrase. Modal shows "Company passphrase incorrect — check it or update via Set Passphrase." (the PassphraseInvalidError message).
    13. Test 3 — Passphrase not set: in a fresh org with no clientPassphraseHash, attempt to invite — Modal shows "Set the company passphrase first via Settings."
    14. Test 4 — Client first-run: in a clean browser/incognito, navigate to baselayers.bedeveloped.com, sign in with the UAT email + the org passphrase. Verify routing to renderFirstRun, set a new personal password, confirm landing on dashboard.
    15. Test 5 — Audit log: in Firebase Console → Firestore → `auditLog/` collection, confirm there is at least one row of type `auth.client.invite` (for the happy path) AND one row of `auth.client.invite.rejected.passphrase-invalid` (for Test 2).

    **Sign-off documentation:**

    16. Operator pastes Test 1-5 outcomes into a new `runbooks/06.1-uat-evidence.md` (timestamps + observed outputs + any deviations).
    17. Operator flips REQUIREMENTS.md AUTH-16/17/18 from `[ ]` to `[x]` and updates the Traceability table status to `Validated <date>`.
    18. Operator updates the cleanup-ledger C-06.1-09 row to closed + commit SHA.
  </how-to-verify>
  <acceptance_criteria>
    - Strip-script execution reports `Scan complete. 0 doc(s) stripped` OR documents deviation.
    - Operator confirms Test 1-5 pass (or documents deviations in `runbooks/06.1-uat-evidence.md`).
    - REQUIREMENTS.md AUTH-16/17/18 flipped to `[x]` after operator validation.
    - `runbooks/06.1-uat-evidence.md` exists with timestamps + commands + observed outcomes.
    - Cleanup-ledger C-06.1-09 row closed with commit SHA.
  </acceptance_criteria>
  <resume-signal>Type "approved — 06.1 verified" or describe deviations.</resume-signal>
  <done>Operator-approved end-to-end UAT; AUTH-16/17/18 validated; Phase 06.1 ready to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cutover commit → git tree | Single atomic deletion event; no dead-code window. |
| Operator gcloud ADC → Firestore Admin SDK | Strip script bypasses rules; only the operator's ADC identity authorises. |
| SECURITY.md / REQUIREMENTS.md → audit reviewer | Citation chain must verify on inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.1-02 | Repudiation | Audit-narrative integrity for the cutover event | mitigate | Single atomic commit with `closes HANDOFF #9` in the message + BREAKING CHANGE marker; git log is the audit trail. SECURITY.md § Phase 06.1 Audit Index cites the SHA. |
| T-06.1-04 | Tampering | Stale documentation references after cutover | mitigate | Task 3 acceptance criteria include Pitfall 19 substrate-honest forbidden-words check (no "compliant" / "certified" outside code spans, preserving canonical "credible, not certified" phrasing). Task 1 acceptance criteria include `grep -r state-machine src/ tests/` returning 0 hits. Cross-document drift gates per Phase 11 pattern. |
| T-06.1-07 | Information disclosure | Strip script with overly-privileged ADC scope | accept | Same risk class as `scripts/seed-internal-allowlist/run.js` (Phase 6 D-20); ADC is the project-approved Pitfall 13 mitigation; operator-paced execution gates blast radius. README documents required IAM role (Cloud Datastore User). |
</threat_model>

<verification>
Wave 3 verification gates:
1. After Task 1: full Vitest suite (root + functions) green; lint clean; build clean; grep gates per Task 1 acceptance criteria.
2. After Task 2: strip script syntactically valid (`node --check`); runbooks ≥ minimum line counts.
3. After Task 3: SECURITY.md + REQUIREMENTS.md + ROADMAP.md updates landed; Pitfall 19 forbidden-words clean; ROADMAP placeholder eliminated.
4. After Task 4: cleanup-ledger zero-out gate set; STATE.md reflects Phase 06.1 close.
5. After Task 5 (operator): strip-script execution evidence + end-to-end UAT evidence + REQUIREMENTS.md `[x]` flips.
</verification>

<success_criteria>
- Atomic cutover commit landed; HANDOFF #9 closed at source level; all tests + build + lint green.
- Strip script + README + App Check backoff runbook authored.
- SECURITY.md gains § Client Authentication + § Phase 06.1 Audit Index sections; ToC updated.
- REQUIREMENTS.md gains AUTH-16/17/18 row definitions + Traceability table entries.
- .planning/ROADMAP.md § Phase 06.1 placeholder replaced with full goal + 7 SC + 3 plans.
- runbooks/phase6-mfa-recovery-drill.md extends Tier-2 procedure for client users.
- runbooks/phase-06.1-cleanup-ledger.md has `phase_06.1_active_rows: 0` zero-out gate + 9 forward-tracking rows queued.
- .planning/STATE.md narrative reflects Phase 06.1 close + HANDOFF #9 closure.
- Operator checkpoint (Task 5) gives final UAT sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-client-auth-completion-firebase-auth-inviteclient-callable-f/06.1-03-SUMMARY.md` per the standard template (artifacts created/modified, decisions taken, deviations, verification status, what unblocks).
</output>
