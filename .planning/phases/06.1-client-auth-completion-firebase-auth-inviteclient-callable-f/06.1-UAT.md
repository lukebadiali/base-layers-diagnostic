---
status: testing
phase: 06.1-client-auth-completion-firebase-auth-inviteclient-callable-f
source:
  - 06.1-01-SUMMARY.md
  - 06.1-02-SUMMARY.md
  - 06.1-03-SUMMARY.md
started: 2026-05-25T00:00:00Z
updated: 2026-05-25T00:00:00Z
preflight:
  typecheck: clean
  lint: clean
  legacy_substrate_grep: zero
  security_md_doc10: present
  requirements_md_auth_16_17_18: present
  roadmap_placeholder: removed
  cleanup_ledger_zero_out: phase_06.1_active_rows=0
  strip_script_files: present
  app_check_backoff_runbook: filled
  mfa_recovery_client_extension: present
  cr_01_fix_landed: existing-privileged-user (3 hits)
---

## Current Test

number: 4
name: Happy-Path Invite — Fresh Client Email
expected: |
  In Invite Client modal (org alpha): enter hugh+uat061-fresh@assume-ai.com,
  enter the org's current passphrase, enter name, submit. Success state
  surfaces (toast or inline confirmation), modal closes. The invited email
  now exists in Firebase Auth with role=client + orgId=alpha claims.
awaiting: user response

## Tests

### 1. Cold-Start Smoke — App Loads
expected: |
  Run `npm run dev` (requires VITE_RECAPTCHA_ENTERPRISE_SITE_KEY in .env).
  Dev server boots cleanly. Opening the served URL renders the diagnostic
  homepage without console errors. Auth state resolves.
result: pass
note: |
  Dev server booted on http://localhost:5178/. User navigated to sign-in
  surface and observed the copy. One in-flight tweak requested + landed
  (internal-only copy neutralized for clients post-06.1 — commit 5ab47cd).
  No console errors reported.

### 2. Org Passphrase Length Floor (≥12 chars)
expected: |
  Sign in as admin → Settings → Set Org Passphrase. Try typing an 11-char
  passphrase and submit. Submit is blocked or shows
  "Passphrase must be at least 12 characters." Submit a ≥12-char passphrase —
  it accepts and stores.
result: pass

### 3. Invite Client Modal — Password Input Row Present
expected: |
  Open the Invite Client modal (admin-side, in an org with passphrase set).
  Modal shows fields in this order: Email → Password (type=password,
  placeholder mentions the company passphrase) → Organisation → Name.
result: pass

### 4. Happy-Path Invite — Fresh Client Email
expected: |
  In Invite Client modal (org alpha): enter hugh+uat061-fresh@assume-ai.com,
  enter org alpha's current passphrase, enter name, submit. Success state
  surfaces (toast or inline confirmation), modal closes. The invited email
  now exists in Firebase Auth with role=client + orgId=alpha claims.
result: issue
reported: |
  Submit shows "Something went wrong — try again" toast with no console
  errors. Network tab shows POST /inviteClient → 200 OK with
  content-type: text/html (SPA fallback index.html, not a callable response).
severity: blocker
root_cause: |
  firebase.json rewrites at lines 42–84 cover every other callable
  (setClaims / auditWrite / getDocumentSignedUrl / softDelete /
  restoreSoftDeleted / permanentlyDeleteSoftDeleted / gdprExportUser /
  gdprEraseUser / checkRateLimit) but DID NOT include /inviteClient.
  Phase 06.1 Wave 2 added the function + client wrapper but never wired
  the Hosting same-origin rewrite. The catch-all `** → /index.html` is
  what serves the SPA HTML instead. Unit + integration tests bypass the
  rewrite layer (direct function invocation / mocked wrapper), so this
  surfaced only at UAT.
artifacts:
  - path: "firebase.json"
    issue: "rewrites array missing /inviteClient entry"
missing:
  - 'Add { "source": "/inviteClient", "function": { "functionId": "inviteClient", "region": "europe-west2" } } to firebase.json rewrites before the catch-all'

### 5. Existing-Client Resend — Confirmation Modal
expected: |
  Re-invite the SAME client email from Test 4. Instead of success, a
  confirmation modal appears asking "User already exists — reset password
  and re-invite?" with a "Yes, reset" / "Cancel" choice. Clicking "Yes,
  reset" completes the resend; the existing user's password is updated to
  the current org passphrase.
result: [pending]

### 6. Cross-Org Refusal — Typed Error
expected: |
  Invite a client email that already belongs to a DIFFERENT org. Submit
  surfaces a clear inline error ("User already belongs to a different org"
  or similar), NOT a raw Firebase error code. No password reset occurs.
result: [pending]

### 7. Wrong-Passphrase Refusal — Typed Error
expected: |
  In Invite Client modal: enter a correct email but the WRONG company
  passphrase. Submit surfaces a clear inline error ("Passphrase incorrect"
  or similar). No user is created. No password reset occurs.
result: [pending]

### 8. Privileged-User Refusal (CR-01 Fix) — Typed Error
expected: |
  As an internal-role caller (or admin), invite an email address that
  already belongs to an existing admin or internal user. Submit surfaces a
  clear typed error ("Email belongs to an existing privileged user" or
  similar). The privileged user's password is NOT reset; their role/orgId
  claims are NOT modified.
result: [pending]

### 9. Legacy UI Absent
expected: |
  Sign-in screen has NO Sign-in/Sign-up tab UI (state.authTab is gone).
  User-menu dropdown has NO "Change password" entry. Admin Clients table
  has NO passwordHash column / cell.
result: [pending]

### 10. Instructions Modal Copy Revised
expected: |
  Open the Instructions modal that admins share with clients before invite.
  Email body does NOT contain "share separately" wording. Descriptive
  paragraph mentions sharing the passphrase "via your usual secure channel"
  (not "separately"). Amber-banner mentions the 12-character minimum and
  points to Settings → Set passphrase.
result: [pending]

### 11. Strip Script `--dry-run` Reports 0
expected: |
  Run `node scripts/strip-legacy-user-passwords/run.js --dry-run` with
  gcloud ADC configured for the project. Output reports 0 docs with
  residual `passwordHash` field on `users/{uid}` (matches HANDOFF.md
  expected count). Operator-paced — skip if gcloud ADC not configured.
result: [pending]

## Summary

total: 11
passed: 3
issues: 1
pending: 7
skipped: 0
blocked: 0

## Gaps

- truth: "Invite Client modal submits via inviteClient callable and reaches the Cloud Function over the Hosting same-origin rewrite"
  status: failed
  reason: "User reported: 'Something went wrong — try again' toast with no console errors. Network tab: POST /inviteClient → 200 OK content-type text/html (SPA fallback)."
  severity: blocker
  test: 4
  root_cause: "firebase.json rewrites missing /inviteClient entry. Phase 06.1 Wave 2 added function + wrapper but never wired the Hosting same-origin rewrite."
  artifacts:
    - path: "firebase.json"
      issue: "rewrites array missing /inviteClient entry"
  missing:
    - 'Add { "source": "/inviteClient", "function": { "functionId": "inviteClient", "region": "europe-west2" } } to firebase.json rewrites before the catch-all'
  fix: "Patched in this same conversation — firebase.json:83-86. Needs hosting redeploy + functions deploy to take effect."
