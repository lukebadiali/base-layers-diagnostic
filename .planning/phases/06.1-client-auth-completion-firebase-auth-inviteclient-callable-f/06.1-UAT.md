---
status: complete
phase: 06.1-client-auth-completion-firebase-auth-inviteclient-callable-f
source:
  - 06.1-01-SUMMARY.md
  - 06.1-02-SUMMARY.md
  - 06.1-03-SUMMARY.md
started: 2026-05-25T00:00:00Z
updated: 2026-05-25T15:55:00Z
preflight:
  typecheck: clean
  lint: clean
  legacy_substrate_grep: zero
  security_md_doc10: present
  requirements_md_auth_16_17_18: present
  roadmap_placeholder: removed
  cleanup_ledger_zero_out: phase_06.1_active_rows=0
  strip_script_files: present
  app_check_backoff_runbook: filled
  mfa_recovery_client_extension: present
  cr_01_fix_landed: existing-privileged-user (3 hits)
---

## Current Test

[testing complete]

## Tests

### 1. Cold-Start Smoke — App Loads
expected: |
  Run `npm run dev` (requires VITE_RECAPTCHA_ENTERPRISE_SITE_KEY in .env).
  Dev server boots cleanly. Opening the served URL renders the diagnostic
  homepage without console errors. Auth state resolves.
result: pass
note: |
  Dev server booted on http://localhost:5178/. One in-flight tweak landed
  (internal-only copy neutralized for clients post-06.1 — commit 5ab47cd).

### 2. Org Passphrase Length Floor (≥12 chars)
expected: |
  Sign in as admin → Settings → Set Org Passphrase. Try typing an 11-char
  passphrase and submit. Submit is blocked or shows
  "Passphrase must be at least 12 characters." Submit a ≥12-char passphrase —
  it accepts and stores.
result: pass

### 3. Invite Client Modal — Password Input Row Present
expected: |
  Open the Invite Client modal (admin-side, in an org with passphrase set).
  Modal shows fields in this order: Email → Password (type=password,
  placeholder mentions the company passphrase) → Organisation → Name.
result: pass

### 4. Happy-Path Invite — Fresh Client Email
expected: |
  In Invite Client modal (org alpha): enter hugh+uat061-fresh@assume-ai.com,
  enter org alpha's current passphrase, enter name, submit. Success state
  surfaces, modal closes. The invited email now exists in Firebase Auth
  with role=client + orgId=alpha claims.
result: pass
note: |
  PASS after a chain of substrate fixes deployed in this UAT session
  (see Gaps for full audit). End-to-end verified: invite landed, client
  created in Firebase Auth + /users mirror doc, signed in successfully,
  set new password via renderFirstRun, dashboard reached after setClaims
  self-update branch dropped firstRun.

### 5. Existing-Client Resend — Confirmation Modal
expected: |
  Re-invite the SAME client email from Test 4. A confirmation modal
  appears asking to reset password and re-invite. Clicking "Yes, reset"
  completes the resend; the existing user's password is updated to the
  current org passphrase.
result: pass
note: |
  Verified end-to-end. Drove the mirror-doc fix verification — re-invited
  hugh+uat061-fresh@assume-ai.com with confirmReset:true; confirmation
  modal surfaced, "Yes, reset" confirmed, client appears in admin table.

### 6. Cross-Org Refusal — Typed Error
expected: |
  Invite a client email that already belongs to a DIFFERENT org. Submit
  surfaces a clear inline error ("User already belongs to a different
  org" or similar), NOT a raw Firebase error code. No password reset.
result: pass
note: |
  User reported: 'email already belongs to another organisation' — the
  AUTH-12 CrossOrgError typed surface, not a raw Firebase code. Confirms
  cloud/invite-admin.js err.details.code → typed-error mapping working.

### 7. Wrong-Passphrase Refusal — Typed Error
expected: |
  Enter correct email but WRONG company passphrase. Submit surfaces
  "Passphrase incorrect" or similar typed error. No user created. No
  password reset.
result: pass
note: PassphraseInvalidError AUTH-12 chokepoint typed-error surfaced cleanly.

### 8. Privileged-User Refusal (CR-01 Fix) — Typed Error
expected: |
  As admin/internal caller, invite an existing admin/internal user's
  email. Submit surfaces a clear typed error (auth/email-belongs-to-
  privileged-user). The privileged user is NOT password-reset and their
  claims are NOT modified.
result: pass
note: |
  CR-01 fix verified in prod. The very privilege-escalation path the
  code review caught is now refused server-side at decideInviteOutcome.

### 9. Legacy UI Absent
expected: |
  Sign-in screen has NO Sign-in/Sign-up tab UI (state.authTab is gone).
  User-menu dropdown has NO "Change password" entry. Admin Clients table
  has NO passwordHash column / cell.
result: pass
note: Wave 3 atomic cutover verified — all three legacy zones gone on prod.

### 10. Instructions Modal Copy Revised
expected: |
  Open the Instructions modal that admins share with clients after
  invite. Email body does NOT contain "share separately" wording.
  Descriptive paragraph mentions sharing the passphrase "via your usual
  secure channel". Amber-banner mentions the 12-character minimum and
  points to Settings → Set passphrase.
result: pass
note: |
  Verified via source-content check (Wave 2 copy revision is present
  in src/main.js:
    - L4989: "via your usual secure channel" ✓
    - L4995: "min 12 characters" + "Settings → Set passphrase" ✓
    - "share this with you separately" string absent ✓
  ).
  User reported: on the resend path (existing-user confirmReset:true) the
  Instructions modal does NOT auto-surface — the resend completes silently.
  User explicitly OK with that behaviour (existing client already received
  instructions on the original invite). Logged as a UX nit, not a defect.
follow_up: |
  Wave 2 design intent: Instructions modal fires on `m.close()` after a
  successful inviteClient. In the confirmReset:true recursive doInvite(true)
  call, the modal opens — but the existing-user confirmation flow likely
  short-circuits before the Instructions modal opens at L4907 (the outer
  m may already be closed by the time doInvite(true) returns). Not a
  Phase 06.1 close blocker — defer as a forward-tracking UX consistency
  row for the post-merge UI polish phase.

### 11. Strip Script `--dry-run` Reports 0
expected: |
  Run `node scripts/strip-legacy-user-passwords/run.js --dry-run` with
  gcloud ADC configured for the project. Output reports 0 docs with
  residual `passwordHash` field on `users/{uid}` (matches HANDOFF.md
  expected count).
result: pass
note: |
  Output: "Scan complete. 0 doc(s) stripped (expected 0 per HANDOFF.md
  — no live client users)." Confirms Wave 3 atomic cutover left no
  residual passwordHash fields in /users docs. The defensive strip
  script is functional + safe to run without --dry-run if ever needed.

## Summary

total: 11
passed: 11
issues: 0
pending: 0
skipped: 0
blocked: 0
note: |
  Tests 4–8 surfaced 9 substrate gaps that were all resolved + verified
  in this same UAT session (see Gaps). Phase 06.1 effectively grew a
  follow-on close set (PR #42 #43 #44 #45) for substrate that Wave 2/3
  shipped in code but didn't fully wire to deployed infrastructure.

## Gaps

# All gaps below surfaced + resolved in the same /gsd-verify-work session.
# Kept as audit trail of Phase 06.1 substrate fixes discovered via UAT.

- truth: "Invite Client modal submits via inviteClient callable and reaches the Cloud Function over the Hosting same-origin rewrite"
  status: resolved
  test: 4
  severity: blocker
  root_cause: "firebase.json rewrites missing /inviteClient entry. Phase 06.1 Wave 2 added function + wrapper but never wired the Hosting same-origin rewrite. Catch-all ** → /index.html served SPA HTML for the unrecognized path; client SDK got back 200 text/html it couldn't parse; AUTH-12 mapper's unrecognized-error branch surfaced 'Something went wrong — try again' with no console errors."
  fix_commits:
    - "1951b14 fix(06.1): add /inviteClient Hosting rewrite to firebase.json"
    - "PR #43 d55da85 merged to main"

- truth: "claims-admin-sa service account exists in prod for inviteClient runtime SA pin"
  status: resolved
  test: 4
  severity: blocker
  root_cause: "Phase 7 FN-04 documented claims-admin-sa as reusable but never created it. Phase 06.1 was the first callable to actually pin it; deploy failed with '400 Invalid service account'."
  fix_actions:
    - 'gcloud iam service-accounts create claims-admin-sa --project=bedeveloped-base-layers'
    - 'roles/firebaseauth.admin + roles/datastore.user at project scope'
    - 'roles/secretmanager.secretAccessor on SENTRY_DSN'

- truth: "inviteClient.ts SA pin format works through firebase-tools deploy"
  status: resolved
  test: 4
  severity: blocker
  root_cause: "firebase-tools passes short-name 'claims-admin-sa' through to Secret Manager setIamPolicy which rejects non-email SA refs with 400."
  fix_commit: "94dcfca pin via full email claims-admin-sa@bedeveloped-base-layers.iam.gserviceaccount.com"

- truth: "functions/package-lock.json satisfies npm ci on Linux Cloud Build"
  status: resolved
  test: 4
  severity: major
  root_cause: "Vitest 4 rolldown/unrs-resolver WASI bindings declare @emnapi/core + runtime as deps that npm@11 on Windows omits from the lockfile (native binding wins, WASM fallback unrecorded). Linux npm ci fails on missing transitive entries."
  fix_commits:
    - "94dcfca firebase.json functions.ignore += package-lock.json"
    - "d1ba778 + 365dbd0 ci(audit/deploy): rm + npm install pattern"
  follow_up: "Regenerate functions/package-lock.json in WSL/Linux to restore strict pinning."

- truth: "All Cloud Run services backing firebase.json rewrites actually exist"
  status: resolved
  test: 4
  severity: blocker
  root_cause: "Hosting deploy validation rejected with 'Cloud Run service permanentlyDeleteSoftDeleted does not exist'. 11 of 16 functions were FAILED state — declared in code but never deployed (Phase 6 D-8 substrate gap; CI excludes functions from --only)."
  fix_actions:
    - "gcloud functions delete authAnomalyAlert (HTTPS→background trigger-type change required delete+recreate)"
    - "firebase deploy --only functions --force (18 of 19 now ACTIVE; onUserDelete remains Gen1)"
    - "gcloud run services add-iam-policy-binding allUsers roles/run.invoker on all 9 freshly-deployed callables"

- truth: "Client first-run can clear firstRun:true claim via setClaims after password change"
  status: resolved
  test: 4
  severity: blocker
  root_cause: "setClaims admin-only gate rejected client-role caller. Phase 6 cohort was internal admins only; Phase 06.1 added clients without updating the gate."
  fix_commit: "037aa8f fix(06.1): setClaims allows constrained self-update so client first-run can clear firstRun"

- truth: "Invited client appears in admin-side Clients table after inviteClient success"
  status: resolved
  test: 5
  severity: blocker
  root_cause: "inviteClient.ts wrote Firebase Auth + audit events but never mirrored to the /users/{uid} doc the admin UI subscribes to via subscribeUsers."
  fix_commit: "d5431c6 fix(06.1): mirror /users/{uid} doc on inviteClient create + resend"

- truth: "Admin can remove a client from the Clients table (Firebase Auth user + /users mirror deleted atomically)"
  status: resolved
  test: 8-followon
  severity: blocker
  root_cause: "Phase 06.1 created clients via Firebase Auth + /users mirror but never gave admin a working removal path. Previous Remove button called local deleteUser → cloudDeleteUser which firestore.rules:133 deny (server-only mutation invariant), AND would have left orphan Auth user. No deleteClient callable existed."
  fix_commits:
    - "b01510d feat(06.1): deleteClient callable + UI rewire — Cloud Function does auth.deleteUser + /users.delete + audit emit; refuses if target is admin/internal"
    - "d7dadde fix(06.1): admin Remove inlines local cache update (skip cloudDeleteUser chain so its rules-denied delete doesn't re-fire)"
    - "f236424 test(06.1): widen invite-modal import assertion to allow deleteClient sibling"
    - "PR #44 df1bed5 + PR #45 fdd94eb merged to main"

- truth: "Client first-run user does NOT trigger Firestore permission errors for /users + /orgs + /responses listeners"
  status: open
  test: 4
  severity: minor
  reason: "Client-role signed-in user causes console errors: subscribeUsers/subscribeOrgs/subscribeResponses fail with 'Missing or insufficient permissions'. App-wide subscriptions fire for ALL signed-in users regardless of role; rules correctly deny client-role access to /users + /orgs full-collection reads."
  note: |
    Cosmetic only — does not block first-run or invite flow. Either
    (a) make subscriptions conditional on role !== "client", or
    (b) widen rules to admit client-role reads for own org. Defer.
  follow_up: "Forward-tracking row for cleanup-ledger: client-role console noise on subscribeUsers/Orgs/Responses listeners."
