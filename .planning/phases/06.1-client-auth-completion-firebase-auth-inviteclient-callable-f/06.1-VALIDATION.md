---
phase: 06.1
slug: client-auth-completion-firebase-auth-inviteclient-callable-f
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-22
---

# Phase 06.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Populated by the planner from `06.1-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest (vanilla JS + functions/ workspace) + Firebase rules-unit-testing for rules cells |
| **Config files** | `vitest.config.js` (root), `functions/vitest.config.ts` |
| **Quick run command** | `npm test -- --run <changed-file>` |
| **Full suite command** | `npm test -- --run && (cd functions && npm test -- --run)` |
| **Estimated runtime** | ~45–90 seconds (depending on integration tests) |

---

## Sampling Rate

- **After every task commit:** Run quick command scoped to the changed file
- **After every plan wave:** Run full suite (root + functions/)
- **Before `/gsd-verify-work`:** Full suite must be green AND `runbooks/phase6-mfa-recovery-drill.md` Tier-2 client row exercised
- **Max feedback latency:** 90 seconds

---

## Per-Task Verification Map

> Populated by the planner from `06.1-RESEARCH.md` § Validation Architecture during PLAN.md authoring.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | AUTH-16 / AUTH-17 / AUTH-18 | TBD | TBD | TBD | TBD | TBD | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `functions/test/auth/invite-builder.unit.test.ts` — pure-logic stubs for AUTH-16 happy paths + error branches (cross-org, passphrase-invalid, passphrase-not-set, existed-flag)
- [ ] `functions/test/auth/inviteClient.unit.test.ts` — callable unit tests with Admin SDK mocks (covers createUser + setCustomUserClaims ordering, idempotency-marker write, audit-event emission, all 4 audit enum entries)
- [ ] `functions/test/auth/inviteClient.integration.test.ts` — firebase-functions-test integration suite vs emulator (TEST-09 substrate; planner decides Wave 2 or defer)
- [ ] `tests/cloud/invite-admin.test.js` — httpsCallable wrapper unit tests with mocked httpsCallable (mirrors `tests/cloud/claims-admin.test.js`)
- [ ] `tests/main/invite-modal.test.js` — Invite modal rewire unit tests (mock invite-admin.js); covers passphrase input, submit, existed-user confirmation, error mapping
- [ ] `tests/firebase/auth-errors.test.js` — 3 new error classes (`PassphraseInvalidError`, `CrossOrgError`, `PassphraseNotSetError`) at AUTH-12 chokepoint
- [ ] `tests/main/set-org-passphrase-policy.test.js` — `setOrgClientPassphrase` enforces ≥ 12 chars BEFORE hashing (load-bearing per research finding A1)
- [ ] No new rules tests expected (Phase 5 D-14 already covers `request.auth.token.role == "client"`); Wave 1 pre-flight grep confirms

*If none: "Existing infrastructure covers all phase requirements." — N/A for this phase.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Admin invites a client end-to-end (hugh+uat-alpha@) | AUTH-16 | Requires Identity Platform Console + admin browser + real org passphrase | `docs/UAT.md` Step 5; cross-references runbook |
| Client first-run flow: sign in with org passphrase → renderFirstRun → set personal password → land on dashboard | AUTH-17 | Requires UAT account provisioning + visual confirmation of route | UAT script in `docs/UAT.md` |
| App Check 22h backoff workaround (if active during cutover) | AUTH-16 operational | Per-browser backoff state | `runbooks/06.1-app-check-backoff.md` (Wave 3 authors) |
| Strip script behaviour against zero-row prod data | AUTH-18 | Operator-paced ADC execution per Phase 6 D-20 | `scripts/strip-legacy-user-passwords/README.md` |
| Tier-2 client-user MFA un-enrol drill | AUTH-10 (carry-forward, scope-extended) | Admin SDK ops + multi-step recovery | `runbooks/phase6-mfa-recovery-drill.md` § Client users (Wave 3 adds row) |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 90s
- [ ] `nyquist_compliant: true` set in frontmatter once planner fills Per-Task table

**Approval:** pending
