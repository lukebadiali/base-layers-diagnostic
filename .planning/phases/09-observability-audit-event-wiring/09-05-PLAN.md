---
phase: 09-observability-audit-event-wiring
plan: 05
type: execute
wave: 6
depends_on: ["09-observability-audit-event-wiring/09-04"]
files_modified:
  - scripts/setup-uptime-check/run.js
  - scripts/setup-uptime-check/README.md
  - scripts/setup-budget-alerts/run.js
  - scripts/setup-budget-alerts/README.md
  - runbooks/phase-9-monitors-bootstrap.md
  - runbooks/phase-9-deploy-checkpoint.md
autonomous: false
requirements: [OBS-04, OBS-06, OBS-07, OBS-08]
must_haves:
  truths:
    - "scripts/setup-uptime-check/run.js shells out to gcloud monitoring uptime create with regions=USA,EUROPE,ASIA_PACIFIC and period=60s"
    - "scripts/setup-budget-alerts/run.js shells out to gcloud billing budgets create with thresholds 0.5/0.8/1.0 of monthly amount"
    - "runbooks/phase-9-monitors-bootstrap.md documents 4 operator-paced steps: provision audit-alert-sa, run setup-uptime-check, run setup-budget-alerts, configure Sentry 70% quota alert"
    - "runbooks/phase-9-deploy-checkpoint.md captures the single-session deploy procedure: secrets set in Secret Manager, deploy authAnomalyAlert, run scripts/test-slack-alert/run.js, verify Slack receipt"
    - "Operator close-gate: VITE_SENTRY_DSN + SENTRY_AUTH_TOKEN GitHub secrets set; SLACK_WEBHOOK_URL + SENTRY_DSN Secret Manager secrets set; audit-alert-sa provisioned; authAnomalyAlert deployed; uptime check + budget alerts + Sentry quota alert all configured"
  artifacts:
    - path: "scripts/setup-uptime-check/run.js"
      provides: "Idempotent gcloud uptime check provisioner"
      contains: "gcloud monitoring uptime create"
    - path: "scripts/setup-budget-alerts/run.js"
      provides: "Idempotent gcloud billing budget provisioner with 50/80/100 thresholds"
      contains: "gcloud billing budgets create"
    - path: "runbooks/phase-9-monitors-bootstrap.md"
      provides: "Operator runbook for the 4 GCP-tier monitor setup steps"
      contains: "OBS-06"
    - path: "runbooks/phase-9-deploy-checkpoint.md"
      provides: "Single-session deploy procedure with verification gates"
      contains: "audit-alert-sa"
  key_links:
    - from: "runbooks/phase-9-monitors-bootstrap.md"
      to: "scripts/setup-uptime-check/run.js"
      via: "Step 2 invokes the script"
      pattern: "scripts/setup-uptime-check"
    - from: "runbooks/phase-9-deploy-checkpoint.md"
      to: "scripts/test-slack-alert/run.js"
      via: "Synthetic alert verification step"
      pattern: "scripts/test-slack-alert"
---

<objective>
Phase 9 Wave 6 — GCP-tier monitors (uptime check + budget alerts + Sentry quota alert) + audit-alert-sa SA provisioning + deploy checkpoint runbook. **autonomous: false** — this wave has 1 operator checkpoint (Task 3b) that cannot be automated.

Purpose: Land the out-of-band observability — uptime monitor that fires from external regions independent of the project itself; Firebase budget alerts at 50/80/100% (denial-of-wallet defence); Sentry quota alert at 70% of free-tier (already documented in Wave 1 runbook; this wave's checkpoint verifies it). Wave 6 also bundles the operator-paced steps for Wave 5 deploy: provision `audit-alert-sa` SA, set `SLACK_WEBHOOK_URL` + `SENTRY_DSN` in Secret Manager, deploy `authAnomalyAlert`, run synthetic-alert verification.

Output: 2 idempotent operator scripts (uptime + budget); 2 operator runbooks (monitors-bootstrap + deploy-checkpoint with explicit verification steps); single deploy + verify session that closes Wave 5 + Wave 6.

D-09-07 (per RESEARCH.md §Pattern 7 line 670): gcloud monitoring uptime requires AT LEAST 3 regions when `--regions` is specified. Use `USA,EUROPE,ASIA_PACIFIC` to satisfy gcloud constraint AND exceed the OBS-06 success criterion (≥2 regions).

D-09-08 (per RESEARCH.md §Pattern 8 line 696 — substrate-honest): GCP budget alerts NOTIFY only — they do not cap spend. Auto-cutoff via Pub/Sub-driven project-disable Function is OUT OF SCOPE for Phase 9 (v2 deferral). The runbook documents this honestly per Pitfall 19.

D-09-09 (WARNING 6 fix — split Task 3): Separating runbook AUTHORING (autonomous, Claude-doable) from runbook EXECUTION (checkpoint, operator-only) per planner anti-pattern guidance. Task 3a authors `runbooks/phase-9-deploy-checkpoint.md` autonomously; Task 3b is the operator checkpoint that BLOCKS until the operator records evidence in the runbook's Cutover Log section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md
@.planning/phases/09-observability-audit-event-wiring/09-PATTERNS.md
@runbooks/phase-9-sentry-bootstrap.md
@scripts/test-slack-alert/run.js
@scripts/provision-function-sas/run.js
@scripts/setup-backup-bucket/run.js
@runbooks/phase-7-bigquery-sink-bootstrap.md
@runbooks/phase-8-backup-setup.md

<interfaces>
gcloud monitoring uptime CLI [VERIFIED 2026-05-10]:
```bash
gcloud monitoring uptime create <name> \
  --resource-type=uptime-url \
  --resource-labels=host=<host>,project_id=<project> \
  --protocol=https --request-method=get --path=/ \
  --period=60s --timeout=10s \
  --regions=USA,EUROPE,ASIA_PACIFIC \
  --project=<project>
```

gcloud billing budgets CLI [VERIFIED 2026-05-10]:
```bash
gcloud billing budgets create \
  --billing-account="$BILLING_ACCOUNT" \
  --display-name="<name>" \
  --budget-amount=<amount><currency> \
  --threshold-rule=percent=0.5 \
  --threshold-rule=percent=0.8 \
  --threshold-rule=percent=1.0 \
  --filter-projects=projects/<project>
```

Phase 7 SA provisioning script (scripts/provision-function-sas/run.js) is the analog for the new audit-alert-sa addition. The existing script can be EXTENDED to include audit-alert-sa OR a new entry can be added to its SA list.
</interfaces>

<patterns_reference>
PATTERNS.md analogs used in this plan:
- scripts/setup-uptime-check/run.js → analog Phase 7 `scripts/provision-function-sas/run.js` (Pattern E — idempotent operator script via child_process.execSync)
- scripts/setup-budget-alerts/run.js → analog Phase 8 `scripts/setup-backup-bucket/run.js` (gcloud shell-out style)
- runbooks/phase-9-monitors-bootstrap.md → analog `runbooks/phase-7-bigquery-sink-bootstrap.md` style
- runbooks/phase-9-deploy-checkpoint.md → analog `runbooks/phase8-backup-setup.md` + `runbooks/phase-7-cleanup-ledger.md` operator-step style
</patterns_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/setup-uptime-check/run.js + scripts/setup-budget-alerts/run.js (idempotent operator scripts)</name>
  <read_first>
    - scripts/provision-function-sas/run.js (Phase 7 — Pattern E idempotent operator script via execSync; the canonical reference for "describe first, create only if absent" style)
    - scripts/setup-backup-bucket/run.js (Phase 8 — gcloud shell-out idempotency)
    - .planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md §Pattern 7 (lines 638-670 — uptime check; line 670 IMPORTANT: regions minimum 3) + §Pattern 8 (lines 676-696 — budget alerts; line 696 IMPORTANT: alerts notify only, no cap)
    - .planning/phases/09-observability-audit-event-wiring/09-PATTERNS.md (section "scripts/setup-uptime-check/run.js + scripts/setup-budget-alerts/run.js" — research-pattern marker)
  </read_first>
  <action>
    Step 1 — create `scripts/setup-uptime-check/run.js`. Idempotency: shell out to `gcloud monitoring uptime list` first; if a check named `base-layers-diagnostic-prod` exists, skip create. Otherwise run `gcloud monitoring uptime create ...`.

    Required contents:
    - Header banner: "Phase 9 Wave 6 (OBS-06): GCP Cloud Monitoring uptime check provisioner."
    - Imports: `import { execSync } from "node:child_process";`
    - `PROJECT = "bedeveloped-base-layers"`, `HOST = "baselayers.bedeveloped.com"`, `CHECK_NAME = "base-layers-diagnostic-prod"`
    - Idempotency check: `const existing = execSync(\`gcloud monitoring uptime list --project=${PROJECT} --format="value(name)"\`).toString();`
    - If `existing.includes(CHECK_NAME)`: log "OK — uptime check already exists; skipping create" and `process.exit(0)`
    - Else run:
      ```bash
      gcloud monitoring uptime create base-layers-diagnostic-prod \
        --resource-type=uptime-url \
        --resource-labels=host=baselayers.bedeveloped.com,project_id=bedeveloped-base-layers \
        --protocol=https --request-method=get --path=/ \
        --period=60s --timeout=10s \
        --regions=USA,EUROPE,ASIA_PACIFIC \
        --project=bedeveloped-base-layers
      ```
    - On error: log stderr; `process.exit(1)`
    - On success: log "OK — uptime check created. Verify in Cloud Console > Monitoring > Uptime checks."

    Add a comment block at the top citing RESEARCH.md §Pattern 7 line 670: "regions minimum is 3 — USA,EUROPE,ASIA_PACIFIC exceeds OBS-06's ≥2 region threshold."

    Step 2 — create `scripts/setup-uptime-check/README.md`. Match Phase 7/8 README style. Sections: Purpose, Pre-conditions (gcloud CLI authenticated; project ID matches), Usage (`node scripts/setup-uptime-check/run.js`), Idempotency (re-runnable), Verification (Cloud Console URL), Related (Wave 6 runbook).

    Step 3 — create `scripts/setup-budget-alerts/run.js`. Idempotency check via `gcloud billing budgets list --billing-account=$BILLING_ACCOUNT`. If a budget named `base-layers-monthly` exists, skip; else create.

    Required contents:
    - Header banner: "Phase 9 Wave 6 (OBS-07): GCP Cloud Billing budget provisioner — 50/80/100 percent thresholds."
    - Two-step flow:
      1. Resolve `BILLING_ACCOUNT` via:
         ```bash
         gcloud billing projects describe bedeveloped-base-layers --format="value(billingAccountName)" | sed 's|billingAccounts/||'
         ```
         Or use `--format=value(billingAccountName)` and strip the prefix in JS.
      2. List existing budgets: `gcloud billing budgets list --billing-account=$BILLING_ACCOUNT --format="value(displayName)"`. If `base-layers-monthly` present, skip.
      3. Create budget:
         ```bash
         gcloud billing budgets create \
           --billing-account="$BILLING_ACCOUNT" \
           --display-name="base-layers-monthly" \
           --budget-amount=100GBP \
           --threshold-rule=percent=0.5 \
           --threshold-rule=percent=0.8 \
           --threshold-rule=percent=1.0 \
           --filter-projects=projects/bedeveloped-base-layers
         ```
    - Document in inline comments: "Budget alert NOTIFIES only — does NOT cap spend (per Firebase docs / Pitfall 19 / RESEARCH.md §Pattern 8 line 696). Auto-disable via Pub/Sub-driven Function is v2 — out of scope for Phase 9."
    - Allow `BUDGET_AMOUNT` and `BUDGET_CURRENCY` to be overridden via env (defaults: 100, GBP) — operator may want to set initial budget at lower threshold.
    - On error: log stderr; `process.exit(1)`
    - On success: log "OK — budget created. Verify in Cloud Console > Billing > Budgets & alerts. NOTE: this notifies only; does not cap spend."

    Step 4 — create `scripts/setup-budget-alerts/README.md`. Same template as Step 2 README. Add an explicit "Limitations" section reproducing the "alerts notify only" disclosure.

    Step 5 — verify: `node -c scripts/setup-uptime-check/run.js` and `node -c scripts/setup-budget-alerts/run.js` both exit 0 (parse-only, no execution because we don't have gcloud auth in test).
  </action>
  <verify>
    <automated>node -c scripts/setup-uptime-check/run.js && node -c scripts/setup-budget-alerts/run.js && ls scripts/setup-uptime-check/README.md scripts/setup-budget-alerts/README.md</automated>
  </verify>
  <done>
    - `scripts/setup-uptime-check/run.js` exists with idempotent gcloud monitoring uptime create + READMEs
    - `scripts/setup-budget-alerts/run.js` exists with idempotent gcloud billing budgets create + READMEs
    - Both scripts pass `node -c` parse check
    - Both READMEs cross-reference the Wave 6 monitors-bootstrap runbook + the relevant success criterion (OBS-06 / OBS-07)
    - Both scripts include the substrate-honest disclosures (3-region minimum for uptime; "alerts notify only" for budget)
  </done>
</task>

<task type="auto">
  <name>Task 2: Author runbooks/phase-9-monitors-bootstrap.md (4 operator steps for OBS-06/07/08)</name>
  <read_first>
    - runbooks/phase-7-bigquery-sink-bootstrap.md (closest analog — operator runbook for a one-shot setup with verification)
    - runbooks/phase-8-backup-setup.md (Phase 8 — operator-paced + gcloud + Console hybrid)
    - runbooks/phase-9-sentry-bootstrap.md (Wave 1 — Step 6 already documents the 70% quota alert; Wave 6 runbook references that step rather than duplicating)
    - .planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md §Pattern 7 + §Pattern 8 + §Pattern 9 (Sentry quota — operator-only)
    - scripts/setup-uptime-check/run.js + scripts/setup-budget-alerts/run.js (Task 1 above — runbook references these)
  </read_first>
  <action>
    Create `runbooks/phase-9-monitors-bootstrap.md` with these sections. Use the Phase 7/8 runbook template style (numbered steps, evidence anchors per step, copy-paste commands, verification gates).

    **Title:** "Phase 9 — GCP Monitors Bootstrap (Operator Runbook)"

    **Purpose:** Single-session operator procedure for OBS-06 (uptime check), OBS-07 (budget alerts), and OBS-08 (Sentry quota alert) — three out-of-band observability monitors.

    **Pre-conditions:**
    - Phase 9 Wave 1 runbook (`runbooks/phase-9-sentry-bootstrap.md`) Steps 1-5 complete (Sentry org + project + DSN + GitHub secrets)
    - gcloud CLI authenticated (`gcloud auth login` and `gcloud config set project bedeveloped-base-layers`)
    - Operator has Sentry organisation admin access (for Step 4 — quota alert)
    - audit-alert-sa SA NOT yet provisioned (queued for `runbooks/phase-9-deploy-checkpoint.md`; that's the Wave 5 deploy precondition, not a Wave 6 monitor precondition)

    **Step 1: Provision audit-alert-sa SA** (Wave 5 deploy precondition — runbook covers it here so the operator session is single-pass)
    - Add `audit-alert-sa` to the `scripts/provision-function-sas/run.js` SA list IF NOT ALREADY THERE (Phase 7 script — read it; if missing, append to SA array)
    - Permissions: `roles/datastore.user` (for authFailureCounters writes) + `roles/datastore.viewer` (for auditLog reads). NO broader roles.
    - Run: `node scripts/provision-function-sas/run.js`
    - Verify via: `gcloud iam service-accounts describe audit-alert-sa@bedeveloped-base-layers.iam.gserviceaccount.com`
    - Document operator timing: `{{ T+0 }} → audit-alert-sa created` (Pitfall 19 substrate-honest disclosure pattern)

    **Step 2: Configure SLACK_WEBHOOK_URL + SENTRY_DSN secrets in Secret Manager**
    - SLACK_WEBHOOK_URL — Slack admin creates an Incoming Webhook for #ops-alerts (or similar); copy the URL
    - Run: `printf "%s" "<webhook url>" | gcloud secrets create SLACK_WEBHOOK_URL --data-file=- --replication-policy=automatic`
    - Run: `printf "%s" "<sentry node DSN>" | gcloud secrets create SENTRY_DSN --data-file=- --replication-policy=automatic`
    - Grant audit-alert-sa access: `gcloud secrets add-iam-policy-binding SLACK_WEBHOOK_URL --member=serviceAccount:audit-alert-sa@bedeveloped-base-layers.iam.gserviceaccount.com --role=roles/secretmanager.secretAccessor` (and same for SENTRY_DSN)
    - Verify: `gcloud secrets versions access latest --secret=SLACK_WEBHOOK_URL` returns the URL

    **Step 3: Run scripts/setup-uptime-check/run.js (OBS-06)**
    - Pre: `gcloud config get-value project` returns `bedeveloped-base-layers`
    - Run: `node scripts/setup-uptime-check/run.js`
    - Expected output: "OK — uptime check created" (or "OK — uptime check already exists; skipping create")
    - Verify in Console: Cloud Console → Monitoring → Uptime checks → see `base-layers-diagnostic-prod` row with check-status=Pass and 3 regions
    - Configure alerting policy (Console-only): Create alerting policy on the uptime check; channel = the Slack webhook secret (operator may need to create a Notification Channel of type Webhook first via `gcloud alpha monitoring channels create`)
    - Document timing: `{{ T+? }} → uptime check first probe results green`

    **Step 4: Run scripts/setup-budget-alerts/run.js (OBS-07)**
    - Pre: `gcloud billing projects describe bedeveloped-base-layers` returns a `billingAccountName`
    - Run: `node scripts/setup-budget-alerts/run.js` (or with `BUDGET_AMOUNT=50 BUDGET_CURRENCY=GBP node scripts/setup-budget-alerts/run.js` to override defaults)
    - Expected output: "OK — budget created" (or "OK — budget already exists; skipping create")
    - Verify in Console: Cloud Console → Billing → Budgets & alerts → see `base-layers-monthly` with 3 thresholds (50%, 80%, 100%)
    - **Substrate-honest disclosure (Pitfall 19):** "Budget alerts NOTIFY only; they do not cap spend. Auto-disable via Pub/Sub-driven Function is OUT OF SCOPE for Phase 9 (v2 deferral)." Document this in the runbook so the SECURITY.md (Wave 7) increment can copy it verbatim.

    **Step 5: Configure Sentry 70% quota alert (OBS-08)**
    - This step duplicates Wave 1 runbook Step 6, kept here for the single-session flow
    - Sentry web UI → Settings → Subscription → Quota Alerts → "Notify when monthly events ≥ 70%"
    - Channel: operator email (or pager if escalation desired)
    - Save + screenshot for `docs/evidence/` (Phase 11 owns evidence; Wave 6 close-gate captures the screenshot)

    **Step 6: Verification gate**
    - All four artifacts visible:
      1. `gcloud iam service-accounts list | grep audit-alert-sa` returns one line
      2. `gcloud secrets list | grep -E "SLACK_WEBHOOK_URL|SENTRY_DSN"` returns 2 lines
      3. `gcloud monitoring uptime list` returns the uptime check with 3 regions
      4. `gcloud billing budgets list --billing-account=$BILLING_ACCOUNT` returns the budget with 3 thresholds
      5. Sentry quota alert visible in operator's email "Subscriptions → Quota Alerts"

    **Step 7: Output**
    - Operator records start/finish times in `runbooks/phase-9-deploy-checkpoint.md` ## Cutover Log section (next runbook)
    - Forward-tracking row queued for Wave 7 cleanup-ledger: "Verify uptime check Slack alerting policy hits Slack channel during a synthetic outage"

    Length target: ~4-6 KB; same density as `runbooks/phase-7-bigquery-sink-bootstrap.md`.
  </action>
  <verify>
    <automated>ls runbooks/phase-9-monitors-bootstrap.md && grep -cE "OBS-06|OBS-07|OBS-08|audit-alert-sa|SLACK_WEBHOOK_URL|SENTRY_DSN" runbooks/phase-9-monitors-bootstrap.md</automated>
  </verify>
  <done>
    - `runbooks/phase-9-monitors-bootstrap.md` exists
    - 7 sections (purpose, pre-conditions, 5 numbered steps, verification gate, output)
    - References scripts/setup-uptime-check/run.js + scripts/setup-budget-alerts/run.js + scripts/provision-function-sas/run.js
    - Substrate-honest disclosures present (3-region minimum; budget alerts notify-only; v2 auto-cutoff out of scope)
    - `grep -c "OBS-06" runbooks/phase-9-monitors-bootstrap.md` returns at least 1
    - `grep -c "audit-alert-sa" runbooks/phase-9-monitors-bootstrap.md` returns at least 1
  </done>
</task>

<task type="auto">
  <name>Task 3a: Author runbooks/phase-9-deploy-checkpoint.md (autonomous — runbook authoring only; operator EXECUTION is split into Task 3b)</name>
  <read_first>
    - runbooks/phase-7-cleanup-ledger.md (Phase 7 — operator runbook style with Cutover Log table)
    - runbooks/phase-8-backup-setup.md (Phase 8 — most recent operator runbook with verification gates)
    - runbooks/phase-9-sentry-bootstrap.md (Wave 1) + runbooks/phase-9-monitors-bootstrap.md (Wave 6 / Task 2 above)
    - scripts/test-slack-alert/run.js (Plan 04 Wave 5 — synthetic alert script)
    - .planning/phases/09-observability-audit-event-wiring/09-PATTERNS.md (operator-runbook section)
  </read_first>
  <action>
    Create `runbooks/phase-9-deploy-checkpoint.md` modelled on `runbooks/phase-8-backup-setup.md` and `runbooks/phase-7-cleanup-ledger.md`. Sections:

    - **Title:** "Phase 9 — Deploy Checkpoint (Wave 5 + Wave 6 single-session operator procedure)"
    - **Purpose:** Single-session deploy of authAnomalyAlert + 3 GCP-tier monitors, with 5 verification gates.
    - **Pre-flight checklist** (operator confirms before starting):
      - [ ] `runbooks/phase-9-sentry-bootstrap.md` Steps 1-5 complete
      - [ ] `runbooks/phase-9-monitors-bootstrap.md` Steps 1-5 complete (audit-alert-sa + secrets + uptime + budget + Sentry quota)
      - [ ] `firebase login` authenticated
      - [ ] Local `git status` clean on the Phase 9 branch
    - **Step A: Deploy authAnomalyAlert + redeploy any neighbouring functions per Pitfall 8 selective-deploy guidance**
      - `firebase deploy --only functions:authAnomalyAlert --project bedeveloped-base-layers`
      - Verify: `gcloud functions describe authAnomalyAlert --region=europe-west2 --project=bedeveloped-base-layers --format="value(serviceConfig.serviceAccountEmail)"` returns `audit-alert-sa@...`
      - Note: per Phase 7 Wave 5 selective-deploy practice, also redeploy any function whose IAM was previously bound — DO NOT mass-deploy.
    - **Step B: Deploy firestore.rules**
      - `firebase deploy --only firestore:rules --project bedeveloped-base-layers`
      - Verify: Cloud Console → Firestore → Rules → see authFailureCounters block in latest deployed version
    - **Step C: Synthetic Slack alert verification (OBS-05 success criterion)**
      - Run: `SLACK_WEBHOOK_URL=$(gcloud secrets versions access latest --secret=SLACK_WEBHOOK_URL) node scripts/test-slack-alert/run.js`
      - Expected: exit 0; "OK — message posted"
      - Operator confirms: message arrives in #ops-alerts (or designated Slack channel) within 10s
      - Record `{{ T+0 }} → synthetic alert posted; received in Slack at {{ T+? }}` in this runbook's Cutover Log
    - **Step D: Synthetic auth-failure burst verification (OBS-05 Rule 1 — currently DORMANT)**
      - Per Plan 03a §mfa_rationale + Plan 04 DORMANT designation: Rule 1 has no current emit source (beforeUserSignedIn substrate is in place but no rejection rules emit failures yet).
      - This step is documented as "DORMANT — skip until rejection rules land". Do NOT force a synthetic 6-failure burst; the trigger code is verified by Plan 04 unit tests.
      - Forward-tracking row: "Re-run Step D when first rejection rule lands in beforeUserSignedIn — confirm Slack receives auth-fail-burst alert."
    - **Step E: Uptime check + budget alert + Sentry quota alert smoke checks**
      - Cloud Console → Monitoring → Uptime checks → first probe ≤ 60s after creation, status=Pass
      - Cloud Console → Billing → Budgets & alerts → `base-layers-monthly` row visible with 3 thresholds
      - Sentry → Settings → Subscription → Quota Alerts → "Notify when monthly events ≥ 70%" toggle ON
    - **Cutover Log** (operator-fill placeholder):
      ```
      | Step | Action | T+0 | Result | Evidence |
      |------|--------|-----|--------|----------|
      | A | firebase deploy authAnomalyAlert | {{ T+? }} | {{ pass/fail }} | {{ link/screenshot }} |
      | B | firebase deploy firestore:rules  | {{ T+? }} | {{ pass/fail }} | {{ link/screenshot }} |
      | C | scripts/test-slack-alert/run.js  | {{ T+? }} | {{ pass/fail }} | {{ Slack screenshot }} |
      | D | DORMANT — defer                  | n/a       | DORMANT       | per Plan 04 DORMANT designation |
      | E | uptime/budget/sentry smokes      | {{ T+? }} | {{ pass/fail }} | {{ Console screenshots }} |
      ```
    - **Rollback procedure** (per Pitfall 8 — Phase 7 substrate):
      - If Step A or B fails post-deploy: `git revert <cutover-sha> && git push` to fire the Phase 3 CI deploy workflow with the previous version
      - Alternative quick disable: Cloud Console → Functions → authAnomalyAlert → Disable trigger (does NOT delete; trigger pauses)
    - **Operator stop signal** (BLOCKING for Phase 9 close): Operator records all 5 verification gates as PASS in the Cutover Log table before resuming. Step D records as "DORMANT — defer" rather than pass/fail.

    This task ONLY authors the runbook. The actual EXECUTION is in Task 3b below — the operator checkpoint.
  </action>
  <verify>
    <automated>ls runbooks/phase-9-deploy-checkpoint.md && grep -cE "Cutover Log|audit-alert-sa|DORMANT" runbooks/phase-9-deploy-checkpoint.md</automated>
  </verify>
  <done>
    - `runbooks/phase-9-deploy-checkpoint.md` exists
    - Sections: Title, Purpose, Pre-flight checklist, Steps A-E, Cutover Log table, Rollback procedure, Operator stop signal
    - Cutover Log has 5 rows (A, B, C, D, E)
    - Step D documented as DORMANT (Plan 03a §mfa_rationale + Plan 04 designation)
    - `grep -c "Cutover Log" runbooks/phase-9-deploy-checkpoint.md` returns at least 1
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3b: Operator deploy checkpoint — execute Wave 5 + Wave 6 in single operator session per runbooks/phase-9-deploy-checkpoint.md</name>
  <what-built>
    Wave 5: authAnomalyAlert Cloud Function (functions/src/observability/authAnomalyAlert.ts) + functions/src/index.ts export + firestore.rules authFailureCounters block + scripts/test-slack-alert/run.js synthetic alert verification + .gitleaks.toml Slack-webhook regex
    Wave 6: scripts/setup-uptime-check/run.js + scripts/setup-budget-alerts/run.js + runbooks/phase-9-monitors-bootstrap.md + runbooks/phase-9-deploy-checkpoint.md (authored by Task 3a) + audit-alert-sa SA + Secret Manager secrets (SLACK_WEBHOOK_URL, SENTRY_DSN)
  </what-built>
  <how-to-verify>
    Operator follows `runbooks/phase-9-deploy-checkpoint.md` (authored by Task 3a) — Steps A through E. The runbook codifies the verification — this checkpoint is BLOCKING until the operator records evidence in the runbook's Cutover Log section.

    Required gates:
    1. **Step A (deploy authAnomalyAlert):** `gcloud functions describe authAnomalyAlert --region=europe-west2 --project=bedeveloped-base-layers --format="value(serviceConfig.serviceAccountEmail)"` returns `audit-alert-sa@...`
    2. **Step B (deploy firestore.rules):** Cloud Console → Firestore → Rules → see authFailureCounters block in latest deployed version
    3. **Step C (synthetic Slack alert):** `SLACK_WEBHOOK_URL=$(gcloud secrets versions access latest --secret=SLACK_WEBHOOK_URL) node scripts/test-slack-alert/run.js` exits 0; operator confirms message arrives in Slack within 10s; screenshot saved
    4. **Step D (auth-fail burst):** marked DORMANT — skip per runbook
    5. **Step E (uptime/budget/sentry smokes):** Cloud Console + Sentry web UI confirmations as documented in runbook Step E

    Operator records each gate's T+0, result, and evidence link in the runbook's Cutover Log table.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" with the following minimum evidence in the runbook's Cutover Log table:
      - Step A T+? recorded; gcloud describe shows audit-alert-sa as service-account
      - Step B T+? recorded; Cloud Console rules diff shows authFailureCounters block
      - Step C T+? recorded; Slack screenshot of the synthetic alert message
      - Step D recorded as "DORMANT — defer"
      - Step E T+? recorded; 3 Console screenshots (uptime / budget / Sentry quota)

    If any gate FAILS, operator types the failure mode + applies rollback procedure; we re-plan as a sub-wave 9.1.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation ↔ GCP Cloud Resource Manager | gcloud CLI authenticated; runs as operator's IAM identity (must have project Editor + Billing Account User roles); commands change billing budget + uptime config + IAM bindings |
| Operator workstation ↔ Sentry SaaS | Operator authenticates via Sentry org admin login; configures quota alert in Sentry settings — out of band from gcloud |
| Cloud Function (authAnomalyAlert) ↔ Secret Manager | Function reads SLACK_WEBHOOK_URL + SENTRY_DSN at invocation via `defineSecret`; SA `audit-alert-sa` requires `roles/secretmanager.secretAccessor` on each secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-05-1 | E (Elevation of privilege) | audit-alert-sa with over-broad IAM at provisioning | mitigate | scripts/provision-function-sas/run.js binds `roles/datastore.user` (scoped to authFailureCounters via Firestore field-level rules) + `roles/datastore.viewer` (auditLog) + `roles/secretmanager.secretAccessor` on the 2 secrets only. NO project-level Editor role. |
| T-9-05-2 | T (Tampering) | Operator pastes Slack webhook into commit during runbook follow | mitigate | Runbook uses `printf "%s" "<webhook url>" | gcloud secrets create ... --data-file=-` (stdin pipe — never committed). gitleaks regex extension landed in Plan 04 Task 4 (catches a Slack URL pattern) as defence in depth. |
| T-9-05-3 | I (Information disclosure) | Budget alert email leaking project cost details to an unintended recipient | mitigate | Budget notification channel = operator email (specific person). Forward-tracking row to switch to a Pub/Sub topic + Slack channel for less-sensitive routing when ops capacity grows. |
| T-9-05-4 | D (Denial of service) | Operator misconfigures budget at very low amount (e.g. £0.01), triggering alerts on every API call | accept | Budget amount is operator-set with a sane default (£100). The default is high enough that PR-test traffic does NOT trip 50%. Operator can lower with `BUDGET_AMOUNT=...` env override; documented in script README. |
| T-9-05-5 | T (Tampering) | Uptime check disabled silently | mitigate | Wave 7 cleanup-ledger row queued: "Quarterly verification — operator confirms `gcloud monitoring uptime list` returns the check; recorded in `runbooks/phase-9-cleanup-ledger.md`." |
| T-9-05-6 | I (Information disclosure / Pitfall 19) | Public claim that budget alerts "cap spend" — they do not | mitigate | Wave 6 runbook + script README + Wave 7 SECURITY.md ALL state "alerts NOTIFY only; do not cap spend; v2 auto-cutoff out of scope." Substrate-honest disclosure pattern. |
</threat_model>

<verification>
- Both scripts pass `node -c` parse check
- `runbooks/phase-9-monitors-bootstrap.md` exists with all 7 sections
- `runbooks/phase-9-deploy-checkpoint.md` exists with Cutover Log placeholder (5 rows including DORMANT D)
- `grep -c "audit-alert-sa" runbooks/phase-9-*.md` returns at least 2 matches
- `grep -cE "OBS-0[5-8]" runbooks/phase-9-*.md` returns at least 4 matches (one per requirement)
- The operator-paced steps are GATED; phase-9 close cannot complete until operator confirms cutover-log
</verification>

<success_criteria>
- OBS-04 (paired with Wave 2): @sentry/vite-plugin in CI ships source maps to the EU Sentry; Wave 6 deploy verifies first deploy logs show source-map upload + .map gate green
- OBS-06: GCP uptime check on https://baselayers.bedeveloped.com from 3 regions every 60s; alerting policy posts to Slack
- OBS-07: GCP budget at £100/month with 50/80/100% thresholds; substrate-honest disclosure that alerts notify only
- OBS-08: Sentry 70% free-tier quota alert configured (operator-paced; verified in close-gate)
- OBS-05 verification: synthetic Slack alert end-to-end (Step C); auth-failure burst observation DORMANT (Step D)
- audit-alert-sa SA provisioned; SLACK_WEBHOOK_URL + SENTRY_DSN secrets in Secret Manager; authAnomalyAlert deployed
- Pitfall 19 substrate-honest pattern preserved: every "do not cap spend" / "ASIA_PACIFIC required for 3-region minimum" / "auto-cutoff is v2" / "Rule 1 DORMANT until rejection rules land" disclosure surfaces in the runbook AND will surface in Wave 7 SECURITY.md
</success_criteria>

<output>
After completion (operator returns "approved"), create `.planning/phases/09-observability-audit-event-wiring/09-05-SUMMARY.md` describing:
- Operator session timing (T+0 → T+? per Cutover Log)
- Each gate's pass/fail/dormant status with evidence link
- audit-alert-sa SA provisioning result (existing-or-created)
- Secret Manager secrets created (SLACK_WEBHOOK_URL, SENTRY_DSN)
- 4 GCP-tier monitors live (uptime check, budget alerts, Sentry quota — Wave 1's OBS-08 step)
- Forward-tracking ledger rows queued for Wave 7: (1) Quarterly uptime-check verification cadence; (2) Sentry quota alert screenshot for docs/evidence/; (3) Re-run Step D when first rejection rule lands in beforeUserSignedIn
- Next plan: 09-06 (cleanup ledger zero-out + DOC-10 SECURITY.md sections + REQUIREMENTS.md row updates)
</output>
</content>
</invoke>