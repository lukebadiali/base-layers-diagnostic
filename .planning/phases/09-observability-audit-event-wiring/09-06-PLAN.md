---
phase: 09-observability-audit-event-wiring
plan: 06
type: execute
wave: 7
depends_on: ["09-observability-audit-event-wiring/09-05"]
files_modified:
  - SECURITY.md
  - .planning/REQUIREMENTS.md
  - runbooks/phase-9-cleanup-ledger.md
  - CONTRIBUTING.md
autonomous: false
requirements: [DOC-10]
must_haves:
  truths:
    - "SECURITY.md gains four new sections under Phase 9 increment (Observability — Sentry; Audit-Event Wiring; Anomaly Alerting; Phase 9 Audit Index)"
    - "Phase 9 Audit Index has 10 rows: OBS-01..08 + AUDIT-05 + DOC-10 — each with code link + test link + framework citation"
    - "REQUIREMENTS.md OBS-01..08 + AUDIT-05 rows updated to Validated 2026-05-?? with closure evidence"
    - "runbooks/phase-9-cleanup-ledger.md zero-out: phase_9_active_rows: 0 (all in-Phase-9 rows closed; carryover rows have explicit Phase 10/11 bounded paths)"
    - "CONTRIBUTING.md gains §Error Messages (no template-literal interpolation of user input — Pitfall 8)"
    - "phase-close human-verify checkpoint confirms operator has run /gsd-verify-work 9 OR similar evidence sweep"
    - "Slack webhook gitleaks regex is NOT in this plan — landed in Plan 04 Task 4 as defence-in-depth alongside the secret introduction (WARNING 7 fix)"
  artifacts:
    - path: "SECURITY.md"
      provides: "4 new sections + Phase 9 Audit Index (10-row table)"
      contains: "Phase 9 Audit Index"
    - path: ".planning/REQUIREMENTS.md"
      provides: "9 row updates with Validated dates + closure evidence (OBS-01..08 + AUDIT-05)"
      contains: "Validated 2026-05"
    - path: "runbooks/phase-9-cleanup-ledger.md"
      provides: "Zero-out gate — phase_9_active_rows: 0 + carryover rows enumerated"
      contains: "phase_9_active_rows: 0"
    - path: "CONTRIBUTING.md"
      provides: "Error message discipline section (Pitfall 8 mitigation)"
      contains: "template literal"
  key_links:
    - from: "SECURITY.md"
      to: "REQUIREMENTS.md OBS rows"
      via: "Each Phase 9 Audit Index row references the requirement ID"
      pattern: "OBS-0[1-8]"
    - from: "runbooks/phase-9-cleanup-ledger.md"
      to: "all Phase 9 plans 01-05 forward-tracking rows"
      via: "Each plan's SUMMARY.md queues a row; this ledger zeros them"
      pattern: "Wave [1-6]"
---

<objective>
Phase 9 Wave 7 — DOC-10 SECURITY.md increment + REQUIREMENTS.md row updates + cleanup-ledger zero-out + CONTRIBUTING.md error-message discipline + phase-close human-verify checkpoint.

Purpose: Close the documentation half of the milestone. Phase 9's controls (Sentry init + PII scrubber + audit-event wiring + Slack anomaly alerts + GCP-tier monitors) become **citation-credible** in SECURITY.md (DOC-10 incremental ownership; Phase 11 owns the canonical pass). REQUIREMENTS.md rows for OBS-01..08 + AUDIT-05 flip from Pending to Validated with evidence anchors. The cleanup-ledger zeroes all Phase 9 active rows; carryover rows are documented with explicit Phase 10/11 bounded closure paths.

Output: SECURITY.md +4 sections (~3 KB increment matching Phase 7+8 cadence); REQUIREMENTS.md 9 row updates; new cleanup-ledger doc; CONTRIBUTING.md error-message guidance; phase-close checkpoint.

D-09-09: Per Phase 7+8 D-18, the SECURITY.md increment lands in ONE atomic commit with the rest of Wave 7 (REQUIREMENTS.md + cleanup-ledger zero-out + CONTRIBUTING.md). The phase-close checkpoint blocks until operator re-runs `/gsd-verify-work 9` OR equivalent evidence sweep against the deployed system.

D-09-10 (WARNING 7 fix): The `.gitleaks.toml` Slack-webhook regex extension was MOVED from this plan's Task 3 to Plan 04 Task 4. Reason: defence-in-depth for the secret introduced in Plan 04 belongs WITH the secret introduction, not in a downstream docs increment. This plan's Task 3 retains ONLY the CONTRIBUTING.md error-message discipline section — keeps the Wave-7 docs increment focused on documentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@SECURITY.md
@.planning/REQUIREMENTS.md
@runbooks/phase-7-cleanup-ledger.md
@runbooks/phase-8-cleanup-ledger.md
@CONTRIBUTING.md
@.planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md
@.planning/phases/09-observability-audit-event-wiring/09-CONTEXT.md
@.planning/phases/09-observability-audit-event-wiring/09-03a-PLAN.md

<interfaces>
SECURITY.md current pattern (Phase 7+8 increments): each phase appends 4-5 sections + an Audit Index table. Phase 7 Audit Index = 16 rows; Phase 8 Audit Index = 19 rows. Phase 9 target = 10 rows (OBS-01..08 + AUDIT-05 + DOC-10).

REQUIREMENTS.md row pattern (from Phase 7+8):
```
| AUDIT-01 to AUDIT-04, AUDIT-06, AUDIT-07 | Phase 7 | Validated 2026-05-10 (Phase 7 Wave 2 application-tier + Wave 5 BigQuery sink substrate; Wave 6 SECURITY.md DOC-10 + Phase 7 Audit Index) | Audit-log infrastructure (...) |
```

The OBS-01 to OBS-08 row currently reads: `Pending | Sentry browser + node + EU region + scrubber + Slack alerts + uptime + budget alerts`.
The AUDIT-05 row currently reads: `Pending | Wiring of \`auditWrite\` calls through every view (depends on Phase 7 infrastructure + Phase 8 lifecycle callers)`.

Per-row entry MUST include: Validated date + Phase X Wave Y reference + brief (1-2 line) evidence summary.
</interfaces>

<patterns_reference>
PATTERNS.md analogs used in this plan:
- runbooks/phase-9-cleanup-ledger.md → analog `runbooks/phase-8-cleanup-ledger.md` (zero-out gate pattern + carryover row format with bounded closure paths)
- SECURITY.md Phase 9 increment → analog Phase 7 Wave 6 increment (4-5 sections + Audit Index table, framework citations per row)
- CONTRIBUTING.md error-message section → no analog, NEW section per RESEARCH.md §Pitfall 8 line 880 recommendation
</patterns_reference>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append SECURITY.md Phase 9 increment (4 sections + Phase 9 Audit Index 10-row table)</name>
  <read_first>
    - SECURITY.md (current full file — locate the existing Phase 7 Audit Index + Phase 8 Audit Index for ordering)
    - .planning/REQUIREMENTS.md (compliance citations for ASVS L2 / ISO 27001 / SOC2 / GDPR — needed for the audit-index rows)
    - runbooks/phase-9-sentry-bootstrap.md (Wave 1)
    - runbooks/phase-9-monitors-bootstrap.md (Wave 6)
    - runbooks/phase-9-deploy-checkpoint.md (Wave 6 — Cutover Log entries)
    - .planning/phases/09-observability-audit-event-wiring/09-{01,02,03a,03,04,05}-SUMMARY.md (each summary feeds evidence anchors)
    - .planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md §Documentation Increment (if section exists; else use Phase 7 Wave 6 SECURITY.md style)
  </read_first>
  <action>
    Step 1 — read SECURITY.md and identify the bottom of the Phase 8 Audit Index. The Phase 9 increment appends BELOW Phase 8 (incremental order = phase order).

    Step 2 — append 4 new sections + 1 Audit Index table:

    **§ Observability — Sentry**
    - **What:** @sentry/browser SDK initialised in src/main.js boot inside fbOnAuthStateChanged (Pitfall 3 — init AFTER auth state); @sentry/node helper in functions/src/util/sentry.ts (Phase 7 substrate, Wave 1 extended); shared PII_KEYS dictionary in src/observability/pii-scrubber.js + functions/src/util/pii-scrubber.ts (parity-tested at PR time).
    - **Where:** src/observability/sentry.js (browser init); src/observability/pii-scrubber.js (browser dictionary); functions/src/util/sentry.ts (server init + withSentry wrapper); functions/src/util/pii-scrubber.ts (server dictionary); functions/test/util/pii-scrubber-parity.test.ts (drift gate).
    - **EU residency:** DSN format `*.ingest.de.sentry.io` encodes the region — operators MUST select EU project at create time (`runbooks/phase-9-sentry-bootstrap.md` Step 2).
    - **Substrate-honest disclosures:**
      - `sendDefaultPii: false` + `tracesSampleRate: 0` (no perf monitoring; defer to v2)
      - Empty-string DSN = silent no-op (kill-switch + local dev path)
      - SDK fingerprint rate-limit drops 11+ events/fp/60s before transport (OBS-03)
      - Free-tier 5k events/month; quota alert at 70% (OBS-08, operator-paced)

    **§ Audit-Event Wiring (AUDIT-05)**
    - **What:** Every sensitive op emits a server-verified-actor audit event. Phase 7 shipped the auditWrite callable + 3 mirror triggers + auditEventSchema enum (28 entries). Phase 9 Plan 03a (Wave 3) EXTENDED the enum with 33 new literals (15 bare data-domain + 18 .requested) AND added server-side bare emissions in setClaims + beforeUserSignedIn substrate + 3 lifecycle callables. Phase 9 Plan 03 (Wave 4) wired emitAuditEvent calls at 9 view-side sites for the .requested companions.
    - **Wiring inventory** (table — copy from Phase 9 RESEARCH.md §AUDIT-05 Wiring Inventory lines 503-517 with code paths):
      | Op | File | Pre/Post | Audit type | Server bare emit |
      |----|------|----------|-----------|------------------|
      | sign-in | src/firebase/auth.js | POST | auth.signin.{success,failure} | n/a (Identity Platform) |
      | sign-out | src/firebase/auth.js | PRE | auth.signout | n/a (Identity Platform) |
      | password change | src/firebase/auth.js | POST | auth.password.change | n/a (Identity Platform) |
      | password reset | src/firebase/auth.js | POST | auth.password.reset | n/a (Identity Platform) |
      | email-link recovery | src/firebase/auth.js | POST | auth.signin.success (payload.method=emailLink) | n/a |
      | claims set | src/cloud/claims-admin.js | POST | iam.claims.set.requested | functions/src/auth/setClaims.ts (Plan 03a) |
      | GDPR export | src/cloud/gdpr.js | POST | compliance.export.user.requested | functions/src/gdpr/gdprExportUser.ts:197 (Phase 8) |
      | GDPR erase | src/cloud/gdpr.js | POST | compliance.erase.user.requested | functions/src/gdpr/gdprEraseUser.ts:289 (Phase 8) |
      | soft-delete / restore / permanently-delete | src/cloud/soft-delete.js | POST | data.<type>.<op>.requested | functions/src/lifecycle/*.ts (Plan 03a) |
      | (substrate, dormant) | functions/src/auth/beforeUserSignedIn.ts | catch | auth.signin.failure | functions/src/auth/beforeUserSignedIn.ts (Plan 03a, dormant) |
    - **Pitfall 17 invariant:** zero PII in payloads; actor identity is server-overlaid by auditWrite from `request.auth.token` (or actor=null/system for the unauthenticated beforeUserSignedIn path).
    - **Pitfall 7 mirror-trigger collision:** Phase 7 mirror triggers fire only if no primary in 60s; Plan 03a server-side bare emissions on softDelete/restore/permanentlyDelete satisfy primary-event existence for the 60s window. View-side `.requested` events on soft-delete callables land BEFORE the underlying server callable, but the bare `data.<type>.softDelete` emit lands AFTER the batch commit — both satisfy mirror dedup.
    - **Substrate-honest disclosure (Plan 03a §mfa_rationale):** MFA enrol/un-enrol audit emission is DEFERRED — bound to `enrollTotp`/`unenrollAllMfa` deps in src/main.js (currently `// deferred to user-testing phase`). Wave 5 Rule 2 (MFA disenrolment alert) is DORMANT — trigger code stays for forward-readiness.

    **§ Anomaly Alerting (OBS-05)**
    - **What:** functions/src/observability/authAnomalyAlert.ts is an onDocumentCreated('auditLog/{eventId}') trigger evaluating 4 rules and dispatching Slack webhook messages. Rolling-window auth-failure counter at authFailureCounters/{ipHash} (server-only collection; rules deny client read+write).
    - **Rules:**
      1. Auth-fail burst — >5/IP/5min window; fires EXACTLY ONCE per (ipHash, window) on the threshold-crossing event. **DORMANT** — beforeUserSignedIn substrate emits no failures yet (no rejection rules exist); trigger code is functional and observation pipeline activates the moment any rejection rule lands.
      2. MFA disenrolment — every event fires. **DORMANT** — no current MFA emit source (Plan 03a §mfa_rationale).
      3. Role escalation — fires when payload.newRole=admin AND previousRole!=admin. **FUNCTIONAL** — feeds from Plan 03a setClaims server emission.
      4. Unusual-hour gdprExportUser — fires when UTC hour ∈ {0..5, 22..23}. **FUNCTIONAL** — feeds from Phase 8 baseline gdprExportUser server emission.
    - **Substrate-honest disclosures:**
      - retryConfig retryCount: 1 (best-effort; do not retry-storm Slack)
      - audit-alert-sa SA scoped to roles/datastore.user (authFailureCounters) + roles/datastore.viewer (auditLog) only
      - Slack webhook URL stored in defineSecret() — Pitfall 4 (gitleaks regex landed in Plan 04 Task 4 as belt-and-braces alongside the secret introduction)
      - Rules 1+2 DORMANT — observation pipeline awaits emission sources

    **§ Out-of-band Monitors (OBS-04 / OBS-06 / OBS-07 / OBS-08)**
    - **OBS-04** Source maps uploaded to Sentry EU via @sentry/vite-plugin in CI; .map files DELETED from dist/ after upload (filesToDeleteAfterUpload: ["dist/**/*.map"] — Pitfall 6); CI gate fails deploy if any .map remains.
    - **OBS-06** GCP Cloud Monitoring uptime check on https://baselayers.bedeveloped.com every 60s from 3 regions (USA, EUROPE, ASIA_PACIFIC — gcloud minimum is 3, exceeds OBS-06's ≥2). Alerting policy posts to Slack via webhook channel.
    - **OBS-07** GCP billing budget at £100/month with 50/80/100% thresholds. **Substrate-honest:** budget alerts NOTIFY only; they do not cap spend (per Firebase docs). Auto-cutoff via Pub/Sub-driven Function is OUT OF SCOPE for v1 (deferred to v2 per Pitfall 19).
    - **OBS-08** Sentry-side dashboard alert at 70% of monthly free-tier (3500 of 5000 events/month). Operator-set in Sentry web UI; not scriptable from gcloud.

    **§ Phase 9 Audit Index** (10-row table — same format as Phase 7 + 8 indexes):

    | Requirement | Control | Code | Test / Evidence | Framework |
    |-------------|---------|------|-----------------|-----------|
    | OBS-01 | Sentry browser+node init with PII scrubber | `src/observability/sentry.js`, `src/observability/pii-scrubber.js`, `functions/src/util/sentry.ts`, `functions/src/util/pii-scrubber.ts` | `tests/observability/sentry-init.test.js`, `functions/test/util/pii-scrubber-parity.test.ts`, `tests/observability/audit-events.test.js`, `tests/observability/pii-scrubber.test.js` | ASVS V8.3.4; ISO A.5.10/A.5.34; GDPR Art. 32 |
    | OBS-02 | Sentry EU residency | `runbooks/phase-9-sentry-bootstrap.md` Step 2 + DSN format | `runbooks/phase-9-deploy-checkpoint.md` Step E (Sentry Console screenshot) | GDPR Art. 32 / Schrems II |
    | OBS-03 | Fingerprint rate-limit at SDK boundary | `src/observability/sentry.js` — `fingerprintRateLimit()` | `tests/observability/sentry-init.test.js` Test 6 | ASVS V8.4.4 |
    | OBS-04 | Source-map upload via @sentry/vite-plugin + hidden source maps | `vite.config.js`, `.github/workflows/ci.yml` | `tests/build/source-map-gate.test.js`; CI deploy log "Assert no .map files" gate | ASVS V14.2.4 |
    | OBS-05 | Auth-anomaly Slack alert via Cloud Function | `functions/src/observability/authAnomalyAlert.ts` (Rules 3+4 functional; Rules 1+2 DORMANT) | `functions/test/observability/authAnomalyAlert.test.ts` (6 cases); `tests/rules/authFailureCounters.test.js` (4 cells); `runbooks/phase-9-deploy-checkpoint.md` Step C synthetic alert | ISO A.5.25/A.8.16; SOC2 CC7.2 |
    | OBS-06 | GCP uptime check 3 regions/60s | `scripts/setup-uptime-check/run.js`; `runbooks/phase-9-monitors-bootstrap.md` Step 3 | `runbooks/phase-9-deploy-checkpoint.md` Step E (Console screenshot) | ISO A.8.16; SOC2 CC7.2 |
    | OBS-07 | GCP budget alerts 50/80/100 | `scripts/setup-budget-alerts/run.js`; `runbooks/phase-9-monitors-bootstrap.md` Step 4 | `runbooks/phase-9-deploy-checkpoint.md` Step E (Console screenshot); SECURITY.md "Substrate-honest: alerts notify only; do not cap spend" disclosure | ISO A.5.6 |
    | OBS-08 | Sentry 70% quota alert | `runbooks/phase-9-sentry-bootstrap.md` Step 6 + `runbooks/phase-9-monitors-bootstrap.md` Step 5 | `runbooks/phase-9-deploy-checkpoint.md` Step E (Sentry settings screenshot) | ISO A.5.6 |
    | AUDIT-05 | View-side audit-event wiring (9 sites; client .requested) + server-side bare substrate (Plan 03a; 4 sites) | `src/firebase/auth.js`, `src/cloud/{claims-admin,gdpr,soft-delete}.js`, `functions/src/audit/auditEventSchema.ts`, `functions/src/auth/{setClaims,beforeUserSignedIn}.ts`, `functions/src/lifecycle/{softDelete,restoreSoftDeleted,permanentlyDeleteSoftDeleted}.ts` | `tests/firebase/auth-audit-emit.test.js`; `tests/audit-wiring.test.js`; `functions/test/audit/auditEventSchema.test.ts`; `functions/test/auth/setClaims-audit-emit.test.ts`; `functions/test/auth/beforeUserSignedIn-audit-emit.test.ts`; `functions/test/lifecycle/*-audit-emit.test.ts` | ASVS V8.4.x; ISO A.8.15/A.8.16; SOC2 CC4.1; GDPR Art. 30 |
    | DOC-10 | Phase 9 incremental SECURITY.md (Pitfall 19) | This Phase 9 Audit Index | this commit | ISO A.5.36 |

    Step 3 — verify by inspection:
      - `grep -c "Phase 9 Audit Index" SECURITY.md` returns at least 1
      - `grep -cE "OBS-0[1-8]" SECURITY.md` returns at least 8 (one per row)
      - `grep -c "AUDIT-05" SECURITY.md` returns at least 1
      - `grep -c "Substrate-honest" SECURITY.md` returns at least 3 (Sentry kill-switch + budget alerts notify-only + 3-region minimum)
      - `grep -c "DORMANT" SECURITY.md` returns at least 2 (Rule 1 + Rule 2)
  </action>
  <verify>
    <automated>grep -c "Phase 9 Audit Index" SECURITY.md && grep -c "AUDIT-05" SECURITY.md && grep -cE "OBS-0[1-8]" SECURITY.md</automated>
  </verify>
  <done>
    - SECURITY.md gains 4 new sections + 1 Audit Index (10 rows)
    - All 9 OBS + AUDIT-05 + DOC-10 rows have explicit code path + test path + framework citation
    - 3 substrate-honest disclosures present (sentry kill-switch, budget alerts notify-only, 3-region minimum)
    - 2 DORMANT designations present (Rule 1 + Rule 2)
    - File grows by ~3 KB (matching Phase 7+8 increment cadence)
  </done>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md OBS-01..08 + AUDIT-05 rows + author runbooks/phase-9-cleanup-ledger.md zero-out gate</name>
  <read_first>
    - .planning/REQUIREMENTS.md current file — locate AUDIT-05 row (line ~126) + OBS-01..08 rows (lines ~159-166)
    - runbooks/phase-7-cleanup-ledger.md (zero-out gate template)
    - runbooks/phase-8-cleanup-ledger.md (Phase 8 zero-out — most recent template; same shape expected)
    - .planning/phases/09-observability-audit-event-wiring/09-{01,02,03a,03,04,05}-SUMMARY.md (each summary should be authored Wave-by-Wave; cumulative reading lets us identify all forward-tracking ledger rows queued by earlier waves)
  </read_first>
  <action>
    Step 1 — REQUIREMENTS.md row updates. For each row, change `[ ]` → `[x]` and append a Validated annotation matching the Phase 7+8 row style. Example for OBS-01 (currently empty):

    Before:
    ```
    - [ ] **OBS-01**: Sentry browser SDK + Sentry node SDK both initialised with shared DSN; `sendDefaultPii: false`; `beforeSend` PII scrubber blocks chat bodies, comments, names, emails (Pitfall 18)
    ```

    After:
    ```
    - [x] **OBS-01**: Sentry browser SDK + Sentry node SDK both initialised with shared DSN; `sendDefaultPii: false`; `beforeSend` PII scrubber blocks chat bodies, comments, names, emails (Pitfall 18) — Closed Phase 9 Wave 1 (09-01) + Wave 4 (09-03 + 09-03a enum extension): `src/observability/sentry.js` + `src/observability/pii-scrubber.js` + `functions/src/util/sentry.ts` (extended) + `functions/src/util/pii-scrubber.ts`. PII_KEYS parity test gates drift between browser and node arrays.
    ```

    Apply the same pattern to OBS-02, OBS-03, OBS-04, OBS-05, OBS-06, OBS-07, OBS-08, AUDIT-05.

    Then update the Traceability table at the bottom of REQUIREMENTS.md. Replace:
    ```
    | OBS-01 to OBS-08                         | Phase 9                                | Pending | Sentry browser + node + EU region + scrubber + Slack alerts + uptime + budget alerts                                                |
    ```
    With:
    ```
    | OBS-01 to OBS-08                         | Phase 9                                | Validated 2026-05-?? (Phase 9 Wave 7 — substrate Wave 1; CI source-map Wave 2; AUDIT-05 substrate Wave 3 (09-03a); client wiring Wave 4 (09-03); authAnomalyAlert Wave 5 (09-04); GCP-tier monitors + Sentry quota alert Wave 6 operator-paced (09-05)) | Sentry browser+node EU + PII_KEYS shared dictionary + fingerprint rate-limit + @sentry/vite-plugin + Slack webhook anomaly alert + uptime check + budget alerts + Sentry 70% quota alert |
    ```

    And replace:
    ```
    | AUDIT-05                                 | Phase 9                                | Pending | Wiring of `auditWrite` calls through every view (depends on Phase 7 infrastructure + Phase 8 lifecycle callers)                     |
    ```
    With:
    ```
    | AUDIT-05                                 | Phase 9                                | Validated 2026-05-?? (Phase 9 Wave 3 substrate — 09-03a; Wave 4 client wiring — 09-03; Wave 7 docs increment — 09-06) | 9 view-side .requested emit sites across `src/firebase/auth.js` + `src/cloud/{claims-admin,gdpr,soft-delete}.js` + 4 server-side bare emit sites across `functions/src/auth/{setClaims,beforeUserSignedIn}.ts` + `functions/src/lifecycle/*.ts`; auditEventSchema enum extended +33 literals (15 bare + 18 .requested); tests/audit-wiring.test.js + tests/firebase/auth-audit-emit.test.js + functions/test/audit/auditEventSchema.test.ts + 5 server-side audit-emit tests gate against regression. MFA emit sites DEFERRED — bound to dep landing in src/main.js (carry-forward row in cleanup ledger). |
    ```

    Use the actual deploy date from runbooks/phase-9-deploy-checkpoint.md Cutover Log if available; if Wave 6 operator session has not yet completed, leave `2026-05-??` as a placeholder (the operator phase-close checkpoint in Task 4 below substitutes the date).

    Step 2 — author runbooks/phase-9-cleanup-ledger.md. Match runbooks/phase-7-cleanup-ledger.md or phase-8-cleanup-ledger.md style. Sections:

    - **YAML frontmatter:**
      ```yaml
      ---
      phase: 9
      created: 2026-05-??
      phase_9_active_rows: 0
      last_updated: 2026-05-??
      ---
      ```
    - **Title + Purpose:** Phase 9 cleanup-ledger zero-out gate. Closes all in-Phase-9 forward-tracking rows; documents persistent carryover rows with bounded closure paths to Phase 10/11.
    - **In-Phase-9 closures table** (rows queued during Waves 1-6; closed in Wave 7):
      | Row | Queued in | Closure |
      |-----|-----------|---------|
      | "Wave 6 close-gate verifies operator-set 70% quota alert" | Wave 1 09-01 | Closed: runbooks/phase-9-deploy-checkpoint.md Step E |
      | "Wave 6 close-gate verifies first deploy log shows source-map upload + Assert no .map files step OK" | Wave 2 09-02 | Closed: runbooks/phase-9-deploy-checkpoint.md Step A + CI run log |
      | "MFA emit-site wiring DEFERRED — bound to dep landing in src/main.js" | Wave 3 09-03a | DEFERRED to v2 (or whenever enrollTotp/unenrollAllMfa deps land — see Plan 03a §mfa_rationale) |
      | "Mirror-trigger collision verification (1 alert per soft-delete cascade)" | Wave 4 09-03 | DEFERRED to Phase 10 — paired with Phase 10 CSP-tightening synthetic-tests phase (one observation per phase saves operator time) |
      | "audit-alert-sa SA provisioning Wave 6 operator session" | Wave 5 09-04 | Closed: runbooks/phase-9-deploy-checkpoint.md Step A |
      | "Slack webhook gitleaks regex extension" | Wave 5 09-04 Task 4 | Closed: .gitleaks.toml extended in Plan 04 Task 4 (moved from Plan 06 Task 3 per WARNING 7) |
      | "#ops-warn vs #ops-page channel split when staffing scales" | Wave 5 09-04 | DEFERRED to v2 (operator policy decision, not a code-level row) |
      | "Quarterly uptime-check verification cadence" | Wave 6 09-05 | DEFERRED to operator follow-up: row queued in this ledger as recurring quarterly task |
      | "Sentry quota alert screenshot for docs/evidence/" | Wave 6 09-05 | DEFERRED to Phase 11 (DOC-09 evidence pack owner) |
      | "Re-run Step D when first rejection rule lands in beforeUserSignedIn" | Wave 6 09-05 | DEFERRED — bound to next phase introducing rejection rule (Phase 10+ probable) |
      | "Phase 8 forward-tracking row: post-erasure-audit first run → operator follow-up" | Phase 8 09-CONTEXT.md | Closed in Phase 9 OBS validation (Sentry will catch any post-erasure-audit residual PII via the global error capture) |

    - **Carryover rows enumerated** (with explicit Phase 10/11 bounded paths):
      | Row | Bounded path |
      |-----|--------------|
      | MFA emit-site wiring | Bound to `enrollTotp`/`unenrollAllMfa` dep landing in src/main.js — currently `// deferred to user-testing phase` |
      | Mirror-trigger collision verification | Phase 10 — synthetic-tests sub-wave |
      | Quarterly uptime-check verification | Recurring operator task; first run Q3-2026 |
      | Sentry quota alert screenshot | Phase 11 (DOC-09 — evidence pack) |
      | #ops-warn vs #ops-page split | v2 (operator policy decision) |
      | Sentry "tunnel" config if 80%+ events being ad-blocked | v2 — re-evaluate after first quarter of metrics |
      | CONTRIBUTING.md error-message review (Pitfall 8) | This phase Task 3 closes; quarterly re-audit recommended |
      | Re-run Step D (auth-fail burst synthetic) | Bound to first rejection rule in beforeUserSignedIn |

    - **Zero-out gate:** Re-run after each PR merge to Phase 9. `phase_9_active_rows` MUST be 0 before Phase 9 close commit lands.

    - **Status:** `phase_9_active_rows: 0` — set at task end after operator confirms.

    Step 3 — verify:
      - `grep -c "phase_9_active_rows: 0" runbooks/phase-9-cleanup-ledger.md` returns at least 1
      - `grep -cE "OBS-0[1-8]" .planning/REQUIREMENTS.md` shows the rows updated (count ≥ 9 — 8 rows + at least 1 Traceability table mention)
      - `grep -c "Validated 2026-05" .planning/REQUIREMENTS.md` returns at least 9 new entries (the 8 OBS rows + AUDIT-05 row)
  </action>
  <verify>
    <automated>grep -c "phase_9_active_rows: 0" runbooks/phase-9-cleanup-ledger.md && grep -c "OBS-01" .planning/REQUIREMENTS.md && grep -c "AUDIT-05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    - 9 REQUIREMENTS.md rows flipped from `[ ]` to `[x]` (OBS-01..08 + AUDIT-05) with Validated annotations
    - Traceability table 2 row updates (OBS-01..08 group + AUDIT-05)
    - `runbooks/phase-9-cleanup-ledger.md` exists with `phase_9_active_rows: 0` + closures table + carryover table
    - All forward-tracking rows queued in Waves 1-6 SUMMARYs are accounted for in this ledger (each either closed or queued with bounded path)
    - All in-phase rows zeroed; carryover rows enumerated
    - Slack webhook gitleaks regex closure REFERENCES Plan 04 Task 4 (not authored here per WARNING 7 fix)
  </done>
</task>

<task type="auto">
  <name>Task 3: Add CONTRIBUTING.md § Error Message Discipline (Pitfall 8) — gitleaks regex MOVED to Plan 04 Task 4 per WARNING 7</name>
  <read_first>
    - CONTRIBUTING.md current file — find the right insertion point (e.g., after "Conventional Commits" section, before any code-style section)
    - .planning/phases/09-observability-audit-event-wiring/09-RESEARCH.md §Pitfall 8 (lines 875-881 — error-message discipline)
  </read_first>
  <action>
    NOTE: The .gitleaks.toml Slack-webhook regex extension is no longer in this task — it was moved to Plan 04 Task 4 per WARNING 7 (defence-in-depth belongs with the secret introduction). This task now has only ONE deliverable: CONTRIBUTING.md error-message section.

    Step 1 — extend CONTRIBUTING.md with a new section after the Conventional Commits + governance sections. Find the right insertion point (likely between the "Test-first" governance section and any existing "Coding conventions" section). Add:

    ```markdown
    ## Error Message Discipline (Phase 9 — Pitfall 8 prevention)

    Sentry's `beforeSend` PII scrubber strips known PII keys (email, name, ip, body, message, etc.) and clips request bodies. It does NOT inspect the freeform `exception.values[0].value` field — meaning that an error like:

    ```javascript
    // ANTI-PATTERN — never do this
    throw new Error(`Invalid email: ${email}`);
    ```

    will leak the email into Sentry's event payload via the exception value.

    **Rule:** error messages MUST be static strings. Put dynamic context in `error.cause` or in a `Sentry.addBreadcrumb({ data: { ... } })` (which IS scrubbed via `beforeBreadcrumb`).

    Compliant pattern:
    ```javascript
    // GOOD — static message; user input in cause/breadcrumb (both scrubbed)
    Sentry.addBreadcrumb({ category: "auth", message: "invalid email submitted" });
    throw new Error("Invalid email");

    // ALSO GOOD — typed error class with constant message
    export class SignInError extends Error {
      constructor() { super("Email or password incorrect"); this.name = "SignInError"; }
    }
    ```

    The Phase 6 `SignInError` is the canonical exemplar.

    **Code review checklist:** for any new `throw new Error(...)` in `src/**` or `functions/src/**`, confirm the message has no template-literal interpolation of user input. If interpolation is necessary for debugging, route it through breadcrumbs.

    Quarterly audit (forward-tracking ledger row): operator manually samples 50 random Sentry events from the prior 90 days and confirms zero PII in `exception.value` strings.
    ```

    Step 2 — verify:
      - `grep -c "Error Message Discipline" CONTRIBUTING.md` returns at least 1
      - `grep -c "Pitfall 8" CONTRIBUTING.md` returns at least 1
      - `grep -c "template literal" CONTRIBUTING.md` returns at least 1
  </action>
  <verify>
    <automated>grep -c "Error Message Discipline" CONTRIBUTING.md && grep -c "Pitfall 8" CONTRIBUTING.md && grep -c "template literal" CONTRIBUTING.md</automated>
  </verify>
  <done>
    - CONTRIBUTING.md gains § Error Message Discipline section with anti-pattern + good-pattern examples + quarterly audit row
    - .gitleaks.toml is NOT touched by this task (WARNING 7 fix — closure verified by Plan 04 Task 4)
    - SignInError reference present (exemplar)
    - Quarterly audit forward-tracking row documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase-close human-verify checkpoint — operator runs /gsd-verify-work 9 OR equivalent evidence sweep</name>
  <what-built>
    Phase 9 complete (across Waves 1-7):
      - Sentry browser + node init with shared PII_KEYS dictionary + EU residency + fingerprint rate-limit
      - @sentry/vite-plugin source-map upload in CI + .map deletion gate
      - Server-side audit-event substrate (Plan 03a): 4 server emit sites (setClaims + beforeUserSignedIn substrate + 3 lifecycle callables) + 33 enum literal extensions
      - 9 view-side audit-event emit sites (.requested companions)
      - authAnomalyAlert Cloud Function with 4 anomaly rules (2 functional, 2 DORMANT) + Slack webhook + audit-alert-sa SA + authFailureCounters server-only collection + .gitleaks.toml Slack-URL regex
      - GCP uptime check + budget alerts + Sentry 70% quota alert
      - SECURITY.md +4 sections + Phase 9 Audit Index (10 rows)
      - REQUIREMENTS.md 9 rows flipped to Validated
      - CONTRIBUTING.md error-message discipline
      - Cleanup ledger zero-out (phase_9_active_rows: 0)
  </what-built>
  <how-to-verify>
    Operator runs the standard phase-close evidence sweep:

    1. **Code health gates** (all must exit 0):
      - `npm test -- --run` (root)
      - `cd functions; npm test -- --run; cd ..`
      - `npm run test:rules`
      - `npm run lint && npm run typecheck`
      - `cd functions; npm run lint; npm run typecheck; cd ..`
      - `npm run build`

    2. **Deploy verification** (per runbooks/phase-9-deploy-checkpoint.md):
      - All 5 Cutover Log gates marked PASS or DORMANT with timing + evidence
      - `gcloud functions describe authAnomalyAlert --region=europe-west2` returns `audit-alert-sa@...` as service-account
      - `gcloud secrets list | grep -E "SLACK_WEBHOOK_URL|SENTRY_DSN"` returns 2 lines
      - `gcloud monitoring uptime list` returns the uptime check
      - `gcloud billing budgets list --billing-account=$BILLING_ACCOUNT` returns the budget
      - Sentry quota alert visible in operator's email

    3. **Synthetic alert verification** (OBS-05 success criterion):
      - `SLACK_WEBHOOK_URL=$(gcloud secrets versions access latest --secret=SLACK_WEBHOOK_URL) node scripts/test-slack-alert/run.js` exit 0
      - Slack channel #ops-alerts (or designated) shows the synthetic message

    4. **Source-map upload verification** (OBS-04 success criterion):
      - Latest CI deploy run on main shows `[sentry-vite-plugin]` upload log lines
      - `Assert no .map files served from dist (OBS-04 / Pitfall 6)` step shows OK
      - `curl -sI https://baselayers.bedeveloped.com/main-<hash>.js.map` returns 404 (no .map served)

    5. **Documentation verification:**
      - `runbooks/phase-9-cleanup-ledger.md` shows `phase_9_active_rows: 0`
      - `git diff main..HEAD --stat SECURITY.md` shows ~3 KB increment under Phase 9 Audit Index
      - `git log --grep="phase 9" --oneline | head -10` shows the conventional-commit log of the Phase 9 work

    6. **gitleaks defence-in-depth** (Plan 04 Task 4 closure):
      - `grep -c "slack-webhook-url" .gitleaks.toml` returns at least 1
      - `npx gitleaks detect --no-git --source . --config .gitleaks.toml --redact` finds zero leaks

    7. **Run /gsd-verify-work 9 if available** (preferred): the verifier agent walks the must_haves across all 7 plans + reports any gaps. Operator addresses any missing items as a sub-wave 9.1 if needed.
  </how-to-verify>
  <resume-signal>
    Operator types "approved" with:
      - All 6 code-health gates green (output OK)
      - Deploy Cutover Log fully filled (5 rows including Step D as DORMANT)
      - Synthetic alert received in Slack (screenshot attached)
      - Source-map upload + .map gate evidence in CI run logs
      - gitleaks scan green
      - Cleanup-ledger phase_9_active_rows confirmed 0
      - /gsd-verify-work 9 (if run) returns 0 unresolved must_have failures

    OR the operator types failure-mode + sub-wave 9.1 sub-plan if any verification fails (e.g., "uptime check Slack alerting policy not yet wired — opening 9.1 sub-plan to add it"). The phase does NOT close until either operator approval OR the sub-plan resolves.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation ↔ git remote | Phase 9 close commit lands via PR or direct push to main; CI gates the merge |
| SECURITY.md citation chain ↔ external auditor | A reader following SECURITY.md citations into the codebase + tests should reproduce all claims via `npm test` + `gcloud monitoring uptime list` etc. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-06-1 | I (Information disclosure) | SECURITY.md claims drift from actual implementation | mitigate | Each Audit Index row cites a specific code path AND a test path; an auditor following the citation MUST find the claim implemented. The phase-close checkpoint (Task 4) re-runs all tests, reasserting the claim chain. DORMANT designations are explicit (Rules 1+2) — no false claim that all 4 rules fire today. |
| T-9-06-2 | T (Tampering) | Cleanup-ledger declared 0 while rows still active | mitigate | The `phase_9_active_rows: 0` gate is set ONLY after Tasks 1-3 close all queued rows AND the operator session in Task 4 confirms. Pre-close gate: `grep -E "(forward-tracking|TODO Phase 9|FIXME Phase 9)" -r runbooks/ .planning/ src/ functions/` returns 0 (informal verification — no leftover Phase 9 markers). |
| T-9-06-3 | I (Information disclosure) | Substrate-honest disclosures omitted from SECURITY.md (Pitfall 19) | mitigate | Task 1's section structure mandates 3 explicit substrate-honest disclosures: Sentry kill-switch + budget alerts notify-only + 3-region minimum. The audit-index gate has explicit row for OBS-07 calling out "alerts notify only; do not cap spend." Plus 2 DORMANT designations (Rules 1+2). |
</threat_model>

<verification>
- All checkpoint Cutover Log gates green/dormant (operator-paced; gated on Task 4)
- `phase_9_active_rows: 0` in runbooks/phase-9-cleanup-ledger.md
- 9 REQUIREMENTS.md row updates with Validated dates + closure evidence
- SECURITY.md grows ~3 KB with 4 new sections + 10-row Phase 9 Audit Index
- CONTRIBUTING.md gains § Error Message Discipline section
- .gitleaks.toml NOT touched by this plan (closure deferred to Plan 04 Task 4 verification)
- All pre-existing tests still pass (root + functions + rules suites)
</verification>

<success_criteria>
- DOC-10 (incremental): Phase 9 SECURITY.md sections published; Phase 11 owns canonical pass — Phase 9 increment matches Phase 7+8 cadence (4-5 sections + Audit Index)
- All 9 OBS + AUDIT-05 requirements rows flipped to Validated in REQUIREMENTS.md
- Cleanup-ledger zero-out gate met; carryover rows have explicit bounded closure paths
- Pitfall 19 substrate-honest pattern preserved across SECURITY.md (3 explicit disclosures + 2 DORMANT designations) + cleanup-ledger (carryover table)
- Pitfall 8 error-message discipline lands in CONTRIBUTING.md
- Pitfall 4 gitleaks regex closure verified (lives in Plan 04 — referenced here, not authored)
- Phase 9 closes; ROADMAP.md Progress table updates 0/0 → 7/7 (Wave 7 commits include the ROADMAP edit)
</success_criteria>

<output>
After operator returns "approved":
- Update `.planning/ROADMAP.md` Phase 9 progress: `0/0 | Not started` → `7/7 | Complete (operator-paced 6/7 deferred items in 09-HUMAN-UAT.md if any)`
- Update `.planning/STATE.md` to reflect Phase 9 completion + Phase 10 unblocked
- Create `.planning/phases/09-observability-audit-event-wiring/09-06-SUMMARY.md` describing:
  - Final SECURITY.md structure (4 sections + Phase 9 Audit Index 10 rows)
  - REQUIREMENTS.md row updates (9 rows + Traceability table)
  - Forward-tracking rows queued for Phase 10 (mirror-trigger collision verification, sub-wave option), Phase 11 (Sentry quota screenshot for evidence pack), v2 (Sentry tunnel, #ops-warn/page channel split, auto-cutoff Pub/Sub, MFA emit-site wiring)
  - Phase 9 close artefacts: SECURITY.md DOC-10 increment, REQUIREMENTS.md updates, runbooks/phase-9-cleanup-ledger.md zero-out, CONTRIBUTING.md § Error Message Discipline
  - .gitleaks.toml extension NOT in this plan — landed in Plan 04 Task 4 (cross-reference only)
- Phase 9 ROADMAP entry shows `[x]`; STATE.md "Current Position" updated to Phase 10
</output>
</content>
</invoke>