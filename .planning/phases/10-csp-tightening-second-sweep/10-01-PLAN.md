---
phase: 10-csp-tightening-second-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main.js
  - styles.css
  - index.html
autonomous: true
requirements: [HOST-07, CODE-06]
# Note: CODE-06 — inline-style portion only; IIFE body migration deferred to v2 (forward-tracked in 10-cleanup-ledger F2)
tags: [csp, inline-style, utility-classes, phase-4-sub-wave-4.1-closure]

must_haves:
  truths:
    - "grep -c 'style:\\s*\"' src/main.js returns 0 (was 130)"
    - "Vitest snapshot tests for dashboard, diagnostic, and report render byte-identical HTML to pre-migration baseline"
    - "styles.css has a Phase 10 Wave 1 utility-class block at end-of-file with the new classes declared exactly once each"
    - "index.html cache-busting query bumped from ?v=52 to ?v=53 on styles.css + data/pillars.js + ./src/main.js (3 sites)"
    - "Phase 4 sub-wave 4.1 inline-style cleanup-ledger row closes"
  artifacts:
    - path: "src/main.js"
      provides: "IIFE view-render bodies with 130 static style attrs migrated to utility class names"
      contains: "u-mt-0"
    - path: "styles.css"
      provides: "Utility class declarations consumed by src/main.js (Wave 1 block at end-of-file with header comment citing Phase 10 Wave 1 + HOST-07)"
      contains: "Phase 10 Wave 1 (HOST-07)"
    - path: "index.html"
      provides: "Cache-busting query bumped to ?v=53 (per existing ?v=N convention)"
      contains: "?v=53"
  key_links:
    - from: "src/main.js"
      to: "styles.css utility classes"
      via: "class=\"...\" attribute carries class names declared in styles.css Wave 1 block"
      pattern: "class=\"[^\"]*u-(mt|pt|mb|pb|text|pad|no-border)"
    - from: "index.html"
      to: "styles.css?v=53"
      via: "<link rel=\"stylesheet\"> cache-buster forces fresh fetch after Wave 1 class additions"
      pattern: "styles\\.css\\?v=53"
---

<objective>
Close Phase 4 sub-wave 4.1 carryover: migrate the 130 static `style: "..."` h()-attributes preserved in `src/main.js` IIFE to CSS utility classes declared in `styles.css`. This is the load-bearing gate for Phase 10 — `style-src 'self'` enforcement (Wave 2/3/4) WILL break every one of these sites silently unless they are migrated first. The migration is mechanical and snapshot-tested: pre/post DOM output must be byte-identical.

Purpose: Phase 10 Wave 2 cannot tighten CSP-RO until this gate closes (per Pitfall 10A in 10-RESEARCH.md). Phase 4 cleanup-ledger explicitly queued this as "atomic with Phase 10 work" — Wave 1 is where it lands.

Output: Zero `style: "..."` h()-attributes remain in `src/main.js`. A Wave 1 utility-class block lands at end-of-file in `styles.css`. Phase 2 snapshot tests pass without changes (any drift = forbidden DOM regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-csp-tightening-second-sweep/10-CONTEXT.md
@.planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md
@.planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md
@.planning/phases/10-csp-tightening-second-sweep/10-VALIDATION.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@src/main.js
@src/ui/dom.js
@styles.css
@index.html
@tests/__snapshots__

<interfaces>
<!-- The h() function (consumed throughout src/main.js) — extracted from src/ui/dom.js lines 27-44 -->
<!-- Wave 1 migration changes `attrs.style` into additional class tokens in `attrs.class`. NOTHING else changes. -->

From src/ui/dom.js:
```js
/**
 * @param {string} tag
 * @param {Record<string, any>} [attrs]
 * @param {*} [children]
 * @returns {HTMLElement}
 */
export const h = (tag, attrs = {}, children = []) => {
  const el = document.createElement(tag);
  for (const [k, v] of Object.entries(attrs)) {
    if (k === "class") el.className = v;
    else if (k.startsWith("on") && typeof v === "function") el.addEventListener(k.slice(2), v);
    else if (v === false || v === null || v === undefined) continue;
    else if (v === true) el.setAttribute(k, "");
    else el.setAttribute(k, v);  // ← line 35: this is the CSP-violating path when k === "style"
  }
  ...
};
```

Existing utility-class block in styles.css (end-of-file, lines 2774-2802) — established convention to extend:
```css
/* Phase 4 Wave 1 utility-class block ... */
.is-hidden { display: none !important; }
.is-shown-block { display: block !important; }
.roadmap-drop { ... }
.roadmap-drop.is-dragover { ... }
```
Naming convention extracted: `.u-*` for single-property utility (margin/padding/text-align/border), `.is-*` for state toggles.
</interfaces>

<verification_commands>
<!-- Substrate count BEFORE Wave 1 (run first to confirm starting state): -->
<!-- grep -c 'style:\s*"' src/main.js   →  130 -->

<!-- Substrate count AFTER Wave 1 (acceptance gate): -->
<!-- grep -c 'style:\s*"' src/main.js   →  0   -->

<!-- Snapshot tests (must show zero rendered-HTML drift — drift = task failure): -->
<!-- npm test -- --run tests/__snapshots__ -->
</verification_commands>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enumerate unique inline-style patterns and mint utility classes in styles.css</name>
  <files>styles.css</files>
  <read_first>
    - src/main.js (skim only sections containing `style:` properties — use grep first: `grep -n 'style:\s*"' src/main.js | head -50` to scope reading)
    - styles.css lines 2774-2802 (existing Phase 4 Wave 1 utility-class block — convention reference)
    - .planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md §File 2 "styles.css — utility-class additions"
  </read_first>
  <behavior>
    - All 130 static `style: "..."` h()-attributes from src/main.js are deduped into the smallest set of utility-class declarations
    - The new declarations land at the end of styles.css in a single dedicated block with a header comment citing "Phase 10 Wave 1 (HOST-07)"
    - Naming follows the existing convention: `.u-{property}-{value}` for single-property atoms (e.g. `.u-mt-0`, `.u-text-center`, `.u-pad-48`); semantic class names for compound multi-property patterns (e.g. `.card-empty-text` for "color + max-width + margin auto")
    - No `!important` on layout/margin/padding utilities (only state utilities like `.is-hidden` use `!important`, per existing convention)
    - No utility class is declared twice
  </behavior>
  <action>
    1. Run `grep -n 'style:\s*"' src/main.js > /tmp/inline-style-inventory.txt` (or PowerShell equivalent — `Select-String -Path src/main.js -Pattern 'style:\s*"'`) to capture all 130 sites with line numbers and full style strings.
    2. Parse the inventory: extract every unique `style: "..."` value. Group by single-property atoms vs multi-property compounds.
    3. For each unique single-property value, mint a `.u-{prop}-{value}` class (e.g. `style: "margin-top:0;"` → `.u-mt-0 { margin-top: 0; }`; `style: "padding:48px;"` → `.u-pad-48 { padding: 48px; }`; `style: "text-align:center;"` → `.u-text-center { text-align: center; }`).
    4. For multi-property compounds with semantic meaning (e.g. "color:var(--ink-3); max-width:480px; margin:0 auto;" on the no-org empty-state paragraph at line 1219), mint a single named class (e.g. `.card-empty-text`).
    5. For negative-value utilities (e.g. `margin-top:-6px;`), use a `-neg-` infix: `.u-mt-neg-6 { margin-top: -6px; }`.
    6. For complex inline flex layouts that recur (e.g. line 1269-1271 banner — `display:flex; align-items:center; gap:14px; padding:12px 16px; margin-bottom:16px; background:var(--red-bg); border:1px solid var(--red); border-radius:10px;`), mint a semantic class (e.g. `.alert-banner-flex` or `.unread-chat-banner`).
    7. Append the new utility-class block to the end of `styles.css` (after line 2802 / after the existing `.roadmap-drop.is-dragover` block). Use the header comment template:
       ```css
       /* ============================================================
          Phase 10 Wave 1 (HOST-07): utility classes minted to absorb
          the 130 static `style="..."` h()-attributes in src/main.js
          IIFE so style-src 'self' enforcement does not silently drop
          layout. Closes the row in runbooks/phase-4-cleanup-ledger.md
          "132 static `style="..."` inline-attr strings".
          ============================================================ */
       ```
    8. Save the inventory mapping (style-string → class-name) for Task 2 to consume. Recommended location: a temporary scratch buffer or terminal session — does NOT need to be committed.
    9. Do NOT touch any other section of styles.css. The migration is append-only.
  </action>
  <verify>
    <automated>npm test -- --run firebase-config</automated>
    <manual>
      - `grep -c 'Phase 10 Wave 1 (HOST-07)' styles.css` returns exactly 1 (the new block header comment)
      - The new block lives strictly after line 2802 (cat -n styles.css | tail -100 shows the new utilities at the end)
      - No utility class is declared twice (`grep -oE '^\.u-[a-z0-9-]+ ' styles.css | sort | uniq -d` returns empty)
      - Existing classes (`.is-hidden`, `.is-shown-block`, `.roadmap-drop`, etc.) are unchanged
    </manual>
  </verify>
  <done>
    styles.css gains a Phase 10 Wave 1 utility-class block at end-of-file with the deduped set of `.u-*` and semantic compound classes needed to absorb all 130 inline-style patterns from src/main.js. Existing styles are unchanged. firebase-config schema test still passes (sanity — Task 1 does not touch firebase.json).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate 130 inline-style attributes in src/main.js to utility class names</name>
  <files>src/main.js, index.html</files>
  <read_first>
    - Task 1 output: the style-string → class-name mapping minted in Task 1
    - src/main.js (relevant ranges — driven by the grep inventory from Task 1)
    - .planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md §File 1 "src/main.js — inline-style attribute migration"
    - .planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md §Code Examples Example 3
    - .planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md §Inline-Style Remnant Inventory §Sample call sites (4 representative examples at lines 1097, 1180, 1215, 1216)
  </read_first>
  <behavior>
    - All 130 sites where `style: "..."` appears in h() attrs in src/main.js are replaced: the `style:` property is removed, and its corresponding class names (from Task 1's mapping) are appended to the existing `class:` value (or a new `class:` is added if none existed)
    - DOM-text output of every migrated site is byte-identical to pre-migration — verified by Phase 2 view snapshot tests at tests/__snapshots__/views/{dashboard,diagnostic,report}.html
    - index.html cache-busting query is bumped from `?v=52` to `?v=53` on all 3 sites (styles.css link + data/pillars.js script + ./src/main.js module script)
    - No `style: "..."` h()-attribute remains in src/main.js (grep returns 0)
    - Non-static `style:` properties with computed template-literal values (the ~43 sites grep-counted in 10-RESEARCH.md line 259 — total 173 vs static 130) are LEFT IN PLACE for this wave; they are template-literal expressions, not static strings, and are handled by sub-wave 4.1 proper (NOT Phase 10's scope)
  </behavior>
  <action>
    1. For each line surfaced by `grep -n 'style:\s*"' src/main.js`:
       - Read the line and 1-2 surrounding lines for context
       - Identify the matching class-name(s) from Task 1's mapping
       - Replace `style: "..."` with the appropriate class-name addition:
         - If `class: "..."` exists in the same attrs object, append the new class names space-separated: `class: "auth-help"` → `class: "auth-help u-mt-0 u-pt-0 u-no-border"`
         - If no `class:` key exists, add one: `{ style: "margin-top:0;" }` → `{ class: "u-mt-0" }`
       - Remove the `style: "..."` property entirely (no empty string left, no trailing comma orphan)
    2. Confirm the surgical pattern for the four documented sample sites first (these are the ground-truth verified examples in 10-RESEARCH.md):
       - Line 1097: `{ class: "auth-help", style: "margin-top:0; padding-top:0; border:0;" }` → `{ class: "auth-help u-mt-0 u-pt-0 u-no-border" }`
       - Line 1180: `{ class: "auth-field", style: "margin-top:-6px;" }` → `{ class: "auth-field u-mt-neg-6" }`
       - Line 1215: `{ class: "card", style: "text-align:center; padding:48px;" }` → `{ class: "card u-text-center u-pad-48" }`
       - Line 1216: `{ style: "margin-top:0;" }` → `{ class: "u-mt-0" }`
    3. Iterate site-by-site through the full inventory. Do NOT use sed/awk regex replacement on the whole file — line context is sometimes load-bearing (some style strings span continuation patterns, some sit inside compound h() trees). Manual per-site substitution is required to avoid drift.
    4. Bump `index.html` cache-busting:
       - Line 8: `<link rel="stylesheet" href="styles.css?v=52" />` → `<link rel="stylesheet" href="styles.css?v=53" />`
       - Line 23: `<script src="data/pillars.js?v=52"></script>` → `<script src="data/pillars.js?v=53"></script>`
       - Line 24: `<script type="module" src="./src/main.js?v=52"></script>` → `<script type="module" src="./src/main.js?v=53"></script>`
    5. Run `npm test -- --run tests/__snapshots__` to validate that the dashboard / diagnostic / report rendered HTML snapshots show zero drift. If ANY snapshot differs, STOP and investigate — a difference means the class-name substitution did not preserve DOM output, which means Task 1's class definition is wrong (likely a missing property like `border:0` or an `!important` mismatch).
    6. Run `npm test` (full suite) — must remain green. Phase 2 baseline is 376+ tests.
    7. Verify acceptance count: `grep -c 'style:\s*"' src/main.js` returns 0.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    <manual>
      - `grep -c 'style:\s*"' src/main.js` returns exactly 0
      - `grep -c 'style:' src/main.js` returns approximately 43 (the non-static computed-value sites — they remain, this is by design and is OUT OF SCOPE for Phase 10)
      - `grep -c '?v=53' index.html` returns exactly 3
      - `grep -c '?v=52' index.html` returns exactly 0
      - View snapshot tests (tests/__snapshots__/views/{dashboard,diagnostic,report}.html) show zero drift
    </manual>
  </verify>
  <done>
    Zero static `style: "..."` h()-attributes remain in src/main.js. Every migrated site renders identically (DOM snapshot tests green). index.html cache-busting bumped to ?v=53. Full Vitest suite green. Phase 4 sub-wave 4.1 inline-style row in runbooks/phase-4-cleanup-ledger.md is now closable (close-out happens in Plan 10-05).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time CSS authoring | Designer/operator-edited styles.css → Vite bundle → CDN | (no untrusted input crosses; this is a pure-refactor wave) |
| runtime DOM construction | h() factory consumes attrs → DOM elements | Refactor preserves attrs semantics; the ONLY change is `style:` → `class:` token expansion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-1 | Tampering | src/main.js inline-style migration | mitigate | Phase 2 view snapshot tests at tests/__snapshots__/views/{dashboard,diagnostic,report}.html catch any pre/post DOM drift; manual per-site substitution (no sed-based mass-replace) preserves line-context semantics |
| T-10-01-2 | Tampering | styles.css utility-class block | mitigate | Append-only at end-of-file; existing classes untouched; new block has dedicated header comment so future audits can scope the change |
| T-10-01-3 | Denial of Service | Wave 2 dependency gate | mitigate | If this wave does not close `grep -c 'style:\s*"' src/main.js → 0`, Wave 2's CSP-RO tighten (drop `'unsafe-inline'`) will cause Wave 3 staging soak to surface 130+ csp.violation events with `effectiveDirective="style-src"` — wasted soak window. Wave 1 acceptance criterion gates Wave 2. |
| T-10-01-4 | Tampering | index.html cache-busting bump | mitigate | The `?v=53` bump forces browsers to re-fetch styles.css after the Wave 1 class additions. Without it, returning users would render the old CSS (without `.u-mt-0` etc.) and the migrated `class:` references would silently miss — layout drift identical to Pitfall 10A's enforcement-without-migration failure mode. |

**Severity:** All threats LOW or MEDIUM. No high-severity threats — this is a mechanical pure-refactor wave with strong snapshot-test coverage.
</threat_model>

<verification>
- `grep -c 'style:\s*"' src/main.js` returns 0
- `grep -c '?v=53' index.html` returns 3
- `grep -c 'Phase 10 Wave 1 (HOST-07)' styles.css` returns 1
- `npm test -- --run` (full suite — 376+ tests) green
- View snapshot tests at tests/__snapshots__/views/{dashboard,diagnostic,report}.html show zero drift
</verification>

<success_criteria>
- HOST-07 inline-style sweep prerequisite closed: src/main.js has zero static `style: "..."` h()-attributes
- styles.css has a new end-of-file utility-class block with header comment citing "Phase 10 Wave 1 (HOST-07)"
- index.html cache-busting bumped to `?v=53`
- All Phase 2 view snapshot tests pass without snapshot updates (any update = DOM drift = task failure)
- Full Vitest root suite green
- Phase 4 sub-wave 4.1 inline-style cleanup-ledger row is closeable in Plan 10-05
</success_criteria>

<output>
After completion, create `.planning/phases/10-csp-tightening-second-sweep/10-01-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Summary must include:
- Final `grep -c 'style:\s*"' src/main.js` count (must be 0)
- Total unique utility classes minted in styles.css
- Total semantic compound classes minted (e.g. `.card-empty-text`, `.alert-banner-flex`)
- Confirmation that all 3 cache-buster sites bumped to `?v=53`
- Confirmation that Phase 2 view snapshot tests passed without snapshot updates
- Commit SHA(s) for the wave
</output>
</content>
</invoke>