---
phase: 10-csp-tightening-second-sweep
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - .planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md
  - runbooks/phase-10-csp-soak-bootstrap.md
autonomous: false
requirements: [HOST-07]
tags: [csp, soak, operator-paced, cloud-logging, pitfall-16-stage-b]

must_haves:
  truths:
    - "runbooks/phase-10-csp-soak-bootstrap.md exists with the gcloud logging soak-observation query + interpretation guide"
    - ".planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md exists with a ## Soak Log section ready for operator-fill"
    - "Tightened CSP-RO is live in production (`curl -sI https://baselayers.bedeveloped.com` shows `content-security-policy-report-only: ... style-src 'self'; ... https://de.sentry.io; ... frame-src 'self'; ...`)"
    - "10-PREFLIGHT.md Soak Log Day 0 row carries the deploy commit SHA + timestamp (selective deploy per Pitfall 8 — `firebase deploy --only hosting --project bedeveloped-base-layers`)"
    - "Cloud Logging filter `severity=WARNING jsonPayload.message=\"csp.violation\"` returns ZERO new violations from non-extension origins across the 7-day window (extension noise filtered by cspReportSink, Phase 3 substrate)"
    - "10-PREFLIGHT.md Soak Log Day 7 row is marked CLEAN — Wave 4 (Plan 10-04) is authorized to run"
    - "Any triage-and-restart cycles within the soak window are documented in Soak Log rows with root-cause + fix-commit-SHA + restart-Day-0 timestamp (substrate-honest per Pitfall 19)"
  artifacts:
    - path: "runbooks/phase-10-csp-soak-bootstrap.md"
      provides: "Operator runbook — selective hosting deploy + daily gcloud logging soak query + violation triage decision tree"
      min_lines: 120
    - path: ".planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md"
      provides: "Phase 10 preflight log with ## Soak Log table — operator-fill template for 7-day daily checks"
      contains: "## Soak Log"
  key_links:
    - from: "runbooks/phase-10-csp-soak-bootstrap.md"
      to: "functions/src/csp/cspReportSink.ts (Phase 3 substrate, READ-ONLY)"
      via: "Cloud Logging filter `resource.labels.service_name=\"cspreportsink\" AND severity=WARNING AND jsonPayload.message=\"csp.violation\"`"
      pattern: "cspreportsink"
    - from: "10-PREFLIGHT.md ## Soak Log"
      to: "Wave 4 (Plan 10-04) enforcement flip"
      via: "Soak Log Row E (Day 7 clean) gates Wave 4 cutover authorization"
      pattern: "Day 7"
---

<objective>
Operator-paced wave: deploy Wave 2's tightened CSP-RO to production via selective `firebase deploy --only hosting`, then run a 7-day calendar soak observing the `cspReportSink` Cloud Logging stream for new violations. This is Stage B of Pitfall 16's three-stage CSP rollout (Phase 3 ran Stage A with `'unsafe-inline'`; Wave 4 will run Stage C — enforced). Wave 3 produces two artifacts (an operator runbook + a preflight log with a Soak Log table) and gates Wave 4 on a clean 7-day window.

Purpose: Phase 3 soaked under permissive style-src — that soak data does NOT predict what fires when `'unsafe-inline'` is removed. Wave 3 produces fresh observability with the tightened shape before any enforcement flip. If violations appear, root-cause them (most likely culprits: missed inline-style site from Wave 1, Sentry sub-host ingest origin per 10-RESEARCH.md A4, or an unexpected popup-flow trigger) and restart the soak — calendar slip but never a phase failure.

Output: The operator runbook + preflight log + a documented clean 7-day soak that authorises Wave 4 (Plan 10-04) to flip the enforcement key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-csp-tightening-second-sweep/10-CONTEXT.md
@.planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md
@.planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md
@.planning/phases/10-csp-tightening-second-sweep/10-VALIDATION.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@firebase.json
@functions/src/csp/cspReportSink.ts
@runbooks/hosting-cutover.md
@runbooks/phase-9-deploy-checkpoint.md

<interfaces>
<!-- Phase 3 cspReportSink soak filter — verified working since 2026-05-07 -->

```bash
gcloud logging read \
  'resource.type="cloud_run_revision"
   AND resource.labels.service_name="cspreportsink"
   AND severity=WARNING
   AND jsonPayload.message="csp.violation"
   AND jsonPayload.report.disposition="report"' \
  --project=bedeveloped-base-layers \
  --limit=50 \
  --format=json \
  --freshness=24h
```

Note: `service_name` is `cspreportsink` (lowercase, no hyphen) — Firebase 2nd-gen lowercases function names when minting Cloud Run services. Verified at runbooks/hosting-cutover.md:69-73.

<!-- Pitfall 8 selective-deploy guard — Phase 7 substrate, reused unchanged -->

```bash
firebase deploy --only hosting --project bedeveloped-base-layers
```

DO NOT run `firebase deploy --only functions:cspReportSink` — Phase 10 leaves cspReportSink READ-ONLY (FN-04-pinned to csp-sink-sa with bound invoker permissions that selective full-functions deploy would disturb).
</interfaces>

<known_failure_modes>
Per 10-RESEARCH.md §Pitfall 16 "Warning signs during soak":

- `effectiveDirective="connect-src"` + `blockedUri` starts `https://de.sentry.io` or `https://o<orgId>.ingest.de.sentry.io` → Sentry submission origin mismatch (10-RESEARCH.md A4). Fix: update connect-src to the actual ingest host; redeploy; restart 7-day window.
- `effectiveDirective="style-src"` + `blockedUri="inline"` → Wave 1 missed a site OR a new `style:` attribute was added since Wave 1. Fix: grep src/main.js + any other src/* file; migrate; redeploy; restart soak.
- `violatedDirective="frame-src"` → unexpected popup-flow trigger. Fix: investigate the source (likely a federated OAuth feature added since Wave 1 verification); decision tree — either restore firebaseapp.com to frame-src OR remove the popup feature; redeploy; restart soak.
- Extension-noise violations (chrome-extension://, moz-extension://) → already filtered by Phase 3's cspReportSink filter at functions/src/csp/filter.ts. If these still leak through, the filter has regressed (NOT a Phase 10 problem — would be a Phase 3 substrate failure).
</known_failure_modes>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author runbooks/phase-10-csp-soak-bootstrap.md (operator runbook for selective deploy + 7-day soak)</name>
  <files>runbooks/phase-10-csp-soak-bootstrap.md, .planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md</files>
  <read_first>
    - runbooks/phase-9-monitors-bootstrap.md (Phase 9 — closest operator-runbook-with-stepwise-checks analog)
    - runbooks/phase-9-deploy-checkpoint.md (Phase 9 — Cutover Log tabular shape with PASS/FAIL/PENDING markers and Pitfall 19 substrate-honest disclosure pattern)
    - runbooks/hosting-cutover.md sections "Pre-cutover Smoke Checklist" + "Cutover Steps" (Phase 3 — analog for header-verification curl smokes)
    - .planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md §File 5 "runbooks/csp-enforcement-cutover.md" (Wave 4 sibling — DO NOT confuse with Wave 3; this Wave 3 runbook is a SEPARATE deliverable)
    - functions/src/csp/cspReportSink.ts (Phase 3 — to understand what the soak monitor actually logs)
  </read_first>
  <behavior>
    - runbooks/phase-10-csp-soak-bootstrap.md is a single-operator-session deploy + multi-day soak observation runbook (~120-200 lines)
    - The runbook has these sections in order: Header / Pre-conditions / Step 1 (selective deploy) / Step 2 (post-deploy header verification via curl) / Step 3 (daily soak observation — runs once per day for 7 days) / Step 4 (Day 7 close-out decision) / Failure-mode triage / Citations
    - Step 1 uses `firebase deploy --only hosting` (NOT a full deploy; Pitfall 8 reminder block present)
    - Step 2 uses `curl -sI https://baselayers.bedeveloped.com` and confirms the new directive substring `style-src 'self';` (no `'unsafe-inline'`) AND `https://de.sentry.io` in connect-src AND `frame-src 'self'` — all in the `content-security-policy-report-only:` response header (key NOT yet flipped)
    - Step 3 is the gcloud logging read pattern (verified-working from Phase 3) parameterised at `--freshness=24h` per-day OR `--freshness=7d` for the close-out check
    - Step 4 (Day 7 close-out) has a binary gate: ZERO new violations from non-extension origins → soak clean → Wave 4 authorised. ANY violations → triage section + restart clock.
    - The Failure-mode triage section lists the three known failure modes from 10-RESEARCH.md §Pitfall 16 with decision trees (Sentry sub-host, missed inline-style, unexpected popup)
    - A 10-PREFLIGHT.md skeleton lands alongside with a Soak Log table (operator-fill rows for Day 0 deploy + Day 1-7 daily check + Day 7 close-out)
  </behavior>
  <action>
    1. Create runbooks/phase-10-csp-soak-bootstrap.md with the following section structure (paste-ready scaffolding; operator fills in actual timestamps):

       ```markdown
       # Phase 10 — CSP-RO Tightened Soak Bootstrap (Operator Runbook)

       > Phase: 10 — CSP Tightening (Second Sweep)
       > Wave: 3 (Plan 10-03)
       > Requirement: HOST-07 (Stage B of Pitfall 16 — Report-Only with TIGHTENED directives before enforcement flip)
       > Date authored: 2026-05-10
       > Objective: Deploy Wave 2's tightened CSP-RO + run a 7-day calendar soak that returns zero new csp.violation events from production traffic before Wave 4 (Plan 10-04) flips the enforcement key.

       ## Pre-conditions
       - [ ] Plan 10-01 closed: `grep -c 'style:\s*"' src/main.js` returns 0 (inline-style migration complete)
       - [ ] Plan 10-02 closed: `npm test -- --run firebase-config` returns 23 green (17 existing + 6 new Phase 10 assertions)
       - [ ] Local working tree is clean OR the Wave 2 commit is the HEAD of `main`
       - [ ] Operator has `gcloud auth login` completed for `bedeveloped-base-layers` project
       - [ ] Operator has `firebase login` valid for `bedeveloped-base-layers` project (or CI deploy job will handle it on push to main)

       ## Step 1: Selective hosting deploy (~5 min)
       Per Pitfall 8 (Phase 7 substrate): use selective deploy to avoid disturbing
       unrelated functions/hosting routes. The Phase 10 Wave 3 deploy is hosting-only:

       ```bash
       firebase deploy --only hosting --project bedeveloped-base-layers
       ```

       DO NOT use `firebase deploy --only functions` — Phase 10 leaves cspReportSink
       READ-ONLY (FN-04-pinned to csp-sink-sa per Phase 7 Wave 5 rebind).

       Record deploy timestamp + commit SHA in `## Soak Log` Day 0 row.

       ## Step 2: Post-deploy header verification (~2 min)

       ```bash
       curl -sI https://baselayers.bedeveloped.com | grep -i content-security
       ```

       Expected response line:
       - `content-security-policy-report-only: default-src 'self'; script-src 'self'; style-src 'self'; connect-src 'self' https://*.googleapis.com ... https://de.sentry.io; frame-src 'self'; ...`

       Verification checklist:
       - [ ] Header KEY is `content-security-policy-report-only` (not enforced yet)
       - [ ] `style-src 'self'` present; no `'unsafe-inline'`
       - [ ] `https://de.sentry.io` in connect-src
       - [ ] `frame-src 'self'` (no `bedeveloped-base-layers.firebaseapp.com`)

       If any item fails, STOP. Investigate (likely a deploy timing or cache issue;
       re-run `firebase deploy --only hosting` and retry curl after 30s).

       ## Step 3: Daily soak observation (run once per day for 7 days)

       ```bash
       gcloud logging read \
         'resource.type="cloud_run_revision"
          AND resource.labels.service_name="cspreportsink"
          AND severity=WARNING
          AND jsonPayload.message="csp.violation"
          AND jsonPayload.report.disposition="report"' \
         --project=bedeveloped-base-layers \
         --limit=50 \
         --format=json \
         --freshness=24h
       ```

       Expected output during clean soak: empty array `[]`.

       If output is non-empty:
       1. Read each entry's `jsonPayload.report` object
       2. Classify by `effectiveDirective` (style-src / connect-src / frame-src / ...) and `blockedUri`
       3. Cross-reference with §Failure-Mode Triage below
       4. Fix root cause (code change + redeploy + restart 7-day clock from Day 0)

       Record each daily check in `## Soak Log` Day N row with the count
       (0 or >0) + a one-line summary.

       ## Step 4: Day 7 close-out (~5 min)

       Run the soak query with `--freshness=7d`:

       ```bash
       gcloud logging read \
         'resource.type="cloud_run_revision"
          AND resource.labels.service_name="cspreportsink"
          AND severity=WARNING
          AND jsonPayload.message="csp.violation"
          AND jsonPayload.report.disposition="report"' \
         --project=bedeveloped-base-layers \
         --limit=200 \
         --format=json \
         --freshness=7d
       ```

       Decision gate:
       - **EMPTY response (zero violations across all 7 days)** → soak clean → mark `## Soak Log` Day 7 row PASS → Wave 4 (Plan 10-04) authorised
       - **ANY violations from non-extension origins** → triage per §Failure-Mode Triage; FIX → REDEPLOY → RESTART 7-day clock from Day 0; update Soak Log with restart-reason

       ## Failure-Mode Triage

       (Sourced from 10-RESEARCH.md §Pitfall 16 + §Open Question 2 §A4)

       | Symptom (jsonPayload.report.*) | Likely Cause | Fix |
       |-----|------|-----|
       | `effectiveDirective="connect-src"` + `blockedUri` starts `https://de.sentry.io` | Sentry SDK posts to project-specific subdomain (e.g. `o<orgId>.ingest.de.sentry.io`), not plain `de.sentry.io` | (a) Capture exact origin from browser DevTools Network tab on a forced error; (b) update firebase.json connect-src to include the precise ingest host (e.g. add `https://o<orgId>.ingest.de.sentry.io` or use a wildcard `https://*.ingest.de.sentry.io`); (c) re-run Plan 10-02 schema test; (d) `firebase deploy --only hosting`; (e) restart 7-day soak |
       | `effectiveDirective="style-src"` + `blockedUri="inline"` | Wave 1 missed an inline-style site OR a new `style:` attribute landed since Plan 10-01 | (a) `grep -rn 'style:\s*"' src/`; (b) classify + mint utility class (or extend Plan 10-01 block in styles.css); (c) deploy; (d) restart soak |
       | `violatedDirective="frame-src"` + non-`'self'` blockedUri | An unexpected popup-flow trigger (federated OAuth?) added since the Wave 2 verification | (a) Identify the feature (recent commits + `grep signInWithPopup\|signInWithRedirect src/`); (b) operator decision — restore `https://bedeveloped-base-layers.firebaseapp.com` to frame-src OR remove the popup feature; (c) deploy; (d) restart soak |
       | chrome-extension:// or moz-extension:// blockedUri | Browser extension noise — should already be filtered by Phase 3 cspReportSink filter | If these leak through, Phase 3's `functions/src/csp/filter.ts` has regressed. NOT a Phase 10 issue. Open a forward-tracking row in `runbooks/phase-10-cleanup-ledger.md` and proceed (does not block soak clean status) |

       ## Citations
       - HOST-07 (REQUIREMENTS.md) — Strict CSP rolled to enforced + style-src 'self'
       - Pitfall 8 (PITFALLS.md) — Selective deploy discipline
       - Pitfall 16 (PITFALLS.md) — Three-stage CSP rollout pattern
       - functions/src/csp/cspReportSink.ts — Phase 3 substrate (READ-ONLY here)
       - 10-RESEARCH.md §Pitfall 16 + §A4 (Sentry origin assumption)
       ```

    2. Create .planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md with a Soak Log table skeleton:

       ```markdown
       ---
       phase: 10
       wave: 3
       owner: operator
       status: pending-soak
       ---

       # Phase 10 — Preflight Log

       ## Soak Log

       > Operator-fills as the 7-day soak progresses. Substrate-honest: leave
       > `{{ ... }}` placeholders for not-yet-run rows so the audit trail of what
       > is still pending stays explicit (Pitfall 19).

       | Day | Date (UTC)   | Action / Check                                                  | Violations Count | Status                     | Evidence / Notes                          |
       |-----|--------------|-----------------------------------------------------------------|------------------|----------------------------|-------------------------------------------|
       | 0   | `{{ DATE }}` | `firebase deploy --only hosting` (Wave 2 commit SHA `{{ SHA }}`) | n/a              | `{{ PASS / FAIL }}`        | `curl -sI` showed tightened CSP-RO        |
       | 1   | `{{ DATE }}` | `gcloud logging read ... --freshness=24h`                       | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 2   | `{{ DATE }}` | same                                                            | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 3   | `{{ DATE }}` | same                                                            | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 4   | `{{ DATE }}` | same                                                            | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 5   | `{{ DATE }}` | same                                                            | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 6   | `{{ DATE }}` | same                                                            | `{{ N }}`        | `{{ PASS / TRIAGE }}`      | `{{ summary or empty }}`                  |
       | 7   | `{{ DATE }}` | Close-out: `--freshness=7d`                                     | `{{ N }}`        | `{{ CLEAN / RESTART }}`    | Authorises Wave 4 (Plan 10-04) if CLEAN   |

       ## Wave 3 Close Decision

       - [ ] Soak Log Day 7 row marks `CLEAN`
       - [ ] Wave 4 (Plan 10-04) checkpoint is now operator-runnable

       OR (restart case):

       - [ ] Soak surfaced violations on Day N; root cause classified per `runbooks/phase-10-csp-soak-bootstrap.md` §Failure-Mode Triage; fix landed via micro-plan / sub-wave 10.x; soak restarted on Day 0 fresh
       ```
    3. Run `npm test -- --run firebase-config` to confirm Wave 2 schema test remains green (sanity — Task 1 is documentation, not code).
    4. The operator runs the deploy + 7-day soak per the runbook AFTER the documentation lands. This task closes when the runbook + preflight exist; the actual soak observation is captured by Task 2's checkpoint.
  </action>
  <verify>
    <automated>npm test -- --run firebase-config</automated>
    <manual>
      - `runbooks/phase-10-csp-soak-bootstrap.md` exists and is at least 120 lines
      - The runbook contains a `## Step 1: Selective hosting deploy` section with the exact `firebase deploy --only hosting --project bedeveloped-base-layers` command
      - The runbook contains a `## Step 3: Daily soak observation` section with the gcloud logging read command
      - The runbook contains a `## Failure-Mode Triage` section with at least 3 documented failure-mode rows
      - `.planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md` exists and contains a `## Soak Log` table with 9 rows (Day 0 + Day 1-7 + Day 7 close-out)
      - Vitest schema test green (no firebase.json drift)
    </manual>
  </verify>
  <done>
    Operator runbook + preflight log exist with paste-ready commands. Operator can run Step 1-4 end-to-end without further guidance. Failure-mode triage covers the three known concerns from 10-RESEARCH.md (Sentry sub-host A4, missed inline-style, popup-flow surprise).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator session — deploy + 7-day soak + close-out</name>
  <what-built>
    Plan 10-01 (Wave 1) closed the inline-style prerequisite. Plan 10-02 (Wave 2) tightened the firebase.json CSP-RO value + extended the schema test. Task 1 above authored the operator runbook + preflight log. This checkpoint is the operator-paced execution: deploy Wave 2 to production + observe the cspReportSink soak stream daily for 7 calendar days + decide whether to authorise Wave 4.
  </what-built>
  <how-to-verify>
    1. **Deploy (~5 min):**
       - Open `runbooks/phase-10-csp-soak-bootstrap.md` Step 1
       - Run `firebase deploy --only hosting --project bedeveloped-base-layers`
       - Record commit SHA + deploy timestamp in `10-PREFLIGHT.md ## Soak Log` Day 0 row

    2. **Post-deploy header verification (~2 min):**
       - Open `runbooks/phase-10-csp-soak-bootstrap.md` Step 2
       - Run `curl -sI https://baselayers.bedeveloped.com | grep -i content-security`
       - Confirm: header key is `content-security-policy-report-only`; value has `style-src 'self'` (no `'unsafe-inline'`); value has `https://de.sentry.io`; value has `frame-src 'self'`

    3. **Daily soak (7 calendar days; ~5 min/day):**
       - Each day, run the Step 3 gcloud logging query with `--freshness=24h`
       - Record count + status in the Soak Log row for that day
       - If non-zero violations appear, consult §Failure-Mode Triage in the runbook; the soak is NOT clean — fix + redeploy + restart Day 0 fresh

    4. **Day 7 close-out (~10 min):**
       - Run Step 4 with `--freshness=7d`
       - If response is empty (zero violations from non-extension origins across all 7 days) → mark Soak Log Day 7 row `CLEAN` and authorise Wave 4 (Plan 10-04)
       - If any violations remain → restart Day 0 with a fix; this checkpoint stays open until clean

    Expected outcome: A CLEAN 7-day window, authorising Wave 4. Triage-and-restart cycles are acceptable Pitfall-16 expected behavior, not phase failure.
  </how-to-verify>
  <resume-signal>
    Operator types "soak clean — authorise Wave 4" once Soak Log Day 7 row marks CLEAN, OR describes the restart cycle that closed before resuming.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Firebase Hosting | `firebase deploy --only hosting` pushes Wave 2's firebase.json to production CDN edge |
| production CDN edge → browser → cspReportSink | Real users' violation reports stream to the Phase 3 sink |
| operator ← Cloud Logging | gcloud logging read returns the structured violation events for triage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-1 | Tampering | selective-deploy discipline | mitigate | Operator runbook Step 1 hard-codes `firebase deploy --only hosting`; explicit DO-NOT-USE line for `--only functions:cspReportSink`. Pitfall 8 substrate. |
| T-10-03-2 | Denial of Service | Wave 4 prematurely authorised | mitigate | Soak Log Day 7 close-out decision gate requires explicit "CLEAN" status before Wave 4 is operator-runnable. Substrate-honest disclosure (Pitfall 19) — empty rows stay marked `{{ ... }}` until filled. |
| T-10-03-3 | Information Disclosure | gcloud logging query exposes blockedUri values | accept | blockedUri may contain user-agent or page URL; jsonPayload is logged at WARNING severity; no PII per Phase 9 OBS-01 PII scrubber substrate. Operator IAM gates Cloud Logging access. |
| T-10-03-4 | Denial of Service | 7-day calendar slip from violations | accept | Pitfall 16's expected behavior — Stage B exists precisely because the permissive Stage A soak cannot predict tightened-shape violations. Triage + restart is the design, not the failure mode. |
| T-10-03-5 | Repudiation | operator skips daily checks | mitigate | Soak Log table has 7 daily rows that MUST be filled; empty rows are visible audit trail. `/gsd-progress` will surface Wave 3 as pending until close-out row is filled. |

**Severity:** All threats LOW or MEDIUM. No high-severity — this is observability, not enforcement; rollback is not even needed because nothing is being enforced.
</threat_model>

<verification>
- `runbooks/phase-10-csp-soak-bootstrap.md` exists with all 4 numbered steps + Failure-Mode Triage section
- `.planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md` exists with a 9-row Soak Log table
- `npm test -- --run firebase-config` green (sanity — Wave 2 invariants preserved)
- Operator checkpoint resolves with "soak clean" signal OR documented restart cycle
- Soak Log Day 7 row marks CLEAN before this wave closes
</verification>

<success_criteria>
- Plan 10-02's tightened CSP-RO is deployed to production via selective deploy (Pitfall 8 honored)
- `curl -sI https://baselayers.bedeveloped.com` confirms the new directive shape on the wire
- 7-day Cloud Logging soak returns ZERO new violations from non-extension origins
- Soak Log Day 7 marked CLEAN → Wave 4 (Plan 10-04) authorised
- Any triage-and-restart cycles are documented in the Soak Log with root-cause + fix
</success_criteria>

<output>
After completion, create `.planning/phases/10-csp-tightening-second-sweep/10-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Summary must include:
- The deploy commit SHA + timestamp (from Soak Log Day 0)
- Total daily-check count (7 days OR 7 days × N restarts if cycles occurred)
- Final Day 7 violation count (must be 0 across all non-extension origins)
- Any triage incidents — root cause + fix + restart-reason
- Final Soak Log status: CLEAN
- Commit SHA for the runbook + preflight log authoring task
</output>
</content>
</invoke>