---
phase: 10-csp-tightening-second-sweep
plan: 05
type: execute
wave: 5
depends_on: [10-04]
files_modified:
  - runbooks/hsts-preload-submission.md
  - runbooks/phase-10-cleanup-ledger.md
  - SECURITY.md
  - REQUIREMENTS.md
  - .planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md
autonomous: false
requirements: [HOST-06, HOST-07, DOC-10]
tags: [hsts-preload, securityheaders, security-md, audit-index, cleanup-ledger, phase-close]

must_haves:
  truths:
    - "runbooks/hsts-preload-submission.md exists with the hstspreload.org operator submission flow + apex-vs-subdomain decision tree + status-verification commands"
    - "Operator submits `baselayers.bedeveloped.com` to hstspreload.org (subdomain-only per 10-RESEARCH.md A2 recommendation; apex submission decision recorded explicitly in runbook)"
    - "Operator captures securityheaders.com A+ rating screenshot at `docs/evidence/phase-10-securityheaders-rating.png`"
    - "Operator captures hstspreload.org submission confirmation screenshot at `docs/evidence/phase-10-hsts-preload-submission.png`"
    - "SECURITY.md gains a new `## § Content Security Policy (enforced)` section (replaces Phase 3's `## § Content Security Policy (Report-Only)`) + new `## § HSTS Preload Status` section + new `## § Phase 10 Audit Index` 3-row table"
    - "REQUIREMENTS.md Traceability rows for HOST-07 + HOST-06 flip to `Validated YYYY-MM-DD`; DOC-10 row gains Phase 10 increment entry"
    - "runbooks/phase-10-cleanup-ledger.md zero-out gate closes Phase 4 sub-wave 4.1 inline-style row + closes Phase 6+7 forward-tracking row (drop firebaseapp.com from frame-src); forward-tracks the hstspreload.org listing-status row (weeks-months) and post-enforcement 7-day soak completion if not already PASS"
    - "10-PREFLIGHT.md Cutover Log rows D + E close (D = A+ rating; E = post-enforcement 7-day soak clean)"
  artifacts:
    - path: "runbooks/hsts-preload-submission.md"
      provides: "Operator runbook — hstspreload.org submission flow + apex/subdomain decision tree + status check commands + Cutover Log"
      min_lines: 100
    - path: "runbooks/phase-10-cleanup-ledger.md"
      provides: "Phase 10 cleanup ledger — in-phase rows zero-out + forward-tracking rows (hstspreload listing weeks-months)"
      contains: "phase_10_active_rows: 0"
    - path: "SECURITY.md"
      provides: "Phase 10 DOC-10 increment — 3 new sections (§ CSP enforced + § HSTS Preload Status + § Phase 10 Audit Index)"
      contains: "Phase 10 Audit Index"
    - path: "REQUIREMENTS.md"
      provides: "Traceability table HOST-07 + HOST-06 + DOC-10 rows updated to Validated"
      contains: "HOST-07"
  key_links:
    - from: "SECURITY.md § Phase 10 Audit Index"
      to: "runbooks/csp-enforcement-cutover.md + runbooks/hsts-preload-submission.md + docs/evidence/phase-10-*.png"
      via: "3-row Pattern G table with framework citations (ASVS V14.4, ISO A.8.23/A.13.1, SOC 2 CC6.6/CC6.7, GDPR Art.32)"
      pattern: "HOST-07|HOST-06|DOC-10"
    - from: "REQUIREMENTS.md HOST-07 + HOST-06 + DOC-10 rows"
      to: "Phase 10 commits"
      via: "Validated 2026-MM-DD annotation with commit refs"
      pattern: "Validated"
  user_setup:
    - service: hstspreload.org
      why: "HSTS preload list inclusion (HOST-06) — only path is the third-party Chrome-curated preload list submission web form"
      dashboard_config:
        - task: "Submit baselayers.bedeveloped.com via the web form; the form auto-checks live HSTS header against the URL"
          location: "https://hstspreload.org"
        - task: "Verify listing status periodically (weeks-months for inclusion in Chrome preload cache)"
          location: "https://hstspreload.org/?domain=baselayers.bedeveloped.com"
    - service: securityheaders.com
      why: "A+ rating verification (HOST-07 SC#4) — third-party scanner produces the auditable artefact"
      dashboard_config:
        - task: "Run scan + capture rating screenshot"
          location: "https://securityheaders.com/?q=baselayers.bedeveloped.com&followRedirects=on"
---

<objective>
Close Phase 10 with the operator-paced HSTS preload submission + securityheaders.com A+ rating capture, then ship the DOC-10 SECURITY.md increment + Audit Index + cleanup-ledger zero-out. This wave covers HOST-06 (the only Phase 10 requirement still open after Plan 10-04) + finalises HOST-07 SC#4 (A+ rating verification) + finalises HOST-07 SC#2 (7-day post-enforcement clean soak) + lands the DOC-10 documentation pass.

Purpose: Final closing step of Phase 10. The HSTS preload submission is operator-paced (web form only), and inclusion takes weeks-months — Phase 10 closes with `PENDING` on the listing-status row + a forward-tracking entry in the cleanup ledger. The autonomous portion of this wave is documentation: SECURITY.md sections + REQUIREMENTS.md traceability + cleanup ledger.

Output: HOST-07 + HOST-06 + DOC-10 Phase 10 increment all closed (or substrate-complete-pending where calendar-deferred, matching Phase 8 BACKUP + Phase 9 OBS patterns). Phase 10 close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-csp-tightening-second-sweep/10-CONTEXT.md
@.planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md
@.planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md
@.planning/phases/10-csp-tightening-second-sweep/10-VALIDATION.md
@.planning/phases/10-csp-tightening-second-sweep/10-PREFLIGHT.md
@CLAUDE.md
@firebase.json
@SECURITY.md
@REQUIREMENTS.md
@runbooks/phase-9-cleanup-ledger.md
@runbooks/phase-9-monitors-bootstrap.md
@runbooks/phase-8-cleanup-ledger.md
@runbooks/phase-4-cleanup-ledger.md

<interfaces>
<!-- Phase 9 SECURITY.md Audit Index pattern (analog per 10-PATTERNS.md §File 7 — verbatim shape, swap rows) -->

From SECURITY.md:1177-1201 (Phase 9 Audit Index):
```markdown
## § Phase 9 Audit Index

Auditor walk-through pointer for Phase 9. Each row maps a Phase 9 control to its
requirement ID, the code/config that implements it, the test or operator evidence
that verifies it, and the framework citations it satisfies.

| Requirement | Control | Code | Test / Evidence | Framework |
|-------------|---------|------|-----------------|-----------|
| OBS-01 | ... | ... | ... | ASVS ... |
| ... |
```

<!-- 10-PATTERNS.md §File 7 paste-ready Phase 10 Audit Index table -->

Phase 10 Audit Index target shape (3 rows — HOST-07, HOST-06, DOC-10):

```markdown
| Requirement | Control | Code | Test / Evidence | Framework |
|-------------|---------|------|-----------------|-----------|
| HOST-07 | CSP enforced (no Report-Only) + style-src 'self' + frame-src 'self' + base-uri 'self' + form-action 'self' | `firebase.json` `hosting.headers[0]` Content-Security-Policy + `src/main.js` + `styles.css` utility classes | `tests/firebase-config.test.js` 6+1 Phase 10 assertions; `runbooks/csp-enforcement-cutover.md` (operator); `docs/evidence/phase-10-securityheaders-rating.png` (A+) | ASVS V14.4; ISO A.8.23 + A.13.1; SOC 2 CC6.6; GDPR Art. 32(1)(b) |
| HOST-06 | HSTS preload submitted to hstspreload.org for baselayers.bedeveloped.com | `firebase.json` Strict-Transport-Security (preload directive present since Phase 3) | `tests/firebase-config.test.js` HSTS-preload-eligibility assertion; `runbooks/hsts-preload-submission.md` Step 2; `docs/evidence/phase-10-hsts-preload-submission.png` | ASVS V14.4; GDPR Art. 32(1)(a); SOC 2 CC6.7 |
| DOC-10 | Phase 10 incremental SECURITY.md (§ CSP enforced + § HSTS Preload Status + Phase 10 Audit Index) | This file | this commit; Phase 11 owns canonical DOC-10 pass | ISO A.5.36 |
```

<!-- Cleanup-ledger zero-out gate pattern (analog: runbooks/phase-9-cleanup-ledger.md) -->

The phase-N cleanup ledger has a YAML frontmatter with `phase_N_active_rows: 0` as the close gate. Each in-phase row is either closed (struck through) OR forward-tracked (moved to a "Forward-Tracking" section with the receiving-phase target). The hstspreload.org listing status (weeks-months calendar wait) is forward-tracked, not in-phase.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author runbooks/hsts-preload-submission.md (operator runbook for hstspreload.org submission)</name>
  <files>runbooks/hsts-preload-submission.md</files>
  <read_first>
    - runbooks/phase-9-monitors-bootstrap.md (Phase 9 — closest "operator visits SaaS web UI" analog per 10-PATTERNS.md §File 6)
    - .planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md §File 6 "runbooks/hsts-preload-submission.md (NEW — Wave 5 deliverable)"
    - .planning/phases/10-csp-tightening-second-sweep/10-RESEARCH.md §HSTS Preload Submission Procedure (full eligibility checklist + decision tree)
    - firebase.json line 9 — verify HSTS header is `max-age=63072000; includeSubDomains; preload`
  </read_first>
  <behavior>
    - runbooks/hsts-preload-submission.md is a short single-operator-session runbook (~100-150 lines, smaller than the cutover runbook — single web form + check)
    - Sections: Header / Pre-conditions / Step 1 (verify live HSTS header via curl) / Step 2 (apex-vs-subdomain decision) / Step 3 (submit at hstspreload.org) / Step 4 (capture confirmation + screenshot) / Step 5 (periodic status check — calendar-deferred) / Cutover Log (3-row tabular form per 10-PATTERNS.md §Shared Patterns §Operator-fill ## Cutover Log)
    - Step 2 is an explicit operator decision: subdomain-only (`baselayers.bedeveloped.com`) is the planner-recommended path per 10-RESEARCH.md A2; apex (`bedeveloped.com` + `includeSubDomains`) is documented as the alternative + the risk (irreversible-for-other-subdomains)
    - Step 5 (listing-status check) is calendar-deferred (weeks-months) and forward-tracked to runbooks/phase-10-cleanup-ledger.md as a PENDING row that closes when the listing flips to `preloaded`
  </behavior>
  <action>
    1. Create runbooks/hsts-preload-submission.md with the following content:

       ```markdown
       # Phase 10 — HSTS Preload Submission (Operator Runbook)

       > Phase: 10 — CSP Tightening (Second Sweep)
       > Wave: 5 (Plan 10-05)
       > Requirement: HOST-06 (HSTS preload submitted to hstspreload.org once policy stable for >= 7 days)
       > Date authored: 2026-05-10
       > Objective: Single-operator-session procedure that submits `baselayers.bedeveloped.com` to the Chrome HSTS preload list at hstspreload.org. Operator-paced because the form is web-only and inclusion in browser preload caches takes weeks-months after submission.

       ## Pre-conditions
       - [ ] Plan 10-04 closed: CSP is enforced (`curl -sI` shows `content-security-policy:` without `-report-only` suffix)
       - [ ] 7-day post-enforcement soak shows zero new violations (per HOST-07 SC#2; tracked in Wave 4 Cutover Log Row E and finalised in Task 3 below before this task runs)
       - [ ] `firebase.json` HSTS header has `max-age=63072000; includeSubDomains; preload` (Phase 3 substrate; verified by `tests/firebase-config.test.js` Phase 10 HSTS-preload-eligibility assertion)
       - [ ] Operator confirms `bedeveloped.com` apex domain decision per Step 2 below

       ## Step 1: Verify live HSTS header (~1 min)

       ```bash
       curl -sI https://baselayers.bedeveloped.com | grep -i strict-transport-security
       ```

       Expected: `strict-transport-security: max-age=63072000; includeSubDomains; preload`

       If `preload` token absent OR `max-age < 31536000`, STOP — firebase.json is misconfigured. Investigate (likely a Plan 10-04 rollback that also touched HSTS — unlikely but possible).

       ## Step 2: Apex-vs-subdomain decision (~5 min — operator decision)

       Per 10-RESEARCH.md §HSTS Preload Submission Procedure §Submission decision tree + Open Question 1:

       | Option | Pros | Cons | Recommended? |
       |--------|------|------|--------------|
       | Submit subdomain `baselayers.bedeveloped.com` only | Bounded scope; reversible with effort; doesn't commit other subdomains | None for our use case | **YES — default** |
       | Submit apex `bedeveloped.com` + `includeSubDomains` | Covers all subdomains in one shot | IRREVERSIBLE — every current and future subdomain of bedeveloped.com is forced to HTTPS forever; would break email, marketing site, any service that doesn't already support HTTPS | NO — too broad |

       Operator decision (tick one):
       - [ ] Subdomain-only (`baselayers.bedeveloped.com`) — planner-recommended
       - [ ] Apex+includeSubDomains (`bedeveloped.com`) — chose explicitly, accepting the irreversibility

       Record decision in Cutover Log Row 1.

       ## Step 3: Visit hstspreload.org (~5 min)

       1. Open https://hstspreload.org in a browser
       2. Enter the chosen hostname in the form (`baselayers.bedeveloped.com` if subdomain-only)
       3. The site auto-checks the live header against the URL — wait for all green checks
       4. If all green, click "Submit"
       5. Capture confirmation email screenshot → save to `docs/evidence/phase-10-hsts-preload-submission.png`

       If any check fails:
       - Recheck Step 1 (live header)
       - If subdomain-only chosen, the form may complain about apex inconsistency — switch to subdomain-only is fine per Chrome's standard pattern; ignore the apex-inconsistency caveat IF apex serves HTTPS at minimum
       - If the apex is HTTP-only or unreachable, choose the subdomain-only path explicitly

       ## Step 4: Status verification (~2 min — same-session)

       ```bash
       curl https://hstspreload.org/api/v2/status?domain=baselayers.bedeveloped.com
       ```

       Expected JSON: `{"status": "pending", ...}` or `{"status": "in-pending-list", ...}` (depends on hstspreload.org's current state machine).

       Record in Cutover Log Row 2.

       ## Step 5: Periodic listing check (calendar-deferred — weeks to months)

       The Chrome preload list update cycle is weeks-months. Schedule:
       - Re-check status weekly via https://hstspreload.org/?domain=baselayers.bedeveloped.com
       - When status flips to `preloaded`, capture screenshot → save to `docs/evidence/phase-10-hsts-preload-listed.png`
       - Update `runbooks/phase-10-cleanup-ledger.md` Forward-Tracking row to CLOSED

       ## Cutover Log

       > Operator-fill template. Substrate-honest disclosure (Pitfall 19):
       > Step 3 status remains `PENDING` until Chrome propagates the listing.

       | Step | Action                                                            | T+0 (UTC)    | Result                       | Evidence                                                                 |
       |------|-------------------------------------------------------------------|--------------|------------------------------|--------------------------------------------------------------------------|
       | 1    | Verify live HSTS header + apex/subdomain decision recorded        | `{{ T+? }}`  | `{{ PASS / FAIL }}`         | curl output + decision-tree tick                                          |
       | 2    | hstspreload.org form submission for `{{ hostname }}`              | `{{ T+? }}`  | `{{ PASS / FAIL }}`         | `docs/evidence/phase-10-hsts-preload-submission.png` + email screenshot  |
       | 3    | hstspreload.org status = `preloaded` (Chrome propagation)         | `{{ T+? }}`  | `{{ PENDING / PASS }}`      | `docs/evidence/phase-10-hsts-preload-listed.png` (calendar-deferred)     |

       Note: Row 3 is `PENDING` at Phase 10 close — listing takes weeks-months and is forward-tracked in `runbooks/phase-10-cleanup-ledger.md`. Phase 10 closes with rows 1 + 2 PASS + row 3 PENDING (substrate-honest, mirrors Phase 8 BACKUP-07 drill-evidence-deferred pattern).

       ## Citations
       - HOST-06 (REQUIREMENTS.md)
       - 10-RESEARCH.md §HSTS Preload Submission Procedure + Open Question 1
       - https://hstspreload.org submission rules
       ```

    2. Run `npm test -- --run firebase-config` to confirm Wave 4 invariants remain green (sanity — Task 1 is documentation, not code).
  </action>
  <verify>
    <automated>npm test -- --run firebase-config</automated>
    <manual>
      - `runbooks/hsts-preload-submission.md` exists with all 5 numbered Steps + Cutover Log (3 rows)
      - File is at least 100 lines
      - The runbook contains an explicit apex-vs-subdomain decision tree at Step 2
      - The runbook contains the `curl https://hstspreload.org/api/v2/status?domain=...` status command
    </manual>
  </verify>
  <done>
    Operator runbook exists. Submission + screenshot capture + status verification are operator-runnable in a single session. Calendar-deferred listing status is documented as forward-tracking material.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator session — hstspreload.org submission + securityheaders.com A+ verification + 7-day post-enforcement soak close</name>
  <what-built>
    Task 1 above authored the HSTS preload submission runbook. This checkpoint is the operator-paced execution: complete the post-Plan-10-04 7-day soak (Cutover Log Row E), capture securityheaders.com A+ rating (Cutover Log Row D), and submit + verify HSTS preload (HOST-06 + new runbook Cutover Log rows 1+2).
  </what-built>
  <how-to-verify>
    1. **7-day post-enforcement soak (~5 min check at Day 7):**
       - Plan 10-04 Cutover Log Row B records the deploy timestamp; this Task 2 closes 7 calendar days later
       - Run:
         ```bash
         gcloud logging read \
           'resource.type="cloud_run_revision"
            AND resource.labels.service_name="cspreportsink"
            AND severity=WARNING
            AND jsonPayload.message="csp.violation"
            AND jsonPayload.report.disposition="enforce"' \
           --project=bedeveloped-base-layers \
           --limit=200 --format=json --freshness=7d
         ```
       - Expected: empty array `[]` from non-extension origins
       - If non-empty: triage per runbooks/phase-10-csp-soak-bootstrap.md §Failure-Mode Triage; this is enforcement-mode now, so violations are user-impacting — FIX immediately + REDEPLOY + restart 7-day clock OR rollback per Plan 10-04 Rollback Procedure
       - Record outcome in `10-PREFLIGHT.md ## Cutover Log` Row E

    2. **securityheaders.com A+ rating (~5 min):**
       - Visit https://securityheaders.com/?q=baselayers.bedeveloped.com&followRedirects=on
       - Expected rating: **A+** (vs Phase 3's A)
       - Capture screenshot → save to `docs/evidence/phase-10-securityheaders-rating.png`
       - If rating < A+, investigate (CSP must be enforced — not Report-Only; verify via Step 1 curl; if CSP looks correct, check whether browser hint headers like Permissions-Policy are still complete)
       - Record outcome in `10-PREFLIGHT.md ## Cutover Log` Row D

    3. **HSTS preload submission (~15 min):**
       - Follow runbooks/hsts-preload-submission.md Steps 1-4
       - Operator records the apex-vs-subdomain decision at Step 2 (subdomain-only recommended)
       - Capture submission confirmation screenshot → save to `docs/evidence/phase-10-hsts-preload-submission.png`
       - Record outcomes in the new runbook's own Cutover Log (rows 1 + 2 close to PASS; row 3 remains PENDING until weeks-months later)

    4. **Phase 10 PREFLIGHT Cutover Log close-out (~5 min):**
       - Open `10-PREFLIGHT.md`
       - Fill remaining Cutover Log rows D (A+ rating) + E (post-enforcement soak clean)
       - Confirm all 5 rows (A through E) marked PASS or, for any restart cycles, documented
       - Save

    Expected outcome: all 5 Cutover Log rows closed (A/B/C from Plan 10-04 + D/E from this task); hstspreload submission confirmation captured; A+ screenshot captured; 7-day enforcement soak clean.
  </how-to-verify>
  <resume-signal>
    Operator types "Wave 5 operator session complete — A+ captured, HSTS submitted (subdomain-only|apex), post-enforcement soak CLEAN" with paths to the two screenshot files.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: SECURITY.md DOC-10 Phase 10 increment + REQUIREMENTS.md traceability update + Phase 10 cleanup ledger zero-out</name>
  <files>SECURITY.md, REQUIREMENTS.md, runbooks/phase-10-cleanup-ledger.md</files>
  <read_first>
    - SECURITY.md lines 436-463 (the existing `## § Content Security Policy (Report-Only)` section being replaced — verify line numbers; section title from Phase 3)
    - SECURITY.md lines 1177-1201 (the Phase 9 Audit Index — copy structure verbatim per 10-PATTERNS.md §File 7)
    - SECURITY.md (search for "Phase 3 Audit Index" — likely around line 494-523 per 10-PATTERNS.md §File 7 §Maintenance update; if found, update the soak-window row to note Phase 10 closure)
    - REQUIREMENTS.md (search for HOST-07 + HOST-06 + DOC-10 — Traceability table around line 250+)
    - runbooks/phase-9-cleanup-ledger.md (analog for zero-out gate + forward-tracking pattern)
    - runbooks/phase-4-cleanup-ledger.md (find the "132 static style=" row + the row queueing the firebaseapp.com frame-src drop; both close in this task)
    - runbooks/phase-7-cleanup-ledger.md (find the "Phase 10 forward-tracking — drop firebaseapp.com" row if present; close)
    - .planning/phases/10-csp-tightening-second-sweep/10-PATTERNS.md §File 7 "SECURITY.md Phase 10 increment" (all three edit blocks A/B/C — paste-ready)
  </read_first>
  <behavior>
    - SECURITY.md gains/replaces 3 sections:
      - REPLACE `## § Content Security Policy (Report-Only)` → `## § Content Security Policy (enforced)` with the per-directive rationale table + soak evidence + framework citations (per 10-PATTERNS.md §File 7 Edit A)
      - INSERT new `## § HSTS Preload Status` section after § CSP enforced (per 10-PATTERNS.md §File 7 Edit B)
      - INSERT new `## § Phase 10 Audit Index` 3-row Pattern G table (per 10-PATTERNS.md §File 7 Edit C — HOST-07 + HOST-06 + DOC-10 rows)
    - SECURITY.md `## § Phase 3 Audit Index` (if present) gets a maintenance update: soak-window row notes "soak window CLOSED Phase 10 Wave 4"
    - REQUIREMENTS.md Traceability table rows for HOST-07 + HOST-06 + DOC-10:
      - HOST-07 + HOST-06 rows: Status changes from `Pending` to `Validated 2026-MM-DD (Phase 10 — Plan 10-01 + 10-02 + 10-04 + 10-05)` with cross-references; the v1 Requirements list flips `[x]` for HOST-07 + HOST-06
      - DOC-10 row: appends Phase 10 increment annotation (matches Phase 7/8/9 pattern)
    - runbooks/phase-10-cleanup-ledger.md is created with `phase_10_active_rows: 0` close-gate frontmatter; in-phase rows closed (style attrs migrated; firebaseapp.com dropped; Sentry connect-src added; CSP enforced; A+ rating captured); forward-tracking rows queued (hstspreload listing-status weeks-months calendar-deferred — Phase 11/v2; any open carry-forward from Phase 4 sub-wave 4.1 IIFE migration remains forward-tracked because Plan 10-01 only closed the inline-style portion, NOT the broader IIFE migration)
    - runbooks/phase-4-cleanup-ledger.md "132 static style=" row is marked CLOSED with cross-reference to Plan 10-01
    - runbooks/phase-7-cleanup-ledger.md (or phase-6 — wherever the row lives) Phase 10-forward-tracking "drop firebaseapp.com" row is marked CLOSED with cross-reference to Plan 10-02
  </behavior>
  <action>
    1. **SECURITY.md edits.** Open SECURITY.md.

       Edit A — REPLACE § CSP (Report-Only) section:
       - Locate `## § Content Security Policy (Report-Only)` (around line 436)
       - Read the section to its closing boundary (typically the next `## §` marker)
       - Replace the entire section with the new `## § Content Security Policy (enforced)` content from 10-PATTERNS.md §File 7 §Edit A (the new title + control narrative + per-directive table + soak-evidence paragraph + Evidence list + Framework citations)

       Edit B — INSERT § HSTS Preload Status:
       - After the new § CSP (enforced) section, insert the `## § HSTS Preload Status` section per 10-PATTERNS.md §File 7 §Edit B (~80 words)
       - The submission date placeholder `<submission-date>` should be the actual date from Task 2 (Cutover Log Row 1/2)

       Edit C — INSERT § Phase 10 Audit Index:
       - At the end of the file (or after the most recent Phase Audit Index section — typically Phase 9 Audit Index), insert the `## § Phase 10 Audit Index` per 10-PATTERNS.md §File 7 §Edit C
       - 3 rows: HOST-07 + HOST-06 + DOC-10 with the paste-ready content

       Edit D — Maintenance update to § Phase 3 Audit Index:
       - Locate `## § Phase 3 Audit Index` (around line 494-523)
       - Find the row mentioning "soak window through Phase 10" (or similar reference to the Stage A → B → C rollout)
       - Update the row to note "soak window CLOSED Phase 10 Wave 4; enforcement live Phase 10 Wave 4 commit `<sha>`"

    2. **REQUIREMENTS.md edits.** Open REQUIREMENTS.md.

       For the HOST-07 entry in the v1 Requirements list:
       - Flip `[ ]` to `[x]`
       - Append: ` — Closed Phase 10 (10-01 inline-style migration + 10-02 directive tighten + 10-04 enforcement flip)`

       For the HOST-06 entry in the v1 Requirements list:
       - Flip `[ ]` to `[x]` (substrate-complete-with-PENDING — same pattern as Phase 8/9)
       - Append: ` — Substrate-complete Phase 10 (Plan 10-05 hstspreload.org submission landed; listing-status weeks-months calendar-deferred per runbooks/phase-10-cleanup-ledger.md)`

       For the DOC-10 entry (the long incremental row):
       - Append the Phase 10 increment annotation to the existing chain (matches Phase 7/8/9 pattern):
       ```
       ; **Phase 10 Wave 5** (10-05): § Content Security Policy (enforced — replaces Report-Only) + § HSTS Preload Status + § Phase 10 Audit Index (3-row table: HOST-07 + HOST-06 + DOC-10)
       ```

       For the Traceability table (around line 250+):
       - HOST-07 + HOST-06 row(s): Update Status column from `Pending` to `Validated 2026-MM-DD` + Notes column to cross-reference Plan 10-04 + Plan 10-05
       - DOC-10 row: leave `Complete` as-is (DOC-10 is cross-cutting; Phase 11 owns canonical)

    3. **Create runbooks/phase-10-cleanup-ledger.md** with the following content:

       ```markdown
       ---
       phase: 10
       phase_10_active_rows: 0
       status: closed
       closed_date: 2026-MM-DD
       ---

       # Phase 10 — Cleanup Ledger

       > Substrate-honest disclosure (Pitfall 19): in-phase rows CLOSED below;
       > forward-tracking rows are calendar-deferred and explicitly named with
       > target phase.

       ## In-Phase Rows (CLOSED — zero-out gate)

       | Row | Description | Closed By | Status |
       |-----|-------------|-----------|--------|
       | 1 | 130 static `style="..."` h()-attributes in src/main.js (Phase 4 sub-wave 4.1 inline-style carryover) | Plan 10-01 commit `<sha>` | CLOSED |
       | 2 | Drop `'unsafe-inline'` from style-src in firebase.json CSP-RO | Plan 10-02 commit `<sha>` | CLOSED |
       | 3 | Add `https://de.sentry.io` to connect-src (Pitfall 10B — Phase 9 OBS-04 substrate) | Plan 10-02 commit `<sha>` | CLOSED |
       | 4 | Drop `https://bedeveloped-base-layers.firebaseapp.com` from frame-src (Phase 6 D-09 forward-tracking row) | Plan 10-02 commit `<sha>` | CLOSED |
       | 5 | 7-day Stage B Report-Only soak with tightened shape | Plan 10-03 operator session | CLOSED |
       | 6 | Single-knob enforcement flip + 5-target smoke matrix | Plan 10-04 operator session | CLOSED |
       | 7 | securityheaders.com A+ rating capture | Plan 10-05 Task 2 + `docs/evidence/phase-10-securityheaders-rating.png` | CLOSED |
       | 8 | 7-day post-enforcement soak clean | Plan 10-05 Task 2 (Cutover Log Row E) | CLOSED |
       | 9 | hstspreload.org submission for baselayers.bedeveloped.com | Plan 10-05 Task 2 + `docs/evidence/phase-10-hsts-preload-submission.png` | CLOSED (submission filed) |

       ## Forward-Tracking Rows (PENDING — calendar-deferred or future-phase)

       | Row | Description | Tracking Target | Status |
       |-----|-------------|-----------------|--------|
       | F1 | hstspreload.org listing-status flips to `preloaded` (Chrome propagation; weeks-months) | Phase 11 (DOC-09 evidence pack) + Phase 12 (audit walkthrough) | PENDING |
       | F2 | Phase 4 sub-wave 4.1 IIFE main.js body migration (the broader carryover beyond just inline-style attrs; src/main.js still hosts the IIFE body + window.FB.* bridges + Chart bare globals) | v2 / separate milestone | DEFERRED (NOT a Phase 10 prerequisite — inline-style portion closed here, body-migration is independent) |
       | F3 | If a future feature adds federated OAuth-popup sign-in, frame-src will need re-extension | v2 / AUTH-V2-* requirements | DEFERRED |

       ## Phase 10 Close Gate

       - [x] phase_10_active_rows = 0 (all in-phase rows CLOSED)
       - [x] All 3 phase requirements addressed (HOST-07 + HOST-06 + DOC-10)
       - [x] Forward-tracking rows have explicit target phase
       - [x] SECURITY.md Phase 10 Audit Index landed
       - [x] REQUIREMENTS.md Traceability rows updated

       Phase 10 CLOSED.
       ```

    4. **Close cross-phase carry-forward rows:**
       - runbooks/phase-4-cleanup-ledger.md — locate the "132 static style=" row (or "inline-style remnant" / "sub-wave 4.1") → mark CLOSED with cross-reference: `→ closed by Plan 10-01 commit <sha>`
       - runbooks/phase-7-cleanup-ledger.md (or phase-6 — search both) — locate any "Phase 10 forward-tracking" row mentioning `firebaseapp.com` / `frame-src` → mark CLOSED with cross-reference: `→ closed by Plan 10-02 commit <sha>`
       - Both edits are surgical row updates only; no other ledger content touched

    5. Run `npm test -- --run firebase-config` — confirm green (sanity).

    6. Run `npm test -- --run` (full root suite) — confirm green.
  </action>
  <verify>
    <automated>npm test -- --run</automated>
    <manual>
      - `grep -c '## § Content Security Policy (enforced)' SECURITY.md` returns 1
      - `grep -c '## § Content Security Policy (Report-Only)' SECURITY.md` returns 0 (replaced)
      - `grep -c '## § HSTS Preload Status' SECURITY.md` returns 1
      - `grep -c '## § Phase 10 Audit Index' SECURITY.md` returns 1
      - `grep -c '\[x\] \*\*HOST-07' REQUIREMENTS.md` returns 1 (or pattern-equivalent — HOST-07 marked done)
      - `grep -c '\[x\] \*\*HOST-06' REQUIREMENTS.md` returns 1
      - `grep -c 'Phase 10 Wave 5' REQUIREMENTS.md` returns 1 (DOC-10 increment annotation)
      - `runbooks/phase-10-cleanup-ledger.md` exists with `phase_10_active_rows: 0` in frontmatter
      - `runbooks/phase-4-cleanup-ledger.md` "132 static style=" row references Plan 10-01
      - `runbooks/phase-7-cleanup-ledger.md` (or phase-6) firebaseapp.com row references Plan 10-02
    </manual>
  </verify>
  <done>
    SECURITY.md gains the 3 new Phase 10 sections + Phase 3 Audit Index maintenance update. REQUIREMENTS.md HOST-07 + HOST-06 marked complete; DOC-10 Phase 10 increment appended. Phase 10 cleanup ledger created with zero-out gate. Cross-phase carry-forward rows (Phase 4 inline-style, Phase 6/7 frame-src) marked CLOSED with Plan 10-* cross-references. Full Vitest root suite green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Phase 10 close — operator verifies SECURITY.md + REQUIREMENTS.md + cleanup-ledger and runs /gsd-verify-work 10</name>
  <what-built>
    Tasks 1-3 above completed the autonomous Phase 10 close (operator-paced submission + screenshot capture + documentation + traceability + cleanup ledger). This final checkpoint is the operator phase-close human-verify: read the SECURITY.md Phase 10 increment, confirm the Audit Index rows match the captured evidence + commit SHAs, and run `/gsd-verify-work 10` to produce the verification.md report.
  </what-built>
  <how-to-verify>
    1. **Read the Phase 10 SECURITY.md increment (~10 min):**
       - Open `SECURITY.md`
       - Read § Content Security Policy (enforced) — confirm per-directive table accurately describes the live state
       - Read § HSTS Preload Status — confirm submission date matches Task 2's actual date + subdomain-vs-apex matches Task 2's decision
       - Read § Phase 10 Audit Index — confirm 3 rows (HOST-07 + HOST-06 + DOC-10) all point to real files/commits

    2. **Verify REQUIREMENTS.md (~5 min):**
       - HOST-07 + HOST-06 marked `[x]` in v1 Requirements list
       - DOC-10 row has Phase 10 increment annotation
       - Traceability table HOST-07 + HOST-06 rows status updated to Validated

    3. **Verify cleanup ledgers (~5 min):**
       - runbooks/phase-10-cleanup-ledger.md frontmatter shows `phase_10_active_rows: 0`
       - runbooks/phase-4-cleanup-ledger.md "132 static style=" row marked CLOSED with Plan 10-01 cross-ref
       - runbooks/phase-7-cleanup-ledger.md (or phase-6) firebaseapp.com row marked CLOSED with Plan 10-02 cross-ref

    4. **Verify evidence files exist (~3 min):**
       - `docs/evidence/phase-10-securityheaders-rating.png` (A+ screenshot)
       - `docs/evidence/phase-10-hsts-preload-submission.png` (submission confirmation)
       - `docs/evidence/phase-10-enforcement-smoke-console.png` (Plan 10-04 5-target smoke console screenshot)

    5. **Run `/gsd-verify-work 10` (~15 min — produces 10-VERIFICATION.md):**
       - The verifier checks must_haves across all 5 plans + ROADMAP success criteria + REQUIREMENTS traceability
       - Expected outcome: all 4 ROADMAP success criteria PASS (or 3/4 if hstspreload listing-status is treated strictly — substrate-honest disclosure means it counts as "filed + listing pending")
       - Any verifier-flagged gap goes to a sub-wave 10.x micro-plan via `/gsd-plan-phase 10 --gaps`

    Expected outcome: Phase 10 closes. STATE.md updates `completed_phases: 8 → ?` (depending on Phase 8/9 operator session resolution). ROADMAP.md Phase 10 entry flips `[ ]` → `[x]`.
  </how-to-verify>
  <resume-signal>
    Operator types "Phase 10 verified — close" with the 10-VERIFICATION.md path, OR "Phase 10 has gaps: <list>" with the specific verifier-flagged items so `/gsd-plan-phase 10 --gaps` can run.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → hstspreload.org | Third-party web form; one-shot submission; inclusion in Chrome preload list is irreversible-with-effort |
| operator → securityheaders.com | Third-party scanner; produces auditable A+ rating screenshot |
| docs author → SECURITY.md | DOC-10 increment is the auditor-visible narrative — must match the live state, not aspirational |
| auditor ← SECURITY.md § Phase 10 Audit Index | 3-row Pattern G table is the cross-reference into code + tests + framework citations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05-1 | Repudiation | apex domain accidentally submitted | mitigate | Runbook Step 2 has an explicit decision tree + operator tick-box; subdomain-only is the planner-recommended default with an explicit irreversibility warning on apex |
| T-10-05-2 | Tampering | SECURITY.md narrative diverges from live state | mitigate | Per-directive table mirrors firebase.json; framework citations are concrete (V14.4 / A.8.23 / CC6.6 / Art.32) not hand-waved; Phase 11 final pass owns canonical DOC-10 — Phase 10 increment is intermediate |
| T-10-05-3 | Information Disclosure | Sentry origin assumption (10-RESEARCH.md A4) wrong → Phase 9 OBS observability silently dies under enforcement | accept | Wave 3 + Wave 4 + Wave 5 7-day soak surfaces this via `csp.violation` + `effectiveDirective="connect-src"` + `blockedUri="https://*.sentry.io"`; if observed, sub-wave 10.x fixes |
| T-10-05-4 | Tampering | hstspreload.org listing-status row stays PENDING indefinitely | mitigate | Forward-tracked in runbooks/phase-10-cleanup-ledger.md F1 row; Phase 11 evidence pack picks up the screenshot when listing arrives; calendar-deferred is not a phase failure (Pitfall 19 substrate-honest) |
| T-10-05-5 | Repudiation | cross-phase ledger references go stale | mitigate | Task 3 surgically updates phase-4 + phase-7 ledgers with explicit Plan 10-* cross-references; the rows close, they don't disappear (audit trail preserved) |

**Severity:** All threats LOW or MEDIUM. The apex-vs-subdomain decision is the highest-stakes operator choice in the wave; the runbook gates it explicitly.
</threat_model>

<verification>
- `runbooks/hsts-preload-submission.md` exists with all 5 numbered Steps + Cutover Log
- `runbooks/phase-10-cleanup-ledger.md` exists with `phase_10_active_rows: 0`
- SECURITY.md has new § CSP (enforced) + § HSTS Preload Status + § Phase 10 Audit Index sections
- SECURITY.md § Phase 3 Audit Index updated with "soak window CLOSED Phase 10 Wave 4" annotation
- REQUIREMENTS.md HOST-07 + HOST-06 `[x]`; DOC-10 Phase 10 Wave 5 annotation appended
- Cross-phase ledger rows (phase-4 inline-style + phase-7 firebaseapp.com) marked CLOSED with Plan 10-* cross-references
- `docs/evidence/phase-10-securityheaders-rating.png` exists
- `docs/evidence/phase-10-hsts-preload-submission.png` exists
- `docs/evidence/phase-10-enforcement-smoke-console.png` exists (from Plan 10-04)
- `npm test -- --run` (full root suite) green
- `/gsd-verify-work 10` produces 10-VERIFICATION.md with all 4 ROADMAP SC PASS (or substrate-honest "listing-status calendar-deferred" annotation on SC#3)
</verification>

<success_criteria>
- HOST-07 closed: SC#1 (CSP enforced + directive matrix), SC#2 (5-target smoke + 7-day post-enforcement clean soak), SC#4 (A+ rating)
- HOST-06 closed (substrate-complete): SC#3 (hstspreload.org submission filed; listing-status calendar-deferred is forward-tracked, not blocking)
- DOC-10 Phase 10 increment landed
- Phase 10 cleanup ledger zero-out gate passed
- Phase 4 sub-wave 4.1 inline-style row CLOSED (carry-forward zero-out)
- Phase 6/7 frame-src firebaseapp.com forward-tracking row CLOSED
- ROADMAP.md Phase 10 entry flips `[ ]` → `[x]`
</success_criteria>

<output>
After completion, create `.planning/phases/10-csp-tightening-second-sweep/10-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`.

Summary must include:
- HSTS submission outcome (subdomain-only vs apex; submission timestamp; hstspreload.org API status)
- securityheaders.com rating outcome (A+ achieved? screenshot path)
- 7-day post-enforcement soak status (clean / restart cycles)
- SECURITY.md sections added (3) + Phase 3 Audit Index maintenance update
- REQUIREMENTS.md rows updated (HOST-07 / HOST-06 / DOC-10 + Traceability table)
- Cross-phase ledger rows closed (Phase 4 inline-style + Phase 6/7 frame-src)
- runbooks/phase-10-cleanup-ledger.md zero-out confirmation
- Forward-tracking rows queued (hstspreload listing status weeks-months; Phase 4 sub-wave 4.1 IIFE body migration to v2)
- Commit SHAs for Task 1 (HSTS runbook), Task 3 (docs + ledger), Task 4 (any final tidy)

> **Operator note — Task 3 cross-phase ledger surgery (Warning 3 from Plan 10 checker):**
> Task 3 touches 5 files (SECURITY.md + REQUIREMENTS.md + runbooks/phase-10-cleanup-ledger.md + runbooks/phase-4-cleanup-ledger.md + runbooks/phase-6-or-7-cleanup-ledger.md). If any of those ledgers has merge conflicts at execution time (e.g. an in-flight sub-wave 10.x micro-plan also touched phase-4 ledger), SPLIT Task 3 into two atomic commits: (a) SECURITY.md + REQUIREMENTS.md + phase-10-cleanup-ledger.md (the in-phase docs); (b) phase-4 + phase-6/7 ledger cross-references (the carry-forward closures). Operator-paced flanking checkpoints (Tasks 2 + 4) gate the surgery.
</output>
</content>
</invoke>