---
phase: 11-documentation-pack-evidence-pack
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - docs/CONTROL_MATRIX.md
  - tests/security-md-toc.test.js
  - tests/security-md-citation-format.test.js
autonomous: true
requirements: [DOC-01, DOC-04]
must_haves:
  truths:
    - "SECURITY.md opens with a Table of Contents linking to every ## § section"
    - "SECURITY.md has a top-level ## § Vulnerability Disclosure Policy section (not a blockquote)"
    - "SECURITY.md has a top-level ## § MFA Recovery Procedure section documenting Tier-1 email-link + Tier-2 admin un-enrol"
    - "SECURITY.md has a top-level ## § Rotation Schedule section listing every rotating secret with cadence"
    - "Every ASVS citation in SECURITY.md uses the canonical 'OWASP ASVS L2 v5.0 V<N>.<N>' form"
    - "docs/CONTROL_MATRIX.md exists with frontmatter, How-to-use orientation, and 15 REQ-prefix section anchors (TOOL/TEST/HOST/CODE/DATA/RULES/AUTH/FN/AUDIT/LIFE/GDPR/BACKUP/OBS/DOC/WALK)"
  artifacts:
    - path: SECURITY.md
      provides: "DOC-01 canonical content with ToC + 3 promoted sections + normalised citations"
      contains: "## Table of Contents"
    - path: docs/CONTROL_MATRIX.md
      provides: "DOC-04 skeleton with section anchors (rows populated in Wave 6)"
      contains: "REQ-prefix-ordered section anchors"
    - path: tests/security-md-toc.test.js
      provides: "ToC + 3 promoted sections shape gate"
    - path: tests/security-md-citation-format.test.js
      provides: "Citation-format normalisation gate (no bare 'ASVS L2 V' without v5.0)"
  key_links:
    - from: "SECURITY.md ## Table of Contents"
      to: "every ## § section anchor"
      via: "markdown anchor links"
      pattern: "\\[.*\\]\\(#[a-z0-9-]+\\)"
    - from: "SECURITY.md § Vulnerability Disclosure Policy"
      to: "/.well-known/security.txt"
      via: "explicit cross-reference paragraph (Wave 5 finalises file; Wave 1 establishes link target)"
      pattern: "/.well-known/security.txt"
---

<objective>
Establish the Phase 11 structural skeleton. Add a Table of Contents to SECURITY.md, promote three currently-scattered topics (Vulnerability Disclosure Policy, MFA Recovery Procedure, Rotation Schedule) into top-level `## §` sections, normalise the ASVS citation format across the document, and create the `docs/CONTROL_MATRIX.md` skeleton with REQ-prefix-ordered section anchors (rows populated in Wave 6).

Purpose: DOC-01 is the canonical-pass deliverable; auditors enter via SECURITY.md, so it needs a navigable index + the three explicitly-required sections (DOC-01 spec verbatim: "MFA recovery procedure; rotation schedule"). DOC-04 substrate (the empty-shell CONTROL_MATRIX.md) lands here so subsequent waves can cross-reference it without race conditions.

Output: SECURITY.md with ToC + 3 new top-level sections + uniform citations; docs/CONTROL_MATRIX.md skeleton; 2 doc-shape tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md
@.planning/phases/11-documentation-pack-evidence-pack/11-CONTEXT.md
@SECURITY.md
@CLAUDE.md

<interfaces>
<!-- Existing SECURITY.md structure executors must preserve. Read directly; do not reorder. -->

SECURITY.md current top-of-file (lines 1-17):
```markdown
# Security

**Disclosure contact:** [security@bedeveloped.com](mailto:security@bedeveloped.com)
**Supported versions:** main branch only (no released versions yet).

> **Vulnerability disclosure policy.** If you believe you have found a security
> vulnerability... [BLOCKQUOTE PLACEHOLDER — replace with top-level §]

---
```

Existing top-level `## §` sections in source order (do NOT renumber):
1. § Build & Supply Chain (line 19)
2. § Data Handling (line 105)
3. § Code Quality + Module Boundaries (line 160)
4. § Dependency Monitoring (line 226)
5. § Secret Scanning (line 280)
6. § HTTP Security Headers (line 385)
7. § Content Security Policy (enforced) (line 436)
8. § HSTS Preload Status (line 491)
9. § Hosting & Deployment (line 513)
10. § Phase 3 Audit Index (line 540)
11. § Firestore Data Model (line 581)
12. § Firestore Security Rules — Authored, Not Yet Deployed (line 622)
13. § Storage Rules (line 667)
14. § Phase 5 Audit Index (line 694)
15. § Authentication & Sessions (line 727)
16. § Multi-Factor Authentication (line 741)
17. § Anonymous Auth Disabled (line 754)
18. § Production Rules Deployment (line 762)
19. § Phase 6 Audit Index (line 770)
20. § Cloud Functions Workspace (line 800)
21. § App Check (line 823)
22. § Audit Log Infrastructure (line 846)
23. § Rate Limiting (line 869)
24. § Phase 7 Audit Index (line 885)
25. § Data Lifecycle (Soft-Delete + Purge) (line 918)
26. § GDPR (Export + Erasure) (line 939)
27. § Backups + DR + PITR + Storage Versioning (line 970)
28. § Phase 8 Audit Index (line 1007)
29. § Observability — Sentry (line 1043)
30. § Audit-Event Wiring (AUDIT-05) (line 1090)
31. § Anomaly Alerting (OBS-05) (line 1136)
32. § Out-of-band Monitors (OBS-04 / OBS-06 / OBS-07 / OBS-08) (line 1187)
33. § Phase 9 Audit Index (line 1223)
34. § Phase 10 Audit Index (~line 1240)
35. (Compliance posture statement footer — lines 1271-1278; preserve verbatim — "credible, not certified")

Three NEW sections to add this wave (placement decisions in tasks):
- § Vulnerability Disclosure Policy — replaces the blockquote at lines 7-15
- § MFA Recovery Procedure — content currently inline in § Multi-Factor Authentication (line 741) and § Authentication & Sessions; promote to its own section
- § Rotation Schedule — content currently scattered across § Secret Scanning (line 280), § Audit Log Infrastructure (line 846 — BigQuery sink rotation), § Anomaly Alerting (line 1136 — secret rotation row); promote to its own section

Test pattern (mirror existing tests/build/source-map-gate.test.js):
```javascript
import { describe, it, expect } from "vitest";
import { readFileSync } from "node:fs";
import { resolve } from "node:path";

describe("SECURITY.md — DOC-01 shape (Phase 11 Wave 1)", () => {
  const src = readFileSync(resolve(process.cwd(), "SECURITY.md"), "utf-8");
  // assertions
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author tests/security-md-toc.test.js + tests/security-md-citation-format.test.js (RED — assert behaviour before edit)</name>
  <files>tests/security-md-toc.test.js, tests/security-md-citation-format.test.js</files>
  <read_first>
    - SECURITY.md (full read; observe existing 34 ## § sections + line 7-15 blockquote placeholder + lines 1271-1278 compliance posture footer)
    - tests/build/source-map-gate.test.js (regex-over-file-body pattern; mirror this style)
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Final-Pass Checklist" (citation normalisation rule = "OWASP ASVS L2 v5.0 V<N>.<N>" canonical form)
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Wave 1" (test names + scope)
  </read_first>
  <behavior>
    tests/security-md-toc.test.js (5 cases):
    - Test 1: SECURITY.md contains a `## Table of Contents` heading within the first 50 lines.
    - Test 2: ToC contains anchor links for ALL of: § Vulnerability Disclosure Policy, § MFA Recovery Procedure, § Rotation Schedule, § Build & Supply Chain, § Phase 10 Audit Index (sample 5 — mix of new + existing).
    - Test 3: SECURITY.md contains exactly ONE top-level `## § Vulnerability Disclosure Policy` heading (top-level `## ` prefix, not `### `, not blockquote).
    - Test 4: SECURITY.md contains exactly ONE top-level `## § MFA Recovery Procedure` heading.
    - Test 5: SECURITY.md contains exactly ONE top-level `## § Rotation Schedule` heading.

    tests/security-md-citation-format.test.js (4 cases):
    - Test 1: every ASVS citation matches the canonical form — `/OWASP ASVS L2 v5\.0 V\d+\.\d+(\.\d+)?/` ; assert that `/OWASP ASVS L2 V\d/` (the form WITHOUT `v5.0`) appears ZERO times.
    - Test 2: every ISO citation matches `/ISO\/IEC 27001:2022 Annex A\.\d+\.\d+/` (assert `/ISO 27001 (?!:2022)/` zero hits to catch un-versioned drift).
    - Test 3: SOC 2 citations match `/SOC ?2 CC\d+\.\d+/` (allow optional space — existing pattern is `SOC 2 CC6.6`); no `/SOC2(?!\s)/` form.
    - Test 4: GDPR citations match `/GDPR Art\. \d+(\(\d+\))?/`; no `/GDPR Article \d/` long-form drift.

    These tests MUST FAIL initially (red gate) — the blockquote at lines 7-15 is not a `##` section, the new sections don't exist, and the existing SECURITY.md likely has at least one citation drift instance.
  </behavior>
  <action>
Create both test files using the read-file-and-regex-assert pattern from `tests/build/source-map-gate.test.js`. Use `readFileSync(resolve(process.cwd(), "SECURITY.md"), "utf-8")` for the body. Use Vitest `describe`/`it`/`expect` (already in project — see vitest 4.x in package.json).

For tests/security-md-toc.test.js — assertions per the behavior block. Use `expect(src).toMatch(/^## Table of Contents$/m)` (multiline flag) for the heading; use `expect(src.split("## Table of Contents")[1]?.split(/^## /m)[0]).toContain("# § Vulnerability Disclosure Policy")` (or similar — extract the ToC body slice, then check for each anchor link); use `expect(src.match(/^## § Vulnerability Disclosure Policy$/gm)?.length).toBe(1)` for exactly-one assertions.

For tests/security-md-citation-format.test.js — assertions per the behavior block. Use `expect(src.match(/OWASP ASVS L2 V\d/g)?.length || 0).toBe(0)` for the negative-form check (drift detection).

Run `npm test -- --run security-md-toc security-md-citation-format` and verify BOTH suites FAIL (RED gate — Test 1 of toc.test.js fails because no ToC exists yet; toc Tests 3-5 fail because the sections don't exist yet; citation tests will fail if any drift exists in current SECURITY.md). Capture and commit the red-gate failure log path in the commit message: `test(11-01): add SECURITY.md ToC + citation-format shape tests (RED)`.

Do NOT modify SECURITY.md in this task — that's Task 2's job. The point of TDD here is to lock in the contract before edit.
  </action>
  <verify>
    <automated>npm test -- --run security-md-toc security-md-citation-format 2>&1 | grep -E "FAIL|failed" (must show failures — RED gate)</automated>
  </verify>
  <done>Both test files exist with the cases above. Running `npm test -- --run security-md-toc security-md-citation-format` produces FAILURES (test count > 0; failure count > 0). Committed as `test(11-01): add SECURITY.md DOC-01 shape tests (RED)`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SECURITY.md — add ToC + promote 3 sections + normalise citations (GREEN)</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (full read)
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Existing SECURITY.md Inventory" + §"Final-Pass Checklist" (final-pass items 1-6)
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Code Examples" → "Vulnerability disclosure policy paragraph (SECURITY.md final wording)" — paste this verbatim
    - tests/security-md-toc.test.js + tests/security-md-citation-format.test.js (the contracts to satisfy)
    - .planning/phases/06-real-auth-mfa-rules-deploy/ — search for any `06-*-SUMMARY.md` referencing D-07 (email-link recovery decision) for MFA Recovery Procedure section content
  </read_first>
  <behavior>
    After this edit:
    - SECURITY.md line 1-5 frontmatter preserved (`# Security` + Disclosure contact + Supported versions)
    - Insert `## Table of Contents` immediately after line 5 (before the `---` separator OR before the blockquote — see action). ToC enumerates ALL `## §` sections in source order including the 3 new ones, with markdown anchor links.
    - The existing blockquote at lines 7-15 is REMOVED and REPLACED by a top-level `## § Vulnerability Disclosure Policy` section using the verbatim wording from RESEARCH.md §"Code Examples". Place this section as the FIRST `## §` section (before § Build & Supply Chain).
    - New top-level `## § MFA Recovery Procedure` section inserted after § Multi-Factor Authentication (current line 741 area). Content: Tier-1 user-side email-link recovery (`signInWithEmailLink` + `sendSignInLinkToEmail` per Phase 6 D-07 superseding AUTH-09 recovery codes); Tier-2 operator-side Admin SDK un-enrol (`admin.auth().updateUser(uid, { multiFactor: { enrolledFactors: [] } })` referenced from `runbooks/phase6-mfa-recovery-drill.md`); reference to `docs/evidence/phase-6-mfa-recovery-drill-pass.png` (PENDING-OPERATOR — see `06-DEFERRED-CHECKPOINT.md` if present, else mark "PENDING-OPERATOR — Phase 6 user-testing batch").
    - New top-level `## § Rotation Schedule` section inserted after § Secret Scanning (current line 280 area). Table format with columns: Secret | Cadence | Mechanism | Owner | Citation. Rows: SOPs/secrets quarterly (manual rotation via Firebase Console + Secret Manager); GitHub Actions OIDC (no rotation needed — short-lived tokens); Firebase service-account JSON (NONE — using OIDC); Sentry DSN (manual; on suspected leak); Slack webhook URL (manual; on suspected leak); GDPR_PSEUDONYM_SECRET (annually OR on suspected leak). Cross-reference `runbooks/phase-7-bigquery-sink-bootstrap.md` + `runbooks/phase-9-monitors-bootstrap.md` for the operator paths.
    - Citation normalisation: every `OWASP ASVS L2 V<N>` (without `v5.0`) becomes `OWASP ASVS L2 v5.0 V<N>`. Every `ISO 27001 ` (without `:2022`) becomes `ISO/IEC 27001:2022 ` where the cited control numbering matches the 2022 revision (verify per-citation — most existing already use `:2022`; spot-check the 5 sections that may not). Every `GDPR Article ` long-form becomes `GDPR Art. `. Every `SOC2 ` (no space) becomes `SOC 2 `.
    - Compliance posture footer at lines 1271-1278 preserved VERBATIM (DO NOT modify the "credible, not certified" boilerplate).
  </behavior>
  <action>
Step 1 — ToC. Use Edit tool. Insert immediately after `**Supported versions:** main branch only (no released versions yet).` (line 5):

```markdown

## Table of Contents

- [§ Vulnerability Disclosure Policy](#-vulnerability-disclosure-policy)
- [§ Build & Supply Chain](#-build--supply-chain)
- [§ Data Handling](#-data-handling)
- [§ Code Quality + Module Boundaries](#-code-quality--module-boundaries)
- [§ Dependency Monitoring](#-dependency-monitoring)
- [§ Secret Scanning](#-secret-scanning)
- [§ Rotation Schedule](#-rotation-schedule)
- [§ HTTP Security Headers](#-http-security-headers)
- [§ Content Security Policy (enforced)](#-content-security-policy-enforced)
- [§ HSTS Preload Status](#-hsts-preload-status)
- [§ Hosting & Deployment](#-hosting--deployment)
- [§ Phase 3 Audit Index](#-phase-3-audit-index)
- [§ Firestore Data Model](#-firestore-data-model)
- [§ Firestore Security Rules — Authored, Not Yet Deployed](#-firestore-security-rules--authored-not-yet-deployed)
- [§ Storage Rules](#-storage-rules)
- [§ Phase 5 Audit Index](#-phase-5-audit-index)
- [§ Authentication & Sessions](#-authentication--sessions)
- [§ Multi-Factor Authentication](#-multi-factor-authentication)
- [§ MFA Recovery Procedure](#-mfa-recovery-procedure)
- [§ Anonymous Auth Disabled](#-anonymous-auth-disabled)
- [§ Production Rules Deployment](#-production-rules-deployment)
- [§ Phase 6 Audit Index](#-phase-6-audit-index)
- [§ Cloud Functions Workspace](#-cloud-functions-workspace)
- [§ App Check](#-app-check)
- [§ Audit Log Infrastructure](#-audit-log-infrastructure)
- [§ Rate Limiting](#-rate-limiting)
- [§ Phase 7 Audit Index](#-phase-7-audit-index)
- [§ Data Lifecycle (Soft-Delete + Purge)](#-data-lifecycle-soft-delete--purge)
- [§ GDPR (Export + Erasure)](#-gdpr-export--erasure)
- [§ Backups + DR + PITR + Storage Versioning](#-backups--dr--pitr--storage-versioning)
- [§ Phase 8 Audit Index](#-phase-8-audit-index)
- [§ Observability — Sentry](#-observability--sentry)
- [§ Audit-Event Wiring (AUDIT-05)](#-audit-event-wiring-audit-05)
- [§ Anomaly Alerting (OBS-05)](#-anomaly-alerting-obs-05)
- [§ Out-of-band Monitors (OBS-04 / OBS-06 / OBS-07 / OBS-08)](#-out-of-band-monitors-obs-04--obs-06--obs-07--obs-08)
- [§ Phase 9 Audit Index](#-phase-9-audit-index)
- [§ Phase 10 Audit Index](#-phase-10-audit-index)
```

(GitHub anchor convention: lowercase + spaces → hyphens + special chars except hyphens/underscores stripped; `&` becomes empty string producing `--`. The `§` glyph is stripped to nothing leaving leading hyphen — verified by spot-test on a GitHub PR or via cross-reference to the existing Audit Index inline links if any. If the anchor format doesn't match GitHub's algorithm, fix in this task — verification = open the file in GitHub web UI post-commit.)

Step 2 — § Vulnerability Disclosure Policy. Use Edit tool. Replace the blockquote (current lines 7-15 — the `> **Vulnerability disclosure policy.** ...` block) with the verbatim wording from RESEARCH.md §"Code Examples" → "Vulnerability disclosure policy paragraph (SECURITY.md final wording)":

```markdown
## § Vulnerability Disclosure Policy

If you believe you have found a security vulnerability in this codebase
or in the deployed application at https://baselayers.bedeveloped.com,
please email **security@bedeveloped.com**. We will:

1. Acknowledge your report within **5 business days**.
2. Provide a substantive update within 10 business days.
3. Credit you in `docs/evidence/acknowledgments.md` (with your permission).

We will NOT take legal action against good-faith security researchers
who:

- Avoid privacy violations, destruction of data, and interruption or
  degradation of our services.
- Only interact with accounts you own or with explicit permission of
  the account holder.
- Do not exploit a finding beyond the minimum necessary to demonstrate it.
- Give us a reasonable time to respond before disclosure.

**In scope:** the production application + this source repository.

**Out of scope:** third-party services we use (Google / Firebase + Sentry —
report directly to those vendors); social engineering of staff;
denial-of-service testing.

This policy is referenced from `/.well-known/security.txt` per RFC 9116
(Wave 5 ships the file).
```

Place this section directly above the existing § Build & Supply Chain heading. Preserve the `---` separator placement.

Step 3 — § MFA Recovery Procedure. Use Edit tool. Insert as a NEW top-level section immediately after the closing of § Multi-Factor Authentication (~line 754 boundary, BEFORE § Anonymous Auth Disabled):

```markdown
## § MFA Recovery Procedure

Recovery codes (AUTH-09) were superseded by **email-link recovery** per Phase 6
D-07. Two tiers:

### Tier 1 — User-side email-link recovery

If a user loses their TOTP device, they invoke email-link recovery via
`sendSignInLinkToEmail` (`src/firebase/auth.js`); the link, sent to the
registered email, completes a one-time sign-in via `signInWithEmailLink`,
after which the user re-enrols TOTP from their account settings.

- Code: `src/firebase/auth.js` (helper exports)
- Test: `tests/firebase/auth-audit-emit.test.js` (Test 5 — email-link path)
- Auth event: `auth.recovery.requested` audited via the standard pipeline

### Tier 2 — Operator-side Admin SDK un-enrol

Used when email access itself is compromised. Operator runs the documented
`runbooks/phase6-mfa-recovery-drill.md` procedure, calling Admin SDK
`admin.auth().updateUser(uid, { multiFactor: { enrolledFactors: [] } })` from a
trusted environment. The audit-log entry `auth.mfa.unenrolled.byAdmin` MUST be
written by the same operator session.

- Runbook: `runbooks/phase6-mfa-recovery-drill.md`
- Evidence: `docs/evidence/phase-6-mfa-recovery-drill-pass.png` — **PENDING-OPERATOR** (Phase 6 user-testing batch; see `.planning/phases/06-real-auth-mfa-rules-deploy/06-RESUME-NOTE.md`)

**Framework citations:** OWASP ASVS L2 v5.0 V2.5.4 (alternative authenticator);
ISO/IEC 27001:2022 Annex A.5.16 (identity management); SOC 2 CC6.1; GDPR
Art. 32(1)(b).
```

Step 4 — § Rotation Schedule. Use Edit tool. Insert as a NEW top-level section immediately after § Secret Scanning (~line 345 boundary, BEFORE the next `## §`):

```markdown
## § Rotation Schedule

| Secret / Credential | Cadence | Mechanism | Owner | Citation |
|---------------------|---------|-----------|-------|----------|
| `GDPR_PSEUDONYM_SECRET` (Phase 8 GDPR-02 erasure pseudonym salt) | Annually OR on suspected leak | Google Secret Manager rotate via `gcloud secrets versions add` then redeploy `gdprEraseUser` | Hugh | OWASP ASVS L2 v5.0 V2.10.4; ISO/IEC 27001:2022 Annex A.8.24; SOC 2 CC6.1 |
| Sentry DSN (`vite.config.js` env reference) | On suspected leak (no scheduled rotation — DSN is non-secret per Sentry docs but treat as secret out of caution) | Regenerate via Sentry Project Settings → Client Keys; update GitHub Actions secret + local `.env`; redeploy | Hugh | OWASP ASVS L2 v5.0 V14.3.2; ISO A.8.24; SOC 2 CC6.1 |
| Slack webhook URL (`SLACK_WEBHOOK_URL` Secret Manager — Phase 9 OBS-05) | On suspected leak | Regenerate via Slack App config → revoke-and-recreate; update Secret Manager via `gcloud secrets versions add`; redeploy `authAnomalyAlert` | Hugh | OWASP ASVS L2 v5.0 V14.3.2; ISO A.8.24; SOC 2 CC6.1 |
| BigQuery sink service-account JSON | NONE — uses Workload Identity Federation (Phase 7 BigQuery sink bootstrap) | n/a | n/a | OIDC pattern — no JSON to rotate |
| Firebase deploy service-account JSON | NONE — uses GitHub Actions OIDC (Phase 1 firebase-oidc-bootstrap) | n/a — short-lived tokens issued per CI run | n/a | OIDC pattern — no JSON to rotate |
| `audit-alert-sa` service account (Phase 9 monitors) | NONE — Workload Identity Federation; no JSON | n/a | n/a | OIDC pattern |
| TLS certificate (`baselayers.bedeveloped.com`) | Automatic (Firebase Hosting) | Firebase-managed Let's Encrypt rotation | Firebase | Managed substrate |

**Operator runbook references:**
- `runbooks/phase-7-bigquery-sink-bootstrap.md` — BigQuery sink WIF setup (no rotation)
- `runbooks/phase-9-monitors-bootstrap.md` — `SLACK_WEBHOOK_URL` + `SENTRY_DSN` Secret Manager bootstrap; rotation procedure mirrors bootstrap with `gcloud secrets versions add` then redeploy
- `runbooks/firebase-oidc-bootstrap.md` — GitHub Actions OIDC federation (no rotation)

**Framework citations:** OWASP ASVS L2 v5.0 V14.3.2 (secrets management); ISO/IEC 27001:2022 Annex A.8.24 (use of cryptography) + A.5.17 (authentication information); SOC 2 CC6.1; GDPR Art. 32(1)(d).
```

Step 5 — Citation normalisation. Use Edit tool with multi-occurrence replacements:
- Replace `OWASP ASVS L2 V` → `OWASP ASVS L2 v5.0 V` BUT ONLY where `v5.0` is not already present immediately before `V`. Use Grep to enumerate offending lines first; then per-line Edit where needed.
- Replace `GDPR Article ` → `GDPR Art. ` (everywhere; the `Art. ` form is canonical per RESEARCH.md).
- Replace `SOC2 ` (no space) → `SOC 2 ` (with space) — Grep first to check incidence; existing dominant pattern is already `SOC 2`.
- For `ISO 27001 ` without `:2022` — Grep first; if any exist, replace with `ISO/IEC 27001:2022 ` (the canonical form per RESEARCH.md).

Run `npm test -- --run security-md-toc security-md-citation-format` and verify BOTH suites now PASS (GREEN gate). Run `npm test -- --run` (full suite — currently 484/484 + 6 skipped per Phase 10 close) to confirm zero regressions.

Commit as: `docs(11-01): add SECURITY.md ToC + Vuln Disclosure + MFA Recovery + Rotation Schedule sections + normalise citations (GREEN)`.
  </action>
  <verify>
    <automated>npm test -- --run security-md-toc security-md-citation-format 2>&1 | grep -E "passed|failed" (must show all green)</automated>
    <automated>npm test -- --run 2>&1 | tail -10 (full suite — must show same pass count as Phase 10 close minus zero regressions)</automated>
  </verify>
  <done>SECURITY.md has ToC + § Vulnerability Disclosure Policy + § MFA Recovery Procedure + § Rotation Schedule top-level sections; citation format uniform; both shape tests green; full suite green; lines 1271-1278 footer untouched verbatim.</done>
</task>

<task type="auto">
  <name>Task 3: Create docs/CONTROL_MATRIX.md skeleton</name>
  <files>docs/CONTROL_MATRIX.md</files>
  <read_first>
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Code Examples" → "CONTROL_MATRIX.md skeleton" — paste-ready template
    - .planning/REQUIREMENTS.md (verify the 15 REQ-prefix categories: TOOL, TEST, HOST, CODE, DATA, RULES, AUTH, FN, AUDIT, LIFE, GDPR, BACKUP, OBS, DOC, WALK)
    - SECURITY.md (just-edited; source of compliance posture cross-reference)
  </read_first>
  <action>
Create `docs/CONTROL_MATRIX.md` using the Write tool. Use the skeleton from RESEARCH.md §"Code Examples" verbatim, with these adaptations:

```markdown
# Control Matrix — Base Layers Diagnostic

**Last updated:** 2026-05-10 (Phase 11 Wave 1 — skeleton; rows populated Wave 6)
**Owner:** Phase 11 (DOC-04); maintained per-phase as findings close
**Source:** This matrix is the auditor-friendly index over `SECURITY.md`. Every row maps a `REQUIREMENTS.md` ID to its implementation, test/evidence, and framework citation.
**Compliance posture:** credible, not certified — see `SECURITY.md` (compliance posture statement footer).

---

## How to use this matrix

Find a control by `REQ-ID`. Follow `Code Path(s)` to the implementation, `Test / Evidence` to the verification, and `Framework` to the standards mapping.

Conventions:

- Citations use canonical short-forms: `OWASP ASVS L2 v5.0 V<N>.<N>.<N>`; `ISO/IEC 27001:2022 Annex A.<N>.<N>`; `SOC 2 CC<N>.<N>`; `GDPR Art. <N>(<N>)`.
- `**PENDING-OPERATOR**` annotations point to the specific `NN-DEFERRED-CHECKPOINT.md` step where the evidence will be captured.
- Code paths are paths only — never line numbers. Phase 11 Wave 6 sweeps for line-number drift.
- Rows are REQ-prefix-ordered (alphabetical by category, then numeric within); chronological narrative lives in `SECURITY.md` Audit Indexes.

---

## AUDIT — Audit Log Infrastructure + Wiring (Phase 7 + Phase 9)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## AUTH — Real Auth + MFA (Phase 6)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## BACKUP — Backups + DR + PITR (Phase 8)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## CODE — Modular Split + Quick Wins (Phase 4)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## DATA — Firestore Data Model (Phase 5)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## DOC — Documentation Pack (Phase 11)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## FN — Cloud Functions + App Check (Phase 7)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## GDPR — GDPR Export + Erasure (Phase 8)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## HOST — Hosting & Headers (Phase 3 + Phase 10)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## LIFE — Data Lifecycle / Soft-Delete (Phase 8)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## OBS — Observability (Phase 9)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## RULES — Firestore + Storage Rules (Phase 5 + Phase 6)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## TEST — Test Suite Foundation (Phase 2)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## TOOL — Engineering Foundation (Phase 1)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

## WALK — Audit Walkthrough (Phase 12)

| REQ | Control | Code Path(s) | Test / Evidence | Framework |
|-----|---------|--------------|-----------------|-----------|

---

**Population status:** SKELETON. Rows populated in Phase 11 Wave 6 (`11-06-PLAN.md`) by merging the 6 existing Audit Indexes in `SECURITY.md` (Phase 3 / 5 / 6 / 7 / 8 / 9 / 10) plus filling in Phase 1 / 2 / 4 rows that don't yet have Audit Indexes.
```

Run `git add docs/CONTROL_MATRIX.md SECURITY.md tests/security-md-toc.test.js tests/security-md-citation-format.test.js`. Commit: `docs(11-01): add CONTROL_MATRIX.md skeleton (REQ-prefix-ordered; rows in Wave 6)`.

NOTE: The skeleton has zero rows — that's correct. Wave 6 populates. The skeleton MUST exist now so Waves 2-5 can cross-reference its anchors without race conditions.
  </action>
  <verify>
    <automated>test -f docs/CONTROL_MATRIX.md && grep -c "^## " docs/CONTROL_MATRIX.md (must be >= 16 — 15 REQ categories + How-to-use heading)</automated>
  </verify>
  <done>docs/CONTROL_MATRIX.md exists with 15 REQ-prefix-ordered section anchors + How-to-use orientation + skeleton frontmatter; committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader (auditor) → SECURITY.md content | Auditor relies on doc accuracy; misleading or stale content fails Pitfall 19 |
| SECURITY.md → cross-referenced files | ToC anchors + section cross-references must resolve; broken anchors break navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Repudiation | SECURITY.md citations | mitigate | tests/security-md-citation-format.test.js gates the regex contract; Wave 6 cross-checks every cited path exists |
| T-11-01-02 | Repudiation | SECURITY.md ToC accuracy | mitigate | tests/security-md-toc.test.js asserts the 3 promoted sections exist + ToC contains anchor links |
| T-11-01-03 | Repudiation | § Vulnerability Disclosure Policy 5-business-day commitment | mitigate | RESEARCH.md A7 assumption recorded; if commitment is aspirational, Wave 5 docs adjust before security.txt ships |
| T-11-01-04 | Repudiation | § Rotation Schedule cadence claims | mitigate | Each row carries operator-runbook cross-reference; cadences mirror existing operator practice (no aspirational claims) |
| T-11-01-05 | Information Disclosure | Compliance posture statement at lines 1271-1278 | mitigate | Boilerplate preserved verbatim; Edit tool diff review confirms no language drift |

</threat_model>

<verification>
- All 3 tasks committed via Conventional Commits prefix `docs(11-01)` or `test(11-01)`
- `npm test -- --run security-md-toc security-md-citation-format` GREEN
- `npm test -- --run` full-suite count matches pre-Wave count (no regressions)
- `git diff SECURITY.md | grep -E '^[-+].*credible.*not certified'` shows zero hits (footer untouched)
- `wc -l SECURITY.md` shows count > 1278 (lines added — ToC + 3 sections; subtract blockquote replacement net effect ~+150 lines expected)
- `head -50 SECURITY.md` shows the new ToC structure
</verification>

<success_criteria>
- DOC-01 substrate landed: ToC + § Vulnerability Disclosure Policy + § MFA Recovery Procedure + § Rotation Schedule + uniform citation format
- DOC-04 skeleton landed: docs/CONTROL_MATRIX.md with 15 REQ-prefix section anchors (rows in Wave 6)
- 2 doc-shape tests green: tests/security-md-toc.test.js + tests/security-md-citation-format.test.js
- Zero regressions in existing 484-case suite
- Compliance posture footer (lines 1271-1278) preserved verbatim ("credible, not certified")
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-pack-evidence-pack/11-01-SUMMARY.md` capturing: ToC anchor count, sections promoted (3), citation drift instances fixed (count from grep), CONTROL_MATRIX.md skeleton row count (0 — by design), commits SHAs.
</output>
