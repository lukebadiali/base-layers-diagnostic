---
phase: 11-documentation-pack-evidence-pack
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - PRIVACY.md
  - tests/privacy-md-shape.test.js
  - .planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md
autonomous: true
requirements: [DOC-02]
must_haves:
  truths:
    - "PRIVACY.md exists at repo root with 7 sections (Data we process / Sub-processors / Data residency / Retention summary / Data subject rights / International transfers / Contact)"
    - "Sub-processor table lists exactly TWO entries: Google LLC (Firebase/GCP) + Functional Software Inc. (Sentry)"
    - "Google Fonts is explicitly NOT listed (post-Phase-4 self-hosted; verified via curl)"
    - "Firestore data residency is documented as 'europe-west2' with the verification source cited (.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md)"
    - "Cloud Storage region is documented from a fresh gcloud verification (assumption A1 resolved)"
    - "Identity Platform region documented from a verification step (assumption A3 resolved)"
    - "Sentry EU residency confirmed from de.sentry.io URL grep + Sentry DPA URL"
  artifacts:
    - path: PRIVACY.md
      provides: "DOC-02 canonical sub-processor + residency + retention-summary + DSR-flow document"
      contains: "## 2. Sub-processors"
    - path: tests/privacy-md-shape.test.js
      provides: "DOC-02 doc-shape gate — 7 sections, sub-processor table shape, no Google Fonts entry"
    - path: .planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md
      provides: "Audit trail of A1+A3 assumption-resolution gcloud commands + curl outputs (per RESEARCH.md Pitfall 19)"
  key_links:
    - from: "PRIVACY.md ## 4. Retention"
      to: "docs/RETENTION.md"
      via: "explicit cross-reference (RETENTION.md fully populated in Wave 4)"
      pattern: "docs/RETENTION.md"
    - from: "PRIVACY.md ## 5. Data subject rights"
      to: "src/cloud/gdpr.js gdprExportUser + gdprEraseUser callables"
      via: "Phase 8 GDPR-01 + GDPR-02 cross-reference"
      pattern: "gdprExportUser|gdprEraseUser"
---

<objective>
Author `PRIVACY.md` at repo root with 7 sections, listing only verified sub-processors (Google/Firebase + Sentry — Google Fonts is NOT a sub-processor post-Phase-4), verified data residency regions (Firestore + Cloud Storage + Identity Platform + Sentry), and the GDPR Art. 15/17/21 DSR flow shipped Phase 8. Resolve assumptions A1 (Cloud Storage region) and A3 (Identity Platform region) via gcloud verification before content lands. Produce a verification log capturing the exact commands run + their outputs (Pitfall 19 substrate-honest disclosure).

Purpose: DOC-02 is the canonical privacy document for vendor questionnaires. Every claim must be verifiable; assumed regions cannot ship as fact.

Output: PRIVACY.md + 1 doc-shape test + 1 verification-log artefact under `.planning/phases/11-documentation-pack-evidence-pack/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md
@.planning/phases/11-documentation-pack-evidence-pack/11-01-SUMMARY.md
@.planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md
@SECURITY.md
@CLAUDE.md

<interfaces>
<!-- Verified facts to lift verbatim into PRIVACY.md (do NOT re-derive). Source: 11-RESEARCH.md. -->

VERIFIED — Firestore region (Phase 6 PREFLIGHT line 37):
- Database `(default)` for project `bedeveloped-base-layers` is `europe-west2 / FIRESTORE_NATIVE`
- Verified 2026-05-08T20:30:00Z via `gcloud firestore databases describe`
- Cite the PREFLIGHT line as evidence

VERIFIED — Sentry EU residency:
- vite.config.js `url: "https://de.sentry.io/"` (verified existing config)
- Sentry DSN format includes `*.ingest.de.sentry.io`
- Sentry DPA URL: https://sentry.io/legal/dpa/

VERIFIED — Google Fonts NOT a sub-processor:
- SECURITY.md line 207 confirms self-hosted Inter + Bebas Neue under `assets/fonts/`
- firebase.json line 22 (CSP value) shows no `fonts.googleapis.com` allowlist entry
- Wave 2 verification step: `curl https://baselayers.bedeveloped.com` + grep response HTML for `fonts.googleapis.com` (expected zero hits)

UNVERIFIED — needs gcloud verification this wave:
- A1: Cloud Storage bucket region (RESEARCH.md assumes `europe-west2`)
- A3: Identity Platform region (RESEARCH.md assumes EU; Phase 6 PREFLIGHT verified Firestore only)

DPA URLs (verbatim — RESEARCH.md):
- Google Cloud Data Processing Addendum: https://cloud.google.com/terms/data-processing-addendum
- Firebase Data Processing Terms: https://firebase.google.com/terms/data-processing-terms
- Sentry DPA: https://sentry.io/legal/dpa/

GDPR DSR flow (verified — Phase 8 shipped):
- gdprExportUser callable (`src/cloud/gdpr.js` exportUser wrapper; functions/src/gdpr/exportUser.ts) — V4 signed URL 24h TTL
- gdprEraseUser callable (`src/cloud/gdpr.js`; functions/src/gdpr/eraseUser.ts) — cascades + tombstone
- Response SLA: 30 days per GDPR Art. 12(3)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify A1 (Cloud Storage region) + A3 (Identity Platform region) + Google Fonts negative + Sentry EU; record outputs</name>
  <files>.planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md</files>
  <read_first>
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Assumptions Log" A1 + A2 + A3 (verification commands)
    - .planning/phases/06-real-auth-mfa-rules-deploy/06-PREFLIGHT.md (existing pattern for gcloud verification logs)
    - SECURITY.md § Hosting & Deployment (current site + project ID context)
    - vite.config.js (Sentry URL grep target)
  </read_first>
  <action>
Create `.planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md` documenting four verifications. The log structure mirrors `06-PREFLIGHT.md`'s gcloud-output capture pattern.

Run these commands and paste the EXACT output (timestamp + return code + stdout) into the log:

**A1 — Cloud Storage region:**
```bash
gcloud storage buckets describe gs://bedeveloped-base-layers.firebasestorage.app --format="value(location,locationType,storageClass)" 2>&1
```
ESCALATE branch: if the output shows a region OTHER than `europe-west2`, do NOT proceed to Task 2 — instead document the actual region and surface a `**ESCALATE**` block at the bottom of the verification log saying "Cloud Storage residency differs from Firestore; PRIVACY.md must reflect both regions; Wave 2 plan needs operator review before authoring." Then STOP and report to operator.

**A3 — Identity Platform region:**
```bash
gcloud identity-platform config describe --project=bedeveloped-base-layers --format="value(name,subtype)" 2>&1
```
If the command lacks `--format=value(region)` (Identity Platform region is sometimes only available via the Firebase Console UI), fall back to:
```bash
gcloud firebase apps:list --project=bedeveloped-base-layers 2>&1
```
And document any region indicators found. ESCALATE branch: if NO regional indicator can be obtained programmatically, document this honestly in the log ("Identity Platform region: indirect verification only — multi-region default assumed per A3; recommend operator verify in Firebase Console at https://console.firebase.google.com/project/bedeveloped-base-layers/authentication/settings") and proceed to Task 2 with the **ASSUMED-PER-A3** annotation in PRIVACY.md Section 3.

**Google Fonts negative verification:**
```bash
curl -s https://baselayers.bedeveloped.com | grep -ci "fonts\.googleapis\.com\|fonts\.gstatic\.com" 2>&1
```
Expected: `0` (zero hits). If the production site is unreachable (e.g. authentication required, hosting down), fall back to:
```bash
grep -rn "fonts.googleapis.com\|fonts.gstatic.com" src/ index.html public/ firebase.json 2>&1
```
Expected: zero hits in source/config. Document either path in the log.

**Sentry EU residency verification:**
```bash
grep -n "de\.sentry\.io" vite.config.js src/observability/sentry.js 2>&1
```
Expected: at least one hit per file confirming EU ingest endpoint.

**Sentry DPA URL liveness:**
```bash
curl -sIL https://sentry.io/legal/dpa/ 2>&1 | head -5
```
Expected: HTTP/2 200 or 301 chain to a 200. Capture the final status. If the URL 404s or moves, search Sentry's legal page (`curl -sIL https://sentry.io/legal/`) for the current DPA URL and update PRIVACY.md accordingly.

Structure the verification log with sections: Frontmatter (date, owner: Phase 11 Wave 2) / A1 Cloud Storage / A3 Identity Platform / Google Fonts negative / Sentry EU / Sentry DPA / Summary table (Assumption | Status | Resolved-As) / Escalations (empty if none).

Commit: `docs(11-02): record DOC-02 sub-processor + residency verification log`.
  </action>
  <verify>
    <automated>test -f .planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md && grep -c "^## " .planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md (must be >= 5 — 5 verification sections)</automated>
  </verify>
  <done>Verification log exists with EXACT command outputs for A1 / A3 / Google Fonts negative / Sentry EU / Sentry DPA URL. If any ESCALATE branch tripped, operator surfaced before Task 2 begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author tests/privacy-md-shape.test.js (RED)</name>
  <files>tests/privacy-md-shape.test.js</files>
  <read_first>
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Pattern 2 — PRIVACY.md" (recommended structure with 7 sections)
    - tests/security-md-toc.test.js (Wave 1 test pattern — read-file-then-regex-assert)
    - 11-02-VERIFICATION-LOG.md (just created — confirms which residency claims are VERIFIED vs ASSUMED-PER-A3)
  </read_first>
  <behavior>
    tests/privacy-md-shape.test.js (8 cases):
    - Test 1: PRIVACY.md exists at repo root (resolve(process.cwd(), "PRIVACY.md"))
    - Test 2: PRIVACY.md has all 7 required `## N.` numbered sections (Data we process / Sub-processors / Data residency / Retention / Data subject rights / International transfers / Contact)
    - Test 3: Section 2 (Sub-processors) contains a markdown table with exactly 2 vendor rows (Google LLC + Functional Software, Inc.) — count rows by counting `| ` row separators after the table header, excluding header + delimiter rows
    - Test 4: Section 2 explicitly states Google Fonts is NOT a sub-processor (search for the phrase "Not a sub-processor" or "no longer a sub-processor" + "Google Fonts" within Section 2 boundaries)
    - Test 5: Section 3 (Data residency) cites `europe-west2` AT LEAST twice (Firestore + Functions; Storage may also be europe-west2)
    - Test 6: Section 2 contains the verbatim DPA URLs: `https://cloud.google.com/terms/data-processing-addendum` AND `https://firebase.google.com/terms/data-processing-terms` AND `https://sentry.io/legal/dpa/`
    - Test 7: Section 5 (Data subject rights) cites both `gdprExportUser` AND `gdprEraseUser` callable names; cites GDPR Art. 12(3) 30-day SLA
    - Test 8: PRIVACY.md does NOT contain the word "compliant" or "certified" outside scare quotes (Pitfall 19 forbidden-words check) — pattern: `/\b(compliant|certified)\b/i` returns ZERO matches except inside backtick code spans

    These tests MUST FAIL initially — PRIVACY.md doesn't exist yet.
  </behavior>
  <action>
Create `tests/privacy-md-shape.test.js` using the same readFileSync-and-regex pattern as `tests/security-md-toc.test.js`. Implement the 8 cases per behavior block above.

For Test 2 (numbered sections), use a multiline regex like `/^## 1\. /m`, `/^## 2\. /m`, etc. — each ## NN must exist exactly once.

For Test 3 (sub-processor row count), slice the document at `## 2.` and `## 3.` boundaries to isolate Section 2; within that slice, find the table block (lines starting with `|`); skip header (`| Sub-processor |...`) and delimiter (`|---|...`) rows; count remaining `| ` rows; assert == 2.

For Test 4 (Google Fonts negative), within the Section 2 slice: assert the slice contains BOTH "Google Fonts" AND ("Not a sub-processor" OR "no longer a sub-processor"). Use a positive-form check (the document explicitly disclaims it) — not a negative existence check (which would also fail if the doc never mentioned Fonts).

For Test 8 (Pitfall 19), strip backtick code spans before regex check: `const stripped = src.replace(/`[^`]+`/g, "")`; then assert `expect(stripped.match(/\b(compliant|certified)\b/gi)?.length || 0).toBe(0)`. Allow "non-certified" if it surfaces (the negation form is acceptable; tighten regex to `/\b(?<!non-)(compliant|certified)\b/i` if needed — but Phase 11 prefers "credible, not certified" wording so plain forbidden-words check should suffice).

Run `npm test -- --run privacy-md-shape` and confirm FAILURES (RED — file doesn't exist; all 8 cases fail). Commit: `test(11-02): add PRIVACY.md DOC-02 shape test (RED)`.
  </action>
  <verify>
    <automated>npm test -- --run privacy-md-shape 2>&1 | grep -E "FAIL|failed" (must show failures — file doesn't exist yet)</automated>
  </verify>
  <done>tests/privacy-md-shape.test.js exists with 8 cases; running shows RED gate; committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author PRIVACY.md (GREEN)</name>
  <files>PRIVACY.md</files>
  <read_first>
    - .planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md §"Pattern 2 — PRIVACY.md" (recommended structure verbatim)
    - 11-02-VERIFICATION-LOG.md (Task 1 outputs — use VERIFIED facts only)
    - tests/privacy-md-shape.test.js (the contract to satisfy)
    - SECURITY.md § Observability — Sentry (existing Sentry sub-processor language to mirror)
  </read_first>
  <behavior>
    After Task 3, PRIVACY.md exists at repo root with the structure from RESEARCH.md Pattern 2, adapted as follows:
    - Section 3 (Data residency) cites the verification log produced in Task 1; uses `**VERIFIED 2026-05-10**` annotation for facts confirmed via Task 1 commands; uses `**ASSUMED-PER-A3**` annotation only for facts that the Task 1 verification could not pin down; cross-references both verification source and the operator-action escalation if any
    - Section 2 (Sub-processors) explicitly disclaims Google Fonts as a sub-processor with the cited self-hosting evidence (`SECURITY.md` § Build & Supply Chain → Wave 1 self-hosted fonts; `firebase.json` CSP allowlist excludes `fonts.googleapis.com`)
    - Section 4 (Retention) is a one-paragraph orientation with `See docs/RETENTION.md` link only — no duplication (RESEARCH.md Open Question 6 recommendation)
    - Section 5 (Data subject rights) cites `gdprExportUser` + `gdprEraseUser` callables + GDPR Art. 15/17/21 + 30-day SLA per Art. 12(3); cross-references SECURITY.md § GDPR (Export + Erasure)
    - Section 6 (International transfers) cites SCCs incorporated by Google DPA + Sentry EU residency
    - Section 7 (Contact) is `security@bedeveloped.com`
    - Pitfall 19 substrate-honest: every region claim is either VERIFIED or annotated; never plain assertion
  </behavior>
  <action>
Use the Write tool. Author `PRIVACY.md` at repo root using the template from RESEARCH.md §"Pattern 2 — PRIVACY.md" verbatim, with these substitutions based on Task 1's verification log:

- `**Last updated:** 2026-05-10` (today)
- `**Maintained by:** BeDeveloped (security@bedeveloped.com)`
- `**Related documents:** SECURITY.md, docs/RETENTION.md, docs/CONTROL_MATRIX.md`

Section 1 — paste from RESEARCH.md Pattern 2 verbatim.

Section 2 — paste the sub-processor table from RESEARCH.md Pattern 2 verbatim with the two rows (Google LLC + Functional Software, Inc.). Add the explicit Google Fonts disclaimer paragraph below the table (verbatim from RESEARCH.md). Tweak the Google sub-processor row's "Region" column based on Task 1 A1 verification — if Cloud Storage region matches Firestore (`europe-west2`), use the unified row; if it differs, add a separate "Cloud Storage region: <verified>" annotation.

Section 3 — paste from RESEARCH.md Pattern 2 verbatim with these adjustments:
- For Cloud Storage region: replace `{VERIFY-PHASE-11-WAVE-2; recommend europe-west2}` with the VERIFIED region from Task 1 OR `**ESCALATE**` if Task 1 hit ESCALATE branch (in that case stop here and surface to operator).
- For Identity Platform region: write `Auth (Identity Platform): EU multi-region — verification path: <Task 1 output excerpt or "Firebase Console-only verification path; recommend operator confirm at https://console.firebase.google.com/project/bedeveloped-base-layers/authentication/settings"> (assumption A3 of `.planning/phases/11-documentation-pack-evidence-pack/11-RESEARCH.md`)`. Use `**ASSUMED-PER-A3**` annotation.
- Cross-reference Task 1's verification log via: `Verification audit trail: see .planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md`

Section 4 — paste from RESEARCH.md Pattern 2 verbatim. Bullet list summary (~4 lines) + `See docs/RETENTION.md` link.

Section 5 — paste from RESEARCH.md Pattern 2 verbatim. Three bullets (Art. 15 / Art. 17 / Art. 21) + 30-day SLA paragraph + cross-reference to `gdprExportUser` (`src/cloud/gdpr.js exportUser`; `functions/src/gdpr/exportUser.ts`) + `gdprEraseUser`.

Section 6 — paste from RESEARCH.md Pattern 2 verbatim. Adjust the Storage region sentence based on Task 1 verification.

Section 7 — `security@bedeveloped.com`.

Run `npm test -- --run privacy-md-shape` — must be GREEN. Run `npm test -- --run` full suite — zero regressions.

Commit: `docs(11-02): author PRIVACY.md (DOC-02 — sub-processors + residency + DSR flow; GREEN)`.
  </action>
  <verify>
    <automated>npm test -- --run privacy-md-shape 2>&1 | grep -E "passed|failed" (must show all green)</automated>
    <automated>npm test -- --run 2>&1 | tail -5 (full suite zero regressions)</automated>
  </verify>
  <done>PRIVACY.md at repo root with 7 sections; sub-processor table contains exactly 2 vendor rows; Google Fonts disclaimed; gcloud-verified regions cited; assumption A3 annotated; DPA URLs present; DSR flow cited; Pitfall 19 substrate-honest; doc-shape test green; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reader (auditor / customer DPO) → PRIVACY.md | Reader expects every region claim verifiable; unverified claims are GDPR-risk |
| PRIVACY.md → SECURITY.md cross-references | Section 5 cites Phase 8 GDPR callables; broken cross-references undermine traceability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Information Disclosure | PRIVACY.md sub-processor list staleness | mitigate | Task 1 gcloud + curl verification step before authoring; Wave 6 cleanup-ledger queues quarterly review row |
| T-11-02-02 | Repudiation | Unverified region claim shipped | mitigate | A1 + A3 verification commands; ESCALATE branch if A1 fails; ASSUMED-PER-A3 annotation if A3 only verifiable via Firebase Console |
| T-11-02-03 | Information Disclosure | Google Fonts incorrectly listed as sub-processor | mitigate | Task 1 negative verification via curl + grep; explicit disclaimer in Section 2 |
| T-11-02-04 | Repudiation | Pitfall 19 forbidden-words drift ("compliant" / "certified") | mitigate | tests/privacy-md-shape.test.js Test 8 forbidden-words check |
| T-11-02-05 | Repudiation | Sentry DPA URL stale (404) | mitigate | Task 1 curl liveness check; if 404, search and update before authoring |

</threat_model>

<verification>
- All 3 tasks committed via Conventional Commits prefix `docs(11-02)` or `test(11-02)`
- `npm test -- --run privacy-md-shape` GREEN
- `npm test -- --run` full-suite zero regressions
- `test -f PRIVACY.md && wc -l PRIVACY.md` shows file exists with reasonable line count (~80-150 lines expected)
- `grep -c "europe-west2" PRIVACY.md` shows >= 2 (Firestore + Functions verified)
- `grep -c "ASSUMED-PER\|VERIFIED" PRIVACY.md` shows the substrate-honest annotation pattern (>= 1)
</verification>

<success_criteria>
- PRIVACY.md exists at repo root with 7 sections per RESEARCH.md Pattern 2
- Sub-processor table has exactly 2 rows (Google + Sentry); Google Fonts explicitly disclaimed
- A1 (Cloud Storage region) verified or ESCALATED
- A3 (Identity Platform region) verified or annotated ASSUMED-PER-A3
- Sentry EU residency confirmed via grep evidence
- DPA URLs present (Google + Firebase + Sentry)
- DSR flow cites callable names + Art. 12(3) 30-day SLA
- Pitfall 19 forbidden-words ("compliant" / "certified") return zero hits outside code spans
- Verification log captured at `.planning/phases/11-documentation-pack-evidence-pack/11-02-VERIFICATION-LOG.md`
</success_criteria>

<output>
After completion, append a paragraph to `.planning/phases/11-documentation-pack-evidence-pack/11-02-SUMMARY.md` capturing: which assumptions resolved (A1 / A3 verdict), DPA URL liveness statuses, sub-processor table row count, commits SHAs.
</output>
